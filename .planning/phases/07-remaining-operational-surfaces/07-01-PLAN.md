---
phase: 07-remaining-operational-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/room_state.py
  - web/ifo.py
  - web/guard.py
  - web/urls.py
  - web/tests.py
autonomous: true
requirements: [GRD-05]
must_haves:
  truths:
    - "A POST to /guard/monitor, /guard/monitor/rows or /guard/locate returns 405, not 200."
    - "The five-state room-tile derivation is importable by both the IFO and the Guard module from one shared place, with no role module importing a private name from another role module."
    - "The faculty locator is labelled GRD-03 in both the view docstring and the URL block, so the real GRD-02 (per-room schedule, plan 07-09) can be added without a label collision."
    - "The IFO room board renders exactly as before the extraction (no behaviour change)."
  artifacts:
    - web/room_state.py
    - web/tests.py (GuardReadOnlyTests)
  key_links:
    - "web/ifo.py imports occupies/room_tile from web/room_state.py instead of defining them."
    - "web/guard.py imports require_http_methods from django.views.decorators.http."
---

<objective>
Close GRD-05 by contract rather than by omission, correct the GRD-02/GRD-03 mislabel before the real GRD-02 is built, and lift the room-tile derivation into a shared module so the Guard per-room schedule (plan 07-09) can consume it without reaching into a private IFO name.

Purpose: this is the cheap-safety + shared-extraction foundation for the phase. Every later Guard plan depends on the extraction; every later Guard view depends on the read-only decorator convention being in place first.
Output: `web/room_state.py`, a GET-only `web/guard.py`, corrected GRD labels, and a `GuardReadOnlyTests` class proving POST is refused.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-remaining-operational-surfaces/07-CONTEXT.md
@.planning/phases/07-remaining-operational-surfaces/PATTERNS.md

@web/guard.py
@web/ifo.py
@web/urls.py
@web/hr.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the room-tile derivation into web/room_state.py</name>
  <files>web/room_state.py, web/ifo.py</files>
  <action>
Create `web/room_state.py` as the shared, role-neutral room-state derivation module. Move four things out of `web/ifo.py` verbatim, preserving their docstrings and inline comments (which carry the reasoning for the online-occupancy rule and the past-grace rule and must not be lost): the `ROOM_STATE_ORDER` dict, the `ROOM_PROBLEM_STATES` tuple, the `_occupies` function, and the `_room_tile` function.

Rename the two functions to public names on the way out — `occupies(session, room)` and `room_tile(room, sessions, now, grace)` — because they are now a cross-module contract, and PATTERNS.md §4.1 rejects `from web.ifo import _room_tile` in `web/guard.py` precisely because it imports a private across role boundaries. The module docstring must state that this module is consumed by BOTH `web/ifo.py` (IFO room board, IFO-07/IFO-11) and `web/guard.py` (GRD-01/GRD-02), and that it holds no role gating of its own — authorization stays in the calling role module.

In `web/ifo.py`, delete the moved definitions and add `from web.room_state import ROOM_PROBLEM_STATES, ROOM_STATE_ORDER, occupies, room_tile`. Update all existing call sites: `_room_board` (two references), `room_panel` (two references), and `room_detail` (one reference in the `upcoming` comprehension). This is a pure move — no logic changes, no signature changes beyond the leading underscore. `SessionStatus` and `Modality` imports move with the functions; remove them from `web/ifo.py` only if nothing else there still uses them.
  </action>
  <verify>
    <automated>py -3.12 manage.py test web.tests_ifo_board -v 2</automated>
  </verify>
  <done>`web/room_state.py` exists exporting `occupies`, `room_tile`, `ROOM_STATE_ORDER`, `ROOM_PROBLEM_STATES`; `web/ifo.py` defines none of them and imports all four; the IFO board test module is green with no behaviour change.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce GRD-05 read-only and correct the GRD-02/GRD-03 labels</name>
  <files>web/guard.py, web/urls.py</files>
  <action>
GRD-05 enforcement. `web/guard.py` is the only role module in `web/` with zero method decorators, so all three views currently answer POST with a 200 — read-only in practice, not by contract. Add `from django.views.decorators.http import require_http_methods` and put `@require_http_methods(["GET"])` on `monitor`, `monitor_rows` and `locate`. Decorator order is the house order shown at `web/hr.py:178` and `web/ifo.py:477`: the role gate outermost, `require_http_methods` directly beneath it, then the function. Every Guard view added by later plans in this phase follows the same order.

Label correction. The locator view is GRD-03 (locate a faculty member), not GRD-02 (per-room schedule) — the real GRD-02 lands in plan 07-09 and the two will collide if the wrong label is left in place. Fix three spots: the `web/guard.py` module docstring, which currently opens by naming the pair as GRD-01/GRD-02; the `locate` view docstring, which currently opens with the GRD-02 identifier; and the `web/guard.py` module docstring's read-only sentence, which should now also name GRD-05 as the enforced contract rather than an observed habit. In `web/urls.py`, the Guard block comment above the three guard paths currently reads as the GRD-01/02 pair — change it to name GRD-01 (monitor), GRD-03 (locator) and GRD-05 (read-only), and leave a note that GRD-02 is added by plan 07-09.

Do not change any view body, query, template, or URL path in this task. This is decorators plus comments only.
  </action>
  <verify>
    <automated>py -3.12 manage.py test web.tests.GuardSurfaceTests -v 2</automated>
  </verify>
  <done>All three guard views carry `@require_http_methods(["GET"])` under `@guard_required`; `web/guard.py` and `web/urls.py` label the locator as GRD-03; the existing `GuardSurfaceTests` class still passes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GuardReadOnlyTests — prove POST is refused on every Guard surface</name>
  <files>web/tests.py</files>
  <behavior>
    - An authenticated GUARD POSTing to `guard_monitor` gets 405.
    - An authenticated GUARD POSTing to `guard_monitor_rows` gets 405.
    - An authenticated GUARD POSTing to `guard_locate` gets 405.
    - The same three URLs still return 200 on GET for that guard (the decorator refuses the method, it does not break the surface).
    - A non-GUARD authenticated user still gets 403 on GET (role gate is outermost — the method decorator must not shadow it).
  </behavior>
  <action>
Add a `GuardReadOnlyTests(TestCase)` class to `web/tests.py`, immediately after the existing `GuardSurfaceTests`, following the class-per-surface convention (PATTERNS.md §5). Reuse the same fixture shape `GuardSurfaceTests.setUp` uses — a Building, a Floor, a Room with `qr_token`/`manual_code`, and a User with `role=Role.GUARD` — rather than importing from that class, so the two classes stay independent.

Write the five assertions in the behaviour block, iterating the three URL names (`guard_monitor`, `guard_monitor_rows`, `guard_locate`) via `reverse()`. The 403 case asserts that the role gate still fires first for a FACULTY user, which is the regression this decorator change could plausibly introduce if the decorator order were inverted.

Class docstring must state that GRD-05 ("a Guard has no write access anywhere") is enforced here as a contract, not merely observed, and that any Guard view added later must be added to this class's URL list.
  </action>
  <verify>
    <automated>py -3.12 manage.py test web.tests.GuardReadOnlyTests web.tests.GuardSurfaceTests -v 2</automated>
  </verify>
  <done>`GuardReadOnlyTests` exists with the five behaviours green; the pre-existing `GuardSurfaceTests` is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated Guard browser → Django view | A GUARD-role session is the least-privileged authenticated role; anything it can reach must be read-only. |
| role module → role module | A private helper reached across role modules is an implicit, unversioned contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | `web/guard.py` monitor / monitor_rows / locate | medium | mitigate | `@require_http_methods(["GET"])` on all three, asserted by `GuardReadOnlyTests`; every future Guard view joins that list. |
| T-07-02 | Tampering | cross-module import of `web.ifo._room_tile` | low | mitigate | Lift to `web/room_state.py` as a public, role-neutral contract; no role module imports a private from another. |
| T-07-03 | Information disclosure | locator surface scope | low | accept | GRD-03 already excludes attendance status and flag history per D-07; this plan changes no query, only labels. |
</threat_model>

<verification>
1. `py -3.12 manage.py test web.tests_ifo_board web.tests.GuardSurfaceTests web.tests.GuardReadOnlyTests -v 2` — all green.
2. `py -3.12 manage.py test ops scheduling verification -v 1` — no collateral damage from the extraction.
3. Manual: `py -3.12 manage.py runserver`, sign in as the demo guard, load `/guard/monitor` and `/guard/locate` — both render as before.
4. Manual: sign in as IFO, load `/ifo/rooms` and open a room panel — tiles, states and ordering are identical to before the extraction.
5. Confirm no new static asset was added by this plan; if any `.css`/`.js` file was created or edited, run `py -3.12 manage.py collectstatic --noinput` before finishing.
</verification>

<success_criteria>
- GRD-05 is enforced: POST returns 405 on all three Guard URLs, proven by an automated test.
- `web/room_state.py` is the single home of the room-tile derivation and is import-clean from both `web/ifo.py` and (later) `web/guard.py`.
- The locator is labelled GRD-03 everywhere; `web/urls.py` reserves GRD-02 for plan 07-09.
- The IFO room board is behaviourally unchanged.
</success_criteria>

<output>
Create `.planning/phases/07-remaining-operational-surfaces/07-01-SUMMARY.md` when done.
</output>
