---
phase: 04.2-co-scheduled-session-attendance-inserted
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - web/scan.py
  - web/tests_merge_scan.py
autonomous: true
requirements: [SCAN-01, FAC-03, FAC-04, FAC-09]
must_haves:
  truths:
    - "One faculty check-in on GARAY section A301 (room A408-A) marks section A302 (room A408-B) ACTIVE with checkin_method=MERGED, the SAME actual_start, and a session.merged_present AuditLog — all inside one transaction.atomic() (ROADMAP criterion #1)."
    - "The scanned anchor keeps its REAL checkin_method (qr_scan / manual_code / force_handover), never MERGED (D-09)."
    - "A non-merged session that shares neither room nor course with the scanned session is unaffected by the check-in (ROADMAP criterion #4/#5)."
    - "A force-handover check-in (ROOM_OCCUPIED) also propagates present across the merged group."
  artifacts:
    - "web/scan.py _apply: CHECKED_IN and ROOM_OCCUPIED branches call propagate_merged_present inside transaction.atomic()"
    - "web/tests_merge_scan.py"
  key_links:
    - "web/scan.py:_apply -> scheduling.merge.propagate_merged_present (anchor save + propagation in ONE transaction)"
---

<objective>
Wire the faculty check-in seam to the merge core: after a scan drives one session
SCHEDULED->ACTIVE, propagate present across its D-01 merged group in the same DB
transaction, so GARAY's single scan satisfies both co-scheduled sections and the sweep
never marks the sibling Absent.

Purpose: Deliver ROADMAP criteria #1 (single event marks the whole group present) and
guard #4/#5 (non-merged sessions untouched; rows stay distinct) at the faculty scan seam.
Output: the propagation call-sites in `web/scan.py:_apply` and their end-to-end tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-01-SUMMARY.md

# Seam + source-of-truth analogs
@web/scan.py
@scheduling/merge.py
@scheduling/test_support.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate present on faculty CHECKED_IN (one scan flips the group)</name>
  <read_first>
    - web/scan.py (_apply CHECKED_IN branch lines 78-83; the module has no transaction import yet; note session is fetched at line 68 without select_related on schedule)
    - scheduling/merge.py (propagate_merged_present contract — must be called with the anchor already saved ACTIVE, inside a transaction that also holds the anchor write)
    - scheduling/test_support.py (make_merge_fixture — GARAY pair + control from Plan 01)
  </read_first>
  <files>web/scan.py, web/tests_merge_scan.py</files>
  <behavior>
    - A faculty scan resolving CHECKED_IN on GARAY A301 sets A301 ACTIVE (real method) and A302 ACTIVE with checkin_method=MERGED, shared actual_start, and one session.merged_present AuditLog for A302.
    - The anchor A301's checkin_method is the scan method (qr_scan/manual_code), NOT MERGED.
    - The anchor write + the sibling fill occur in ONE transaction (rollback of one rolls back both).
    - A non-merged control session at the same start (different room AND course) stays SCHEDULED.
    - The seam fills EXACTLY the id set that merged_sibling_ids returns for the same fixture (coupling).
  </behavior>
  <action>
    In web/scan.py add `from django.db import transaction` and `from scheduling.merge import propagate_merged_present`. In `_apply`, wrap the CHECKED_IN branch's `session.save(...)` and a new `propagate_merged_present(session, now, user)` call in a single `with transaction.atomic():` block (D-04 same-transaction). Keep the existing `audit("session.checked_in", ...)` write. The anchor keeps `method` (D-09); only siblings become MERGED via the helper. Author web/tests_merge_scan.py as a `TestCase` importing `make_merge_fixture`; drive `_apply` (or the `resolve` view) for a CHECKED_IN on the GARAY anchor and assert every bullet in the behavior block, including a coupling assertion that the filled sibling set equals `merged_sibling_ids(anchor, candidates)` for the same rows.
  </action>
  <verify>
    <automated>python manage.py test web.tests_merge_scan</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test web.tests_merge_scan` exits 0.
    - A test asserts A302 ends ACTIVE + checkin_method=MERGED + actual_start == A301.actual_start + exactly one session.merged_present AuditLog (payload merged_from == A301.pk).
    - A test asserts the anchor A301 checkin_method is the scan method, not MERGED.
    - A test asserts the non-merged control stays SCHEDULED.
    - A coupling test asserts the seam's filled set == merged_sibling_ids output for the fixture.
  </acceptance_criteria>
  <done>A single faculty CHECKED_IN scan flips the whole GARAY merged group present in one transaction; the anchor keeps its real method; the control is untouched; all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate present on force-handover + idempotency</name>
  <read_first>
    - web/scan.py (ROOM_OCCUPIED force-handover branch lines 110-127 — session -> ACTIVE with checkin_method=FORCE_HANDOVER)
    - web/scan.py (resolve view idempotency guard lines 160-167 — scan-idem cache keeps a re-scan from reapplying)
    - scheduling/merge.py (propagate_merged_present is itself idempotent via the status=SCHEDULED guard)
  </read_first>
  <files>web/scan.py, web/tests_merge_scan.py</files>
  <behavior>
    - A confirmed force-handover (ROOM_OCCUPIED) that drives the anchor ACTIVE also propagates present to the merged group (siblings ACTIVE + MERGED + audit), in one transaction.
    - The anchor keeps checkin_method=FORCE_HANDOVER (its real method), never MERGED.
    - Re-running the same check-in does not double-fill or write a second session.merged_present AuditLog for a sibling (idempotent SCHEDULED->ACTIVE).
    - The prior occupant's session is unrelated to the merged group and is not merge-filled.
  </behavior>
  <action>
    Wrap the ROOM_OCCUPIED branch's prior/session saves and a `propagate_merged_present(session, now, user)` call in a single `with transaction.atomic():` block, mirroring Task 1. Do NOT change the force-handover semantics for the prior session. Extend web/tests_merge_scan.py with a force-handover case (using make_merge_fixture plus a competing occupant) and an idempotency case that applies the merge check-in twice and asserts the sibling is filled once (one AuditLog, unchanged actual_start).
  </action>
  <verify>
    <automated>python manage.py test web.tests_merge_scan</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test web.tests_merge_scan` exits 0.
    - A test asserts a force-handover check-in propagates present to the merged siblings (ACTIVE + MERGED + audit) while the anchor stays FORCE_HANDOVER.
    - A test asserts a repeated merge check-in fills the sibling exactly once (idempotent; no duplicate AuditLog).
  </acceptance_criteria>
  <done>Force-handover propagates present across the group and re-application is idempotent; all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Faculty client -> /scan | The scanned payload names a room; the merged group is derived server-side from the resolved session, never from the POST. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-01 | Elevation of Privilege | web/scan.py:_apply propagation | high | mitigate | propagate_merged_present scopes to `faculty_id == session.faculty_id`; a scan can only fill the acting faculty's own siblings. Test asserts a foreign-faculty same-start session is never filled. |
| T-04.2-02 | Tampering (integrity) | anchor save + propagation | high | mitigate | Both wrapped in one `transaction.atomic()` (D-04); a partial failure rolls back the whole group. Idempotency preserved by the existing scan-idem cache + the helper's SCHEDULED guard. |
| T-04.2-SC | Tampering | package installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `python manage.py test web.tests_merge_scan` green.
- `python manage.py test web scheduling verification` green (no regression to existing scan/FAC suites).
- Anchor rows keep their real checkin_method in every path; only siblings are MERGED.
</verification>

<success_criteria>
A single faculty check-in — normal or force-handover — marks every co-scheduled sibling present (ACTIVE, MERGED, shared actual_start, audited) in one transaction, the anchor keeps its real method, non-merged sessions are untouched, and re-scans are idempotent.
</success_criteria>

<output>
Create `.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-02-SUMMARY.md` when done.
</output>
