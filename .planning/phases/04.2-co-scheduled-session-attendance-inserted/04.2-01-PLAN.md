---
phase: 04.2-co-scheduled-session-attendance-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduling/models.py
  - scheduling/migrations/0004_alter_session_checkin_method.py
  - scheduling/merge.py
  - scheduling/test_support.py
  - scheduling/tests_merge.py
autonomous: true
requirements: [JOB-02b, SCAN-01, CHK-04]
must_haves:
  truths:
    - "merged_sibling_ids returns exactly the D-01 sibling set: same faculty_id + exact scheduled_start + (same room_id OR same course_code); different-room-AND-different-course, different-faculty, and a 1-minute start offset all return the empty set (criterion #5 + D-03: detection is dynamic from existing fields at check-in/verify/sweep time — no new merge-group model, no grouping migration, no roster/data merge; D-02: the domain term is \"merged sections\", used throughout code/docs)."
    - "propagate_merged_present flips ONLY status=SCHEDULED siblings to ACTIVE with a shared actual_start and checkin_method=MERGED, writing one session.merged_present AuditLog per filled sibling; an ACTIVE/ABSENT/COMPLETED sibling is left untouched (D-04/D-05/D-09)."
    - "A batch of N>=2 merged siblings propagates on MSSQL LocalDB with NO HY010 cursor error (mirrors scheduling.tests.SweepTests.test_batch_of_no_shows_all_marked_absent)."
    - "Reporting still sees distinct Session rows: propagation changes only status/actual_start/checkin_method, never schedule/section/course identity (criterion #5)."
  artifacts:
    - "scheduling/merge.py — merged_sibling_ids (pure), propagate_merged_present, propagate_merged_absent"
    - "CheckinMethod.MERGED enum choice + metadata-only migration"
    - "scheduling/test_support.py make_merge_fixture()"
    - "scheduling/tests_merge.py (MergeEnumTests, MergeDetectorTests, MergePropagationTests)"
  key_links:
    - "propagate_merged_present/absent -> merged_sibling_ids (impure seam calls the pure detector; coupling-tested)"
    - "filtered .update() carries status=SCHEDULED guard -> idempotent SCHEDULED->ACTIVE/ABSENT (D-05)"
---

<objective>
Build the shared merge core the whole phase depends on: the pure D-01 detector, the
`CheckinMethod.MERGED` enum choice and its metadata migration, the shared GARAY test
fixture, and the two ORM propagation helpers (present + absent) with their direct
unit/integration tests. Every Wave-2 seam plan (scan, checker, sweep-confirm) imports
these artifacts.

Purpose: Establish the pure + coupling-tested merge detector and the MSSQL-safe atomic
propagation helpers ONCE, so the three call-sites in Wave 2 are thin wiring, not logic.
Output: `scheduling/merge.py`, the enum + migration, `make_merge_fixture()`, and
`scheduling/tests_merge.py` (the Nyquist Wave-0 scaffolding).

## Artifacts This Phase Produces (canonical list)

Downstream plans and verification reference these exact symbols:

- `CheckinMethod.MERGED = "merged"` — new enum choice in `scheduling/models.py` (D-09).
- `scheduling/migrations/0004_alter_session_checkin_method.py` — metadata-only migration for the choice (no DDL on MSSQL).
- `scheduling/merge.py`:
  - `merged_sibling_ids(anchor, candidates)` — PURE D-01 detector (no ORM, no `timezone.now()`).
  - `propagate_merged_present(anchor, now, actor)` — atomic SCHEDULED->ACTIVE group fill (checkin_method=MERGED, shared actual_start, `session.merged_present` AuditLog per row). Returns the list of filled pks.
  - `propagate_merged_absent(anchor, actor)` — atomic SCHEDULED->ABSENT group fill for the online D-07 path (`session.merged_absent` AuditLog per row). Returns the list of absented pks.
- `session.merged_present` AuditLog event_type, payload `{"merged_from": <anchor pk>}` (present fill).
- `session.merged_absent` AuditLog event_type, payload `{"merged_from": <anchor pk>}` (online absent fill).
- `scheduling/test_support.py::make_merge_fixture()` — the GARAY canonical fixture.
- Propagation call-sites added in Wave 2: `web/scan.py:_apply` (Plan 02) and `web/checker.py:_apply_action` (Plan 03).
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-RESEARCH.md

# Source-of-truth analogs (patterns to mirror)
@scheduling/resolver.py
@scheduling/jobs.py
@scheduling/models.py
@scheduling/test_support.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CheckinMethod.MERGED enum choice + metadata migration</name>
  <read_first>
    - scheduling/models.py (CheckinMethod TextChoices at lines 77-82; Session.checkin_method at line 99, max_length=15)
    - scheduling/migrations/0003_modality_shift_request.py (latest scheduling migration — confirms next number is 0004)
  </read_first>
  <files>scheduling/models.py, scheduling/migrations/0004_alter_session_checkin_method.py, scheduling/tests_merge.py</files>
  <behavior>
    - CheckinMethod.MERGED evaluates to the string "merged" and appears in CheckinMethod.values.
    - "merged" is 6 chars, fits the existing max_length=15 on Session.checkin_method (no column change).
    - makemigrations produces exactly ONE new migration and leaves the app with no pending changes.
  </behavior>
  <action>
    Add a `MERGED = "merged", "Merged (sibling fill)"` member to the `CheckinMethod` TextChoices in scheduling/models.py, after FORCE_HANDOVER (D-09). Then run `python manage.py makemigrations scheduling` — Django emits an `AlterField` on `Session.checkin_method` because its `choices` changed; the generated file is `0004_alter_session_checkin_method.py`. This is metadata-only: `choices` are enforced at the app layer, MSSQL has no CHECK constraint for a CharField's choices, and `max_length` is unchanged, so no DDL runs (confirm with `python manage.py sqlmigrate scheduling 0004` showing a no-op / empty statement set). Do NOT hand-edit the migration. Create `scheduling/tests_merge.py` with a `MergeEnumTests(SimpleTestCase)` asserting `CheckinMethod.MERGED == "merged"` and membership in `CheckinMethod.values`.
  </action>
  <verify>
    <automated>python manage.py makemigrations scheduling --check --dry-run; python manage.py migrate scheduling; python manage.py test scheduling.tests_merge.MergeEnumTests</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py makemigrations scheduling --check --dry-run` exits 0 (no pending changes) after the migration exists.
    - `python manage.py migrate scheduling` applies cleanly on LocalDB.
    - `python manage.py test scheduling.tests_merge.MergeEnumTests` exits 0.
    - `python manage.py sqlmigrate scheduling 0004` emits no ALTER COLUMN / CHECK DDL (metadata-only, A2 confirmed).
  </acceptance_criteria>
  <done>CheckinMethod.MERGED exists, the 0004 migration is applied and metadata-only, and the enum test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure D-01 merge detector + truth-table tests</name>
  <read_first>
    - scheduling/resolver.py (is_no_show_past_grace lines 39-52 — the "pure, no ORM, no now(), coupling-tested" convention to mirror)
    - scheduling/tests.py (CouplingIntegrityTests lines 146-162 — the SimpleTestCase truth-table style to mirror)
    - .planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-RESEARCH.md (Pattern 1, lines 153-185 — the merged_sibling_ids contract)
  </read_first>
  <files>scheduling/merge.py, scheduling/tests_merge.py</files>
  <behavior>
    - GARAY row: same faculty, same start, different room, SAME course_code -> MERGE (caught by course arm).
    - Same faculty, same start, SAME room, different course_code -> MERGE (caught by room arm).
    - Same faculty, same start, different room AND different course_code -> NOT a merge (empty set).
    - Different faculty (else all same) -> NOT a merge.
    - scheduled_start off by 1 minute (else all same) -> NOT a merge (exactness boundary).
    - The anchor never merges with itself (id equality is skipped).
    - N>=3 candidates return every qualifying sibling id, none of the disqualified ones.
  </behavior>
  <action>
    Create scheduling/merge.py with a module docstring citing the mirror of is_no_show_past_grace. Implement `merged_sibling_ids(anchor, candidates)` returning a `set` of candidate ids that merge with `anchor` under D-01: skip `c.id == anchor.id`; require `c.faculty_id == anchor.faculty_id`; require `c.scheduled_start == anchor.scheduled_start` (compare the full aware DateTimeField — NEVER `.date()` or time-of-day only); include when `c.room_id == anchor.room_id OR c.course_code == anchor.course_code`. Keep it pure: `anchor`/`candidates` are Session-like objects exposing `.id`, `.faculty_id`, `.scheduled_start`, `.room_id`, `.course_code`; no ORM query and no `timezone.now()` inside. In scheduling/tests_merge.py add `MergeDetectorTests(SimpleTestCase)` that builds plain objects via `types.SimpleNamespace` and exhaustively enumerates the truth table in the behavior block above, using `subTest` per row (mirror CouplingIntegrityTests). Include one Manila-aware-instant case to lock the exact-start boundary.
  </action>
  <verify>
    <automated>python manage.py test scheduling.tests_merge.MergeDetectorTests</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test scheduling.tests_merge.MergeDetectorTests` exits 0.
    - MergeDetectorTests is a SimpleTestCase (no DB) and covers every row named in the behavior block, each in its own subTest.
    - merged_sibling_ids contains no `import`/`objects`/`filter`/`timezone.now` — it is pure (grep the function body: no ORM tokens).
  </acceptance_criteria>
  <done>The pure D-01 detector exists and its exhaustive truth-table SimpleTestCase is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GARAY fixture + atomic propagation helpers + propagation tests</name>
  <read_first>
    - scheduling/jobs.py (sweep_no_shows lines 35-74 — the materialize-before-mutate + transaction.atomic() + AuditLog pattern; and the session.marked_absent audit shape to mirror)
    - scheduling/test_support.py (make_shift_fixture lines 40-146 — the prefix-namespaced fixture-builder pattern to mirror)
    - ops/models.py (AuditLog lines 57-74 — actor/event_type/target_type/target_id/payload shape)
    - scheduling/tests.py (SweepTests.test_batch_of_no_shows_all_marked_absent lines 348-360 — the HY010 batch regression to mirror)
  </read_first>
  <files>scheduling/test_support.py, scheduling/merge.py, scheduling/tests_merge.py</files>
  <behavior>
    - make_merge_fixture() seeds: faculty (cdgaray-shaped), a merged F2F pair (course MMA116, section A301 in room A408-A + section A302 in room A408-B, same faculty, same M/W 15:45 start), a non-merged control (same faculty+start but different room AND different course, so NOT a merge), and an online sibling pair sharing course_code (different V-rooms, modality online). Every unique column is prefix-namespaced.
    - propagate_merged_present(anchor, now, actor): after the anchor is ACTIVE, the SCHEDULED merged siblings become ACTIVE, share the anchor's actual_start (== now), get checkin_method=MERGED, and each gets one session.merged_present AuditLog with payload {"merged_from": anchor.pk}. Returns the filled pks.
    - The anchor row itself is NOT re-stamped MERGED (helper only touches siblings).
    - Status guard: an already-ACTIVE, ABSENT, or COMPLETED sibling is left untouched (D-05).
    - Idempotency: a second call fills nothing and writes no new AuditLog.
    - HY010 batch: with N=3 SCHEDULED siblings the fill runs on LocalDB with no cursor error; all 3 end ACTIVE.
    - propagate_merged_absent(anchor, actor): SCHEDULED merged siblings become ABSENT (status guard identical), each with one session.merged_absent AuditLog; ACTIVE/COMPLETED siblings untouched.
    - The control session is never touched by either helper.
  </behavior>
  <action>
    Add `make_merge_fixture(prefix="mmf")` to scheduling/test_support.py returning a SimpleNamespace with the objects named in the behavior block (reuse the `_aware` + prefix-namespacing idiom from make_shift_fixture; keep ASCII-only). Implement `propagate_merged_present(anchor, now, actor)` and `propagate_merged_absent(anchor, actor)` in scheduling/merge.py. Each helper: opens `with transaction.atomic():`; materializes candidates as `list(Session.objects.filter(faculty_id=anchor.faculty_id, scheduled_start=anchor.scheduled_start).exclude(pk=anchor.pk).select_related("schedule"))`; attaches `c.course_code = c.schedule.course_code` to each candidate and `anchor.course_code = anchor.schedule.course_code`; computes `sib_ids = merged_sibling_ids(anchor, candidates)`; re-filters to only SCHEDULED via `fill_ids = list(Session.objects.filter(pk__in=sib_ids, status=SessionStatus.SCHEDULED).values_list("pk", flat=True))`; applies ONE filtered `.update()` — present: `.update(status=ACTIVE, actual_start=now, checkin_method=CheckinMethod.MERGED)`, absent: `.update(status=ABSENT)`; then `AuditLog.objects.bulk_create([...])` with event_type `session.merged_present` (present) or `session.merged_absent` (absent), payload `{"merged_from": anchor.pk}` per filled pk; returns `fill_ids`. Do NOT loop `.save()` over a live queryset (Pitfall 1 HY010). Do NOT create CheckerValidation rows (D-09). In scheduling/tests_merge.py add `MergePropagationTests(TestCase)` covering every behavior-block bullet, including a `test_batch_no_hy010` mirroring test_batch_of_no_shows_all_marked_absent (N=3 siblings) and a `test_idempotent_and_status_guard`.
  </action>
  <verify>
    <automated>python manage.py test scheduling.tests_merge.MergePropagationTests</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test scheduling.tests_merge.MergePropagationTests` exits 0 on LocalDB.
    - A test asserts the anchor keeps its own checkin_method and only siblings become MERGED.
    - A test asserts an already-ABSENT sibling and the non-merged control are untouched (status-guard + scoping).
    - A test asserts N=3 siblings all reach ACTIVE with no HY010 (batch/cursor safety).
    - A test asserts exactly one session.merged_present AuditLog per filled sibling with payload merged_from == anchor.pk, and NO CheckerValidation rows are created for siblings.
  </acceptance_criteria>
  <done>make_merge_fixture() and both propagation helpers exist; MergePropagationTests (present, absent, idempotency, status-guard, HY010 batch, audit shape, no-CheckerValidation) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB row set -> merge fill | The helper derives the sibling set server-side from existing rows; no client input crosses into the merge decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-01 | Elevation of Privilege | merged_sibling_ids / propagate_* | high | mitigate | Merge set is scoped to `faculty_id == anchor.faculty_id` in both the pure detector AND the ORM candidate filter, so a fill can never reach another instructor's sessions. MergePropagationTests asserts a different-faculty session is never filled. |
| T-04.2-02 | Tampering (integrity) | propagate_* filtered .update() | high | mitigate | The `status=SCHEDULED` guard on the `.update()` prevents resurrecting an ABSENT sibling or clobbering an ACTIVE/COMPLETED one (Pitfall 5); asserted by test_idempotent_and_status_guard. |
| T-04.2-03 | Tampering | atomic group write on MSSQL | high | mitigate | `transaction.atomic()` + single filtered `.update()` + `bulk_create` avoids a half-flipped group and the HY010 mutate-while-iterate trap; test_batch_no_hy010 runs on LocalDB. |
| T-04.2-04 | Tampering | 0004 migration | low | accept | Choices are app-level; metadata-only migration, no DDL. sqlmigrate confirms no-op. |
| T-04.2-SC | Tampering | package installs | low | accept | Phase installs NO npm/pip/cargo packages (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain surface. |
</threat_model>

<verification>
- `python manage.py test scheduling.tests_merge` is fully green (enum, detector, propagation).
- `python manage.py test scheduling web verification` stays green (no regression to existing sweep/scan/checker suites).
- `merged_sibling_ids` is pure (no ORM tokens in the function body).
- The 0004 migration is metadata-only (sqlmigrate no-op).
</verification>

<success_criteria>
The shared merge core exists and is proven: the pure D-01 detector matches its truth table, the two atomic helpers flip only SCHEDULED siblings (present->ACTIVE+MERGED+audit, absent->ABSENT+audit) with faculty-scoping, status-guard, idempotency, and no HY010 on MSSQL, and make_merge_fixture() is available for the Wave-2 seams.
</success_criteria>

<output>
Create `.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-01-SUMMARY.md` when done.
</output>
