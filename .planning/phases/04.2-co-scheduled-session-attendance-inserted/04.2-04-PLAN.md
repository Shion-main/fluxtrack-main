---
phase: 04.2-co-scheduled-session-attendance-inserted
plan: 04
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - scheduling/tests_merge_sweep.py
  - scheduling/management/commands/audit_merge_coverage.py
autonomous: true
requirements: [JOB-02b, CHK-02]
must_haves:
  truths:
    - "After a merge check-in, sweep_no_shows marks NOTHING for that group — the siblings are ACTIVE and the sweep only touches SCHEDULED (ROADMAP criterion #2), with ZERO change to scheduling/jobs.py."
    - "A no-event merged group (all SCHEDULED past shared grace) is marked Absent together by the UNCHANGED sweep because siblings share scheduled_start (D-08)."
    - "A genuinely-missed non-merged session is still marked Absent as today (ROADMAP criterion #4)."
    - "audit_merge_coverage confirms every real online merged group in the active term is caught by D-01 (room OR course_code), empirically validating ROADMAP criterion #3 against loaded-term data (Assumption A1 / Open Q1)."
  artifacts:
    - "scheduling/tests_merge_sweep.py"
    - "scheduling/management/commands/audit_merge_coverage.py"
  key_links:
    - "sweep_no_shows (UNCHANGED) -> ACTIVE siblings skipped; SCHEDULED group crosses grace together (D-04/D-08)"
---

<objective>
Prove the subtractive half of the design and close the online-coverage open question:
confirm the JOB-02 sweep needs NO change (present path skips ACTIVE siblings; no-event path
sweeps the whole SCHEDULED group together), keep the non-merged control still-Absent, and
ship an `audit_merge_coverage` management command that empirically validates D-01 catches
every real online merged group in the loaded term (no teams_link clause needed).

Purpose: Lock ROADMAP criteria #2 and #4 as regression-guarded, and make criterion #3
empirically checkable against the live 4226-session term rather than assumed.
Output: `scheduling/tests_merge_sweep.py` and the `audit_merge_coverage` command.

NOTE: This plan makes NO production change to scheduling/jobs.py. The sweep is confirmed
unchanged by construction (D-04/D-08); this plan tests that invariant and adds a read-only
audit tool.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-01-SUMMARY.md

# Source-of-truth analogs
@scheduling/jobs.py
@scheduling/merge.py
@scheduling/test_support.py
@scheduling/management/commands/run_status_sweep.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirm the sweep needs no change (criteria #2, #4, D-08)</name>
  <read_first>
    - scheduling/jobs.py (sweep_no_shows lines 35-74 — only touches status=SCHEDULED; re-affirms is_no_show_past_grace; this plan changes NOTHING here)
    - scheduling/merge.py (propagate_merged_present — the present path that makes siblings ACTIVE)
    - scheduling/tests.py (SweepTests lines 330-398 — the existing sweep fixtures/assertions to mirror)
    - scheduling/test_support.py (make_merge_fixture from Plan 01)
  </read_first>
  <files>scheduling/tests_merge_sweep.py</files>
  <behavior>
    - Present path (criterion #2): after propagate_merged_present flips a merged group ACTIVE at a past-grace start, sweep_no_shows marks 0 of that group Absent (all skipped as ACTIVE).
    - No-event path (D-08): a merged group left ALL SCHEDULED with a shared scheduled_start past grace is marked Absent TOGETHER by the unchanged sweep (both siblings ABSENT in one sweep run).
    - Control (criterion #4): a genuinely-missed non-merged SCHEDULED session past grace is still marked Absent by the same sweep run.
    - The sweep's returned count matches the number actually flipped in each case.
  </behavior>
  <action>
    Create scheduling/tests_merge_sweep.py as a `TestCase` importing make_merge_fixture and sweep_no_shows. Build the GARAY merged group with a past-grace scheduled_start (reuse the fixture but set/scheduled the start so `is_no_show_past_grace` is True at the test's `now`). Write three cases mirroring SweepTests: (1) call propagate_merged_present on the anchor then sweep_no_shows(now=...) and assert the group's siblings stay ACTIVE and marked==0 for them; (2) leave the merged group all SCHEDULED and assert sweep marks BOTH ABSENT in one run (shared grace, D-08); (3) a non-merged control SCHEDULED no-show is still marked ABSENT. Do NOT edit scheduling/jobs.py — the assertion IS that the unchanged sweep already behaves correctly.
  </action>
  <verify>
    <automated>python manage.py test scheduling.tests_merge_sweep</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test scheduling.tests_merge_sweep` exits 0.
    - A test asserts a merge-present group is skipped by the sweep (0 marked for the group; siblings stay ACTIVE).
    - A test asserts a no-event merged group is marked Absent together in one sweep run (D-08).
    - A test asserts a non-merged no-show is still marked Absent (criterion #4).
    - No diff to scheduling/jobs.py (the sweep is unchanged).
  </acceptance_criteria>
  <done>The unchanged sweep provably skips present merged groups, absents no-event groups together, and still absents genuine misses; green.</done>
</task>

<task type="auto">
  <name>Task 2: audit_merge_coverage command — empirical online D-01 coverage</name>
  <read_first>
    - scheduling/management/commands/run_status_sweep.py (the BaseCommand + handle() shape to mirror)
    - scheduling/merge.py (merged_sibling_ids — reuse the pure D-01 detector to classify real groups)
    - scheduling/models.py (Session.scheduled_start/faculty/room, Schedule.course_code, Modality; AcademicTerm.is_active)
    - .planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md (Post-Research Clarification #1 — validate online coverage against real loaded-term data, not a teams_link clause)
  </read_first>
  <files>scheduling/management/commands/audit_merge_coverage.py, scheduling/tests_merge_sweep.py</files>
  <behavior>
    - The command scans the ACTIVE term's effective-online sessions, groups them by (faculty_id, scheduled_start), and for each group of 2+ classifies whether every sibling is caught by D-01 (room_id OR course_code match to at least the group anchor) using merged_sibling_ids.
    - It prints a summary: total online same-start groups, groups fully CAUGHT by D-01, and any group that shares ONLY teams_link (distinct room AND distinct course) which D-01 would MISS.
    - Exit status is 0 when every online group is caught (criterion #3 empirically holds); it prints a clear escalation line naming any missed group.
    - The command is READ-ONLY — it mutates no rows and creates no AuditLog.
    - A unit test drives the command's grouping/classification against make_merge_fixture's online pair (shared course_code) and asserts it is classified CAUGHT with zero missed groups.
  </behavior>
  <action>
    Create scheduling/management/commands/audit_merge_coverage.py as a Django `BaseCommand`. In `handle`, select the active AcademicTerm's sessions whose effective modality (declared_modality or schedule.modality) is ONLINE, select_related("schedule"), attach course_code, group by (faculty_id, scheduled_start), and for each 2+ group compute coverage via `merged_sibling_ids` (a group is fully caught when the union of anchor+siblings under D-01 spans the whole group). Use `self.stdout.write` for the summary and a MISSED list; never write to the DB. Add a `MergeCoverageCommandTests(TestCase)` to scheduling/tests_merge_sweep.py that builds make_merge_fixture, runs the command via `call_command("audit_merge_coverage")` capturing stdout, and asserts the online pair is reported CAUGHT with zero missed groups. Document (docstring) that this command is the empirical check the user runs against the live loaded term per criterion #3.
  </action>
  <verify>
    <automated>python manage.py test scheduling.tests_merge_sweep.MergeCoverageCommandTests</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test scheduling.tests_merge_sweep.MergeCoverageCommandTests` exits 0.
    - The command runs read-only (a test asserts no Session status changes and no AuditLog rows created).
    - The command classifies the fixture's shared-course_code online pair as CAUGHT with zero missed groups.
    - The command prints a MISSED escalation line for a synthesized online pair that shares neither room nor course (distinct-both), proving it would surface a real D-01 gap.
  </acceptance_criteria>
  <done>audit_merge_coverage empirically validates D-01 online coverage against the loaded term (read-only) and its test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI -> audit_merge_coverage | A management command run by an operator; read-only, no external input beyond the active-term DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-03 | Tampering (integrity) | sweep confirmation | medium | mitigate | The plan makes NO change to scheduling/jobs.py; tests lock that the unchanged sweep skips ACTIVE merged groups and absents SCHEDULED groups together, preventing a future edit from silently forking the shared predicate. |
| T-04.2-06 | Information Disclosure | audit_merge_coverage | low | mitigate | Command is read-only, mutates nothing, creates no AuditLog; a test asserts zero writes. Output is a coverage summary, no PII beyond course/section labels already visible to operators. |
| T-04.2-SC | Tampering | package installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `python manage.py test scheduling.tests_merge_sweep` green (sweep confirmation + coverage command).
- `python manage.py test scheduling web verification` green (no regression to existing SweepTests).
- `git diff --stat scheduling/jobs.py` is empty (sweep provably unchanged).
- Operator step: `python manage.py audit_merge_coverage` against the loaded term reports all online groups CAUGHT (manual, per VALIDATION.md).
</verification>

<success_criteria>
The unchanged JOB-02 sweep is proven to skip present merged groups (criterion #2), absent no-event groups together (D-08), and still absent genuine misses (criterion #4); and audit_merge_coverage empirically confirms D-01 covers the real online merged groups (criterion #3) with no teams_link clause.
</success_criteria>

<output>
Create `.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-04-SUMMARY.md` when done.
</output>
