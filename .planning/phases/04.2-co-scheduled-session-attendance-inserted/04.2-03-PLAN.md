---
phase: 04.2-co-scheduled-session-attendance-inserted
plan: 03
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - web/checker.py
  - web/tests_merge_checker.py
autonomous: true
requirements: [CHK-02, CHK-03, CHK-04]
must_haves:
  truths:
    - "One Checker online Verify of a merged online class marks its online siblings ACTIVE (checkin_method=MERGED, shared actual_start, session.merged_present AuditLog), covering ROADMAP criterion #3 via the SAME D-01 key (shared course_code / V-room) — no teams_link clause (D-06: Checker Verify, including the online Teams path, is the second presence trigger alongside faculty check-in)."
    - "A Checker online Flag-not-present marks the whole online merged group ABSENT immediately (D-07 online), matching today's online-writes-ABSENT behavior; the anchor and siblings all end ABSENT."
    - "An F2F Checker flag stays record-only (unchanged): it writes the flag + notifies but does NOT force the merged group ABSENT — the sweep (Plan 04 / D-08) handles the F2F none-held case."
    - "Merge-filled siblings get NO CheckerValidation row, so sibling.verified_by_checker stays False (D-09; CHK-04 coverage is not inflated)."
  artifacts:
    - "web/checker.py _apply_action: online VERIFIED -> propagate_merged_present; online FLAG_NOT_PRESENT -> propagate_merged_absent (both in transaction.atomic())"
    - "web/tests_merge_checker.py"
  key_links:
    - "web/checker.py:_apply_action(online=True) -> propagate_merged_present / propagate_merged_absent"
---

<objective>
Wire the Checker online verification seam to the merge core: an online Verify propagates
present across the online merged group, and an online Flag-not-present propagates ABSENT
across it — each in one transaction — while the F2F flag path is left record-only exactly
as today. This delivers the online analog (ROADMAP criterion #3) without a teams_link merge
key and honors the authoritative per-modality D-07 refinement.

Purpose: Cover online co-scheduled siblings from a single Checker action while keeping
CHK-04 coverage honest (no fabricated CheckerValidation rows) and F2F semantics unchanged.
Output: the two online propagation call-sites in `web/checker.py:_apply_action` and their tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md
@.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-01-SUMMARY.md

# Seam + source-of-truth analogs
@web/checker.py
@scheduling/merge.py
@verification/models.py
@scheduling/test_support.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate present on online Verify (criterion #3, no teams_link key)</name>
  <read_first>
    - web/checker.py (_apply_action online VERIFIED branch lines 280-285 — anchor set ACTIVE + ONLINE_MANUAL; module has no transaction import yet)
    - web/checker.py (_online_action lines 526-572 — the server-side re-gate that calls _apply_action with online=True; ownership/on-duty already enforced)
    - scheduling/merge.py (propagate_merged_present — same helper the scan seam uses; online siblings caught by shared course_code)
    - .planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md (Post-Research Clarification #1 — NO teams_link in the key; validate online coverage empirically, not via a new clause)
  </read_first>
  <files>web/checker.py, web/tests_merge_checker.py</files>
  <behavior>
    - An online Verify on one online sibling (owned, on-duty) sets it ACTIVE + ONLINE_MANUAL and records its CheckerValidation(verified) as today.
    - The other online sibling (same faculty, same start, shared course_code, different V-room) becomes ACTIVE + checkin_method=MERGED + shared actual_start + one session.merged_present AuditLog.
    - The merge-filled online sibling has NO CheckerValidation row -> sibling.verified_by_checker is False (D-09).
    - Anchor write + sibling fill occur in ONE transaction.
    - Online siblings are found by the D-01 course_code arm, with teams_link never consulted.
  </behavior>
  <action>
    In web/checker.py add `from django.db import transaction` and `from scheduling.merge import propagate_merged_present, propagate_merged_absent`. In `_apply_action`, inside the existing `if online and session is not None:` VERIFIED branch, wrap the anchor `session.save(...)` and a new `propagate_merged_present(session, session.actual_start, actor=request.user)` call in one `with transaction.atomic():` block (pass the anchor's just-set actual_start so siblings share it). Do NOT create CheckerValidation rows for siblings (D-09). Author web/tests_merge_checker.py as a `TestCase` using make_merge_fixture's online pair; assign online-duty + ownership so the online re-gate passes, POST/drive the online Verify, and assert every behavior-block bullet including the negative `verified_by_checker is False` on the filled sibling.
  </action>
  <verify>
    <automated>python manage.py test web.tests_merge_checker</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test web.tests_merge_checker` exits 0.
    - A test asserts the online sibling ends ACTIVE + MERGED + shared actual_start + one session.merged_present AuditLog.
    - A test asserts the merge-filled sibling has NO CheckerValidation and verified_by_checker is False (CHK-04 not inflated).
    - A test asserts the online siblings merge via course_code with teams_link left blank (no teams_link dependency).
  </acceptance_criteria>
  <done>One online Verify covers the online merged siblings (present-filled, audited, not checker-verified) in one transaction; green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate ABSENT on online Flag-not-present; F2F stays record-only</name>
  <read_first>
    - web/checker.py (_apply_action FLAG_NOT_PRESENT online branch lines 286-288 — anchor -> ABSENT authoritatively; and the record-only comment at lines 264-266 governing F2F)
    - scheduling/merge.py (propagate_merged_absent — SCHEDULED->ABSENT group fill with session.merged_absent audit)
    - .planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-CONTEXT.md (Post-Research Clarification #2 — F2F flag record-only, online flag propagates ABSENT immediately)
  </read_first>
  <files>web/checker.py, web/tests_merge_checker.py</files>
  <behavior>
    - An online Flag-not-present on one online sibling sets it ABSENT (as today) AND marks the online merged siblings ABSENT (each with a session.merged_absent AuditLog), in one transaction (D-07 online).
    - An F2F Checker Flag-not-present writes the CheckerValidation + notifies IFO/HR but does NOT change the merged group's status (record-only, unchanged); F2F siblings stay SCHEDULED, left to the sweep (D-08).
    - The absent propagation respects the status guard: an already-ACTIVE sibling is not forced ABSENT.
  </behavior>
  <action>
    In `_apply_action`, inside the existing online FLAG_NOT_PRESENT branch, wrap the anchor `session.save(...)` (status=ABSENT) and a new `propagate_merged_absent(session, actor=request.user)` call in one `with transaction.atomic():` block. Do NOT add any status write to the F2F path — leave the record-only behavior at lines 264-266 exactly as-is (the F2F none-held case is handled by the unchanged sweep in Plan 04). Extend web/tests_merge_checker.py with: an online Flag-not-present case asserting the whole online group goes ABSENT with session.merged_absent audits; and an F2F Flag-not-present case asserting the merged F2F group's status is unchanged (still SCHEDULED) after the flag (record-only).
  </action>
  <verify>
    <automated>python manage.py test web.tests_merge_checker</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py test web.tests_merge_checker` exits 0.
    - A test asserts an online Flag-not-present marks the whole online merged group ABSENT (siblings + anchor) with session.merged_absent audits.
    - A test asserts an F2F Flag-not-present leaves the merged F2F group SCHEDULED (record-only; no status override).
    - A test asserts propagate_merged_absent does not force an already-ACTIVE sibling ABSENT (status guard).
  </acceptance_criteria>
  <done>Online not-present fails the online group immediately while F2F stays record-only; status guard respected; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checker client -> /checker/action (online) | The POST names session_id only; ownership, on-duty, and the merged set are ALL re-derived server-side (never client-asserted). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-05 | Tampering | web/checker.py:_online_action fan-out | high | mitigate | Merge-fill runs ONLY after the existing online re-gate (ownership + on-duty + actionable) and the atomic cache.add idempotency pass; the merged set is server-derived via D-01 (faculty-scoped), so a forged/replayed POST cannot fan ABSENT/ACTIVE to a foreign group. Test asserts an off-duty/foreign online action writes nothing. |
| T-04.2-01 | Elevation of Privilege | propagate_* faculty scoping | high | mitigate | Same faculty_id scoping as the scan seam; a Checker action can only affect the online session's own faculty siblings. |
| T-04.2-04b | Tampering (integrity) | CHK-04 coverage | medium | mitigate | Siblings get NO CheckerValidation (D-09) so verified_by_checker coverage stays honest; asserted by test. |
| T-04.2-SC | Tampering | package installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
- `python manage.py test web.tests_merge_checker` green.
- `python manage.py test web scheduling verification` green (no regression to CHK online/F2F suites).
- F2F flag behavior is provably unchanged (record-only); only online Verify/Flag propagate status.
</verification>

<success_criteria>
One Checker online Verify covers the online merged siblings present (not checker-verified), one online Flag-not-present fails the whole online group ABSENT, and the F2F flag path remains record-only — all per-modality, in single transactions, without a teams_link merge key.
</success_criteria>

<output>
Create `.planning/phases/04.2-co-scheduled-session-attendance-inserted/04.2-03-SUMMARY.md` when done.
</output>
