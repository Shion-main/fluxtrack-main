# Phase 04.2: Co-Scheduled Session Attendance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-07
**Phase:** 04.2-co-scheduled-session-attendance-inserted
**Areas discussed:** Sibling/merge definition, Fix mechanism, Verification path, Record & integrity

---

## Sibling / Merge Definition — Time match

| Option | Description | Selected |
|--------|-------------|----------|
| Any time overlap | Any overlap makes them siblings; handles staggered blocks | |
| Exact same start only | Siblings share the same scheduled_start + date | ✓ |

**User's choice:** Exact same start only.
**Notes:** Matches the real GARAY case (both M/W 3:45). Staggered overlaps not expected.

## Sibling / Merge Definition — Scope (revised mid-discussion)

| Option | Description | Selected |
|--------|-------------|----------|
| Co-location-plausible only | Divisible halves + shared online link; flag others | |
| Any same-time sessions | Every same-faculty same-time pair (initial pick) | (superseded) |
| Same fac + time + (room OR course) | Merge key from user's domain correction | ✓ |

**User's choice:** Same faculty + same start time + (same room OR same course code).
**Notes:** User corrected the framing — these are **merged sections** (one class taught
to 2+ sections together), not generic "co-scheduled" sessions. The merge is detectable
by same room OR same course code. This replaced the initially-picked looser "any
same-time" scope and resolved the physical-integrity concern (it's genuinely one class,
so one presence legitimately covers all rows).

## Fix Mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| A — activate the whole group | One event marks all rows ACTIVE; sweep skips; reporting=held | ✓ |
| B — sweep exemption only | Siblings stay SCHEDULED; reporting can't classify them | |
| C — manual correction | Checker/IFO fixes false absents by hand | |

**User's choice:** A — activate the whole group.
**Notes:** Cleanest for reporting; one rule, no sweep special-case on the present path.

## Verification Path — Triggers

| Option | Description | Selected |
|--------|-------------|----------|
| Any presence event | Faculty check-in AND Checker Verify (incl. online Teams link) | ✓ |
| Only faculty check-in | Checker Verify marks only the scanned row | |

**User's choice:** Any presence event.
**Notes:** Group-activation fires wherever a session first becomes present, online included.

## Verification Path — Not-present

| Option | Description | Selected |
|--------|-------------|----------|
| Only that session | Not-present affects only the scanned row | |
| Fail all merged rows | Not-present fails the whole merged group | ✓ |

**User's choice:** Fail all merged rows.
**Notes:** Reframed after the merge correction — since it's genuinely one class, a
not-present means none of the rows are held, so all go Absent. (This differed from my
initial lean of "only that session," which was based on the looser any-same-time scope.)

## Record & Integrity

| Option | Description | Selected |
|--------|-------------|----------|
| Distinct 'merged' marker + audit | Scanned row keeps real method; merged rows = MERGED + AuditLog | ✓ |
| Identical to scanned row | Copy same method/time to all rows | |

**User's choice:** Distinct 'merged' marker + audit.
**Notes:** New `CheckinMethod = MERGED`, shared actual_start, AuditLog `merged_from`.

---

## Claude's Discretion

- Where the pure merge-sibling detector lives + its tests (follow `is_no_show_past_grace` pattern).
- Transaction ordering and idempotency-guard reuse.
- One AuditLog per row vs one per group for absence propagation.

## Deferred Ideas

- Staggered/overlapping-time merges and mixed-modality-within-one-merge — not handled;
  revisit if such data surfaces.
- Two genuinely-separate rooms + different courses at one time = scheduling conflict,
  not a merge; out of scope.
