---
phase: 06-reporting-engine-reporting-surfaces
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - ops/reports.py
  - scheduling/management/commands/runscheduler.py
  - scheduling/management/commands/generate_weekly_report.py
  - ops/tests_reports.py
autonomous: true
requirements: [RPT-02]
must_haves:
  truths:
    - "generate_weekly_report(week_start, week_end, department) writes CSV + PDF via default_storage and upserts a WeeklyReport row, idempotently (re-running the same week/department never creates a second row) (RPT-02)."
    - "The existing JOB-03 stub _job_weekly_report() is filled to generate the prior week's reports per department and returns the count generated — NO second scheduler and NO new job is added (ENV-04)."
    - "On generation, IFO and only the relevant department's Dean(s) are notified through the single notify() write path with type WEEKLY_REPORT_READY (RPT-02, NOTIF-00)."
    - "An on-demand `generate_weekly_report` management command produces the same reports on demand, ASCII-only output."
  artifacts:
    - "ops/reports.py: generate_weekly_report(), notify_report_ready(), report_week_bounds()."
    - "Filled scheduling/management/commands/runscheduler.py::_job_weekly_report."
    - "scheduling/management/commands/generate_weekly_report.py (on-demand CLI)."
    - "ops/tests_reports.py: IdempotencyTests, NotifyTargetingTests, WeekBoundaryTests, JobFillTests."
  key_links:
    - "Report file storage paths are built from department.code + week_start ONLY (server-controlled) via default_storage.save — never from request input (path-traversal control)."
    - "Idempotency rides WeeklyReport.unique_together(week_start, department) via get_or_create; on-demand regeneration overwrites the existing row's csv_path/pdf_path."
    - "The SchedulerWiringTests invariant (exactly 4 jobs: materialize/sweep/weekly_report/push_outbox) MUST still pass — the stub body is filled, the job set is unchanged."
  prohibitions:
    - "MUST NOT add a second APScheduler job or a second scheduler process (ENV-04 / SchedulerWiringTests)."
    - "MUST NOT create the Notification row directly — only notify() may (NOTIF-00)."
    - "MUST NOT filter the week by scheduled_start (UTC) — use Session.date local bounds (Pitfall 1)."
    - "MUST NOT write report files to a hardcoded path or a request-derived path — use default_storage with a server-built name."
---

<objective>
Fill the reserved JOB-03 slot: generate the weekly consolidated report per department (idempotent, stored via default_storage), notify IFO + the relevant Dean(s), and expose an on-demand management command — all reusing the aggregate + render layers.

Purpose: RPT-02 is the auto-weekly + on-demand generation, storage, and notification path. The scheduler slot already exists (_job_weekly_report stub, Mon 06:00) and the WeeklyReport model + WEEKLY_REPORT_READY push contract are pre-wired; this plan only supplies the body without disturbing the ENV-04 one-scheduler invariant.
Output: ops/reports.py service, filled JOB-03, on-demand command, tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@scheduling/reporting.py
@scheduling/report_render.py
@ops/models.py
@ops/notify.py
@ops/notifications.py
@scheduling/management/commands/runscheduler.py
@accounts/models.py
</context>

<assumptions>
Locked (RESEARCH Assumptions + Open Questions; no CONTEXT.md):
- A4: report files are stored via default_storage to MEDIA_ROOT now; the S3 swap is Phase 8 config-only (the WeeklyReport docstring says "S3" but STORAGES is FileSystemStorage today — that is the intended roadmap).
- A7 / Open Q2: JOB-03 generates one WeeklyReport row per department that has sessions in the prior week, PLUS one ALL roll-up row (department=None) for IFO. The nullable department + unique_together(week_start, department) supports both.
- RPT-02 "on demand": satisfied by the `generate_weekly_report` management command (mirrors the existing _job_materialize -> command indirection), reusing the same generate_weekly_report service the job uses. A UI "generate now" button is not required for RPT-02 acceptance.
- Week bounds: reporting_week_start policy = "monday"; JOB-03 (Mon 06:00) generates the PRIOR Mon-Sun computed in Asia/Manila local dates, filtered on Session.date.
- notify() targets IFO via role=Role.IFO_ADMIN and each Dean via users=<department-filtered active Dean queryset> (there is no per-department role fan-out).
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Weekly report generation service + report-ready notify (RPT-02)</name>
  <files>ops/reports.py, ops/tests_reports.py</files>
  <read_first>
    - scheduling/reporting.py (faculty_attendance -> FacultyRow list, the input to the renderers)
    - scheduling/report_render.py (build_csv, build_pdf -> bytes)
    - ops/models.py (WeeklyReport: week_start, department, csv_path, pdf_path, unique_together(week_start, department))
    - ops/notify.py (notify(*, type, title, body, link, role=None, users=None))
    - ops/notifications.py (WEEKLY_REPORT_READY constant; it is already in CATEGORY_TYPES REPORTS + PUSH_TYPES)
    - accounts/models.py (Role.IFO_ADMIN, Role.DEAN, User.department, Department.code)
    - config/settings.py (STORAGES default = FileSystemStorage; MEDIA_ROOT; TIME_ZONE Asia/Manila; reporting_week_start policy)
    - .planning/phases/06-reporting-engine-reporting-surfaces/06-RESEARCH.md (Code Examples: generate_weekly_report + notify_report_ready patterns)
  </read_first>
  <action>
    Create ops/reports.py with a module docstring citing RPT-02/NOTIF-00. Implement report_week_bounds(reference_date) returning the (monday, sunday) local dates of the week containing (or prior to) reference_date per the reporting_week_start policy via get_policy — pure, date in / dates out. Implement generate_weekly_report(week_start, week_end, department): call faculty_attendance(start=week_start, end=week_end, department=department) to get rows; build csv/pdf bytes via build_csv / build_pdf; upsert with WeeklyReport.objects.get_or_create(week_start=week_start, department=department) (idempotency key); build the storage names from department.code (or "ALL" when None) + week_start ONLY — never from request input — and save each via default_storage.save(name, ContentFile(bytes)); store the returned names in csv_path/pdf_path and save(update_fields=[...]); return the WeeklyReport. Implement notify_report_ready(department, week_start, link): notify(role=Role.IFO_ADMIN, type=WEEKLY_REPORT_READY, title="Weekly attendance report ready", body="{DEPT} · week of {week_start}", link=link) for IFO, and for a non-None department also notify(users=<active Deans filtered to that department>, type=WEEKLY_REPORT_READY, ...) so each Dean gets only their department's report. Do not create Notification rows directly.

    Create ops/tests_reports.py with IdempotencyTests (generate twice for the same week+department -> exactly one WeeklyReport row; csv_path/pdf_path repopulated), NotifyTargetingTests (generating a department report notifies IFO + that department's Dean, and NOT a Dean of another department), and WeekBoundaryTests (report_week_bounds returns Monday..Sunday local dates; a Session on the Sunday local date is included while the following Monday is excluded). Seed with make_reporting_fixture; use the test MEDIA_ROOT (default_storage) — no real filesystem assumptions beyond a saved name.
  </action>
  <verify>
    <automated>py -3.12 manage.py test ops.tests_reports.IdempotencyTests ops.tests_reports.NotifyTargetingTests ops.tests_reports.WeekBoundaryTests -v1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def generate_weekly_report\|def notify_report_ready\|def report_week_bounds" ops/reports.py` all present.
    - `grep -n "get_or_create" ops/reports.py` present (idempotency via unique_together).
    - `grep -n "default_storage" ops/reports.py` present; `grep -n "Notification.objects.create" ops/reports.py` returns nothing (only notify()).
    - IdempotencyTests + NotifyTargetingTests + WeekBoundaryTests green.
  </acceptance_criteria>
  <done>generate_weekly_report idempotently stores CSV+PDF via default_storage and notify_report_ready fans out to IFO + the relevant Dean via notify(); tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Fill JOB-03 stub + on-demand management command, preserving the 4-job invariant (RPT-02/ENV-04)</name>
  <files>scheduling/management/commands/runscheduler.py, scheduling/management/commands/generate_weekly_report.py, ops/tests_reports.py</files>
  <read_first>
    - scheduling/management/commands/runscheduler.py (_job_weekly_report stub returning 0; _job_materialize -> call_command indirection pattern; build_scheduler wires exactly 4 jobs)
    - scheduling/tests.py (SchedulerWiringTests: build_scheduler get_jobs() == {materialize, sweep, weekly_report, push_outbox} — MUST still pass)
    - ops/reports.py (generate_weekly_report, notify_report_ready, report_week_bounds from Task 1)
    - accounts/models.py (Department to iterate; Role.DEAN)
    - .planning/codebase/CONVENTIONS.md (management command output ASCII-only; self.style; -> arrows)
  </read_first>
  <behavior>
    - _job_weekly_report() computes the prior Mon-Sun (Asia/Manila localdate via report_week_bounds), generates a report for each Department that has sessions that week plus an ALL roll-up, notifies via notify_report_ready, and returns the count of reports generated (so JobRun.rows_affected is meaningful).
    - build_scheduler() still registers exactly the same 4 job ids — no job added or removed.
    - `manage.py generate_weekly_report` generates the same prior-week reports on demand and prints an ASCII summary line; an optional --week YYYY-MM-DD selects a different week's Monday.
  </behavior>
  <action>
    Replace the body of _job_weekly_report() in runscheduler.py: compute the prior week bounds via report_week_bounds(timezone.localdate() minus one week) (or the previous completed week), iterate Departments that have sessions in that range, call generate_weekly_report + notify_report_ready for each, add one ALL (department=None) roll-up, and return the number of reports generated. Keep everything else in build_scheduler unchanged (still 4 jobs; the weekly_report job still wraps _job_weekly_report in run_job on the Mon 06:00 CronTrigger). Import generate_weekly_report/notify_report_ready/report_week_bounds from ops.reports at module top or inside the function per existing import style.

    Create scheduling/management/commands/generate_weekly_report.py: a BaseCommand whose handle() accepts an optional --week (a Monday date) argument, computes bounds via report_week_bounds, runs the same per-department + ALL generation, and writes an ASCII-only success summary (self.style.SUCCESS, "->" arrows). Reuse the shared service — do not duplicate generation logic.

    Append JobFillTests to ops/tests_reports.py: calling _job_weekly_report() with seeded prior-week sessions returns a positive count and creates WeeklyReport rows; a second call is idempotent (row count unchanged). Also assert build_scheduler().get_jobs() ids still equal the 4-job set (guarding ENV-04) — or rely on the existing SchedulerWiringTests and add a comment; prefer an explicit assertion here.
  </action>
  <verify>
    <automated>py -3.12 manage.py test ops.tests_reports scheduling.tests.SchedulerWiringTests -v1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _job_weekly_report" scheduling/management/commands/runscheduler.py` present and its body calls generate_weekly_report (no longer returns a bare 0 stub).
    - scheduling/management/commands/generate_weekly_report.py exists and reuses ops.reports.generate_weekly_report.
    - SchedulerWiringTests still green (exactly 4 jobs: materialize/sweep/weekly_report/push_outbox).
    - JobFillTests green (positive count + idempotent re-run).
  </acceptance_criteria>
  <done>JOB-03 generates + stores + notifies the prior week's per-department reports and returns a count; an on-demand command does the same; the 4-job scheduler invariant is intact.</done>
</task>

</tasks>

<artifacts>
New/edited symbols:
- ops/reports.py: report_week_bounds(), generate_weekly_report(), notify_report_ready().
- scheduling/management/commands/runscheduler.py: _job_weekly_report body filled (job set unchanged).
- scheduling/management/commands/generate_weekly_report.py: on-demand CLI.
- ops/tests_reports.py: IdempotencyTests, NotifyTargetingTests, WeekBoundaryTests, JobFillTests.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| generated report bytes -> storage | Report file paths must be server-controlled, not derived from any request input |
| generation event -> notification recipients | A Dean must be notified about only their own department's report |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | default_storage.save path | medium | mitigate | Storage names built from department.code + week_start only (server-controlled); never from request input, so no path traversal. |
| T-06-06 | Information Disclosure | notify_report_ready targeting | medium | mitigate | Dean recipients are filtered to the report's department; a Dean never receives another department's report notification (tested by NotifyTargetingTests). |
| T-06-12 | Denial of Service | JOB-03 double-fire | medium | mitigate | ENV-04 one-scheduler invariant preserved (no second job/scheduler); WeeklyReport.unique_together + get_or_create make re-runs idempotent (misfire coalesce cannot duplicate). |
</threat_model>

<verification>
- `py -3.12 manage.py test ops.tests_reports scheduling.tests.SchedulerWiringTests -v1` fully green.
- Idempotent generation; Dean-scoped notification; local-date week bounds; server-built storage paths; exactly 4 scheduler jobs.
</verification>

<success_criteria>
RPT-02 is satisfied: the weekly report auto-generates per department (Mon 06:00 JOB-03) and on demand (management command), is stored idempotently via default_storage, and notifies IFO + the relevant Dean(s) through notify() — without adding a second scheduler.
</success_criteria>

<output>
Create `.planning/phases/06-reporting-engine-reporting-surfaces/06-05-SUMMARY.md` when done.
</output>
