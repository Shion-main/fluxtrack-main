---
phase: 06-reporting-engine-reporting-surfaces
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - scheduling/report_render.py
  - scheduling/tests_report_render.py
autonomous: true
requirements: [RPT-03]
must_haves:
  truths:
    - "build_csv(rows) returns bytes with a correct header row and one row per FacultyRow, quoted/escaped by the stdlib csv module (RPT-03)."
    - "build_pdf(rows, week_start, department) returns non-empty PDF bytes (starts with %PDF) rendered by ReportLab Platypus with a repeating header row (RPT-03)."
    - "csv_safe() neutralizes leading = + - @ tab CR in any text cell so a faculty name cannot become an Excel formula (CSV-injection control)."
  artifacts:
    - "scheduling/report_render.py with csv_safe(), build_csv(), build_pdf()."
    - "scheduling/tests_report_render.py: CsvBuildTests, CsvInjectionTests, PdfBuildTests."
  key_links:
    - "csv_safe() is the single CSV-injection neutralizer imported by BOTH the weekly-report CSV (06-05) and the HR payroll CSV (06-07) so the two exports can never disagree."
    - "build_csv/build_pdf take FacultyRow dataclasses from scheduling/reporting.py and return bytes — no ORM access, no storage, no HttpResponse (that is the caller's job)."
  prohibitions:
    - "MUST NOT import WeasyPrint or window.print — the PDF is server-side ReportLab bytes."
    - "MUST NOT hand-roll CSV quoting with string joins — use csv.writer."
    - "MUST NOT write files or touch default_storage here (render is pure bytes-in/bytes-out)."
---

<objective>
Build the CSV and PDF render layer that turns FacultyRow aggregates into downloadable bytes, with the CSV-injection neutralizer shared by every export in the phase.

Purpose: RPT-03 requires the weekly consolidated report exportable as CSV and printable PDF. This layer is pure (dataclasses in, bytes out) so both the stored weekly report (06-05) and ad-hoc exports (06-06) reuse it, and the payroll CSV (06-07) reuses the same injection guard.
Output: scheduling/report_render.py + tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-RESEARCH.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@scheduling/reporting.py
@scheduling/models.py
</context>

<assumptions>
- A3: ReportLab (Platypus Table/TableStyle) is the PDF engine; brand navy #001c43 header fill matches the app theme-color and UI-SPEC.
- CSV-injection control (RESEARCH Security Domain, OWASP): neutralize a text cell whose first character is one of = + - @ (and leading tab / carriage-return) by prefixing a single quote. This is the one net-new security control the phase introduces because the payroll CSV is opened in Excel downstream.
- Depends on 06-01 (FacultyRow dataclass) and 06-02 (reportlab importable).
</assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSV builder + shared CSV-injection neutralizer (RPT-03)</name>
  <files>scheduling/report_render.py, scheduling/tests_report_render.py</files>
  <read_first>
    - scheduling/reporting.py (FacultyRow fields: name, scheduled, held, absent, verified, attendance_pct, absences)
    - scheduling/importing.py or scheduling/xlsx.py is NOT needed — use the Python stdlib csv module
    - .planning/codebase/CONVENTIONS.md (module docstring cites the requirement ID; ASCII output)
  </read_first>
  <behavior>
    - csv_safe("=SUM(A1)") returns a string whose first character is a single quote (formula neutralized); csv_safe("Cruz, Maria") returns the value unchanged (comma is handled by csv quoting, not by the neutralizer).
    - csv_safe on a leading + - @ tab or carriage-return is likewise prefixed; a normal name is untouched.
    - build_csv([FacultyRow, ...]) returns bytes; decoded, the first line is the header (Faculty, Scheduled, Held, Absent, Attendance %, Checker-verified) and there is one line per row; a faculty name beginning with = is neutralized.
    - build_csv([]) returns just the header line (no crash).
  </behavior>
  <action>
    Create scheduling/report_render.py with a module docstring citing RPT-03. Implement csv_safe(value): coerce to str, and if it starts with any of the formula-trigger characters (equals, plus, minus, at-sign, tab, carriage-return) prefix a single quote; return the value otherwise. Implement build_csv(rows) using the stdlib csv module writing into an io.StringIO: a header row then one row per FacultyRow (name run through csv_safe; counts and a formatted attendance percent as plain values); return the encoded bytes (utf-8). Do not build an HttpResponse here — return bytes only.

    Create scheduling/tests_report_render.py with CsvBuildTests (header + one line per row; empty list yields header only) and CsvInjectionTests (a FacultyRow whose name starts with = is neutralized with a leading quote; a plain name is unchanged; a name containing a comma round-trips correctly through csv quoting).
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_report_render.CsvBuildTests scheduling.tests_report_render.CsvInjectionTests -v1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def csv_safe\|def build_csv" scheduling/report_render.py` both present.
    - `grep -n "import csv" scheduling/report_render.py` present (stdlib, not hand-rolled).
    - CsvBuildTests + CsvInjectionTests green.
  </acceptance_criteria>
  <done>build_csv emits a correct header + rows as bytes and csv_safe neutralizes formula-triggering text cells; both test classes pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF builder via ReportLab Platypus (RPT-03)</name>
  <files>scheduling/report_render.py, scheduling/tests_report_render.py</files>
  <read_first>
    - scheduling/report_render.py (Task 1 output; add build_pdf alongside build_csv)
    - .planning/phases/06-reporting-engine-reporting-surfaces/06-RESEARCH.md (Code Examples: ReportLab Platypus Table/TableStyle with navy #001c43 header, repeatRows=1, landscape A4)
    - .planning/phases/06-reporting-engine-reporting-surfaces/06-UI-SPEC.md (brand navy accent #001c43 as the PDF header color)
  </read_first>
  <behavior>
    - build_pdf(rows, week_start, department) returns bytes that start with the %PDF signature and are non-empty.
    - The document contains a title referencing the department code (or "All" when department is None) and the week, and a table with a header row + one data row per FacultyRow.
    - build_pdf([], week_start, None) still returns a valid non-empty PDF (title + empty table / "no data" line), no crash.
  </behavior>
  <action>
    Add build_pdf(rows, week_start, department) to scheduling/report_render.py using ReportLab Platypus: a SimpleDocTemplate over an io.BytesIO in landscape A4, a Paragraph title naming the department code (or "All") and the week, and a Table (header: Faculty, Scheduled, Held, Absent, Attendance %, Checker-verified; one row per FacultyRow) with repeatRows=1 and a TableStyle giving the header row the brand navy fill (HexColor #001c43) with white text, a thin grid, and zebra row backgrounds. Return buf.getvalue(). Handle the empty-rows case gracefully (render the title + a single "No data for this range." line rather than a zero-row table that errors).

    Append PdfBuildTests to scheduling/tests_report_render.py: build_pdf over a couple of FacultyRows returns bytes starting with %PDF and longer than a small threshold; build_pdf over an empty list returns a valid non-empty PDF.
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_report_render.PdfBuildTests -v1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def build_pdf" scheduling/report_render.py` present.
    - `grep -n "SimpleDocTemplate\|platypus" scheduling/report_render.py` present (ReportLab path).
    - PdfBuildTests green; the whole scheduling.tests_report_render module is green.
  </acceptance_criteria>
  <done>build_pdf returns valid %PDF bytes from FacultyRows (with a navy header table) and handles empty input; PdfBuildTests pass.</done>
</task>

</tasks>

<artifacts>
New symbols (consumed by 06-05 weekly generation, 06-06 Dean export, 06-07 HR payroll CSV):
- scheduling/report_render.py: csv_safe(value), build_csv(rows) -> bytes, build_pdf(rows, week_start, department) -> bytes.
- scheduling/tests_report_render.py: CsvBuildTests, CsvInjectionTests, PdfBuildTests.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server data -> exported CSV opened in Excel | A faculty display name is user-controlled text that Excel will evaluate as a formula if it starts with = + - @ |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | build_csv text cells | high | mitigate | csv_safe() prefixes a single quote to any text cell starting with = + - @ tab CR, so no CSV cell becomes an executable Excel formula. Applied to the faculty name in build_csv and reused by 06-07's payroll CSV. |
| T-06-09 | Denial of Service | build_pdf memory | low | accept | The weekly report is bounded (one department, one week) so an in-memory BytesIO PDF is fine; the unbounded case (HR full-term) uses streaming CSV in 06-07, not PDF. |
</threat_model>

<verification>
- `py -3.12 manage.py test scheduling.tests_report_render -v1` fully green.
- csv_safe neutralizes formula-triggering cells; build_csv uses stdlib csv; build_pdf uses ReportLab and returns %PDF bytes.
</verification>

<success_criteria>
RPT-03's CSV and PDF byte builders exist as pure functions over FacultyRow aggregates, with a shared CSV-injection neutralizer and full unit coverage.
</success_criteria>

<output>
Create `.planning/phases/06-reporting-engine-reporting-surfaces/06-03-SUMMARY.md` when done.
</output>
