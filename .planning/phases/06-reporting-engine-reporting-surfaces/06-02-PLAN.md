---
phase: 06-reporting-engine-reporting-surfaces
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: false
requirements: [RPT-03]
user_setup:
  - service: pypi (ReportLab)
    why: "Printable PDF generation for the weekly consolidated report (RPT-03)"
    dashboard_config:
      - task: "Confirm reportlab legitimacy on pypi.org before install (SUS verdict is a download-telemetry false positive)"
        location: "https://pypi.org/project/reportlab/"
must_haves:
  truths:
    - "reportlab is installed and importable under py -3.12, pinned reportlab>=4.2,<5, with Pillow (already present) as its only image dependency."
    - "The install is gated behind a blocking human legitimacy checkpoint per the package protocol before any package-manager command runs."
  artifacts:
    - "requirements.txt gains reportlab>=4.2,<5 under the Media / reports section."
  key_links:
    - "ReportLab is the pure-Python PDF path (no system libraries); WeasyPrint is rejected and xhtml2pdf is fallback only."
  prohibitions:
    - "MUST NOT install reportlab 5.0.0 unpinned (brand-new; pin <5)."
    - "MUST NOT install WeasyPrint or any GTK/Pango/Cairo system dependency."
    - "MUST NOT run the install before the legitimacy checkpoint is approved."
---

<objective>
Add ReportLab as the pure-Python PDF engine for RPT-03, gated behind the mandatory package-legitimacy checkpoint.

Purpose: The weekly consolidated report (06-03/06-05) and Dean export (06-06) render a printable PDF. RESEARCH selected ReportLab (pure-Python wheels, no system libraries, identical on Windows dev + Linux EC2) over WeasyPrint (native GTK/Pango deps, AV false-positives) and xhtml2pdf (fallback). ReportLab's SUS verdict in the legitimacy audit is a download-telemetry false positive on an ~18-year-established library, so it is kept behind a human checkpoint, not dropped.
Output: reportlab installed + pinned in requirements.txt.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-RESEARCH.md
@requirements.txt
</context>

<assumptions>
Locked from RESEARCH:
- A3: ReportLab chosen over xhtml2pdf / WeasyPrint. Pin conservatively reportlab>=4.2,<5 (avoids the ~1-month-old 5.0.0).
- Precedent: 05-02 gated pywebpush behind a blocking human legitimacy checkpoint (T-05-SC) approved after pypi.org verification. This plan mirrors that flow exactly.
</assumptions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: ReportLab package-legitimacy checkpoint (blocking, T-06-SC)</name>
  <action>Do NOT install anything yet. Present the ReportLab legitimacy verification to the operator and block on approval before any pip command runs. Surface the SUS verdict as a download-telemetry false positive (the automated seam could not read PyPI weekly downloads and 5.0.0 is recent), not a slopsquatting signal, and the intended conservative pin (>=4.2,<5). This checkpoint is never auto-approved regardless of auto_advance.</action>
  <what-built>Nothing installed yet. This is the package-legitimacy gate that must be approved before ReportLab is installed (T-06-SC).</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/reportlab/ and confirm: the project is published by the ReportLab maintainers, has a version history going back many years (2.x era), and the current 4.4.x / 5.0.0 releases are recent and dated.
    2. Confirm the project home is reportlab.com (official).
    3. Note that the audit verdict is SUS ONLY because the automated seam could not read PyPI weekly-download telemetry (`unknown-downloads`) and because 5.0.0 is new — not because of any slopsquatting or malicious signal.
    4. Confirm the intended pin is reportlab>=4.2,<5 (mature 4-series, avoids the brand-new 5.0.0).
    5. Confirm Pillow (a ReportLab dependency) is already present in requirements.txt (Pillow>=10.0) so no extra system libraries are pulled.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the reportlab install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install reportlab and pin it in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (the "Media / reports" section already holds qrcode + Pillow + pypandoc_binary — add reportlab there in the same style with a one-line comment citing RPT-03)
  </read_first>
  <action>
    After the checkpoint is approved, install ReportLab into the py -3.12 environment with pip using the pin reportlab>=4.2,<5. Add a line `reportlab>=4.2,<5   # printable weekly-report PDF (RPT-03); pure-Python, no system libs` under the "# Media / reports" section of requirements.txt (next to Pillow). Do NOT bump or touch any other pin. Confirm importability and that Pillow is satisfied (already installed).
  </action>
  <verify>
    <automated>py -3.12 -c "import reportlab; from reportlab.platypus import Table, SimpleDocTemplate; print('reportlab', reportlab.Version)"</automated>
  </verify>
  <acceptance_criteria>
    - The import command prints a reportlab version in the 4.2.x-4.4.x range (<5).
    - `grep -n "reportlab" requirements.txt` shows the pin `>=4.2,<5` under Media / reports.
    - No new WeasyPrint / GTK / Pango / Cairo entry appears anywhere.
  </acceptance_criteria>
  <done>reportlab (>=4.2,<5) imports under py -3.12 and is pinned in requirements.txt; no system-library dependency was introduced.</done>
</task>

</tasks>

<artifacts>
- requirements.txt: reportlab>=4.2,<5 (Media / reports).
- Environment: reportlab importable (reportlab.platypus.Table, SimpleDocTemplate) for 06-03's PDF builder.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PyPI -> dev/prod environment | An installed package runs with full process privilege; a slopsquatted/malicious package is a supply-chain compromise |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | reportlab pip install | high | mitigate | Blocking human legitimacy checkpoint verifies reportlab on pypi.org (official reportlab.com, long history) before install; SUS verdict confirmed as a telemetry false positive; pinned >=4.2,<5. Never auto-approved. |
| T-06-SC2 | Tampering | transitive deps | low | accept | ReportLab's only image dep is Pillow, already vetted/installed in a prior phase; pin range avoids the brand-new 5.0.0. |
</threat_model>

<verification>
- Legitimacy checkpoint approved by the operator before install.
- `py -3.12 -c "import reportlab; ..."` succeeds and reports a <5 version.
- requirements.txt pins reportlab>=4.2,<5; no system-library packages added.
</verification>

<success_criteria>
ReportLab is installed, importable under py -3.12, pinned conservatively, and gated behind an approved legitimacy checkpoint — RPT-03's PDF engine is available for 06-03.
</success_criteria>

<output>
Create `.planning/phases/06-reporting-engine-reporting-surfaces/06-02-SUMMARY.md` when done.
</output>
