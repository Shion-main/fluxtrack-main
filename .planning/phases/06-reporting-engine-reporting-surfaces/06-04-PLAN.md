---
phase: 06-reporting-engine-reporting-surfaces
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - web/ifo.py
  - web/urls.py
  - web/views.py
  - templates/ifo/dashboard.html
  - templates/ifo/_cards.html
  - templates/reports/scorecard.html
  - templates/reports/_error_card.html
  - web/tests_reporting.py
autonomous: true
requirements: [IFO-09, RPT-04, RPT-05]
must_haves:
  truths:
    - "IFO sees an unscoped dashboard of summary cards (Faculty, Sessions scheduled/held, Absences, Attendance %) over a selectable date range, behind ifo_required (IFO-09)."
    - "Each card is computed through safe_card() so one raising aggregate shows its own inline error card while the rest of the page renders (RPT-05)."
    - "A faculty name in the dashboard drills down to a full-page scorecard (early-ends + effective-modality breakdown) reusing the shared aggregate (RPT-04, IFO-09 drill-down)."
    - "An invalid date range degrades to a friendly empty result, never a 500."
  artifacts:
    - "web/ifo.py: dashboard() + scorecard() views behind ifo_required."
    - "templates/ifo/dashboard.html, templates/ifo/_cards.html; shared templates/reports/scorecard.html + templates/reports/_error_card.html."
    - "web/urls.py routes /ifo/dashboard and /ifo/scorecard/<int:faculty_id>; web/views.py IFO 'Reports' nav href updated off '#'."
    - "web/tests_reporting.py: IfoDashboardTests, ScorecardDrilldownTests, CardIsolationViewTests, FilterValidationTests."
  key_links:
    - "The dashboard passes per-card (value, error) pairs from safe_card into templates; _error_card.html renders GENERIC copy only (never the raw exception)."
    - "The shared scorecard.html + _error_card.html partials are created here and reused by the Dean surface (06-06)."
  prohibitions:
    - "MUST NOT continuously poll the dashboard — it is point-in-time, refreshed on filter Apply (assumption A-POLL)."
    - "MUST NOT render the raw exception text in the error card (V7 info-disclosure)."
    - "MUST NOT add any write/mutation endpoint — the dashboard is read-only."
---

<objective>
Build the IFO-09 reporting dashboard (unscoped summary cards over a selectable range) with per-card graceful degradation and a full-page faculty scorecard drill-down, all reading the shared aggregate layer.

Purpose: IFO-09 is the IFO landing view for reporting; it proves the shared aggregates render as summary cards, the RPT-05 per-card isolation contract works end-to-end in a real view, and the RPT-04 scorecard drill-down is reachable. It also establishes the shared scorecard + error-card templates the Dean surface reuses.
Output: IFO dashboard + scorecard views, templates, routes, nav link, and view-level tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-RESEARCH.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@scheduling/reporting.py
@web/ifo.py
@web/urls.py
@web/views.py
@templates/base.html
@templates/ifo/rooms.html
@templates/dean/queue.html
</context>

<assumptions>
Locked from UI-SPEC + RESEARCH (planner-confirmed; no CONTEXT.md):
- A-POLL: reporting dashboards are point-in-time (refresh on filter Apply via a GET form / optional htmx swap), NOT continuously polled like ifo/live.html.
- A-DRILL / A8: the faculty scorecard drill-down opens as a FULL PAGE (consistent with every other detail view), not a modal.
- A-COLOR: attendance-% badge thresholds are >=90 green, 75-89 amber, <75 red, rendered with the existing --green/--amber/--red tokens from faculty.css.
- A-TABLE: the per-faculty report list is a dense uk-table.
- RESEARCH A5: default dashboard range = the current reporting week (reporting_week_start policy); the view clamps the denominator with as_of=today so future sessions do not lower attendance %.
- Default range when no GET params: current reporting week (Mon-today). Invalid dates fall back to that default with a friendly note (mirror ifo.assignment_create validation).
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: IFO-09 dashboard view + KPI cards + Pattern A filter + Pattern E error card (IFO-09/RPT-05)</name>
  <files>web/ifo.py, web/urls.py, web/views.py, templates/ifo/dashboard.html, templates/ifo/_cards.html, templates/reports/_error_card.html</files>
  <read_first>
    - web/ifo.py (ifo_required decorator; _today_sessions; get_policy usage; validated-GET/parse_date idiom in assignment_create)
    - scheduling/reporting.py (dept_summary, faculty_attendance, safe_card, dataclasses)
    - templates/ifo/rooms.html (uk-card grid idiom, grid gap-4 sm:grid-cols-2 lg:grid-cols-3, {% empty %} block)
    - templates/base.html (standard Franken shell the dashboard extends; header)
    - web/urls.py (append routes) and web/views.py (SURFACES dict: Role.IFO_ADMIN "Reports" href is currently "#")
    - .planning/phases/06-reporting-engine-reporting-surfaces/06-UI-SPEC.md (Pattern A date-range selector, Pattern B KPI card, Pattern E error card markup contract, copywriting)
  </read_first>
  <action>
    In web/ifo.py add a dashboard(request) view behind ifo_required. Parse optional `from`/`to` GET params with django.utils.dateparse.parse_date; when absent or invalid, default to the current reporting week (Monday of this week via the reporting_week_start policy through get_policy, through today) and set a friendly note on invalid input rather than raising. Compute cards through safe_card: summary = safe_card(dept_summary, start=..., end=..., department=None, as_of=timezone.localdate()); rows = safe_card(faculty_attendance, start=..., end=..., department=None, as_of=timezone.localdate()). Pass the (value, error) pairs plus the active range into the template context (unscoped — IFO sees all departments, IFO-09). Render templates/ifo/dashboard.html.

    Create templates/reports/_error_card.html as the shared RPT-05 error partial following the UI-SPEC Pattern E markup contract: a uk-card uk-card-body with a triangle-alert icon, the heading "Couldn't load this section", and the generic body copy — GENERIC only, it must never receive or print the raw exception. Create templates/ifo/_cards.html rendering the four KPI cards (Faculty, Sessions scheduled/held, Absences, Attendance %) in the grid gap-4 sm:grid-cols-2 lg:grid-cols-3 idiom, and the per-faculty uk-table (Pattern B / A-TABLE); each card/section checks its (value, error) pair and includes _error_card.html on error else renders the value. Attendance-% pill uses the A-COLOR thresholds via the --green/--amber/--red tokens. Create templates/ifo/dashboard.html extending base.html with the Pattern A GET filter bar (From/To date inputs + Apply + Reset) and a results panel that includes _cards.html.

    Wire web/urls.py: path("ifo/dashboard", ifo.dashboard, name="ifo_dashboard"). Update web/views.py SURFACES Role.IFO_ADMIN "Reports" card href from "#" to "/ifo/dashboard".
  </action>
  <verify>
    <automated>py -3.12 manage.py test web.tests_reporting.IfoDashboardTests web.tests_reporting.FilterValidationTests -v1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def dashboard" web/ifo.py` present and decorated with ifo_required.
    - `grep -n "ifo_dashboard" web/urls.py` present.
    - `grep -n "/ifo/dashboard" web/views.py` present (nav href updated off '#').
    - templates/reports/_error_card.html and templates/ifo/dashboard.html + _cards.html exist.
    - IfoDashboardTests + FilterValidationTests green (defined in Task 3).
  </acceptance_criteria>
  <done>An ifo_required dashboard renders unscoped KPI cards + a per-faculty table over a selectable range via safe_card pairs; invalid dates degrade to the default range with a note; nav points at it.</done>
</task>

<task type="auto">
  <name>Task 2: Faculty scorecard drill-down (full page) reusing the shared aggregate (RPT-04)</name>
  <files>web/ifo.py, web/urls.py, templates/reports/scorecard.html, templates/ifo/_cards.html</files>
  <read_first>
    - scheduling/reporting.py (faculty_scorecard, Scorecard dataclass: early_ends, modality_breakdown, absences)
    - web/ifo.py (Task 1 dashboard; ifo_required; get_object_or_404 idiom from room_detail)
    - templates/ifo/rooms.html + templates/dean/queue.html (back-nav icon button with aria-label="Back"; container widths)
    - .planning/phases/06-reporting-engine-reporting-surfaces/06-UI-SPEC.md (Pattern D drill-down link + chevron-right; scorecard header copy "Attendance scorecard — {Faculty name}"; max-w-4xl)
  </read_first>
  <action>
    In web/ifo.py add scorecard(request, faculty_id) behind ifo_required: fetch the faculty User with get_object_or_404 (IFO is unscoped, any faculty allowed), read the same from/to range (default current reporting week), call safe_card(faculty_scorecard, faculty=..., start=..., end=..., as_of=today), and render the shared templates/reports/scorecard.html. Create templates/reports/scorecard.html extending base.html: a back button (aria-label="Back"), the "Attendance scorecard — {name}" header, KPI cards (scheduled vs held, attendance %, absences, early-ends), the effective-modality breakdown, and the itemized absence list; on the safe_card error path include _error_card.html. In templates/ifo/_cards.html make each faculty row's name a Pattern D drill-down link to the ifo scorecard route with a trailing chevron-right, carrying the current from/to as query params.

    Wire web/urls.py: path("ifo/scorecard/<int:faculty_id>", ifo.scorecard, name="ifo_scorecard").
  </action>
  <verify>
    <automated>py -3.12 manage.py test web.tests_reporting.ScorecardDrilldownTests -v1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def scorecard" web/ifo.py` present and ifo_required.
    - `grep -n "ifo_scorecard" web/urls.py` present.
    - templates/reports/scorecard.html exists and is referenced by the ifo scorecard view.
    - ScorecardDrilldownTests green.
  </acceptance_criteria>
  <done>A full-page scorecard drill-down renders early-ends + effective-modality breakdown + itemized absences from faculty_scorecard, reachable from the dashboard faculty rows.</done>
</task>

<task type="auto">
  <name>Task 3: View-level tests — dashboard render, drill-down, per-card isolation, filter validation (IFO-09/RPT-05)</name>
  <files>web/tests_reporting.py</files>
  <read_first>
    - web/tests.py + web/tests_notifications.py (Django TestCase client-login idioms, role setup, assertContains)
    - scheduling/test_support.py (make_reporting_fixture from 06-01 to seed multi-status sessions across two departments)
    - web/ifo.py (dashboard + scorecard views under test)
    - scheduling/reporting.py (safe_card — to monkeypatch/inject a raising aggregate for the isolation test)
  </read_first>
  <behavior>
    - IfoDashboardTests: an IFO_ADMIN user gets 200 on /ifo/dashboard; the four KPI captions and at least one faculty row render; a non-IFO user is refused (403).
    - ScorecardDrilldownTests: /ifo/scorecard/<faculty_id> returns 200 for an IFO user and shows the scorecard header + a modality-breakdown label; carries the from/to range.
    - CardIsolationViewTests: when one aggregate is patched to raise, the dashboard still returns 200, the failing card shows the generic "Couldn't load this section" copy, and the OTHER card/section still renders its value (RPT-05 end-to-end).
    - FilterValidationTests: an invalid `from`/`to` GET value returns 200 with the default range and a friendly note, never a 500.
  </behavior>
  <action>
    Create web/tests_reporting.py with IfoDashboardTests, ScorecardDrilldownTests, CardIsolationViewTests, and FilterValidationTests using the Django test client. Seed via make_reporting_fixture. For CardIsolationViewTests, patch one aggregate used by the dashboard (e.g. dept_summary) to raise and assert the response is still 200, contains the generic error copy for that card, and still contains the other section's rendered value. Assert the raw exception text does NOT appear in the response (info-disclosure guard).
  </action>
  <verify>
    <automated>py -3.12 manage.py test web.tests_reporting -v1</automated>
  </verify>
  <acceptance_criteria>
    - All four test classes exist and are green.
    - CardIsolationViewTests asserts a 200 response with one card in the generic error state AND the sibling section rendered, and that the raw exception string is absent.
  </acceptance_criteria>
  <done>web.tests_reporting covers dashboard render + role gate, scorecard drill-down, end-to-end per-card isolation, and filter validation; all green.</done>
</task>

</tasks>

<artifacts>
New/edited symbols:
- web/ifo.py: dashboard(request), scorecard(request, faculty_id).
- web/urls.py: ifo_dashboard, ifo_scorecard routes.
- web/views.py: IFO 'Reports' nav href -> /ifo/dashboard.
- templates/ifo/dashboard.html, templates/ifo/_cards.html.
- templates/reports/scorecard.html, templates/reports/_error_card.html (shared; reused by 06-06).
- web/tests_reporting.py: IfoDashboardTests, ScorecardDrilldownTests, CardIsolationViewTests, FilterValidationTests.
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> IFO dashboard/scorecard | Any authenticated user could try to reach a reporting URL; only IFO_ADMIN (or superuser) may |
| aggregate exception -> rendered page | A card failure must not blank the page or leak internals |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | /ifo/dashboard, /ifo/scorecard | high | mitigate | Both views wrapped in ifo_required (login_required + Role.IFO_ADMIN, superuser bypass); a non-IFO user gets 403 (tested). |
| T-06-04 | Information Disclosure | _error_card.html | medium | mitigate | The error card renders fixed generic copy; safe_card never passes raw exception text to the template; a test asserts the exception string is absent from the response. |
| T-06-11 | Input Validation | from/to GET params | low | mitigate | parse_date-validated; invalid input falls back to the default range with a friendly note, never a 500. |
</threat_model>

<verification>
- `py -3.12 manage.py test web.tests_reporting -v1` fully green.
- ifo_required on both new views; non-IFO refused.
- Per-card isolation proven end-to-end: a raising aggregate yields a 200 with one error card + siblings rendered + no raw exception text.
</verification>

<success_criteria>
IFO-09 delivers an unscoped, selectable-range summary dashboard with RPT-05 per-card isolation and an RPT-04 full-page scorecard drill-down, all from the shared aggregates, with view-level tests green.
</success_criteria>

<output>
Create `.planning/phases/06-reporting-engine-reporting-surfaces/06-04-SUMMARY.md` when done.
</output>
