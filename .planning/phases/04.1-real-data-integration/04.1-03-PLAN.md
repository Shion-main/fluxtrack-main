---
phase: 04.1-real-data-integration
plan: 03
type: execute
wave: 2
depends_on: [04.1-01]
files_modified:
  - scheduling/management/commands/import_offerings.py
  - scheduling/tests_import_hardening.py
autonomous: true
requirements: [ENV-02]
must_haves:
  truths:
    - "import_offerings reads the real offerings .xlsx directly (default), while the .csv path + --building/--floor filter still work (ImportPathTests stays green) (D1)."
    - "Online (virtual V) and gym meetings are kept, not skipped — the old skip-V and GYM2-drop behavior is gone (D2)."
    - "Each meeting's modality is stamped by its room: physical -> course mode + scannable, virtual -> online + checker-verified; a blended course yields both (D5)."
    - "Instructors dedup to one account each — by email, then by normalized name — so the 57 blank-email rows collapse to ~10 Users, never one User per row (D7)."
    - "The 44 roomless-physical sections load against a shared 'TBA' placeholder room in the Unassigned building and are flagged; section labels are never treated as rooms (D9)."
    - "Unknown/typo/P/U/digit-only room codes land in the flagged 'Unassigned' building, never silently dropped (D4); the reconciliation report proves the 1,211-row partition."
  artifacts:
    - "import_offerings: xlsx-by-default file handling (extension dispatch)"
    - "import_offerings: prefix-map building/room creation via scheduling.importing.classify_room"
    - "import_offerings: per-meeting modality via modality_for_room"
    - "import_offerings: instructor dedup (email then normalize_name_key)"
    - "import_offerings: 'TBA' placeholder room (Unassigned building) for roomless-physical sections"
    - "import_offerings: reconciliation report consuming scheduling.importing.reconcile (four buckets + flagged lists)"
  key_links:
    - "import_offerings consumes Plan 01's classify_room / parse_meetings / modality_for_room / normalize_name_key / reconcile — one implementation, shared with the loader and the tests."
    - "Schedule.faculty is set here; materialize_sessions copies it onto Session.faculty unchanged (D8) — do not alter that path."
---

<objective>
Harden `import_offerings` so it loads the WHOLE real term from the `.xlsx`: xlsx input, prefix-mapped rooms, per-meeting modality, kept online/gym meetings, deduped instructors, roomless-TBA handling, and a reconciliation report that proves nothing was silently dropped.

Purpose: This is the core of Phase 04.1 (D1-D9) — carrying ENV-02 from the R3 slice to full-term scale. Today the importer silently drops 1,215 of ~2,021 meetings; after this it keeps ~1,100 sections / ~2,021 schedules / ~200 instructors and reports the exact 1,211-row partition.
Output: hardened `scheduling/management/commands/import_offerings.py` + tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-real-data-integration/04.1-CONTEXT.md

# The file being hardened — read it fully before editing:
@scheduling/management/commands/import_offerings.py
# Pure helpers this consumes (from Plan 01) — do NOT re-implement:
@scheduling/xlsx.py
@scheduling/importing.py
# Models written (Schedule get_or_create key; Room PROTECT; Modality) + the wiring to preserve:
@scheduling/models.py
@campus/models.py
@accounts/models.py
# The wiring that must survive (line ~145 faculty=sch.faculty, D8):
@scheduling/management/commands/materialize_sessions.py
# The regression test that MUST stay green (CSV fixture + --building R --floor 3 -> 3 schedules):
@scheduling/tests.py
</context>

<artifacts>
## Artifacts this plan produces

- `import_offerings` new/changed behavior:
  - File dispatch by extension: `.xlsx` (default `data/raw/2T-25-26-Course Offerring (1).xlsx`, sheet Sheet1) read via `scheduling.xlsx.read_grid`; `.csv` still read via `csv.reader` (preserves the r3_synthetic fixture path).
  - Rooms created via `scheduling.importing.classify_room` (prefix map) — replaces the naive `_room` first-letter/second-char derivation.
  - Kept meetings: virtual V-rooms and GYM rooms are imported (D2); the old skip-virtual and GYM2 drop are removed.
  - Per-meeting modality via `modality_for_room` (D5).
  - Instructor dedup via email then `normalize_name_key` (D7) — the 57 blank-email rows collapse to ~10 Users.
  - A shared "TBA" placeholder Room in the "Unassigned" building for the 44 roomless-physical sections (D9).
  - A reconciliation report driven by `scheduling.importing.reconcile` (four buckets + flagged Unassigned/typo rooms + email-less instructors).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: xlsx input + prefix-map rooms + per-meeting modality + online/gym + roomless-TBA + instructor dedup</name>
  <files>scheduling/management/commands/import_offerings.py, scheduling/tests_import_hardening.py</files>
  <read_first>
    - scheduling/management/commands/import_offerings.py (handle, _faculty, _room, parse_room, MEET_RE — all being replaced/generalized)
    - scheduling/importing.py (classify_room, parse_meetings, modality_for_room, normalize_name_key, MODE_MAP, DAY_MAP)
    - scheduling/xlsx.py (read_grid)
    - scheduling/tests.py lines 240-271 (ImportPathTests + R3ParityTests — the CSV + --building/--floor contract to preserve)
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md (D1-D9; D6 term-anchored-to-today; the 57-blank-email -> ~10 people, zero overlap with the 190 emailed)
  </read_first>
  <behavior>
    - Importing the real offerings xlsx (dry-run) reports ~1,100 sections, ~2,021 schedule rows, ~200 instructors — not the old 483/806/145.
    - A virtual V-room meeting produces a Schedule with modality online (kept, not skipped).
    - A GYM2 meeting produces a Schedule (kept, not dropped by a second-char-digit rule).
    - A blended course with both a physical-day and a virtual-day meeting produces one physical scannable Schedule (course mode) AND one online Schedule.
    - The 57 blank-email rows resolve to ~10 User accounts (one per normalized name), each owning multiple schedules — not 57 users.
    - A roomless-physical section (no room token / section-label token) creates Schedule rows pointing at the single shared "TBA" room in the Unassigned building.
    - CSV fixture import with --building R --floor 3 still yields exactly 3 schedules / 2 rooms / 2 faculty (ImportPathTests unchanged).
  </behavior>
  <action>
    Rewrite `import_offerings` to consume Plan 01's helpers instead of its own parsing. Dispatch the input by extension: a `.csv` path keeps the existing `csv.reader` read; anything else (default now the offerings `.xlsx`) is read via `scheduling.xlsx.read_grid(path, "Sheet1")`. Change DEFAULT_FILE to the real xlsx offerings path. Delete the local `parse_room`/`parse_time`/`MEET_RE`/`MODE_MAP`/`DAY_MAP` and import them from `scheduling.importing`. In the per-row loop, parse meetings with `parse_meetings` (optional room token), and for each meeting decide its room via the D9 rule shared with reconcile: a real room when room_raw is non-empty, classifiable, and not equal to the row's Sec; otherwise route the row's physical meetings to a single shared "TBA" placeholder Room (get_or_created in the Unassigned building, modality per course mode) and its online-mode roomless meetings to the virtual/online treatment. Build every room via a new `_room_for(room_info)` that get_or_creates Building(code=room_info.building_code, name=room_info.building_name), Floor(building, room_info.floor), and Room(code=room_info.raw_code) — mirroring the existing token/manual_code generation. Keep virtual V-rooms and GYM rooms (remove the skip-virtual branch and the GYM-dropping second-char rule, D2). Stamp each Schedule's modality with `modality_for_room(room_info, course_mode)` (D5) rather than a single course-level mode. Preserve the `--building`/`--floor` filters by matching against `room_info.prefix` / `room_info.floor` so ImportPathTests still passes. Replace `_faculty` dedup: when the row has an email, dedup by email as today; when it does not, dedup by `normalize_name_key(name)` — look up (or cache in a dict keyed by normalized name) the single User for that key so the 57 blank-email rows collapse to ~10 accounts (D7), each set role FACULTY with an unusable password. Preserve D8 exactly: still set `Schedule.faculty` and change NOTHING in materialize_sessions. Keep D6 (term window centered on today) and the "exactly one active term" logic. Keep the whole write path inside the existing `@transaction.atomic run()`. Iterate over the Python `rows` list (not a live queryset) so pyodbc HY010 is not triggered; get_or_create per row is fine.
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.ImportPathTests scheduling.tests_import_hardening.ImportHardeningTests -v 1</automated>
  </verify>
  <acceptance_criteria>
    - ImportPathTests.test_synthetic_fixture_import_and_materialize_counts still passes UNCHANGED (CSV + --building R --floor 3 -> 3 schedules / 2 rooms / 2 faculty / 3 sessions) — the regression guard for CSV + filter preservation.
    - ImportHardeningTests (skipUnless the offerings xlsx present, TransactionTestCase) assert: a V-room schedule exists with modality online; a GYM room schedule exists; a blended course yields both a physical and an online schedule; the blank-email rows produce ~10 (8-12) faculty, not ~57; the "TBA" room exists in the "Unassigned" building and owns the roomless-physical schedules.
    - `py -3.12 manage.py import_offerings --dry-run` prints ~1,100 sections and ~2,021 schedule rows (not 483/806).
    - Modality is stamped per meeting (a physical meeting of an online-mode-labelled course still scannable if physical; a virtual meeting always online) — asserted in the tests.
  </acceptance_criteria>
  <done>The importer reads the xlsx, keeps online + gym meetings, stamps per-meeting modality, dedups instructors to ~200, and routes the 44 roomless-physical sections to a flagged TBA room — while the CSV + filter regression stays green (D1-D9, D8 preserved).</done>
</task>

<task type="auto">
  <name>Task 2: reconciliation report — four-bucket identity + flagged lists</name>
  <files>scheduling/management/commands/import_offerings.py, scheduling/tests_import_hardening.py</files>
  <read_first>
    - scheduling/importing.py (reconcile, Reconciliation)
    - scheduling/management/commands/import_offerings.py (_report — being replaced by the reconciliation report)
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md (the reconciliation identity: 1211 = intact + roomless-TBA + online-no-room + no-schedule; sum meetings == 2021; flag Unassigned/typo rooms + email-less instructors)
  </read_first>
  <action>
    Replace `_report` with a reconciliation report driven by `scheduling.importing.reconcile(rows, col)`. Print: the four bucket counts (intact / roomless-TBA / online-no-room / no-schedule) with their sum, an explicit assertion line showing `sum == total_rows (1211)`, the total meeting count (target 2021), distinct rooms + distinct instructors, and the FLAGGED lists — the Unassigned/typo room codes (D4) and the email-less instructor normalized-name keys (D7, "cannot authenticate via Entra until an email is supplied"). In a real (non-dry-run) import, after the writes, ALSO print the actual DB counts (schedules created this run, rooms, faculty) next to the reconciliation targets so a human can eyeball parity. Make the report emit a clear WARNING style line whenever the identity does NOT balance or when the flagged lists are non-empty, so a silent drop can never hide. Do not raise on imbalance in the command itself (the assertion lives in the test) — the command reports; the Plan 04 run + the full-file test enforce.
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_import_hardening.ImportReportTests -v 1</automated>
  </verify>
  <acceptance_criteria>
    - ImportReportTests (skipUnless offerings xlsx present) capture the dry-run stdout and assert it contains the four bucket counts, the balanced identity line (1211), the 2021 meeting total, the flagged typo rooms (404, 516), and the ~10 email-less instructor keys.
    - The report distinguishes flagged (Unassigned/typo, email-less) items with a visible warning marker so nothing is silently dropped.
    - Report output is identical in shape for --dry-run and a real run (real run additionally shows live DB counts).
  </acceptance_criteria>
  <done>The importer prints a reconciliation report that proves the 1,211-row partition, the 2,021-meeting count, and flags every Unassigned/typo room and email-less instructor (Success Criterion 6, D4/D7/D9).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registrar offerings `.xlsx` -> mass DB writes | Untrusted spreadsheet rows become Schedule/User/Room rows across the whole term. |
| free-text Schedule cell -> room/modality resolution | Compound free-text drives which room/modality each meeting gets; a mis-parse could mis-route a class. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-05 | Tampering | import_offerings room/section parsing | medium | mitigate | Section labels (A151/C110) are never treated as rooms (Sec-equality + optional-room-token rule); roomless-physical routes to the flagged TBA room, never a wrong Admin/Academic room (D9). |
| T-04.1-06 | Information Disclosure | instructor dedup by name | low | mitigate | Blank-email rows dedup by normalized name only (never merged with the 190 emailed accounts — CONTEXT verified zero overlap); email-less accounts get an unusable password and are flagged as unable to authenticate (D7). |
| T-04.1-07 | Repudiation | silent drop of meetings | medium | mitigate | Every one of the 1,211 rows lands in exactly one reconciliation bucket and the report warns on any imbalance/flag — a dropped meeting cannot hide (D2/D9). |
| T-04.1-SC | Tampering | pip/package installs | high | mitigate | No new dependency (D1); the importer reuses stdlib + existing Django + Plan 01 modules. Nothing to install. |
</threat_model>

<verification>
- `py -3.12 manage.py test scheduling.tests.ImportPathTests scheduling.tests_import_hardening.ImportHardeningTests scheduling.tests_import_hardening.ImportReportTests -v 1` is green.
- `py -3.12 manage.py import_offerings --dry-run` prints the reconciliation report with the balanced 1,211 identity and ~2,021 meetings.
- The D8 wiring (Schedule.faculty -> materialize -> Session.faculty) is unchanged; materialize_sessions.py is not modified by this plan.
</verification>

<success_criteria>
- Reads the real xlsx directly; CSV + --building/--floor regression preserved (Success Criterion 1, D1).
- Online + blended + gym meetings loaded (Success Criterion 2, D2); ~1,100 sections / ~2,021 schedules / ~200 instructors on dry-run.
- Per-meeting modality stamped by room; blended yields both (Success Criterion 4, D5).
- Instructors deduped to ~200; ~10 email-less flagged (Success Criterion 5, D7).
- Reconciliation report proves the 1,211-row partition; Unassigned/typo rooms flagged (Success Criterion 6, D4/D9).
</success_criteria>

<output>
Create `.planning/phases/04.1-real-data-integration/04.1-03-SUMMARY.md` when done.
</output>
