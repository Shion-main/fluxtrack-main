---
phase: 04.1-real-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduling/xlsx.py
  - scheduling/importing.py
  - scheduling/tests_import.py
autonomous: true
requirements: [ENV-02]
must_haves:
  truths:
    - "A real registrar .xlsx is read into rows using only the Python standard library (zipfile + xml.etree) — no new pip dependency (D1)."
    - "The prefix map classifies every room code: R to Academic, A to Admin, GYM to Gym, V to Online, and P/U/digit-only/typo codes to Unassigned + flagged — never dropped (D4)."
    - "Floor derivation tolerates GYM2, A410-A, and digit-only codes without raising."
    - "modality_for_room stamps online for virtual V-rooms and the course mode for physical rooms (D5)."
    - "normalize_name_key merges comma/spacing/middle-initial variants so instructor rows can dedup by name (D7)."
    - "reconcile() partitions all 1,211 offering rows into exactly four buckets (intact + roomless-TBA + online-no-room + no-schedule) so nothing is silently dropped, and counts 2,021 valid meetings."
  artifacts:
    - scheduling/xlsx.py
    - scheduling/importing.py
    - scheduling/tests_import.py
  key_links:
    - "reconcile()'s four-bucket partition is the single source of truth the importer report (Plan 03) and the run/verify assertions (Plan 04) both consume."
    - "parse_meetings makes the trailing room token OPTIONAL so day/time-only meetings are captured, not silently dropped by the regex."
---

<objective>
Build the two pure, DB-free foundations every later plan depends on: a stdlib `.xlsx` reader and a module of pure parsing/classification/reconciliation helpers, both fully unit-tested.

Purpose: These are the reusable primitives (D1 xlsx reading, D4 prefix map, D5 per-meeting modality, D7 name normalization, and the reconciliation partition that proves "nothing silently dropped"). Isolating them here keeps them fast to test (no DB) and lets the room-master loader (Plan 02) and the hardened importer (Plan 03) share one implementation.
Output: `scheduling/xlsx.py`, `scheduling/importing.py`, `scheduling/tests_import.py`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-real-data-integration/04.1-CONTEXT.md

# The code being generalized — read the current parse_time / parse_room / MEET_RE / MODE_MAP:
@scheduling/management/commands/import_offerings.py
# Test conventions (SimpleTestCase, skipUnless for gitignored data files):
@scheduling/tests.py
# Enum values the helpers return (Modality f2f/blended/online, DayOfWeek 0..6):
@scheduling/models.py
</context>

<artifacts>
## Artifacts this plan produces

- `scheduling/xlsx.py` — stdlib `.xlsx` reader: `sheet_names(path) -> list[str]` and `read_grid(path, sheet=None) -> list[list[str]]` (rows of cell strings, sharedStrings resolved). No third-party dependency.
- `scheduling/importing.py` — pure helpers:
  - `PREFIX_BUILDING` — prefix → (building_code, building_name) map (D4).
  - `classify_room(code) -> RoomInfo` (fields: raw_code, prefix, building_code, building_name, floor, is_virtual, is_unassigned, is_typo, scannable).
  - `derive_floor(code) -> int` (tolerates GYM2, A410-A, digit-only).
  - `parse_time(raw) -> datetime.time | None` (moved from import_offerings).
  - `parse_meetings(schedule_str) -> list[MeetingParse]` (day, start, end, room_raw; room OPTIONAL).
  - `normalize_name_key(name) -> str` (D7 name dedup key).
  - `modality_for_room(room_info, course_mode) -> Modality` (D5).
  - `reconcile(rows, col) -> Reconciliation` (the four-bucket partition + meeting count + flagged lists).
  - `MODE_MAP`, `DAY_MAP`, `RoomInfo`, `MeetingParse`, `Reconciliation` dataclasses.
- `scheduling/tests_import.py` — SimpleTestCase parser unit tests + a full-file dry-run reconciliation assertion (skipUnless the gitignored xlsx is present).
</artifacts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stdlib .xlsx reader (scheduling/xlsx.py)</name>
  <files>scheduling/xlsx.py, scheduling/tests_import.py</files>
  <read_first>
    - scheduling/management/commands/import_offerings.py (current CSV read path this generalizes)
    - scheduling/tests.py lines 235-272 (skipUnless(os.path.exists(...)) pattern for gitignored data/raw files)
  </read_first>
  <behavior>
    - sheet_names("data/raw/2T-25-26-All Physical-RoomScheduleStatisticsTemplate1.xlsx") returns 116 names (2 summary tabs + 114 room tabs).
    - read_grid(offerings xlsx, "Sheet1") returns a header row plus 1211 data rows; row 0 contains the columns Code, Sec, Mode, Enrolled, Instructor, Email, Schedule.
    - Cells that are shared strings resolve to their text (not numeric indices); empty cells become "".
    - A room tab's grid exposes C4 (name) and O4 (capacity) by [row][col] indexing (0-based row 3, columns 2 and 14).
    - Reading a workbook with no explicit sheet arg returns the first sheet.
  </behavior>
  <action>
    Create `scheduling/xlsx.py` implementing a minimal Open-XML spreadsheet reader using ONLY the standard library — `zipfile` to open the `.xlsx` container and `xml.etree.ElementTree` to parse its parts (D1: no third-party spreadsheet library such as openpyxl or pandas). Implement `sheet_names(path)` (read `xl/workbook.xml` sheet elements in document order, resolving the r:id to the worksheet part via `xl/_rels/workbook.xml.rels`) and `read_grid(path, sheet=None)` returning a list of rows, each a list of cell strings. Resolve the OOXML namespace by stripping/looking up the `{...}` prefix rather than hardcoding it. Load `xl/sharedStrings.xml` once and index it; for a cell with `t="s"` return `sharedStrings[int(value)]`, for `t="inlineStr"` return the inline text, otherwise return the raw `<v>` text (or "" when absent). Reconstruct column position from the cell `r` reference (e.g. "O4" -> column 14) so sparse rows keep correct columns and missing cells are "". Guard against XML entity/DTD expansion by not enabling any external entity resolution (parse the archive members directly; do not resolve external DTDs). Do NOT read files outside the given path. Keep the module import-free of Django so it can be unit-tested as a SimpleTestCase with no DB.
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_import.XlsxReaderTests -v 1</automated>
  </verify>
  <acceptance_criteria>
    - XlsxReaderTests pass (guarded by skipUnless for the gitignored xlsx files; they run locally where data/raw exists).
    - `python -c "import scheduling.xlsx"` succeeds using only stdlib imports.
    - No import of openpyxl or pandas anywhere in scheduling/xlsx.py.
  </acceptance_criteria>
  <done>The stdlib reader returns the offerings Sheet1 (header + 1211 rows) and enumerates the 116 room-master tabs, resolving shared strings and column positions; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure parse/classify/normalize/modality helpers (scheduling/importing.py)</name>
  <files>scheduling/importing.py, scheduling/tests_import.py</files>
  <read_first>
    - scheduling/management/commands/import_offerings.py (parse_time, parse_room, MEET_RE, DAY_MAP, MODE_MAP being generalized)
    - scheduling/models.py (Modality f2f/blended/online, DayOfWeek 0..6)
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md (D4 prefix map, D5 modality, D7 dedup, verified room-prefix facts: 92 V / 47 R / 20 A / 5 GYM / 2 U / 2 digit-only; typos 404 and 516; real extras A105 A298 GYM1 GYM2 U102)
  </read_first>
  <behavior>
    - PREFIX_BUILDING maps R->("ACAD","Academic Building"), A->("ADMIN","Admin Building"), GYM->("GYM","Gym"), V->("ONLINE","Online (virtual)"); unknown -> ("UNASSIGNED","Unassigned").
    - classify_room("R415") -> building ACAD, floor 4, scannable True, is_virtual False.
    - classify_room("V301") -> building ONLINE, is_virtual True, scannable False.
    - classify_room("GYM2") -> building GYM, scannable True, does not raise (old parse_room dropped it).
    - classify_room("A410-A") -> building ADMIN, floor 4 (hyphen suffix tolerated).
    - classify_room("404") and classify_room("516") -> building UNASSIGNED, is_typo True, is_unassigned True.
    - classify_room("U102") -> building UNASSIGNED, is_unassigned True, is_typo False.
    - classify_room("TBA") -> building UNASSIGNED, floor 0, not virtual.
    - parse_time("7:00AM")->07:00, "12:00P"->12:00, "12:00A"->00:00, "1:15PM"->13:15, "bad"->None.
    - parse_meetings("F [7:00AM-8:15AM] V415,M [7:00AM-8:15AM] R415") -> two meetings with day/start/end and room_raw V415/R415.
    - parse_meetings("M [7:00AM-8:15AM]") -> one meeting with empty room_raw (day/time only; room token OPTIONAL).
    - modality_for_room(virtual, "f2f") -> ONLINE; modality_for_room(physical, "blended") -> BLENDED.
    - normalize_name_key("VILLANUEVA, JUAN P") == normalize_name_key("Villanueva,  Juan  P.") (case/spacing/comma/single-letter-initial insensitive).
  </behavior>
  <action>
    Create `scheduling/importing.py` housing the pure logic (no DB, no Django models imported except the `Modality`/`DayOfWeek` enums for return values). Move `parse_time` verbatim in behavior from import_offerings. Define `PREFIX_BUILDING` and `classify_room(code)` returning a `RoomInfo` dataclass with the fields listed in the artifacts section, implementing D4 exactly: strip the code; a `GYM` prefix (startswith "GYM", case-insensitive) -> Gym building; a leading `V` -> Online building + is_virtual True + scannable False; a leading `R` -> Academic; a leading `A` -> Admin; anything else (P, U, no leading letter / digit-only, unknown letter) -> Unassigned with is_unassigned True and is_typo True when there is no leading alphabetic prefix. `scannable` is True for any physical (non-virtual) room. `derive_floor(code)` extracts the first digit-run after the leading letters and returns its leading digit (R415 -> 4, A101 -> 1), returns 1 for GYM-prefixed codes, and returns 0 when no digit run exists (TBA, bare letters). Tolerate a hyphen suffix (A410-A). Rewrite the meeting regex so the trailing room token is OPTIONAL and expose `parse_meetings(schedule_str)` returning a list of `MeetingParse(day, start, end, room_raw)` where day is the DayOfWeek int (via DAY_MAP) or None, start/end are `parse_time` results or None, and room_raw is "" when absent — this is what stops the silent drop of day/time-only meetings (D2/D9). Add `normalize_name_key(name)` (D7): uppercase, remove commas, collapse internal whitespace, and drop any standalone single-letter token (middle initials) so comma/spacing/initial variants collapse to one key. Add `modality_for_room(room_info, course_mode)` (D5): ONLINE for virtual rooms, else the course mode via MODE_MAP defaulting F2F. Keep MODE_MAP and DAY_MAP here as the single definition.
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_import.ImportingHelperTests -v 1</automated>
  </verify>
  <acceptance_criteria>
    - ImportingHelperTests cover every case in the behavior block, including GYM2, A410-A, the 404/516 typos, U102, TBA, the optional-room meeting, and the VILLANUEVA name-merge (D7).
    - All helper tests are pure SimpleTestCase (no DB access, no data/raw file dependency) and pass.
    - classify_room never raises on any of the 168 real distinct room codes described in CONTEXT (verify a representative sample: R415, A101, GYM1, GYM2, V301, U102, 404, 516, A410-A).
  </acceptance_criteria>
  <done>The pure prefix map, floor derivation, meeting parser (optional room), name-normalizer, and per-room modality are implemented and unit-tested against the CONTEXT's verified edge cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: reconcile() four-bucket partition + full-file reconciliation test</name>
  <files>scheduling/importing.py, scheduling/tests_import.py</files>
  <read_first>
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md ("Row reconciliation" facts: 1,100 with schedule, 1,042 intact, 44 roomless physical, 14 online no-room, 111 no-schedule, 2,021 meetings; the "A151/C110 are section numbers NOT rooms" warning under D9)
    - scheduling/xlsx.py (read_grid, produced in Task 1)
    - scheduling/importing.py (classify_room, parse_meetings, from Task 2)
  </read_first>
  <behavior>
    - reconcile(rows, col) returns a Reconciliation with: total_rows, no_schedule, intact_rows, roomless_tba_rows, online_no_room_rows, total_meetings, flagged_unassigned (list of codes), flagged_typo (list of codes), distinct_rooms, distinct_instructors, emailless_instructor_keys (list).
    - The identity holds: total_rows == intact_rows + roomless_tba_rows + online_no_room_rows + no_schedule (every row lands in exactly one bucket).
    - Against the real offerings xlsx: total_rows == 1211, no_schedule == 111, intact_rows == 1042, roomless_tba_rows == 44, online_no_room_rows == 14, total_meetings == 2021.
    - flagged_typo contains 404 and 516; flagged_unassigned includes U-prefixed and digit-only codes.
    - A row whose only "room" token equals its Sec value is treated as roomless (section label, not a room), not as an Admin/Academic room.
  </behavior>
  <action>
    Add `reconcile(rows, col)` to `scheduling/importing.py` (col is the header->index dict). For each data row, classify into exactly ONE bucket to guarantee the partition (D9, "nothing silently dropped"): (1) empty Schedule cell -> no_schedule; else parse via `parse_meetings`. A meeting's room token counts as a REAL room only when room_raw is non-empty, classify_room says it is not unassigned/typo-with-no-building AND it is not equal to the row's Sec value (the A151/C110 section-label guard). (2) If every parsed meeting has a real room + valid day + valid time -> intact_rows and add each such meeting to total_meetings. (3) If the row has meetings but NO real room: online course Mode -> online_no_room, else -> roomless_tba (the 44 physical sections routed to the TBA placeholder by Plan 03). Count meetings for the intact bucket (each real-room meeting) plus the roomless/virtual meeting slots as CONTEXT defines total_meetings == 2021 — tune the counting rule until the full-file test hits 2021 and the four buckets hit 1042/44/14/111 exactly. Collect flagged_unassigned / flagged_typo from classify_room, distinct_rooms from real room codes, and instructor stats via normalize_name_key over the Instructor/Email columns: distinct_instructors is the count of distinct (email OR normalized-name) keys, emailless_instructor_keys is the normalized-name keys that never carry an email (the ~10, D7). Return a `Reconciliation` dataclass. Then write the full-file test in `scheduling/tests_import.py`: skipUnless the offerings xlsx exists, read it via `scheduling.xlsx.read_grid`, call reconcile, and assert the identity plus the exact CONTEXT counts. Keep it a SimpleTestCase (no DB — dry reconciliation only).
  </action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_import.ReconciliationTests -v 1</automated>
  </verify>
  <acceptance_criteria>
    - The reconciliation identity `1211 = 1042 + 44 + 14 + 111` is asserted and passes against the real file (skipUnless present).
    - total_meetings asserts to 2021.
    - The partition is provably exhaustive: a test asserts every input row increments exactly one of the four bucket counters (their sum equals total_rows) on the real file AND on a small in-line synthetic set of rows (so the invariant is guarded even when data/raw is absent).
    - emailless_instructor_keys length is in the 8-12 range (the ~10 email-less people, D7) on the real file.
  </acceptance_criteria>
  <done>reconcile() partitions all 1,211 rows into the four CONTEXT buckets with the identity asserted, counts 2,021 meetings, and flags typo/unassigned rooms and email-less instructors — the trustworthy basis for the Plan 03 report and Plan 04 assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registrar `.xlsx` file -> stdlib parser | Operator-supplied Open-XML archive (zip + XML) is untrusted structured input parsed by xlsx.py. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Denial of Service | scheduling/xlsx.py XML parse | low | mitigate | Parse archive members directly with xml.etree without enabling external DTD/entity resolution (no XXE / billion-laughs surface); files are operator-provided registrar exports, not attacker-uploaded. |
| T-04.1-02 | Tampering | scheduling/xlsx.py path handling | low | accept | Reader only opens the explicit path passed by a management command; no path traversal from untrusted input in this phase (CLI-only, D-scope). |
| T-04.1-SC | Tampering | pip/package installs | high | mitigate | No new dependency is introduced (D1: stdlib zipfile + xml.etree only); nothing to install, so no supply-chain surface added. Acceptance criteria assert openpyxl/pandas are absent. |
</threat_model>

<verification>
- `py -3.12 manage.py test scheduling.tests_import -v 1` is fully green.
- The parser/helper tests are pure SimpleTestCase (no DB, no data/raw dependency); the reader and full-file reconciliation tests skip cleanly when data/raw is absent and assert the real counts when present.
- `python -c "import scheduling.xlsx, scheduling.importing"` imports with stdlib + Django enums only.
</verification>

<success_criteria>
- Stdlib `.xlsx` reader returns offerings Sheet1 (header + 1211 rows) and the 116 room-master tabs (Success Criterion 1: reads the real xlsx directly, no new dependency).
- The prefix map + floor derivation + optional-room meeting parser + name normalizer + per-room modality are implemented and unit-tested against every CONTEXT edge case (D4/D5/D7, and D2's "keep gym/online").
- reconcile() proves the 1,211-row partition and the 2,021-meeting count (Success Criterion 6 foundation; "nothing silently dropped").
</success_criteria>

<output>
Create `.planning/phases/04.1-real-data-integration/04.1-01-SUMMARY.md` when done.
</output>
