---
phase: 04.1-real-data-integration
plan: 04
type: execute
wave: 3
depends_on: [04.1-02, 04.1-03]
files_modified:
  - scheduling/tests_e2e.py
autonomous: false
requirements: [ENV-02]
must_haves:
  truths:
    - "The full clean-load sequence runs end to end against MSSQL LocalDB: reset_term -> load_room_master -> import_offerings -> materialize_sessions --days 14 (D10 run sequence)."
    - "After the load the live term holds ~1,100 sections / ~2,021 schedules / ~200 instructors / ~213 rooms across the five buildings — the full term, not the R3 slice (ENV-02 at scale)."
    - "The importer's reconciliation report balances the 1,211-row identity and reports 2,021 meetings; nothing silently dropped."
    - "One F2F, one blended, and one online class each appear for the correct instructor over the materialized window (the /faculty/schedule queryset) and are checkable: F2F/blended in a scannable physical room, online routed to the checker (virtual room, online modality) (Success Criterion 6)."
  artifacts:
    - "A live, checkable 2nd Term SY 2025-2026 in MSSQL LocalDB (data outcome, not a code file)."
    - scheduling/tests_e2e.py (end-to-end spot-check assertions)
  key_links:
    - "materialize_sessions copies Schedule.faculty onto Session.faculty (D8); the spot check reads exactly the /faculty/schedule queryset (Session.objects.filter(faculty=..., date in week))."
    - "declared_modality/virtual-room routing is what makes an online session go to the checker rather than the faculty scan."
---

<objective>
Execute the full clean-load run sequence against the live MSSQL LocalDB and verify the term is real, complete, and checkable.

Purpose: This is where Phase 04.1 becomes real — the live term in the dev DB is the full 2nd Term SY 2025-2026 (D10 run sequence), proven by the reconciliation identity and by an end-to-end spot check that one F2F, one blended, and one online class each show for the right instructor and are checkable (Success Criterion 6). It brings ENV-02 from the R3 slice to full-term scale.
Output: a loaded live term in LocalDB + end-to-end spot-check assertions + a human confirmation of the reconciliation report.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-real-data-integration/04.1-CONTEXT.md

# The commands run in sequence (from Plans 02 and 03) + the materializer (unchanged):
@scheduling/management/commands/import_offerings.py
@scheduling/management/commands/materialize_sessions.py
# The queryset the spot check mirrors ("appears on the correct instructor's /faculty/schedule"):
@web/faculty.py
# Models for the spot-check assertions (Session.faculty/room/declared_modality; Modality; Room->Floor->Building):
@scheduling/models.py
@campus/models.py
</context>

<artifacts>
## Artifacts this plan produces

- A live, checkable **2nd Term SY 2025-2026** in the MSSQL LocalDB dev database (the phase's data outcome): ~2,021 active Schedules, materialized Sessions over a 14-day window, ~200 faculty accounts, ~213 rooms across the 5 buildings, including the "TBA" and virtual/online placeholder rooms.
- `scheduling/tests_e2e.py` end-to-end spot-check assertions (F2F / blended / online, mirroring the /faculty/schedule queryset + checkability).
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Run the clean-load sequence + assert live-DB scale</name>
  <files>scheduling/tests_e2e.py</files>
  <read_first>
    - scheduling/management/commands/import_offerings.py (the reconciliation report to read)
    - scheduling/management/commands/materialize_sessions.py (--days horizon; active-term requirement)
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md (run sequence; targets: ~1,100 sections / ~2,021 schedules / ~200 instructors / ~213 rooms; HY010 caution)
  </read_first>
  <action>
    Run the full clean-load sequence against the live LocalDB, in order (D10 sequence), capturing each command's stdout: (1) reset_term for the term "2nd Term SY 2025-2026" with the --yes flag (idempotent on a first run); (2) load_room_master; (3) import_offerings; (4) materialize_sessions with --days 14. Read the import step's reconciliation report and confirm the four-bucket identity balances (sum == 1211) and the meeting total is 2021. Then assert live-DB scale via a manage.py shell check reading counts off the ACTIVE term: active-term Schedules ~2,021, distinct sections ~1,100, FACULTY users ~200, Rooms ~213, Buildings == 5 (Academic/Admin/Gym/Online/Unassigned), the "TBA" room present in the Unassigned building, and materialized Sessions > 0 over the 14-day window. Treat the CONTEXT numbers as targets with a small tolerance band (within ~2%) since exact counts depend on the real file, but the four-bucket identity must be EXACT. If import raises pyodbc HY010, the fix belongs in Plan 03 (iterate the Python rows list, not a live queryset) — stop and report rather than patching here.
  </action>
  <verify>
    <automated>py -3.12 manage.py shell -c "from django.contrib.auth import get_user_model as g; from scheduling.models import AcademicTerm, Schedule, Session; from campus.models import Room, Building; from accounts.models import Role; t = AcademicTerm.objects.get(is_active=True); sc = Schedule.objects.filter(term=t); print('schedules', sc.count(), 'rooms', Room.objects.count(), 'faculty', g().objects.filter(role=Role.FACULTY).count(), 'sessions', Session.objects.count(), 'buildings', Building.objects.count()); assert sc.count() > 2000; assert Building.objects.count() == 5; assert Room.objects.filter(code='TBA').exists(); assert g().objects.filter(role=Role.FACULTY).count() > 180; assert Session.objects.count() > 0; print('SCALE OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The four commands run in sequence without error against LocalDB; the import reconciliation report prints the balanced 1,211 identity and 2,021 meetings.
    - The shell scale check passes: active-term Schedules > 2000, Buildings == 5, the "TBA" room exists, FACULTY > 180, Sessions > 0.
    - Re-running the whole sequence a second time yields the same scale (reset_term keeps it clean; import + materialize are idempotent) — no doubling.
  </acceptance_criteria>
  <done>The live LocalDB holds the full 2nd Term SY 2025-2026 at scale, proven by the reconciliation identity and the DB-count assertions (ENV-02 at full-term scale, D10 sequence).</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end spot check — F2F / blended / online appear + are checkable</name>
  <files>scheduling/tests_e2e.py</files>
  <read_first>
    - web/faculty.py lines 56-67 (the schedule view queryset the spot check must mirror: Session.objects.filter(faculty=user, date in week))
    - scheduling/models.py (Session.faculty/room/declared_modality; Modality; Schedule.modality)
    - campus/models.py (Room -> Floor -> Building; scannable = physical building, has qr_token/manual_code)
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md (spot-check spec: one F2F, one blended, one online; F2F/blended scannable, online routed to checker)
  </read_first>
  <action>
    Add a spot-check that runs against the live-loaded term (a management-shell assertion script, committed as a small helper the executor runs, plus a mirrored assertion documented in scheduling/tests_e2e.py for the record). Pick three sessions from the materialized window: one whose schedule modality is f2f, one blended, one online. For each, resolve the OWNING instructor (session.faculty) and assert the exact /faculty/schedule queryset would surface it — i.e. `Session.objects.filter(faculty=session.faculty, date__gte=today, date__lt=today+7)` includes it (mirroring web.faculty.schedule). Then assert checkability by room type: the F2F and blended sessions sit in a physical (non-virtual) room in a real building (ACAD/ADMIN/GYM) with a qr_token + manual_code (scannable); the online session sits in a virtual room (Online building) with modality online (the checker-verified path, not the faculty scan). Because dev-login impersonation of imported instructors is OUT OF SCOPE (deferred), verify at the queryset + routing level, not via a live browser login. Print the three chosen classes (course_code, section, instructor, date, room, modality) so the human checkpoint can read them.
  </action>
  <verify>
    <automated>py -3.12 manage.py shell -c "from datetime import timedelta; from django.utils import timezone; from scheduling.models import Session, Modality; from campus.models import Building; today = timezone.localdate(); wk = today + timedelta(days=7); picks = {}; [picks.setdefault(s.schedule.modality, s) for s in Session.objects.select_related('schedule','faculty','room__floor__building').filter(date__gte=today, date__lt=wk)]; assert Modality.F2F in picks and Modality.BLENDED in picks and Modality.ONLINE in picks, sorted(picks); [print(m, '->', picks[m].schedule.course_code, picks[m].faculty_id, picks[m].room.code, picks[m].room.floor.building.code) for m in picks]; f2f = picks[Modality.F2F]; on = picks[Modality.ONLINE]; assert Session.objects.filter(faculty=f2f.faculty, date__gte=today, date__lt=wk).filter(pk=f2f.pk).exists(); assert f2f.room.floor.building.code != 'ONLINE' and f2f.room.qr_token and f2f.room.manual_code; assert on.room.floor.building.code == 'ONLINE'; print('SPOTCHECK OK')"</automated>
  </verify>
  <acceptance_criteria>
    - A F2F, a blended, and an online session are all present in the materialized window (the picks dict has all three modalities).
    - Each chosen session is surfaced by the mirrored /faculty/schedule queryset for its own instructor (faculty match + date window).
    - The F2F/blended sessions are in a physical scannable room (real building, qr_token + manual_code); the online session is in a virtual Online-building room with modality online (checker-routed).
  </acceptance_criteria>
  <done>One F2F, one blended, and one online class each appear for the correct instructor on their schedule and are checkable per their modality (Success Criterion 6, D5/D8).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — reconciliation report + three spot-check classes</name>
  <read_first>
    - .planning/phases/04.1-real-data-integration/04.1-CONTEXT.md (the reconciliation identity 1211 = 1042 + 44 + 14 + 111 and the 2021 meeting total; the flagged typo rooms 404/516 and ~10 email-less instructors)
  </read_first>
  <action>
    No code is written in this task. The executor pauses at this blocking gate until the human operator reads the reconciliation report + the three spot-check classes and reports the result. Present the printed report and the Task 1/Task 2 shell outputs for review.
  </action>
  <what-built>
    The full clean-load run (reset_term -> load_room_master -> import_offerings -> materialize_sessions --days 14) has loaded the real 2nd Term SY 2025-2026 into the live LocalDB. Task 1 asserted scale (schedules/rooms/faculty/buildings, TBA room) and Task 2 asserted an F2F, a blended, and an online class each appear for the correct instructor and are checkable.
  </what-built>
  <how-to-verify>
    1. Read the import reconciliation report printed by Task 1 (re-run `py -3.12 manage.py import_offerings --dry-run` if you want to see it again). Confirm the four buckets sum to 1211 (1042 intact + 44 roomless-TBA + 14 online-no-room + 111 no-schedule) and the meeting total is 2021.
    2. Confirm the flagged lists look right: the Unassigned/typo rooms include 404 and 516; the email-less instructors list is ~10 names.
    3. Read the three spot-check classes Task 2 printed (F2F / blended / online with course, instructor id, room, building). Confirm the F2F/blended are physical scannable rooms and the online one is a virtual Online-building room.
    4. Optionally run the Task 1 and Task 2 shell checks again and confirm both print "SCALE OK" and "SPOTCHECK OK".
    5. Confirm nothing you expected is missing (no silent drop) and the term looks like the real full term, not the old R3 slice.
  </how-to-verify>
  <resume-signal>Type "approved" if the reconciliation balances and the three spot-check classes are correct, or describe what looks wrong (wrong counts, a missing modality, an unexpected flagged room).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> live dev DB mutation | The run sequence deletes then reloads a term's Schedule/Session on the live LocalDB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-08 | Denial of Service | run sequence on live DB | medium | mitigate | reset_term is scoped to one named term and preserves reusable User/Room/Building rows; the sequence is reversible by re-running (D10). Seed_demo's CCIS/IT-30x term is left inactive, never mixed with the real active term. |
| T-04.1-09 | Repudiation | verification bypass | low | mitigate | The four-bucket identity is asserted exactly (not just eyeballed); the human checkpoint additionally confirms flagged lists and the three spot-check classes before the phase is accepted. |
| T-04.1-SC | Tampering | pip/package installs | high | mitigate | No new dependency (D1); the run uses only existing management commands. Nothing to install. |
</threat_model>

<verification>
- The four-command sequence completes against LocalDB; the import reconciliation report balances 1,211 and reports 2,021 meetings.
- Task 1 scale check prints "SCALE OK"; Task 2 spot check prints "SPOTCHECK OK".
- The human checkpoint confirms the reconciliation, the flagged lists, and the three spot-check classes.
</verification>

<success_criteria>
- Full clean-load sequence runs end to end against MSSQL LocalDB (D10).
- Live term at full-term scale: ~2,021 schedules / ~200 faculty / ~213 rooms / 5 buildings, materialized over 14 days (Success Criterion 2, ENV-02 at scale).
- Reconciliation identity balances (1,211) with 2,021 meetings; nothing silently dropped (Success Criterion 6).
- One F2F, one blended, one online class each appear for the correct instructor and are checkable (Success Criterion 6).
</success_criteria>

<output>
Create `.planning/phases/04.1-real-data-integration/04.1-04-SUMMARY.md` when done.
</output>
