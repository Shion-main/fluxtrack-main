---
phase: 02-correctness-foundations
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - ops/models.py
  - ops/migrations/0002_roomconflictflag.py
  - scheduling/jobs.py
  - scheduling/management/commands/run_status_sweep.py
  - scheduling/tests.py
autonomous: true
requirements: [JOB-02b, JOB-02c]
notes:
  - "Wave-2 test isolation: run this plan's `manage.py test` with DB_TEST_NAME=test_fluxtrack (default). Plan 02-04 (the other Wave-2 plan) MUST run with DB_TEST_NAME=test_fluxtrack_ops so the two Wave-2 DB test runs never collide under parallel execution (relies on the TEST[NAME] override in settings from Phase 1, 01-01). If DB_TEST_NAME is unavailable, execute-phase must serialize the two Wave-2 test runs."
  - "Migration ordering: this plan creates ops migration 0002_roomconflictflag. Plan 02-05 (Wave 3) creates 0003_jobrun on top of it — 02-05 depends on this plan so 0003 always follows 0002."
  - "Depends on 02-01 (imports is_no_show_past_grace) and 02-02 (imports notify)."

must_haves:
  truths:
    - "A still-SCHEDULED F2F/Blended session whose grace has expired is marked ABSENT by the sweep independent of any scan (JOB-02b), using the SAME is_no_show_past_grace predicate the scanner uses"
    - "The sweep backfills ALL past-date un-decided no-shows regardless of date (self-heals after a scheduler outage), not just today's"
    - "The sweep is idempotent: only SCHEDULED -> ABSENT; active, completed, and already-absent sessions are never touched"
    - "The sweep EXCLUDES online sessions (effective modality = declared_modality or schedule.modality == 'online') from Absent-marking — it leaves them SCHEDULED, with a documented Phase-3 hook"
    - "Every sweep-marked absence writes an AuditLog (event session.marked_absent, payload by=sweep); the sweep NEVER stamps room_released_at (no timer-based auto-release)"
    - "Contradictory occupancy (2+ ACTIVE sessions holding one room, room_released_at NULL) raises ONE IFO room-conflict notification via notify(), deduped by an open RoomConflictFlag until resolved, and auto-resolves when the conflict clears (JOB-02c)"
  artifacts:
    - path: "ops/models.py"
      provides: "RoomConflictFlag(room FK, conflict_key, detected_at, resolved_at) with a filtered UniqueConstraint (one OPEN flag per conflict_key)"
      contains: "class RoomConflictFlag"
    - path: "scheduling/jobs.py"
      provides: "sweep_no_shows(now=None) and detect_room_conflicts(now=None) pure-ish service functions returning counts"
      contains: "def sweep_no_shows"
    - path: "scheduling/management/commands/run_status_sweep.py"
      provides: "ASCII-only management command wrapping the sweep + conflict detection"
      contains: "class Command"
    - path: "scheduling/tests.py"
      provides: "SweepTests (absent/backfill/online-exclusion/idempotency/audit/never-stamps-release) + RoomConflictTests (one flag/dedup/auto-resolve)"
      contains: "class SweepTests"
  key_links:
    - from: "scheduling/jobs.py sweep_no_shows"
      to: "scheduling.resolver.is_no_show_past_grace"
      via: "import + per-session re-affirmation of the ORM cutoff filter"
      pattern: "is_no_show_past_grace\\("
    - from: "scheduling/jobs.py detect_room_conflicts"
      to: "ops.notify.notify + ops.models.RoomConflictFlag"
      via: "create open flag then notify(role=IFO_ADMIN, type=\"room_conflict\") only on first detection"
      pattern: "notify\\(role="
    - from: "sweep-marked absence"
      to: "ops.models.AuditLog"
      via: "AuditLog.objects.create(event_type=\"session.marked_absent\", payload={\"by\": \"sweep\"})"
      pattern: "session.marked_absent"
---

<objective>
Build the JOB-02 status sweep: a service that marks every still-SCHEDULED F2F/Blended no-show ABSENT independent of any scan (using the shared `is_no_show_past_grace` predicate from Plan 02-01), backfilling all past-date no-shows, idempotently, with an AuditLog per absence — and a room-conflict safety net that raises ONE deduped IFO notification via `notify()` (Plan 02-02) backed by a new `RoomConflictFlag`. Online sessions are explicitly excluded from Absent-marking with a documented Phase-3 hook. No timer-based room release: the sweep never stamps `room_released_at`.

Purpose: Locks Phase-2 success criteria #1 (Absent trustworthy without a scan), #2 (deduped IFO room-conflict notification), and #5 (idempotent sweep).
Output: `RoomConflictFlag` model + migration, `scheduling/jobs.py` (sweep + conflict detection), a `run_status_sweep` ASCII command, and `SweepTests` + `RoomConflictTests`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-correctness-foundations/02-CONTEXT.md
@.planning/phases/02-correctness-foundations/02-RESEARCH.md
@.planning/phases/02-correctness-foundations/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@scheduling/models.py
@scheduling/management/commands/materialize_sessions.py
@ops/models.py

<interfaces>
<!-- Session — scheduling/models.py -->
SessionStatus (TextChoices): SCHEDULED="scheduled", ACTIVE="active", COMPLETED="completed", ABSENT="absent"
Modality (TextChoices): F2F="f2f", BLENDED="blended", ONLINE="online"
Session: schedule=FK Schedule, room=FK campus.Room, date, scheduled_start, scheduled_end, status,
         declared_modality (blank), schedule.modality, room_released_at (null), actual_start/end
# Effective modality = session.declared_modality or session.schedule.modality  (mirror resolver L82)
# Scan-time idempotency guard mirrored: only act `if session.status == SessionStatus.SCHEDULED`

<!-- Shared predicate (from Plan 02-01) — scheduling/resolver.py -->
is_no_show_past_grace(scheduled_start, now, grace_min) -> bool   # now > scheduled_start + timedelta(minutes=grace_min)

<!-- notify (from Plan 02-02) — ops/notify.py -->
notify(*, type, title, body="", link="", role=None, users=None) -> list[Notification]
# Role.IFO_ADMIN is the room-conflict recipient.

<!-- Policy — ops/policy.py + config/settings.py -->
get_policy("grace_minutes") -> 15   # NEVER hardcode grace; derive the ORM cutoff from this same value

<!-- Conventions: management commands print ASCII only (use -> not arrows); every state change writes AuditLog;
     AuditLog(actor nullable, event_type dotted noun.verb, target_type, target_id, payload JSON) -->

<!-- RoomConflictFlag shape + release_room note — 02-RESEARCH.md "Room-Conflict Detection & Dedup" -->
RoomConflictFlag(room=FK campus.Room, conflict_key=Char(120), detected_at auto_now_add, resolved_at null)
  Meta.constraints = [UniqueConstraint(fields=["conflict_key"], condition=Q(resolved_at__isnull=True), name="uniq_open_conflict_per_key")]
# Filtered unique on MSSQL is proven (Phase 1 azure_oid). Dedup key = f"room:{room_id}".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SweepTests + RoomConflictTests (RED)</name>
  <files>scheduling/tests.py</files>
  <read_first>
    - scheduling/tests.py (reuse make_session(scheduled_start, scheduled_end=None) at ~L135 for building persisted sessions; append new DB-backed classes in the DB-test region, below the divider — do NOT touch FacultyResolverTests / the pure classes from Plan 02-01)
    - scheduling/models.py (Session/SessionStatus/Modality; how to create ACTIVE sessions sharing one room for conflict tests; declared_modality vs schedule.modality)
    - ops/models.py (AuditLog fields to assert on; Notification for conflict-notification assertions; RoomConflictFlag being introduced in Task 2)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("Code Examples" -> sweep idempotency tests; "Room-Conflict Detection & Dedup" for the conflict + dedup + auto-resolve expectations)
    - .planning/phases/02-correctness-foundations/02-VALIDATION.md (all JOB-02b + JOB-02c rows mapped to SweepTests / RoomConflictTests — this task builds that exact contract)
  </read_first>
  <behavior>
    - SweepTests(TestCase): (a) a SCHEDULED F2F session with scheduled_start = now-20min (past 15-min grace) becomes ABSENT after sweep_no_shows(now=now); (b) a past-DATE SCHEDULED no-show (e.g. 2 days ago) is also marked ABSENT (backfill/self-heal); (c) an ONLINE no-show (declared_modality="online" OR schedule.modality="online") stays SCHEDULED (online exclusion); (d) active/completed/already-absent sessions are unchanged across a rerun (idempotent); (e) each marked absence creates an AuditLog(event_type="session.marked_absent", payload contains by=="sweep"); (f) after a sweep, no swept session has room_released_at set (guard: the sweep never auto-releases a room).
    - RoomConflictTests(TestCase): (a) two ACTIVE sessions holding the same room (room_released_at NULL) trigger detect_room_conflicts(now) creating exactly ONE RoomConflictFlag for that room and exactly one IFO Notification per active IFO admin of type "room_conflict"; (b) a second detection run with the same unresolved conflict creates NO new flag and NO new notification (dedup); (c) once the conflict clears (one session completes/releases), a detection run stamps the flag's resolved_at (auto-resolve).
  </behavior>
  <action>
    Append `SweepTests(TestCase)` and `RoomConflictTests(TestCase)` to the DB-backed region of `scheduling/tests.py`. Import `from scheduling.jobs import sweep_no_shows, detect_room_conflicts` (FAILS until Task 3 — intended RED) and `from ops.models import AuditLog, Notification, RoomConflictFlag` (RoomConflictFlag exists after Task 2). Build fixtures with the existing `make_session(...)` factory; add a small helper that creates two ACTIVE sessions sharing one room (distinct schedules/faculty) for the conflict cases. Assert on concrete states: `session.status == SessionStatus.ABSENT`, `AuditLog.objects.filter(event_type="session.marked_absent").exists()` with `by=="sweep"` in payload, `session.room_released_at is None`, `RoomConflictFlag.objects.filter(resolved_at__isnull=True).count() == 1`, and Notification counts of `type="room_conflict"`. Create active IFO admins so notify() fan-out has recipients.
  </action>
  <acceptance_criteria>
    - `scheduling/tests.py` contains `class SweepTests(TestCase)` and `class RoomConflictTests(TestCase)` in the DB-backed region.
    - Before Tasks 2-3, `py -3.12 manage.py test scheduling.tests.SweepTests` is RED (ImportError: scheduling.jobs / RoomConflictFlag missing).
    - SweepTests includes an explicit online-exclusion case and an explicit `room_released_at is None` guard assertion.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.SweepTests scheduling.tests.RoomConflictTests</automated>
  </verify>
  <done>Wave-0 tests for JOB-02b + JOB-02c exist and are RED; they pin backfill, online-exclusion, idempotency, audit, the no-auto-release guard, and conflict dedup/auto-resolve to concrete assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RoomConflictFlag model + migration (GREEN for model import)</name>
  <files>ops/models.py, ops/migrations/0002_roomconflictflag.py</files>
  <read_first>
    - ops/models.py (append after existing models; follow the module docstring convention; cite JOB-02c/IFO-08)
    - campus/models.py (Room is the FK target: "campus.Room")
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("Room-Conflict Detection & Dedup" -> exact RoomConflictFlag shape + the filtered UniqueConstraint condition Q(resolved_at__isnull=True); note filtered-unique on MSSQL is proven from Phase 1)
    - campus/migrations/0002_cs_collation_tokens.py (Phase-1 precedent for how migrations were authored/named in this repo)
  </read_first>
  <behavior>
    - RoomConflictFlag(room FK campus.Room, conflict_key Char(120), detected_at auto_now_add, resolved_at DateTime null) with a filtered UniqueConstraint guaranteeing at most ONE open (resolved_at IS NULL) flag per conflict_key.
    - `makemigrations` produces a clean migration; `migrate` applies it on SQL Server as a filtered unique index.
  </behavior>
  <action>
    Add `RoomConflictFlag` to `ops/models.py` per the research shape: FK to `"campus.Room"` (related_name "conflict_flags", on_delete CASCADE), `conflict_key = CharField(max_length=120)`, `detected_at = DateTimeField(auto_now_add=True)`, `resolved_at = DateTimeField(null=True, blank=True)`, and `Meta.constraints = [UniqueConstraint(fields=["conflict_key"], condition=Q(resolved_at__isnull=True), name="uniq_open_conflict_per_key")]`. Give it a docstring citing JOB-02c (one open flag per conflict; IFO-08 resolves it later). Generate the migration with `py -3.12 manage.py makemigrations ops` and confirm it lands as `ops/migrations/0002_roomconflictflag.py`. Do not add JobRun here (Plan 02-05 owns it).
  </action>
  <acceptance_criteria>
    - `ops/models.py` contains `class RoomConflictFlag` with the filtered `UniqueConstraint(... name="uniq_open_conflict_per_key")`.
    - `ops/migrations/0002_roomconflictflag.py` exists and `py -3.12 manage.py makemigrations --check --dry-run` reports no missing migrations.
    - `py -3.12 manage.py migrate` applies cleanly on the SQL Server dev DB (filtered unique index created).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py makemigrations --check --dry-run</automated>
  </verify>
  <done>RoomConflictFlag exists with a proven filtered "one open flag per key" unique constraint on MSSQL; the model import in the Wave-0 tests resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build scheduling/jobs.py (sweep + conflict detection) + run_status_sweep command (GREEN)</name>
  <files>scheduling/jobs.py, scheduling/management/commands/run_status_sweep.py</files>
  <read_first>
    - scheduling/resolver.py (import is_no_show_past_grace — the shared predicate; do NOT reuse resolve_faculty_scan wholesale, only the atom)
    - scheduling/models.py (Session/SessionStatus/Modality; the effective-modality expression declared_modality or schedule.modality)
    - web/scan.py (the _apply SCHEDULED->ABSENT idempotency guard + the AuditLog "session.marked_absent" event the sweep mirrors with payload by=sweep)
    - scheduling/management/commands/materialize_sessions.py (ASCII-only self.stdout.write(self.style.SUCCESS(...)) with `->`; idempotent command structure to mirror)
    - ops/notify.py (notify(role=Role.IFO_ADMIN, type="room_conflict", ...)); ops/models.py (RoomConflictFlag, AuditLog)
    - ops/policy.py (get_policy("grace_minutes") — derive the ORM cutoff from the SAME grace value; never hardcode)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("Pattern 2" sweep_no_shows; "Room-Conflict Detection & Dedup" detect_room_conflicts + dedup/auto-resolve; Pitfall 3 online exclusion; Pitfall 5 ASCII output)
  </read_first>
  <behavior>
    - sweep_no_shows(now=None): now defaults to timezone.now(); grace_min = get_policy("grace_minutes"); cutoff = now - timedelta(minutes=grace_min). Iterate `Session.objects.filter(status=SCHEDULED, scheduled_start__lt=cutoff)`; for each, SKIP if effective modality == "online" (leave SCHEDULED — documented Phase-3 hook) and SKIP if `is_no_show_past_grace(s.scheduled_start, now, grace_min)` is False (re-affirm the ORM cutoff via the shared predicate). Otherwise set status=ABSENT (only from SCHEDULED), save(update_fields=["status"]), and write AuditLog(event_type="session.marked_absent", target_type="session", target_id=pk, payload={"by": "sweep"}). NEVER touch room_released_at. Return count marked.
    - detect_room_conflicts(now=None): compute the current conflict set — rooms with 2+ ACTIVE sessions and room_released_at NULL (values("room_id").annotate(n=Count("id")).filter(n__gt=1)). For each conflicting room with conflict_key=f"room:{room_id}" and NO open RoomConflictFlag: create the flag and notify(role=Role.IFO_ADMIN, type="room_conflict", title/body naming the room) — first detection only. For each OPEN flag whose key is no longer in the current conflict set: stamp resolved_at (auto-resolve). Return count of new conflicts flagged.
    - run_status_sweep command: calls sweep_no_shows() then detect_room_conflicts(), prints an ASCII summary (e.g. "Sweep: marked N absent -> flagged M conflicts.").
  </behavior>
  <action>
    Create `scheduling/jobs.py` with a module docstring citing JOB-02b/JOB-02c. Implement `sweep_no_shows(now=None)` and `detect_room_conflicts(now=None)` exactly per the <behavior> above and the research "Pattern 2" + "Room-Conflict Detection & Dedup" sections. Wrap each session mutation in `transaction.atomic()` (mirrors materialize/_apply commit semantics). Use `get_policy("grace_minutes")` for the grace value and derive the ORM `cutoff` from it — do not hardcode 15. Compute effective modality as `s.declared_modality or s.schedule.modality` and skip online. Leave a clear inline comment at the online-skip marking it the Phase-3 hook: Phase 3 (Checker + MS Teams verification) flips online sessions into the sweep once the online-verify path exists — cite Phase 3 (do NOT reference Phase 7). The sweep MUST NOT set or read-modify room_released_at.

    Create `scheduling/management/commands/run_status_sweep.py` as a thin ASCII-only `BaseCommand` (mirror materialize_sessions.py) that calls both service functions and prints an ASCII `->` summary line via `self.style.SUCCESS(...)`. No Unicode arrows or emoji (Windows cp1252).
  </action>
  <acceptance_criteria>
    - `scheduling/jobs.py` defines `def sweep_no_shows(` and `def detect_room_conflicts(`; sweep imports and calls `is_no_show_past_grace(`; conflict detection calls `notify(role=` with `type="room_conflict"` and creates/resolves `RoomConflictFlag`.
    - `scheduling/jobs.py` contains an online-exclusion skip whose comment cites Phase 3 (not Phase 7); the sweep source does not reference `room_released_at`.
    - `grep -c 'grace_minutes' scheduling/jobs.py` >= 1 (grace comes from policy, not a literal 15).
    - `run_status_sweep.py` prints ASCII only (no non-ASCII arrow/emoji bytes).
    - `py -3.12 manage.py test scheduling.tests.SweepTests scheduling.tests.RoomConflictTests` exits 0 (all JOB-02b + JOB-02c behaviours GREEN), and `py -3.12 manage.py test scheduling.tests.FacultyResolverTests` still exits 0.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.SweepTests scheduling.tests.RoomConflictTests</automated>
  </verify>
  <done>The sweep marks F2F/Blended no-shows ABSENT (backfilled, idempotent, audited, online-excluded, never auto-releasing a room) and raises exactly one deduped IFO room-conflict flag that auto-resolves — JOB-02b + JOB-02c green via the shared predicate and the shared notify() path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated status mutation | The sweep changes session state with no human in the loop — an over-broad query could mass-mislabel sessions Absent |
| occupancy signal -> IFO alert | Room-conflict detection drives operational alerts; false positives erode trust, un-deduped alerts spam IFO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering (integrity) | sweep_no_shows status transition | high | mitigate | Only SCHEDULED->ABSENT (idempotency guard mirroring _apply); shared predicate re-affirms the ORM cutoff; online excluded; per-mutation transaction.atomic + AuditLog(by=sweep) |
| T-02-07 | Repudiation | sweep-marked absences | medium | mitigate | Every marked absence writes AuditLog(session.marked_absent, by=sweep), distinguishing sweep- from scan-marked absences |
| T-02-08 | Denial of Service | IFO notification spam | medium | mitigate | RoomConflictFlag open-per-key unique constraint + auto-resolve dedup: one notification per distinct conflict until resolved |
| T-02-09 | Elevation/Scope (unsafe auto-release) | room_released_at | high | mitigate | Sweep never stamps room_released_at (SweepTests guard); room lifecycle only via approved MOD-03 (Phase 4) |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (Django ORM + stdlib only) |
</threat_model>

<verification>
- `py -3.12 manage.py test scheduling ops` exits 0 (SweepTests + RoomConflictTests green; NOTIF-00 tests from 02-02 still green; Phase-1 tests unaffected).
- `py -3.12 manage.py makemigrations --check --dry-run` reports no missing migrations (RoomConflictFlag migration committed).
- The sweep uses `get_policy("grace_minutes")` and `is_no_show_past_grace`; it never references `room_released_at`.
</verification>

<success_criteria>
Phase-2 criteria #1, #2, #5 met: a scan and the sweep never disagree (shared predicate); an unscanned F2F/Blended no-show is marked Absent within one sweep, backfilled and idempotent, with an audit trail; online no-shows are excluded pending the Phase-3 verify path; contradictory occupancy raises a single deduped, auto-resolving IFO room-conflict notification; and the sweep never auto-releases a room.
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-foundations/02-03-SUMMARY.md` documenting: sweep_no_shows/detect_room_conflicts signatures + return values, the chosen dedup key (room:{room_id}), the online-exclusion Phase-3 hook location, confirmation the sweep never stamps room_released_at, and the RoomConflictFlag migration number (0002). Note Plan 02-05 registers `sweep_no_shows` as the scheduler's `sweep` job.
</output>
