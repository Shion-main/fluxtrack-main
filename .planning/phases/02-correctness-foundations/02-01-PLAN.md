---
phase: 02-correctness-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scheduling/resolver.py
  - scheduling/tests.py
autonomous: true
requirements: [JOB-02a]
notes:
  - "Wave-1 test isolation: this plan's new tests are SimpleTestCase (pure, NO database). It builds no test DB, so it never collides with Plan 02-02's DB-backed test run under parallel Wave-1 execution."
  - "Load-bearing refactor: the 16 existing FacultyResolverTests MUST stay green byte-for-byte. resolve_faculty_scan behaviour is preserved; only the inline grace comparison is lifted into a named predicate that the resolver now calls."

must_haves:
  truths:
    - "One pure predicate is_no_show_past_grace(scheduled_start, now, grace_min) exists in scheduling/resolver.py and resolve_faculty_scan calls it for its ABSENT decision (JOB-02a) — the single shared no-show rule the Phase-2 sweep will also import so scan-time and sweep-time can never disagree"
    - "The predicate is True strictly after scheduled_start + grace_min; at exactly +grace it is False (mirrors the existing `now > start + grace` semantics)"
    - "All 16 existing FacultyResolverTests still pass (resolve_faculty_scan outcomes unchanged); the resolver stays pure (no ORM, no timezone.now(), no side effects)"
  artifacts:
    - path: "scheduling/resolver.py"
      provides: "is_no_show_past_grace(scheduled_start, now, grace_min) pure predicate; resolve_faculty_scan wired to call it"
      contains: "def is_no_show_past_grace"
    - path: "scheduling/tests.py"
      provides: "NoShowPredicateTests (boundary 14/15/16) + CouplingIntegrityTests (resolver ABSENT <-> predicate for identical inputs), both SimpleTestCase"
      contains: "class NoShowPredicateTests"
  key_links:
    - from: "resolve_faculty_scan ABSENT branch"
      to: "is_no_show_past_grace"
      via: "direct call replacing the inline `now > candidate.scheduled_start + grace` comparison"
      pattern: "is_no_show_past_grace\\("
---

<objective>
Extract the scan resolver's inline no-show comparison (`now > scheduled_start + grace`) into ONE pure, named predicate `is_no_show_past_grace(scheduled_start, now, grace_min)` and make `resolve_faculty_scan` call it. This is the single shared atom (JOB-02a) that the Phase-2 status sweep (Plan 02-03) will import so scan-time and sweep-time can never diverge on whether a session is a no-show past grace — the highest-risk coupling in the phase.

Purpose: Locks Phase-2 success criterion #1 ("a scan and the sweep never disagree on the same session") behind a shared predicate + a coupling-integrity test, before any sweep code exists.
Output: A new pure predicate in `scheduling/resolver.py`, plus `NoShowPredicateTests` and `CouplingIntegrityTests` in `scheduling/tests.py`. No behaviour change to `resolve_faculty_scan`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-correctness-foundations/02-CONTEXT.md
@.planning/phases/02-correctness-foundations/02-RESEARCH.md
@.planning/phases/02-correctness-foundations/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@scheduling/resolver.py
@scheduling/tests.py

<interfaces>
<!-- Current resolver shape — scheduling/resolver.py -->
resolve_faculty_scan(sessions_today, scanned_room_id, occupying_session_id, now, *, grace_min, early_end_min, open_min=15) -> Resolution
# Line ~52: grace = timedelta(minutes=grace_min)
# Line ~89 (the atom to extract): if now > candidate.scheduled_start + grace: return Resolution(ABSENT, candidate.id)
# Outcome constants: CHECKED_IN, ABSENT, TOO_EARLY, WRONG_ROOM, ROOM_OCCUPIED, EARLY_END, ONLINE_REJECT, NO_SCHEDULE, CHECKED_OUT
# Resolver is PURE (SRS 6.6): no ORM, no timezone.now(), no save(); now + policy passed in as args.

<!-- Existing test harness to REUSE — scheduling/tests.py (imports `from scheduling import resolver as R`) -->
T0 = datetime(2026, 7, 6, 9, 0, tzinfo=timezone.utc)   # a scheduled start
sess(**kw)                # FakeSession: id=1, room_id=10, scheduled_start=T0, scheduled_end=T0+90min, status="scheduled"
resolve(sessions, room_id=10, occupying=None, now=T0, **kw)   # calls resolve_faculty_scan with grace_min=15, early_end_min=15
class FacultyResolverTests(SimpleTestCase)   # 16 pure tests — KEEP as-is, must stay green
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NoShowPredicateTests + CouplingIntegrityTests (RED)</name>
  <files>scheduling/tests.py</files>
  <read_first>
    - scheduling/tests.py (mirror the existing SimpleTestCase/dataclass style at the top of the file; append new classes, do NOT touch FacultyResolverTests or the DB-backed tests below it)
    - scheduling/resolver.py (confirm the ABSENT branch uses `now > candidate.scheduled_start + grace`; the predicate you will call is `is_no_show_past_grace(scheduled_start, now, grace_min)`)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md (Section "Code Examples" -> "Coupling-integrity: scan and sweep never disagree"; Section "Pattern 1: Pure predicate extraction" for the exact signature and boundary semantics)
    - .planning/phases/02-correctness-foundations/02-VALIDATION.md (rows JOB-02a NoShowPredicateTests + CouplingIntegrityTests — the contract these tests satisfy)
  </read_first>
  <behavior>
    - NoShowPredicateTests (SimpleTestCase, no DB): with grace_min=15 and a fixed start T0, `is_no_show_past_grace(T0, T0+14min, 15)` is False; at exactly +15min it is False (boundary — not yet past grace); at +16min it is True. Also assert True far past (e.g. +120min) and False before start.
    - CouplingIntegrityTests (SimpleTestCase, no DB): for a scheduled session whose check-in window contains `now` in the scanned room (reuse `sess()`/`resolve()`), the resolver returns ABSENT if-and-only-if `is_no_show_past_grace(T0, now, 15)` is True, across deltas (-1, 0, 14, 15, 16, 30) minutes from T0. This is the "scan and sweep never disagree" guarantee.
  </behavior>
  <action>
    Append two new SimpleTestCase classes to the pure-tests region at the top of `scheduling/tests.py` (above the `# DB-backed MSSQL foundation tests` divider). Import the predicate as `from scheduling.resolver import is_no_show_past_grace` (this import will FAIL until Task 2 lands — that is the intended RED state).

    NoShowPredicateTests: assert the predicate's boundary math directly using `T0` and `timedelta` — 14 min -> False, exactly 15 min -> False (equal, not strictly past), 16 min -> True, 120 min -> True, and a pre-start `now` -> False. Assert on the bare bool return (the predicate is a pure function of aware datetimes + int minutes; no DB, no policy lookup).

    CouplingIntegrityTests: for each delta in (-1, 0, 14, 15, 16, 30), set `now = T0 + timedelta(minutes=delta)`, call `resolve([sess()], now=now)` (window contains now, room matches, not online, not occupied), and assert `(r.outcome == R.ABSENT) == is_no_show_past_grace(T0, now, 15)`. Comment the class with a one-line note tying it to JOB-02a and success criterion #1 (scan and sweep share ONE predicate).

    Do NOT modify FacultyResolverTests or any DB-backed test below the divider.
  </action>
  <acceptance_criteria>
    - `scheduling/tests.py` contains `class NoShowPredicateTests` and `class CouplingIntegrityTests`, both subclassing `SimpleTestCase`, and imports `is_no_show_past_grace`.
    - Before Task 2, `py -3.12 manage.py test scheduling.tests.NoShowPredicateTests` is RED (ImportError / failure) because the predicate does not yet exist — this confirms the tests are wired to the not-yet-built symbol.
    - The new classes appear ABOVE the `# --- DB-backed MSSQL foundation tests ---` divider (they need no database).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.NoShowPredicateTests scheduling.tests.CouplingIntegrityTests</automated>
  </verify>
  <done>Both new SimpleTestCase classes exist and reference `is_no_show_past_grace`; the run is RED (predicate undefined) — the Nyquist Wave-0 scaffold for JOB-02a is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract is_no_show_past_grace() and wire resolve_faculty_scan (GREEN)</name>
  <files>scheduling/resolver.py</files>
  <read_first>
    - scheduling/resolver.py (the whole file — you will add one module-level pure function near the top and replace exactly one comparison at ~L89)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md (Section "Pattern 1: Pure predicate extraction" — signature `is_no_show_past_grace(scheduled_start, now, grace_min)` returning `now > scheduled_start + timedelta(minutes=grace_min)`; "Critical scope note" explaining why only this atom is shared, not the whole resolver)
    - .planning/codebase/CONVENTIONS.md (Section "Established Patterns" rule 1: resolver stays pure; Section "Docstrings" — cite the requirement ID JOB-02a in the function docstring)
  </read_first>
  <behavior>
    - `is_no_show_past_grace(scheduled_start, now, grace_min)` returns `now > scheduled_start + timedelta(minutes=grace_min)` — a pure bool, no ORM, no side effects, no `timezone.now()`.
    - `resolve_faculty_scan` produces byte-for-byte identical outcomes to before: the ABSENT branch now delegates to the predicate instead of computing the comparison inline.
  </behavior>
  <action>
    In `scheduling/resolver.py`, add a module-level pure function `is_no_show_past_grace(scheduled_start, now, grace_min)` that returns `now > scheduled_start + timedelta(minutes=grace_min)`. Place it after the outcome constants / `Resolution` dataclass and before `resolve_faculty_scan`. Give it a docstring citing JOB-02a and stating it is the SINGLE shared no-show predicate used by both `resolve_faculty_scan` (scan-time) and the Phase-2 sweep (sweep-time), taking aware datetimes + int minutes and returning a bool (SRS 6.6 purity).

    Then replace the inline comparison at ~L89 (`if now > candidate.scheduled_start + grace:`) with a call `if is_no_show_past_grace(candidate.scheduled_start, now, grace_min):`. Keep the local `grace = timedelta(minutes=grace_min)` if it is still used by other branches; if `grace` becomes unused after this change, remove it to avoid a dead local. Do not alter any other branch or the function signature.
  </action>
  <acceptance_criteria>
    - `scheduling/resolver.py` contains `def is_no_show_past_grace(` and `resolve_faculty_scan` calls it (grep `is_no_show_past_grace(` returns >= 2 matches: definition + call site).
    - `py -3.12 manage.py test scheduling.tests.NoShowPredicateTests scheduling.tests.CouplingIntegrityTests` exits 0 (both GREEN).
    - `py -3.12 manage.py test scheduling.tests.FacultyResolverTests` exits 0 — all 16 existing resolver tests still pass (no behaviour drift).
    - The resolver imports no ORM/`timezone` symbols it did not already import (still pure).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.FacultyResolverTests scheduling.tests.NoShowPredicateTests scheduling.tests.CouplingIntegrityTests</automated>
  </verify>
  <done>One shared pure predicate `is_no_show_past_grace` exists and backs the resolver's ABSENT decision; the boundary + coupling tests are GREEN and the 16 legacy resolver tests are unchanged — scan-time and sweep-time now provably share one rule (JOB-02a, criterion #1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan/sweep decision agreement | Two independent code paths must apply the identical no-show rule; divergence silently mislabels attendance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (data integrity) | is_no_show_past_grace vs resolver ABSENT branch | high | mitigate | Single extracted predicate imported by both paths; CouplingIntegrityTests asserts resolver ABSENT <-> predicate for identical inputs, blocking future drift |
| T-02-02 | Denial of Service | resolver purity | low | accept | Predicate is O(1) pure arithmetic; no I/O, no unbounded loop |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (pure refactor + tests); nothing to audit |
</threat_model>

<verification>
- `py -3.12 manage.py test scheduling.tests` exits 0 (NoShowPredicate + CouplingIntegrity GREEN; FacultyResolverTests still green; Phase-1 DB-backed tests unaffected).
- `is_no_show_past_grace` is the only place the `now > start + grace` comparison lives; the resolver calls it (grep confirms a single definition + one call site).
</verification>

<success_criteria>
Phase-2 success criterion #1 is locked at the predicate level: the scan resolver and the (upcoming) sweep share ONE pure `is_no_show_past_grace` predicate, proven by a coupling-integrity test that ties resolver ABSENT decisions to the predicate for identical inputs, with all 16 legacy resolver tests still green.
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-foundations/02-01-SUMMARY.md` documenting: the predicate signature + boundary semantics (strictly `>` past grace), the exact resolver line replaced, and confirmation the 16 FacultyResolverTests remained green. Note that Plan 02-03's sweep will import `is_no_show_past_grace` from `scheduling.resolver`.
</output>
