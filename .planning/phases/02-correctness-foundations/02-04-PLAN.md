---
phase: 02-correctness-foundations
plan: 04
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - ops/occupancy.py
  - ops/tests.py
autonomous: true
requirements: [JOB-02c]
notes:
  - "Wave-2 test isolation: run this plan's `manage.py test` with DB_TEST_NAME=test_fluxtrack_ops so its test-runner builds an isolated test DB and never collides with Plan 02-03's default test_fluxtrack under parallel Wave-2 execution (relies on the TEST[NAME] override in settings from Phase 1, 01-01). If DB_TEST_NAME is unavailable, execute-phase must serialize the two Wave-2 test runs."
  - "depends_on 02-02 is a file-ordering edge: this plan appends ReleaseRoomTests to ops/tests.py, which Plan 02-02 (Wave 1) created first. release_room() itself only uses the existing Session + AuditLog models."
  - "SCOPE: release_room() is built + fully tested here but is NOT called by anything in Phase 2. Its ONLY caller is MOD-03 (Phase 4). The sweep must never call it (the 'sweep never stamps room_released_at' guard lives in Plan 02-03 SweepTests)."

must_haves:
  truths:
    - "release_room(session, *, actor=None, now=None) is the single source of truth for releasing a room: it stamps Session.room_released_at and writes an AuditLog (event session.room_released)"
    - "release_room() is built and fully tested in Phase 2 but is invoked only by MOD-03 (Phase 4), never by the sweep or on a timer (timer-based auto-release was cut 2026-07-03)"
  artifacts:
    - path: "ops/occupancy.py"
      provides: "release_room(session, *, actor=None, now=None) occupancy helper"
      contains: "def release_room"
    - path: "ops/tests.py"
      provides: "ReleaseRoomTests (stamps room_released_at + writes the session.room_released AuditLog)"
      contains: "class ReleaseRoomTests"
  key_links:
    - from: "release_room(session)"
      to: "Session.room_released_at + ops.models.AuditLog"
      via: "session.save(update_fields=['room_released_at']) + AuditLog.objects.create(event_type='session.room_released')"
      pattern: "room_released_at"
---

<objective>
Build the shared `release_room()` occupancy helper (`ops/occupancy.py`) that stamps `Session.room_released_at` and writes an AuditLog — the single source of truth for releasing a room — and fully test it. Per the 2026-07-03 decision, timer-based auto-release is CUT: this helper is built now so Phase 4's modality-shift approval (MOD-03) can call it without re-opening this module, but nothing in Phase 2 invokes it (the sweep never releases rooms).

Purpose: Locks the JOB-02c "release_room() helper exists and is tested but is invoked only by the modality-approval flow (Phase 4), not on a timer" clause of Phase-2 success criterion #2.
Output: `ops/occupancy.py` with `release_room(...)`, and `ReleaseRoomTests` in `ops/tests.py`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-correctness-foundations/02-CONTEXT.md
@.planning/phases/02-correctness-foundations/02-RESEARCH.md
@.planning/phases/02-correctness-foundations/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@scheduling/models.py
@ops/models.py

<interfaces>
<!-- Session — scheduling/models.py -->
Session.room_released_at = DateTimeField(null=True, blank=True)   # the field release_room stamps

<!-- AuditLog — ops/models.py -->
AuditLog(actor nullable SET_NULL, event_type dotted noun.verb, target_type, target_id, payload JSON)
# actor=None is valid (system-initiated); use event_type="session.room_released"

<!-- release_room signature — 02-RESEARCH.md "release_room() Helper" -->
release_room(session, *, actor=None, now=None) -> None
# now = now or timezone.now(); session.room_released_at = now; save(update_fields=["room_released_at"]);
# AuditLog.objects.create(actor=actor, event_type="session.room_released", target_type="session",
#                         target_id=str(session.pk), payload={"released_at": now.isoformat()})
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ReleaseRoomTests (RED)</name>
  <files>ops/tests.py</files>
  <read_first>
    - ops/tests.py (Plan 02-02 populated this file with NotifyTests etc.; APPEND ReleaseRoomTests — do not disturb the existing classes)
    - scheduling/tests.py (reuse the make_session(scheduled_start, scheduled_end=None) factory at ~L135 to build a persisted Session)
    - ops/models.py (AuditLog fields to assert on)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("release_room() Helper" — the two required assertions: it stamps room_released_at + writes the audit row)
    - .planning/phases/02-correctness-foundations/02-VALIDATION.md (JOB-02c ReleaseRoomTests row — the contract; the paired "sweep NEVER stamps it" guard lives in Plan 02-03 SweepTests, not here)
  </read_first>
  <behavior>
    - ReleaseRoomTests(TestCase): given a persisted Session with room_released_at IS NULL, calling release_room(session) sets room_released_at to a non-null aware datetime and creates exactly one AuditLog with event_type="session.room_released" whose target_id is the session pk and whose payload records the release time. Passing an explicit actor records that actor; passing now= records that instant.
  </behavior>
  <action>
    Append `ReleaseRoomTests(TestCase)` to `ops/tests.py`. Import `from ops.occupancy import release_room` (FAILS until Task 2 — intended RED). Build a Session via the `make_session(...)` factory from `scheduling.tests` (import it), assert `room_released_at is None` beforehand, call `release_room(session)`, refresh_from_db, then assert `room_released_at is not None` and that an `AuditLog.objects.filter(event_type="session.room_released", target_id=str(session.pk)).count() == 1`. Add one case passing an explicit `actor` and asserting it is recorded on the AuditLog.
  </action>
  <acceptance_criteria>
    - `ops/tests.py` contains `class ReleaseRoomTests(TestCase)` importing `release_room`.
    - Before Task 2, `py -3.12 manage.py test ops.tests.ReleaseRoomTests` is RED (ImportError: ops.occupancy missing).
    - The existing 02-02 classes (NotifyTests / SingleWritePathTests) are untouched and still present.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests.ReleaseRoomTests</automated>
  </verify>
  <done>Wave-0 test for the release_room() helper exists and is RED, pinning the stamp + audit behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ops/occupancy.py release_room() (GREEN)</name>
  <files>ops/occupancy.py</files>
  <read_first>
    - scheduling/models.py (Session.room_released_at field)
    - ops/models.py (AuditLog shape; actor nullable)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("release_room() Helper" — exact body; the docstring MUST state: built in Phase 2, INVOKED ONLY by MOD-03 in Phase 4, and the sweep must NEVER call it)
    - .planning/codebase/CONVENTIONS.md (rule 2: every state change writes an AuditLog; cite JOB-02c/MOD-03 in the docstring)
  </read_first>
  <behavior>
    - release_room(session, *, actor=None, now=None): now defaults to timezone.now(); stamps session.room_released_at = now; save(update_fields=["room_released_at"]); writes AuditLog(actor=actor, event_type="session.room_released", target_type="session", target_id=str(session.pk), payload={"released_at": now.isoformat()}). Returns None.
  </behavior>
  <action>
    Create `ops/occupancy.py` with a module + function docstring per the research "release_room() Helper" section, explicitly noting: single source of truth for releasing a room, built + tested in Phase 2, INVOKED ONLY by MOD-03 (Phase 4) on an approved ->Online shift, and the status sweep must NEVER call this (no timer-based auto-release, decided 2026-07-03). Implement `release_room(session, *, actor=None, now=None)` exactly per <behavior> using `django.utils.timezone` and `ops.models.AuditLog`. Do not import or call the sweep; do not wire release_room into any Phase-2 caller.
  </action>
  <acceptance_criteria>
    - `ops/occupancy.py` defines `def release_room(session, *, actor=None, now=None)` and writes an AuditLog with `event_type="session.room_released"`.
    - The docstring states release_room is invoked only by MOD-03 (Phase 4) and never by the sweep.
    - `py -3.12 manage.py test ops.tests.ReleaseRoomTests` exits 0.
    - No Phase-2 module imports or calls `release_room` (grep for `release_room(` outside ops/occupancy.py and ops/tests.py returns 0 — the sweep does not call it).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests.ReleaseRoomTests</automated>
  </verify>
  <done>release_room() stamps room_released_at and audits it, is fully tested, and is called by nothing in Phase 2 — ready for MOD-03 (Phase 4) to consume without re-opening the module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| room lifecycle mutation | Releasing a room frees it for reassignment; an unaudited or premature release corrupts occupancy truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Repudiation | release_room room-release | medium | mitigate | Every release writes AuditLog(session.room_released) with actor + released_at; ReleaseRoomTests asserts the row |
| T-02-11 | Elevation/Scope (premature release) | timer-based auto-release | high | mitigate | release_room has zero Phase-2 callers (grep guard); only MOD-03 (Phase 4) will call it; the sweep's no-stamp guard is enforced in Plan 02-03 |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (Django ORM + stdlib only) |
</threat_model>

<verification>
- `py -3.12 manage.py test ops` exits 0 (ReleaseRoomTests green; 02-02 ops tests still green).
- `release_room(` appears only in `ops/occupancy.py` (definition) and `ops/tests.py` (test) — no Phase-2 production caller.
</verification>

<success_criteria>
The JOB-02c helper clause of Phase-2 criterion #2 is met: `release_room()` exists in `ops/occupancy.py`, stamps `room_released_at` with an AuditLog, is fully tested, and is invoked by nothing in Phase 2 (reserved for MOD-03, Phase 4) — proving timer-based auto-release was cut cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-foundations/02-04-SUMMARY.md` documenting: the release_room() signature, the audit event name (session.room_released), and explicit confirmation it has no Phase-2 caller (reserved for MOD-03 / Phase 4).
</output>
