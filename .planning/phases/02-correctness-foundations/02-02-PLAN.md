---
phase: 02-correctness-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ops/notify.py
  - web/scan.py
  - ops/tests.py
  - web/tests.py
autonomous: true
requirements: [NOTIF-00]
notes:
  - "Wave-1 test isolation: this plan runs DB-backed tests (ops + web). Run its `manage.py test` invocations with DB_TEST_NAME=test_fluxtrack (the default) — Plan 02-01 (the other Wave-1 plan) builds NO test DB (pure SimpleTestCase), so there is no parallel test-DB collision."
  - "This plan owns ops/tests.py first. Plans 02-04 (Wave 2) and 02-05 (Wave 3) append to ops/tests.py in LATER waves — no same-wave contention."

must_haves:
  truths:
    - "Every Notification row in the system is created by one shared notify() write path in ops/notify.py (NOTIF-00) — it targets a role (fan out to all active users of that role) OR an explicit user iterable"
    - "The ad-hoc _notify_ifo helper is gone from web/scan.py and both former call sites (room change, force handover) route through notify(role=IFO_ADMIN, type=\"room_event\", ...) with equivalent title/body"
    - "No module outside ops/notify.py constructs Notification rows directly (single-write-path invariant)"
    - "notify(role=...) reproduces the old _notify_ifo query exactly: role match + is_active=True fan-out"
  artifacts:
    - path: "ops/notify.py"
      provides: "notify(*, type, title, body='', link='', role=None, users=None) write-path service returning the created Notification list"
      contains: "def notify"
    - path: "web/scan.py"
      provides: "Room-change + force-handover notifications routed through notify(); the ad-hoc IFO notifier helper removed"
      contains: "from ops.notify import notify"
    - path: "ops/tests.py"
      provides: "NotifyTests (role fan-out to active users) + SingleWritePathTests (source guard: no ad-hoc notifier, no inline Notification create outside ops/notify.py)"
      contains: "class NotifyTests"
    - path: "web/tests.py"
      provides: "ScanNotifyTests (confirmed room-change / force-handover create IFO Notification rows via notify())"
      contains: "class ScanNotifyTests"
  key_links:
    - from: "web/scan.py _apply (WRONG_ROOM + ROOM_OCCUPIED branches)"
      to: "ops.notify.notify"
      via: "notify(role=Role.IFO_ADMIN, type=\"room_event\", title=..., body=...)"
      pattern: "notify\\(role="
    - from: "notify(role=...)"
      to: "active users of that role"
      via: "get_user_model().objects.filter(role=role, is_active=True)"
      pattern: "is_active=True"
---

<objective>
Build the single shared notification write path `notify()` in `ops/notify.py` (NOTIF-00), migrate both `_notify_ifo` call sites in `web/scan.py` onto it, and delete the ad-hoc IFO notifier so that after this plan every `Notification` row is created in exactly one place. This is the chokepoint every later writer uses (the sweep's room-conflict flags, job-failure alerts, and Phases 4/5's notifications).

Purpose: Locks Phase-2 success criterion #3 ("every notification is created by one shared notify() write path; the ad-hoc _notify_ifo is gone and no other inline notifier remains").
Output: `ops/notify.py` (new), a migrated `web/scan.py`, and Wave-0 tests: `NotifyTests` + `SingleWritePathTests` (`ops/tests.py`) and `ScanNotifyTests` (`web/tests.py`).
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-correctness-foundations/02-CONTEXT.md
@.planning/phases/02-correctness-foundations/02-RESEARCH.md
@.planning/phases/02-correctness-foundations/02-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@web/scan.py
@ops/models.py
@accounts/models.py

<interfaces>
<!-- Target model — ops/models.py -->
Notification(user=FK User, type=CharField(40), title=CharField(200), body=TextField blank, link=CharField(255) blank, read_at, created_at)
# ordering = ["-created_at"]. No dedup key, no resolved-state. NOTIF-00 needs NO model change.

<!-- Roles — accounts/models.py -->
Role (TextChoices): FACULTY="faculty", CHECKER="checker", IFO_ADMIN="ifo_admin", HR_ADMIN="hr_admin", GUARD="guard", DEAN="dean", SYSTEM_ADMIN="system_admin"

<!-- The exact behaviour notify() must subsume — web/scan.py (current) -->
# _notify_ifo(title, body, link="") at ~L63: for admin in get_user_model().objects.filter(role=Role.IFO_ADMIN, is_active=True): creates Notification(type="room_event", ...)
# Call site A ~L113 (WRONG_ROOM confirmed): _notify_ifo("Room change", f"{name} moved {course} from {old} to {room.code}.")
# Call site B ~L129 (ROOM_OCCUPIED confirmed): _notify_ifo("Force handover", f"{room.code}: prior session auto-completed; {course} started via handover.")
# web/scan.py already imports: from accounts.models import Role ; from ops.models import AuditLog, Notification

<!-- Recommended notify() signature — 02-RESEARCH.md "notify() Service Design" -->
notify(*, type, title, body="", link="", role=None, users=None) -> list[Notification]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add NotifyTests + SingleWritePathTests + ScanNotifyTests (RED)</name>
  <files>ops/tests.py, web/tests.py</files>
  <read_first>
    - ops/tests.py (currently a 3-line stub; replace with real tests importing TestCase)
    - web/tests.py (currently a 3-line stub)
    - accounts/models.py (User + Role for building active/inactive IFO admins in fixtures)
    - ops/models.py (Notification fields the tests assert on)
    - web/scan.py (understand the confirm flow: WRONG_ROOM/ROOM_OCCUPIED are two-step signed-token confirms via /scan/confirm; the notification fires inside _apply on confirm)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("notify() Service Design" — role fan-out + migration examples)
    - .planning/phases/02-correctness-foundations/02-VALIDATION.md (rows NOTIF-00: NotifyTests, ScanNotifyTests, SingleWritePathTests)
  </read_first>
  <behavior>
    - NotifyTests (TestCase, DB): `notify(role=Role.IFO_ADMIN, type="room_event", title="t", body="b")` creates exactly one Notification per ACTIVE IFO admin and zero for inactive users or other roles; returned list length equals the active-IFO-admin count. `notify(users=[u1, u2], type="x", title="t")` creates one row per given user. Passing neither role nor users creates nothing.
    - SingleWritePathTests (TestCase / source guard): reading the source of `web/scan.py` shows it no longer defines the ad-hoc IFO notifier helper; reading the source of the notifier-using modules (web/scan.py, scheduling/jobs.py if present) shows the Notification-model create call appears only inside ops/notify.py. Implement by reading specific module source files (e.g. via `inspect.getsource` on the imported module, or reading the file path) and asserting the absence — scope to named modules so the test never matches its own source.
    - ScanNotifyTests (TestCase, DB, Django test Client): completing a confirmed room-change and a confirmed force-handover each creates IFO Notification row(s) of `type="room_event"` with the expected title, proving the migrated call sites still notify.
  </behavior>
  <action>
    Replace the stub bodies of `ops/tests.py` and `web/tests.py` with real test classes. Import `from ops.notify import notify` in ops/tests.py — this import FAILS until Task 2, the intended RED state.

    ops/tests.py — NotifyTests(TestCase): create 2 active IFO admins, 1 inactive IFO admin, 1 faculty user; assert `notify(role=Role.IFO_ADMIN, ...)` yields exactly 2 Notification rows (active only) and returns a 2-element list; assert `notify(users=[faculty], ...)` yields 1 row; assert the type/title/body land on the row. SingleWritePathTests(TestCase): assert the ad-hoc IFO notifier helper name is absent from `web/scan.py` source, and that the Notification-model create pattern is absent from every notifier-using module except `ops/notify.py`. Read each target file's text explicitly by path and assert with plain string checks; keep the forbidden token out of comments in the assertions by building it from parts if needed so the guard cannot match itself.

    web/tests.py — ScanNotifyTests(TestCase): use `django.test.Client`, log in a faculty user with an active session, drive a WRONG_ROOM scan then post the signed confirm token to `/scan/confirm` (room change), and separately drive a ROOM_OCCUPIED handover confirm; after each, assert a `Notification` of `type="room_event"` exists for the IFO admin with the migrated title ("Room change" / "Force handover"). Reuse the FK-chain construction style from `scheduling/tests.py::make_session` for building rooms/sessions.
  </action>
  <acceptance_criteria>
    - `ops/tests.py` contains `class NotifyTests(TestCase)` and `class SingleWritePathTests`; `web/tests.py` contains `class ScanNotifyTests`.
    - Before Task 2, `py -3.12 manage.py test ops.tests.NotifyTests` is RED (ImportError: ops.notify missing).
    - SingleWritePathTests reads named module source files (not the test file itself) so it cannot self-match the forbidden pattern.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests.NotifyTests ops.tests.SingleWritePathTests web.tests.ScanNotifyTests</automated>
  </verify>
  <done>Wave-0 tests for NOTIF-00 exist and are RED (notify() undefined); the single-write-path guard and the scan-migration behaviour tests are wired to the not-yet-built service.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ops/notify.py notify() write path (GREEN for NotifyTests)</name>
  <files>ops/notify.py</files>
  <read_first>
    - ops/models.py (Notification field names — user/type/title/body/link)
    - accounts/models.py (Role choices; User model via get_user_model)
    - web/scan.py (the exact _notify_ifo query being reproduced: filter(role=Role.IFO_ADMIN, is_active=True))
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("notify() Service Design" — signature `notify(*, type, title, body='', link='', role=None, users=None)`, role fan-out, "notify() does NOT emit its own AuditLog" recommendation)
    - .planning/codebase/CONVENTIONS.md ("Docstrings" cite NOTIF-00; "Function & Module Design" import directly, no barrel files)
  </read_first>
  <behavior>
    - notify(*, type, title, body="", link="", role=None, users=None): builds a recipient list = explicit `users` (if given) PLUS all active users of `role` (if given: `get_user_model().objects.filter(role=role, is_active=True)`); creates one Notification per recipient with the given fields; returns the created list. Neither role nor users -> empty list, no rows.
    - notify() is keyword-only for all args, does NOT write an AuditLog (the triggering domain action carries the audit — per research recommendation), and is the ONLY place the Notification-model create call is allowed.
  </behavior>
  <action>
    Create `ops/notify.py` with a module docstring citing NOTIF-00 (single Notification write path). Implement `notify(*, type, title, body="", link="", role=None, users=None)` per the research signature: assemble recipients from `users` and/or the active-users-of-role query (`get_user_model().objects.filter(role=role, is_active=True)`), then create and return the list of Notification rows. Do not emit an AuditLog here (documented decision: the domain action that triggers notify is already audited). This module is the single permitted caller of the Notification-model create path.
  </action>
  <acceptance_criteria>
    - `ops/notify.py` exists and defines `def notify(` with keyword-only params `type, title, body, link, role, users`.
    - `py -3.12 manage.py test ops.tests.NotifyTests` exits 0 (active-only fan-out + explicit-users + empty-target cases all GREEN).
    - notify() does not create an AuditLog row (NotifyTests may assert AuditLog count unchanged).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests.NotifyTests</automated>
  </verify>
  <done>The shared notify() write path exists, fans out to active users of a role (reproducing the old IFO query) or to explicit users, and is the single Notification creation site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate web/scan.py onto notify() and remove the ad-hoc IFO notifier (GREEN)</name>
  <files>web/scan.py</files>
  <read_first>
    - web/scan.py (the ad-hoc IFO notifier helper at ~L63 and its two call sites at ~L113 room change and ~L129 force handover; the file already imports Role and Notification)
    - ops/notify.py (the notify() signature just built)
    - .planning/phases/02-correctness-foundations/02-RESEARCH.md ("notify() Service Design" -> "Migration of the two call sites" — preserve type="room_event", titles/bodies; then delete the helper and its local get_user_model import)
    - .planning/codebase/CONVENTIONS.md (rule 2: state changes already emit AuditLog in _apply — leave those audit() calls intact; notify() is the side-notification only)
  </read_first>
  <behavior>
    - The confirmed room-change branch calls `notify(role=Role.IFO_ADMIN, type="room_event", title="Room change", body=...)` with the same body text as before.
    - The confirmed force-handover branch calls `notify(role=Role.IFO_ADMIN, type="room_event", title="Force handover", body=...)` with the same body text.
    - The ad-hoc IFO notifier helper and its local `get_user_model` import are removed; the existing `audit(...)` calls in `_apply` are untouched.
  </behavior>
  <action>
    In `web/scan.py`: add `from ops.notify import notify`. Replace the two ad-hoc IFO notifier calls inside `_apply` (WRONG_ROOM room-change and ROOM_OCCUPIED force-handover branches) with `notify(role=Role.IFO_ADMIN, type="room_event", title=..., body=...)`, keeping the identical title strings ("Room change", "Force handover") and body f-strings. Then remove the ad-hoc IFO notifier helper function entirely and its now-unused local `get_user_model` import. If the top-level `Notification` import becomes unused after removal, drop it from the import line (leave `AuditLog`). Leave every `audit(...)` call and all resolver/idempotency logic unchanged. Do NOT leave a comment in the file that names the removed helper.
  </action>
  <acceptance_criteria>
    - `grep -c 'def _notify_ifo' web/scan.py` returns 0 (the ad-hoc helper is gone).
    - `web/scan.py` contains `from ops.notify import notify` and two `notify(role=Role.IFO_ADMIN, type="room_event"` call sites.
    - `py -3.12 manage.py test ops.tests.SingleWritePathTests web.tests.ScanNotifyTests` exits 0 (single-write-path guard passes; both confirmed flows still create the room_event Notification rows).
    - `py -3.12 manage.py test web scheduling.tests.FacultyResolverTests` exits 0 (no scan-flow regression; resolver untouched).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests.SingleWritePathTests web.tests.ScanNotifyTests</automated>
  </verify>
  <done>Both scan-flow IFO notifications route through the shared notify() path, the ad-hoc notifier is deleted, and the single-write-path guard is GREEN — Phase-2 criterion #3 met at the scan layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain action -> Notification write | All notification rows must funnel through one auditable path; scattered inline notifiers drift and bypass future mute/targeting rules |
| role targeting | notify(role=...) fans out to users by role + active flag — a wrong filter could leak notifications to inactive/other-role accounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Information Disclosure | notify(role=...) recipient query | medium | mitigate | Reproduce the exact `filter(role=role, is_active=True)` query; NotifyTests asserts inactive users and other roles receive nothing |
| T-02-04 | Repudiation | single write path | low | mitigate | One creation site (SingleWritePathTests source guard) keeps notification provenance centralized; the triggering domain action remains AuditLog-recorded in _apply |
| T-02-05 | Tampering | scan-flow migration | medium | mitigate | ScanNotifyTests drives the real confirm endpoints and asserts equivalent room_event rows, preventing a silent notification regression during migration |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (stdlib + existing Django ORM only) |
</threat_model>

<verification>
- `py -3.12 manage.py test ops web` exits 0 (NotifyTests, SingleWritePathTests, ScanNotifyTests all green).
- `grep -c 'def _notify_ifo' web/scan.py` == 0 and the Notification-model create call exists only in `ops/notify.py` among notifier modules.
- `py -3.12 manage.py test scheduling.tests.FacultyResolverTests` still exits 0 (resolver untouched).
</verification>

<success_criteria>
Phase-2 success criterion #3 met: `ops/notify.py::notify()` is the single Notification write path; both `web/scan.py` IFO notifications route through it; the ad-hoc `_notify_ifo` helper is deleted and no inline notifier remains outside `ops/notify.py`, proven by NotifyTests, SingleWritePathTests, and ScanNotifyTests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-foundations/02-02-SUMMARY.md` documenting: the final notify() signature, the AuditLog-in-notify decision (none — domain action carries the audit), the two migrated call sites, and confirmation the ad-hoc helper is gone. Note that Plans 02-03 (room-conflict flags) and 02-05 (job-failure alerts to System Admins) will import `notify` from `ops.notify`.
</output>
