---
phase: 01-mssql-environment-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config/settings.py
  - .env.example
autonomous: false
requirements: [ENV-01]
user_setup:
  - service: sqlserver-express
    why: "App, tests, and prod all run on SQL Server (MSSQL-only decision). The Django test runner needs CREATE DATABASE to build test_fluxtrack."
    env_vars:
      - name: DB_HOST
        source: "127.0.0.1 for local Express (or named-instance host)"
      - name: DB_PORT
        source: "1433 (default) — check SQL Server Configuration Manager if a named instance uses a dynamic port"
      - name: DB_NAME
        source: "fluxtrack"
      - name: DB_USER
        source: "the dedicated SQL Server login you create (NOT sa)"
      - name: DB_PASSWORD
        source: "the dedicated login's password"
      - name: DB_ODBC_EXTRA
        source: "Encrypt=yes;TrustServerCertificate=yes (local self-signed cert)"
    dashboard_config:
      - task: "Install SQL Server Express 2022 natively + Microsoft ODBC Driver 18 for SQL Server"
        location: "Windows MSI installers"
      - task: "Create a dedicated login (NOT sa), grant server role dbcreator, and CREATE DATABASE fluxtrack COLLATE SQL_Latin1_General_CP1_CI_AS"
        location: "SSMS or sqlcmd"

must_haves:
  truths:
    - "The app boots and `migrate` applies the full existing schema on SQL Server with DB_ENGINE unset/mssql (proves nullable-unique azure_oid inserts cleanly)"
    - "seed_demo creates the 7 role users + policy SystemSettings on SQL Server with no IntegrityError"
    - "Django is pinned to 6.0.6 and mssql-django to 1.7.3; the sqlite and mysql branches are gone"
  artifacts:
    - path: "config/settings.py"
      provides: "Single MSSQL DATABASES config (ENGINE mssql, ODBC Driver 18, env-driven encryption)"
      contains: "\"ENGINE\": \"mssql\""
    - path: "requirements.txt"
      provides: "Pinned Django 6.0.6 + mssql-django 1.7.3"
      contains: "mssql-django==1.7.3"
    - path: ".env.example"
      provides: "MSSQL env template (DB_HOST/PORT/NAME/USER/PASSWORD/DB_ODBC_EXTRA)"
      contains: "DB_ODBC_EXTRA"
  key_links:
    - from: "config/settings.py DATABASES.default.OPTIONS.extra_params"
      to: "env('DB_ODBC_EXTRA')"
      via: "env-driven ODBC connection string (dev self-signed vs prod real cert)"
      pattern: "extra_params.*DB_ODBC_EXTRA"
    - from: "config/settings.py DATABASES.default.ENGINE"
      to: "mssql-django backend"
      via: "\"ENGINE\": \"mssql\""
      pattern: "ENGINE.*mssql"
---

<objective>
Cut FluxTrack over to SQL Server: pin Django 6.0.6 + mssql-django 1.7.3, replace the sqlite/mysql DB switch in `config/settings.py` with a single env-driven `mssql` branch, rewrite `.env.example`, then prove the full existing schema migrates and seeds cleanly on a real SQL Server Express instance.

Purpose: Establish the proven MSSQL runtime base that every later phase (and both validation plans in Wave 2) builds on. This is success criterion 1 of the phase (app boots and serves every surface on `DB_ENGINE=mssql`).

Output: MSSQL-only settings, pinned deps, an updated env template, and a fresh migrated + seeded `fluxtrack` database.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mssql-environment-data-foundation/01-CONTEXT.md
@.planning/phases/01-mssql-environment-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Current DB switch to REPLACE — config/settings.py lines 77-96 -->
Current (config/settings.py):
```python
# --- Database: SQLite dev, MySQL prod ---
if env("DB_ENGINE", "sqlite") == "mysql":
    DATABASES = { "default": { "ENGINE": "django.db.backends.mysql", ... } }
else:
    DATABASES = { "default": { "ENGINE": "django.db.backends.sqlite3",
                               "NAME": BASE_DIR / "db.sqlite3" } }
```
The `env()` and `env_bool()` helpers already exist (settings.py lines 16-21). `USE_TZ = True` and `TIME_ZONE = "Asia/Manila"` are already set (lines 109-111) — do NOT change them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin deps and replace the DB switch with an MSSQL-only branch</name>
  <files>requirements.txt, config/settings.py, .env.example</files>
  <read_first>
    - requirements.txt (current pins to replace)
    - config/settings.py (lines 1-5 docstring, 16-21 env helpers, 77-96 DB switch, 107-111 TZ block)
    - .env.example (current SQLite/MySQL template to rewrite)
    - .planning/phases/01-mssql-environment-data-foundation/01-RESEARCH.md ("Pattern 1: The `mssql` settings branch" — copy the config verbatim)
  </read_first>
  <action>
    Make three edits with these EXACT values:

    1. **requirements.txt** — change the Core block so it reads:
       ```
       Django==6.0.6
       mssql-django==1.7.3          # pulls pyodbc; needs ODBC Driver 18 installed system-wide
       djangorestframework>=3.15
       python-dotenv>=1.0
       whitenoise>=6.6              # static file serving on EC2
       ```
       DELETE the line `Django>=5.0,<7.0`. DELETE the trailing block
       `# Production database (local dev uses SQLite by default)` and
       `# mysqlclient>=2.2          # uncomment for MySQL 8.0 on AWS RDS`
       (MySQL is dropped entirely). Leave the Media/reports, Scheduled-jobs, and Identity blocks unchanged.
       Then run: `py -3.12 -m pip install -r requirements.txt`

    2. **config/settings.py docstring (lines 1-5)** — replace the stale
       "SQLite by default … MySQL 8.0 on AWS RDS" text with:
       ```
       """
       Django settings for FluxTrack.

       Env-driven (see .env / .env.example). SQL Server only (dev, test, prod)
       via mssql-django; local Express uses a self-signed cert (Encrypt=yes;
       TrustServerCertificate=yes), prod (RDS) trusts a real cert chain — the
       difference is purely DB_ODBC_EXTRA in each .env.
       """
       ```

    3. **config/settings.py DB block (lines 77-96)** — replace the entire
       `if env("DB_ENGINE"...) == "mysql": ... else: ... sqlite3 ...` block with
       exactly this single config (no sqlite branch, no mysql branch):
       ```python
       # --- Database: SQL Server only (dev, test, prod) ---
       DATABASES = {
           "default": {
               "ENGINE": "mssql",
               "NAME": env("DB_NAME", "fluxtrack"),
               "USER": env("DB_USER", ""),          # dedicated login, NOT sa
               "PASSWORD": env("DB_PASSWORD", ""),
               "HOST": env("DB_HOST", "127.0.0.1"),
               "PORT": env("DB_PORT", "1433"),
               "OPTIONS": {
                   "driver": "ODBC Driver 18 for SQL Server",
                   # env-driven encryption: local Express self-signed (trust it);
                   # RDS real cert chain in prod is just a different DB_ODBC_EXTRA
                   "extra_params": env(
                       "DB_ODBC_EXTRA",
                       "Encrypt=yes;TrustServerCertificate=yes",
                   ),
               },
           }
       }
       ```

    4. **.env.example** — replace the DB section (lines 6-12, the SQLite/MySQL
       comments) with:
       ```
       # Database — SQL Server only (mssql-django + ODBC Driver 18). NOT sa.
       DB_NAME=fluxtrack
       DB_USER=fluxtrack_app
       DB_PASSWORD=
       DB_HOST=127.0.0.1
       DB_PORT=1433
       # Local Express uses a self-signed cert; prod (RDS) uses a real cert chain:
       DB_ODBC_EXTRA=Encrypt=yes;TrustServerCertificate=yes
       ```
       Also update the top comment (line 1) from "all defaults (SQLite)" to
       "Copy to .env and set the DB_* values for your local SQL Server Express instance."
  </action>
  <verify>
    <automated>py -3.12 -c "import django,mssql; print(django.get_version())" | grep -q "^6.0.6$"</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.12 -c "import django; print(django.get_version())"` prints exactly `6.0.6`
    - `py -3.12 -m pip show mssql-django` reports `Version: 1.7.3`
    - `config/settings.py` contains the string `"ENGINE": "mssql"` and does NOT contain `django.db.backends.sqlite3` or `django.db.backends.mysql`
    - `config/settings.py` contains `extra_params` and `DB_ODBC_EXTRA`
    - `requirements.txt` contains `mssql-django==1.7.3` and `Django==6.0.6`, and does NOT contain `mysqlclient` or `Django>=5.0`
    - `.env.example` contains `DB_ODBC_EXTRA` and does NOT contain `DB_ENGINE=mysql`
    - `py -3.12 manage.py check` exits 0
  </acceptance_criteria>
  <done>Django==6.0.6 + mssql-django==1.7.3 installed and imported; settings has one MSSQL branch (sqlite/mysql gone); `.env.example` documents the MSSQL env vars; `manage.py check` passes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision local SQL Server Express + dedicated login + CI database</name>
  <files>.env</files>
  <read_first>
    - .env.example (the DB_* keys to copy into your real .env)
    - .planning/phases/01-mssql-environment-data-foundation/01-RESEARCH.md (Pitfall 4: test-DB default collation must be _CI_; Validation Architecture: dev login needs CREATE DATABASE / dbcreator)
  </read_first>
  <action>
    HUMAN-ONLY setup (native MSI install + login/grant in SSMS — Claude cannot do this).
    Task 1 already pointed Django at SQL Server via mssql-django; this step makes a real
    instance + a case-INsensitive `fluxtrack` DB + a dbcreator login exist so anything
    DB-backed (migrate, seed, tests) can run. Steps:
    1. Install SQL Server Express 2022 natively (not Docker/LocalDB) and Microsoft ODBC Driver 18.
    2. Create a dedicated login (NOT `sa`), e.g. `fluxtrack_app`, with a password, and grant dbcreator:
       `CREATE LOGIN fluxtrack_app WITH PASSWORD = '<strong-pw>';`
       `ALTER SERVER ROLE dbcreator ADD MEMBER fluxtrack_app;`
    3. Create the app DB case-INsensitive (email dedup depends on it — Pitfall 4):
       `CREATE DATABASE fluxtrack COLLATE SQL_Latin1_General_CP1_CI_AS;`
    4. Copy `.env.example` to `.env` and fill DB_USER=fluxtrack_app, DB_PASSWORD, DB_HOST, DB_PORT,
       keeping DB_ODBC_EXTRA=Encrypt=yes;TrustServerCertificate=yes.
    If `SELECT SERVERPROPERTY('Collation')` is `_CS_`, STOP — fix the instance/DB collation to `_CI_` first.
  </action>
  <what-built>
    Task 1 pointed Django at SQL Server via mssql-django. Before anything DB-backed can run,
    a real SQL Server Express instance, a dedicated login, and a case-INsensitive `fluxtrack`
    database must exist. This is genuinely human-only (native MSI install + login/grant in SSMS).
  </what-built>
  <how-to-verify>
    After the setup above, confirm (paste outputs when resuming):
    - `sqlcmd -S localhost -U fluxtrack_app -P <pw> -Q "SELECT SERVERPROPERTY('Collation')"` returns a name containing `_CI_`
    - `sqlcmd -S localhost -U fluxtrack_app -P <pw> -Q "SELECT DATABASEPROPERTYEX('fluxtrack','Collation')"` returns a name containing `_CI_`
    - `sqlcmd -S localhost -U fluxtrack_app -P <pw> -Q "SELECT IS_SRVROLEMEMBER('dbcreator')"` returns `1`
    - `py -3.12 -c "import pyodbc; print([d for d in pyodbc.drivers() if 'ODBC Driver 18' in d])"` prints a non-empty list
  </how-to-verify>
  <verify>
    <automated>py -3.12 -c "import pyodbc,sys; sys.exit(0 if any('ODBC Driver 18' in d for d in pyodbc.drivers()) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `SELECT SERVERPROPERTY('Collation')` output contains `_CI_`
    - `SELECT DATABASEPROPERTYEX('fluxtrack','Collation')` output contains `_CI_`
    - `SELECT IS_SRVROLEMEMBER('dbcreator')` returns `1` for the app login
    - `pyodbc.drivers()` includes `ODBC Driver 18 for SQL Server`
    - `.env` exists with DB_USER (not `sa`), DB_PASSWORD, DB_HOST, DB_PORT, DB_ODBC_EXTRA set
  </acceptance_criteria>
  <done>A SQL Server Express instance, a dbcreator login (not sa), and a case-INsensitive `fluxtrack` DB exist; `.env` is populated; ODBC Driver 18 is installed.</done>
  <resume-signal>Type "approved" with the four verification outputs, or describe the blocker (e.g. server collation is _CS_).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Migrate + seed the fresh MSSQL schema (bring-up smoke)</name>
  <files>config/settings.py</files>
  <read_first>
    - config/settings.py (confirm the mssql branch from Task 1 is in place)
    - accounts/management/commands/seed_demo.py (creates the 7 role users + policy SystemSettings)
    - accounts/models.py (User.azure_oid = CharField(unique=True, null=True) — the nullable-unique field mssql-django implements as a filtered unique index; all 7 seed users have NULL azure_oid)
    - .planning/phases/01-mssql-environment-data-foundation/01-RESEARCH.md (Pitfall 1: nullable-unique azure_oid on CREATE)
  </read_first>
  <action>
    Against the `fluxtrack` DB provisioned in Task 2, run a clean rebuild in order:
    ```
    py -3.12 manage.py migrate
    py -3.12 manage.py seed_demo
    ```
    `migrate` must apply all five apps' `0001_initial` migrations on SQL Server with no error —
    this is the live proof that the nullable-unique `azure_oid` inserts as a filtered unique
    index and that nothing in the existing schema is MSSQL-hostile (per the RESEARCH schema audit).
    `seed_demo` must create the 7 role users (all with `azure_oid = NULL`) with no `IntegrityError` /
    `UNIQUE KEY` violation — the direct proof of Pitfall 1's mitigation.
    Do NOT add any AlterField on `azure_oid`; do NOT squash migrations.
    If a migration op fails on SQL Server, add a corrective fix-forward migration (keep history) —
    do not squash to a new baseline.
    Finally boot-smoke: `py -3.12 manage.py runserver` starts without a DB error (Ctrl-C after it binds).
  </action>
  <verify>
    <automated>py -3.12 manage.py migrate --check 2>&1 | grep -qiv "have changes that are not yet reflected" && py -3.12 manage.py shell -c "from accounts.models import User; print(User.objects.count())"</automated>
  </verify>
  <acceptance_criteria>
    - `py -3.12 manage.py migrate` exits 0 with all migrations applied (`py -3.12 manage.py showmigrations | grep -c "\[ \]"` returns `0`)
    - After `seed_demo`, `User.objects.count()` is `>= 7` and `User.objects.filter(azure_oid__isnull=True).count() >= 7` with no IntegrityError raised
    - `py -3.12 manage.py check --database default` exits 0
    - `manage.py runserver` binds without a database connection error
  </acceptance_criteria>
  <done>Full schema migrated cleanly on SQL Server; 7 role users seeded with NULL azure_oid and no unique violation; server boots against MSSQL.</done>
</task>

</tasks>

<verification>
- `config/settings.py` has exactly one MSSQL DATABASES config; `grep -c "django.db.backends" config/settings.py` returns 0.
- `migrate` applies the full existing schema on SQL Server (nullable-unique azure_oid proven at CREATE).
- `seed_demo` runs with no IntegrityError; 7 role users exist.
- Deps pinned: Django==6.0.6, mssql-django==1.7.3.
</verification>

<success_criteria>
Phase success criterion 1 met: the app boots and `migrate`+`seed_demo` succeed on SQL Server Express with the mssql settings branch, Django pinned to 6.0.6 / mssql-django 1.7.3, sqlite and mysql branches removed, and all DB config env-driven so prod is a `.env` swap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mssql-environment-data-foundation/01-01-SUMMARY.md` documenting: the final settings block, the exact deps installed (re-confirm `pip show mssql-django`), the SQL Server instance/login/collation used, and any fix-forward migration added.
</output>
