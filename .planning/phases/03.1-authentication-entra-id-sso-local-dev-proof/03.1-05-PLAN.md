---
phase: 03.1-authentication-entra-id-sso-local-dev-proof
plan: 05
type: execute
wave: 4
depends_on: ["03.1-04"]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-03]
user_setup:
  - service: microsoft-entra-id
    why: "The D-09 proof requires a real interactive Entra login against the project-owned MMCM tenant, which cannot run headless."
    env_vars:
      - name: SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_KEY
        source: "confirm present in .env before the round-trip (Assumption A3)"
      - name: SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_SECRET
        source: "confirm present in .env"
      - name: SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_TENANT_ID
        source: "confirm present in .env"
    dashboard_config:
      - task: "Have a real MMCM tenant account whose UPN you will bind via link_entra (the faculty proof), and — for D-09 #2 — a second real MMCM account that is NOT bound to any seeded user."
        location: "Microsoft Entra (MMCM tenant) — existing accounts; no portal change needed (redirect URI already registered)"

must_haves:
  truths:
    - "A real Microsoft login round-trips on http://localhost:8000 and lands the bound faculty account on the faculty role-home (D-09 #1)."
    - "A real authenticated MMCM account with no provisioned/bound User is refused — it lands back on /login with a visible refusal and no new User row is created (D-09 #2 / AUTH-03)."
    - "With DEBUG=False, the superuser break-glass still logs in via ModelBackend and the dev-login user list is hidden while the Microsoft button remains (D-09 #3 / D-08)."
    - "The authorize redirect includes a code_challenge param (PKCE is really in effect, Pitfall 1 sanity check)."
  artifacts: []
  key_links:
    - "Open the app via http://localhost:8000 (NOT 127.0.0.1) or Entra returns AADSTS50011 redirect-uri mismatch (Pitfall 3)."
    - "This gate runs only after Plan 04's full automated suite is green; it proves the three D-09 criteria live."
---

<objective>
Prove the phase's definition of done (D-09) end-to-end with the real MMCM Entra tenant on
localhost: (1) a bound faculty account signs in via Microsoft and lands on the faculty
home; (2) an unprovisioned tenant account is refused; (3) the DEBUG dev-login stub and the
superuser break-glass still work. This is the human-in-the-loop gate the automated tests
cannot cover.

Purpose: Close the proof. The automated suite (Plan 04) locks the wiring and policy; this
gate demonstrates the actual OAuth round-trip, the live refusal, and the break-glass path
against the real IdP — the whole point of the local-dev proof.

Output: recorded UAT results (pass/fail per D-09 criterion) captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-CONTEXT.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-RESEARCH.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual UAT — real Entra round-trip, live refusal, and break-glass (D-09)</name>
  <read_first>
    - VALIDATION.md "Manual-Only Verifications" (the three manual rows and their instructions)
    - RESEARCH.md "Pitfall 3" (localhost not 127.0.0.1) and "Open Questions #1" (a second unbound MMCM account is needed for D-09 #2) and Assumption A3 (confirm .env keys)
    - CONTEXT.md D-09 (the three success criteria)
  </read_first>
  <action>
    Human operator runs the three D-09 verifications on the running app; the executor pauses
    at this blocking gate until the operator reports results. No code is written in this plan.
  </action>
  <what-built>
    The full local Entra SSO path is now wired and automatically tested: the PKCE backend,
    the provisioned-only pipeline (deny + oid), the link_entra command, the always-visible
    Microsoft button, and dev-login coexistence. This gate confirms it works live.
  </what-built>
  <how-to-verify>
    Preconditions:
    - Confirm `.env` contains SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_KEY, _SECRET, and _TENANT_ID (A3).
    - Ensure DEBUG=True and the DB is seeded: `py -3.12 manage.py seed_demo`.
    - Bind the faculty slot to your real UPN: `py -3.12 manage.py link_entra mayo <your-real-MMCM-UPN>` (expect a "Linked mayo -> <upn>" line).
    - Start the server: `py -3.12 manage.py runserver`.

    D-09 #1 — real login -> faculty home:
    1. Open http://localhost:8000/ in a browser (use localhost, NOT 127.0.0.1 — Pitfall 3).
    2. On /login click "Sign in with Microsoft".
    3. (Sanity) Confirm the authorize URL in the address bar contains a `code_challenge` param (PKCE really on).
    4. Authenticate as the bound MMCM account.
    5. Expected: you are redirected to "/" and land on the FACULTY home surface (My schedule / Check in cards). Note the User.azure_oid is now populated for mayo.

    D-09 #2 — unprovisioned account refused:
    6. Log out. From /login click "Sign in with Microsoft" again and authenticate as a DIFFERENT real MMCM account that is NOT bound to any seeded user.
    7. Expected: you are refused — returned to /login with a visible refusal message, NOT signed in. Confirm no new row was auto-created: `py -3.12 manage.py shell -c "from accounts.models import User; print(User.objects.count())"` still shows 7.

    D-09 #3 — break-glass + dev-login gating under DEBUG=False:
    8. Stop the server, set DEBUG=False in .env, restart.
    9. On /login confirm the per-user dev-login list is HIDDEN and only the Microsoft button shows (D-08).
    10. Confirm the superuser break-glass still logs in via password (e.g. Django admin at /admin/ with the sysadmin credentials) — ModelBackend path intact (D-03).
    11. Restore DEBUG=True when done.
  </how-to-verify>
  <acceptance_criteria>
    - D-09 #1: bound faculty account round-trips via Microsoft and lands on the faculty home; azure_oid populated.
    - D-09 #2: an unprovisioned tenant account is refused (back to /login, visible message) and User count stays 7 (no auto-creation).
    - D-09 #3: under DEBUG=False the dev-login list is hidden (Microsoft button remains) and the superuser can still log in via password.
    - PKCE sanity: the authorize redirect carried a code_challenge param.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all three D-09 criteria pass, or describe which criterion failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real MMCM user -> live OAuth round-trip | the actual IdP login exercises every mitigation against real tokens |
| DEBUG flag flip -> prod-like login surface | verifies the passwordless stub disappears when DEBUG=False |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-14 | Elevation of privilege | live provisioning gate | critical | mitigate | D-09 #2 live-refuses an unprovisioned tenant account and confirms zero auto-creation — proves T-03.1-04 holds against a real token, not just a fake dict |
| T-03.1-15 | Spoofing | dev-login exposure under DEBUG=False | high | mitigate | D-09 #3 confirms the passwordless list is hidden when DEBUG=False while break-glass survives (T-03.1-08/09 verified live) |
| T-03.1-03 | Info disclosure | dev client secret | medium | accept | Rotation + prod https redirect remain deferred to the deploy phase; this gate stays on localhost/http by design (Pitfall 4) |
</threat_model>

<verification>
- Human-in-the-loop, blocking. The three D-09 criteria are checked live per the steps above.
- No automated command replaces this gate (interactive Entra consent cannot run headless);
  the automated pipeline-level coverage lives in Plan 04.
- Record pass/fail per criterion (and any AADSTS error codes) in the SUMMARY.
</verification>

<success_criteria>
All three D-09 criteria pass live: bound faculty SSO -> faculty home; unprovisioned account
refused with no user created; dev-login hidden and superuser break-glass intact under
DEBUG=False. The phase is proven.
</success_criteria>

<output>
Create `.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-05-SUMMARY.md` when done, recording the D-09 UAT results.
</output>
