---
phase: 03.1-authentication-entra-id-sso-local-dev-proof
plan: 04
subsystem: testing
tags: [entra-id, social-django, pkce, pytest-django, auth, regression-tests]

requires:
  - phase: 03.1-01
    provides: AzureADTenantOAuth2PKCE backend, AUTHENTICATION_BACKENDS, SocialAuthExceptionMiddleware + SOCIAL_AUTH_RAISE_EXCEPTIONS=False, SOCIAL_AUTH_PIPELINE (create_user removed)
  - phase: 03.1-02
    provides: accounts.pipeline.deny_unprovisioned + write_azure_oid, link_entra command
  - phase: 03.1-03
    provides: dev-login login() names ModelBackend (two-backend coexistence)
provides:
  - accounts/tests.py — PkceBackendTests, AuthWiringTests, DenyUnprovisionedTests, WriteAzureOidTests, LinkEntraCommandTests (19 tests)
  - web/tests.py — DevLoginCoexistTests, LogoutTests (3 tests)
  - Automated regression guard for the two research landmines (PKCE silently ignored; dev-login break under two backends) and the provisioned-only + audit policy
affects: [03.1-05, phase-08-auth-cutover]

tech-stack:
  added: []
  patterns:
    - "Pure-function pipeline tests: fake social-auth response/details dicts inline, no network/mock of the OAuth flow"
    - "Settings-wiring assertions read django.conf.settings directly (backends order, pipeline membership, middleware index ordering)"
    - "override_settings(DEBUG=True) to exercise the DEBUG-gated dev-login POST branch"

key-files:
  created: []
  modified:
    - accounts/tests.py
    - web/tests.py

key-decisions:
  - "Tests target pure pipeline functions, the backend class, settings wiring, and the command — zero live Entra/network/browser calls (the manual round-trip is Plan 05)"
  - "Middleware ordering asserted via list.index() comparisons (SocialAuthExceptionMiddleware after AuthenticationMiddleware, before MessageMiddleware) rather than a fixed index"
  - "azure_oid uniqueness (nullable filtered-unique on MSSQL) lets test users be created directly without seed_demo"

patterns-established:
  - "Adversarial-input pipeline tests (user=None, inactive user, cross-user UPN clash) prove the security refusal paths, not just the happy path"
  - "AuditLog assertions (auth.entra_refused / auth.entra_login) lock the audit trail against silent regression (Convention #2)"

requirements-completed: [AUTH-01, AUTH-03, AUTH-05]

coverage:
  - id: D1
    description: "PKCE backend proof — AzureADTenantOAuth2PKCE is a BaseOAuth2PKCE + AzureADTenantOAuth2 subclass, .name unchanged, DEFAULT_USE_PKCE True (closes Pitfall 1)"
    requirement: "AUTH-01"
    verification:
      - kind: unit
        ref: "accounts/tests.py::PkceBackendTests"
        status: pass
    human_judgment: false
  - id: D2
    description: "Settings-wiring proof — social_django installed, backends order, pipeline membership (create_user absent), refusal middleware positioned, SOCIAL_AUTH_RAISE_EXCEPTIONS False, DRF still SessionAuthentication (D-10)"
    verification:
      - kind: unit
        ref: "accounts/tests.py::AuthWiringTests"
        status: pass
    human_judgment: false
  - id: D3
    description: "deny_unprovisioned refuses user=None and inactive user with AuthForbidden + auth.entra_refused AuditLog, passes for active provisioned user"
    requirement: "AUTH-03"
    verification:
      - kind: unit
        ref: "accounts/tests.py::DenyUnprovisionedTests"
        status: pass
    human_judgment: false
  - id: D4
    description: "write_azure_oid persists response['oid'] to User.azure_oid, is idempotent, no-ops on user=None, writes auth.entra_login AuditLog (D-05)"
    verification:
      - kind: unit
        ref: "accounts/tests.py::WriteAzureOidTests"
        status: pass
    human_judgment: false
  - id: D5
    description: "link_entra updates the email, is idempotent, rejects an invalid UPN and a cross-user clash (D-07)"
    verification:
      - kind: unit
        ref: "accounts/tests.py::LinkEntraCommandTests"
        status: pass
    human_judgment: false
  - id: D6
    description: "Dev-login coexistence — DEBUG dev-login POST authenticates under two backends (no ValueError, Pitfall 2), superuser break-glass via ModelBackend password auth (D-08/D-09#3)"
    requirement: "AUTH-05"
    verification:
      - kind: integration
        ref: "web/tests.py::DevLoginCoexistTests"
        status: pass
    human_judgment: false
  - id: D7
    description: "Logout flushes the local session and redirects to /login (D-11)"
    verification:
      - kind: integration
        ref: "web/tests.py::LogoutTests"
        status: pass
    human_judgment: false

duration: 14min
completed: 2026-07-03
status: complete
---

# Phase 03.1 Plan 04: Automated Entra Wiring Proof Summary

**22 network-free tests lock the PKCE backend, the deny/oid pipeline + audit trail, link_entra, the settings wiring (refusal path + DRF-unchanged), and dev-login/logout coexistence — the two research landmines are now regression-guarded and the full 125-test suite is green.**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-07-03T12:05:00Z
- **Completed:** 2026-07-03T12:19:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `accounts/tests.py` — five TestCase classes (19 tests) prove the PKCE backend identity/name/default, the full settings wiring (backends order, pipeline with create_user removed, SocialAuthExceptionMiddleware positioned + SOCIAL_AUTH_RAISE_EXCEPTIONS False, DRF still SessionAuthentication), deny_unprovisioned refusal for None/inactive with an `auth.entra_refused` AuditLog, write_azure_oid set/idempotent/no-op with an `auth.entra_login` AuditLog, and link_entra update/idempotent/bad-UPN/clash.
- `web/tests.py` — DevLoginCoexistTests + LogoutTests (3 tests) prove the DEBUG dev-login POST authenticates under two configured backends (no ValueError, Pitfall 2), superuser break-glass via ModelBackend password auth, and that `/logout` flushes the session and redirects to `/login`.
- Full suite `py -3.12 manage.py test` — **125 tests, OK**, zero network/browser I/O.

## Task Commits

Each task was committed atomically:

1. **Task 1: accounts/tests.py — PKCE backend, wiring, deny, oid-write, link_entra** — `c0b18dd` (test)
2. **Task 2: web/tests.py — dev-login coexistence + logout regression** — `7e9b053` (test)

_These are proof-of-existing-behavior tests; the code under test shipped in Plans 01/02/03, so each test file passed green on first run (no separate RED commit — the RED landmines were already fixed in prior plans)._

## Files Created/Modified
- `accounts/tests.py` — replaced the stub with PkceBackendTests, AuthWiringTests, DenyUnprovisionedTests, WriteAzureOidTests, LinkEntraCommandTests.
- `web/tests.py` — appended DevLoginCoexistTests and LogoutTests; added `override_settings` + `settings` imports.

## Decisions Made
- Middleware ordering asserted via `list.index()` comparisons rather than a hardcoded index, so the assertion survives unrelated middleware insertions.
- Test users created directly with `get_user_model().objects.create(...)` (not via seed_demo), relying on the nullable filtered-unique `azure_oid` proven in Phase 1.
- Superuser break-glass proven with `client.login(password=...)` (ModelBackend password path) rather than the dev-login POST, to specifically exercise ModelBackend authentication under two configured backends.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. A pre-existing `UserWarning: No directory at: staticfiles/` (whitenoise) is emitted during web tests; it is unrelated to this plan (out of scope, not introduced here) and does not affect any result.

## User Setup Required
None - no external service configuration required. (The live Entra round-trip and break-glass-under-DEBUG=False checks are the Plan 05 manual UAT gate, D-09.)

## Threat Surface
No new security surface. This plan turns the Plan 01/02/03 mitigations into enforced, regression-guarded invariants: T-03.1-10 (provisioning gate + PKCE + refusal path + DRF-unchanged + dev-login survives two backends) and T-03.1-11 (auth.entra_login / auth.entra_refused audit rows) are now covered by passing tests.

## Next Phase Readiness
- Wave-3 automated gate is green; Plan 05 (manual Entra round-trip UAT: real sign-in, live unprovisioned refusal, break-glass under DEBUG=False) is unblocked.

## Self-Check: PASSED
- FOUND: accounts/tests.py (modified, commit c0b18dd)
- FOUND: web/tests.py (modified, commit 7e9b053)
- FOUND: .planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-04-SUMMARY.md
- FOUND: commit c0b18dd (Task 1)
- FOUND: commit 7e9b053 (Task 2)
- VERIFIED: `py -3.12 manage.py test` — 125 tests, OK (full suite green)

---
*Phase: 03.1-authentication-entra-id-sso-local-dev-proof*
*Completed: 2026-07-03*
