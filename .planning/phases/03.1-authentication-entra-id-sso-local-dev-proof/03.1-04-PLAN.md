---
phase: 03.1-authentication-entra-id-sso-local-dev-proof
plan: 04
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02", "03.1-03"]
files_modified:
  - accounts/tests.py
  - web/tests.py
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-05]

must_haves:
  truths:
    - "accounts/tests.py proves: the registered backend honors PKCE (AzureADTenantOAuth2PKCE is a BaseOAuth2PKCE subclass and emits a code_challenge / USE_PKCE is effective) — D-02/AUTH-01."
    - "accounts/tests.py proves wiring: social_django is in INSTALLED_APPS, AUTHENTICATION_BACKENDS is [PKCE subclass, ModelBackend] in order, and SOCIAL_AUTH_PIPELINE contains deny_unprovisioned + write_azure_oid + associate_by_email and excludes create_user."
    - "accounts/tests.py proves deny_unprovisioned raises AuthForbidden for user=None and for an inactive user, and passes for an active provisioned user (D-06/AUTH-03/AUTH-05), writing an auth.entra_refused AuditLog on refusal."
    - "accounts/tests.py proves write_azure_oid sets User.azure_oid from response['oid'], is idempotent, and writes an auth.entra_login AuditLog on success (D-05)."
    - "accounts/tests.py proves link_entra updates the email, is idempotent, and rejects a bad UPN and a cross-user clash (D-07)."
    - "web/tests.py proves the DEBUG dev-login POST still authenticates with two backends configured (D-08/D-09#3) and that /logout flushes the session and redirects to /login (D-11)."
    - "The full suite (py -3.12 manage.py test) is green."
  artifacts:
    - accounts/tests.py
    - web/tests.py
  key_links:
    - "These tests are the automated half of the D-09 proof and the mitigation-verification for the Plan 01/02/03 threat register; the manual browser round-trip (D-09#1/#2/#3) is Plan 05."
    - "Tests target pure pipeline functions, the backend class, settings wiring, and the command — NO live Entra/network/browser calls (fake response/details dicts)."
---

<objective>
Deliver the Wave-0 automated test coverage the VALIDATION strategy calls for: prove the
PKCE backend, the settings wiring, the deny/oid pipeline steps, the link_entra command,
and dev-login coexistence + logout — all without a live Entra call or a browser. This is
the automated portion of the D-09 proof and the mitigation-verification for the phase
threat register.

Purpose: Lock the two research landmines (PKCE silently ignored; dev-login break) and the
provisioned-only policy behind fast, repeatable tests so a regression is caught in ~15s.

Output: a populated `accounts/tests.py` (PkceBackendTests, AuthWiringTests,
DenyUnprovisionedTests, WriteAzureOidTests, LinkEntraCommandTests) and additions to
`web/tests.py` (DevLoginCoexistTests, LogoutTests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-CONTEXT.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-RESEARCH.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-VALIDATION.md
@accounts/pipeline.py
@accounts/backends.py
@accounts/management/commands/seed_demo.py
@web/tests.py
@web/views.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: accounts/tests.py — PKCE backend, wiring, deny, oid-write, link_entra</name>
  <files>accounts/tests.py</files>
  <behavior>
    - PkceBackendTests: accounts.backends.AzureADTenantOAuth2PKCE is a subclass of social_core.backends.oauth.BaseOAuth2PKCE and of AzureADTenantOAuth2; its .name == "azuread-tenant-oauth2"; DEFAULT_USE_PKCE is True (PKCE is actually honored, closing Pitfall 1).
    - AuthWiringTests: "social_django" in settings.INSTALLED_APPS; settings.AUTHENTICATION_BACKENDS == ["accounts.backends.AzureADTenantOAuth2PKCE", "django.contrib.auth.backends.ModelBackend"]; the pipeline tuple contains "accounts.pipeline.deny_unprovisioned", "accounts.pipeline.write_azure_oid", "social_core.pipeline.social_auth.associate_by_email" and does NOT contain "social_core.pipeline.user.create_user".
    - DenyUnprovisionedTests: calling deny_unprovisioned with user=None raises AuthForbidden; with an inactive user raises AuthForbidden; with an active provisioned user returns without raising; a refusal creates an AuditLog(event_type="auth.entra_refused").
    - WriteAzureOidTests: write_azure_oid with response={"oid": "<guid>"} and a seeded user sets user.azure_oid to that guid and persists it; a second call with the same oid does not error and does not duplicate; user=None is a no-op; a success creates an AuditLog(event_type="auth.entra_login").
    - LinkEntraCommandTests: call_command("link_entra", "mayo", "real@mcm.edu.ph") updates mayo's email; re-running is idempotent (no error); an invalid UPN raises CommandError; binding a UPN already held by another user raises CommandError.
  </behavior>
  <read_first>
    - accounts/tests.py (current stub — replace it)
    - accounts/pipeline.py and accounts/backends.py (the code under test, from Plans 01/02)
    - RESEARCH.md "Validation Architecture > Phase Requirements -> Test Map" (the exact test class names and requirement anchors) and "Pattern 3" (function signatures + the fake response/details dict approach)
    - accounts/management/commands/seed_demo.py:53-78 (how the 7 role-users incl. faculty "mayo" are created — mirror minimal user creation in tests; do NOT depend on running seed_demo)
    - CONVENTIONS.md §Established Patterns #2 (AuditLog) so assertions target the right event_type strings
    - web/tests.py (house TestCase style: setUp creates users via get_user_model(); django.test.TestCase; call_command import)
  </read_first>
  <action>
    Replace the accounts/tests.py stub with five django.test.TestCase classes named
    PkceBackendTests, AuthWiringTests, DenyUnprovisionedTests, WriteAzureOidTests, and
    LinkEntraCommandTests, asserting the behaviors listed in <behavior> above. Import the
    functions from accounts.pipeline and the class from accounts.backends; import
    AuthForbidden from social_core.exceptions; use call_command for link_entra. Build fake
    social-auth `response` and `details` dicts inline (no network, no mocking of the OAuth
    flow) — the pipeline steps are pure functions of their arguments. Create test users
    directly with get_user_model().objects.create(...) (set is_active, role, email as
    needed); do not invoke seed_demo. For AuditLog assertions import ops.models.AuditLog
    and filter by event_type. Keep each test small and single-behavior; cite the D-NN /
    AUTH-0x id in each test's docstring per the module-docstring convention.
  </action>
  <verify>
    <automated>py -3.12 manage.py test accounts -v1</automated>
  </verify>
  <acceptance_criteria>
    - All five classes exist and pass: PKCE subclass identity + name + DEFAULT_USE_PKCE; backends order + pipeline membership (deny/oid/associate_by_email present, create_user absent); deny raises for None/inactive and passes for active with an auth.entra_refused AuditLog; oid-write sets/idempotent/no-op-on-None with an auth.entra_login AuditLog; link_entra update/idempotent/bad-UPN/clash.
    - `py -3.12 manage.py test accounts -v1` is green with zero network access.
  </acceptance_criteria>
  <done>accounts/tests.py proves PKCE, wiring, deny (AUTH-03/05), oid-write (D-05), and link_entra (D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: web/tests.py — dev-login coexistence + logout regression</name>
  <files>web/tests.py</files>
  <behavior>
    - DevLoginCoexistTests: with settings.DEBUG=True and two AUTHENTICATION_BACKENDS configured, POST /login with a seeded username logs the user in (session authenticated, redirect to /) — i.e. no ValueError from login() (Pitfall 2 / D-08 / D-09#3). A superuser can still authenticate via ModelBackend (break-glass, D-03/D-09#3).
    - LogoutTests: an authenticated client hitting /logout is redirected to /login and the session no longer identifies the user (D-11 — local session flush).
  </behavior>
  <read_first>
    - web/tests.py (existing ScanNotifyTests — reuse its setUp/TestCase idioms: get_user_model(), self.client, timezone)
    - web/views.py:49-76 (login_view DEBUG POST branch after the Plan 03 backend= fix, and logout_view)
    - RESEARCH.md "Pitfall 2" and "Validation Architecture" rows for D-08/D-09#3 and D-11
    - CONTEXT.md D-08, D-09 (#3), D-11
  </read_first>
  <action>
    Append DevLoginCoexistTests and LogoutTests to web/tests.py as django.test.TestCase
    classes. DevLoginCoexistTests: create a seeded active user and a superuser; use
    override_settings(DEBUG=True) as needed so the dev-login POST branch is active;
    self.client.post("/login", {"username": ...}) and assert the response redirects to "/"
    and the session is authenticated (assert on _auth_user_id in the session or a
    follow-up authenticated request) — this fails loudly if login() raises ValueError under
    two backends. Add a case that a superuser authenticates via ModelBackend (force via the
    dev-login path or client.login with password if the seed sets one). LogoutTests:
    force_login a user, GET /logout, assert redirect to /login and that the session no
    longer carries the user. Cite D-08/D-09#3/D-11 in the docstrings. No live Entra call.
  </action>
  <verify>
    <automated>py -3.12 manage.py test web -v1 && py -3.12 manage.py test</automated>
  </verify>
  <acceptance_criteria>
    - DevLoginCoexistTests passes: the DEBUG dev-login POST authenticates with two backends configured (guards Pitfall 2), and superuser break-glass authenticates (D-09#3).
    - LogoutTests passes: /logout flushes the session and redirects to /login (D-11).
    - The full suite `py -3.12 manage.py test` is green (phase-gate: all automated coverage passes together).
  </acceptance_criteria>
  <done>web/tests.py proves dev-login coexistence (D-08/D-09#3) and logout (D-11); full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> pipeline/backend/command | tests exercise the security-critical code paths with adversarial inputs (None user, inactive user, clashing UPN) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-10 | Elevation / Tampering (verification) | provisioning gate + PKCE mixin + dev-login break | high | mitigate | Automated tests assert deny-unprovisioned (incl. inactive), pipeline excludes create_user, PKCE mixin is the registered backend, and dev-login survives two backends — turning the Plan 01/02/03 mitigations into enforced, regression-guarded invariants |
| T-03.1-11 | Repudiation (verification) | auth security-event auditing | medium | mitigate | Tests assert auth.entra_login / auth.entra_refused AuditLog rows are written, so the audit trail cannot silently regress |
</threat_model>

<verification>
- `py -3.12 manage.py test accounts -v1` — five new accounts test classes green.
- `py -3.12 manage.py test web -v1` — dev-login coexistence + logout green.
- `py -3.12 manage.py test` — full suite green (phase gate before the manual UAT in Plan 05).
- No test performs network or browser I/O (pure functions + Django test client only).
</verification>

<success_criteria>
The two research landmines (PKCE silently ignored; dev-login break under two backends) and
the provisioned-only policy (deny + oid + audit) and link_entra are all covered by passing,
network-free automated tests, and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-04-SUMMARY.md` when done.
</output>
