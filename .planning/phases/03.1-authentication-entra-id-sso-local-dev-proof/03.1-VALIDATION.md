---
phase: 03.1
slug: authentication-entra-id-sso-local-dev-proof
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-03
updated: 2026-07-03
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Django built-in test runner (`unittest`-based) — no pytest |
| **Config file** | none |
| **Quick run command** | `py -3.12 manage.py test accounts web` |
| **Full suite command** | `py -3.12 manage.py test` |
| **Estimated runtime** | ~5–15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `py -3.12 manage.py test accounts web`
- **After every plan wave:** Run `py -3.12 manage.py test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

> Anchors each automatable secure behavior to the plan/task that IMPLEMENTS it and
> the Plan 04 test that PROVES it. The behavioral test files (`accounts/tests.py`,
> `web/tests.py`) are authored in Plan 04 (Wave 3) and must be green before the
> Plan 05 manual UAT gate.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | Test Authored In | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|------------------|--------|
| 03.1-01-02 | 01 | 1 | D-01/D-02/D-03 | T-03.1-01 | PKCE mixin is the registered Entra backend; `AUTHENTICATION_BACKENDS` = [PKCE, ModelBackend]; pipeline excludes `create_user` | unit | `py -3.12 manage.py test accounts` | 03.1-04-01 (PkceBackendTests + AuthWiringTests) | ⬜ pending |
| 03.1-01-02 | 01 | 1 | D-06/D-09#2 | T-03.1-16 | refusal path wired: `SocialAuthExceptionMiddleware` in MIDDLEWARE (after Auth, before Message) + `SOCIAL_AUTH_RAISE_EXCEPTIONS=False` → refused login redirects to `/login`, no 500 | unit | `py -3.12 manage.py test accounts` | 03.1-04-01 (AuthWiringTests) | ⬜ pending |
| 03.1-01-02 | 01 | 1 | D-10 | — | DRF unchanged: `REST_FRAMEWORK["DEFAULT_AUTHENTICATION_CLASSES"]` still contains `SessionAuthentication` | unit | `py -3.12 manage.py test accounts` | 03.1-04-01 (AuthWiringTests) | ⬜ pending |
| 03.1-02-01 | 02 | 2 | D-06/AUTH-03/AUTH-05 | T-03.1-04/05 | `deny_unprovisioned` refuses login when no seeded/active User matches (no user created); writes `auth.entra_refused` AuditLog | unit | `py -3.12 manage.py test accounts` | 03.1-04-01 (DenyUnprovisionedTests) | ⬜ pending |
| 03.1-02-01 | 02 | 2 | D-05 | T-03.1-06 | `write_azure_oid` writes `User.azure_oid` from `response['oid']`, idempotent; writes `auth.entra_login` AuditLog | unit | `py -3.12 manage.py test accounts` | 03.1-04-01 (WriteAzureOidTests) | ⬜ pending |
| 03.1-02-02 | 02 | 2 | D-07 | T-03.1-12 | `link_entra` repoints a seeded user's email to a real UPN — validated, idempotent, clash-rejecting, ASCII-only | unit | `py -3.12 manage.py test accounts` | 03.1-04-01 (LinkEntraCommandTests) | ⬜ pending |
| 03.1-03-01 | 03 | 2 | D-08/D-09#3 | T-03.1-08/09 | DEBUG dev-login authenticates via explicit `backend=ModelBackend` under two backends; superuser break-glass | unit | `py -3.12 manage.py test web` | 03.1-04-02 (DevLoginCoexistTests) | ⬜ pending |
| 03.1-03-01 | 03 | 2 | D-11 | — | `/logout` flushes the session and redirects to `/login` (no Entra global sign-out) | unit | `py -3.12 manage.py test web` | 03.1-04-02 (LogoutTests) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Test Authored In: the Plan 04 task/class that creates the automated proof; `accounts/tests.py` is a stub and `web/tests.py` has only `ScanNotifyTests` until Plan 04 runs.*

---

## Wave 0 Requirements

> All test coverage below is delivered by **Plan 04** (Wave 3, `03.1-04-01` /
> `03.1-04-02`) and must be green before the Plan 05 manual UAT gate.

- [ ] `accounts/tests.py` — currently an empty stub; add `TestCase`s for the custom
      pipeline steps (associate-by-email link → azure_oid write from `response['oid']`;
      deny-if-unprovisioned short-circuit) using a fake social-auth `response`/`details`
      dict (no live Entra call). → `03.1-04-01` (DenyUnprovisionedTests, WriteAzureOidTests,
      LinkEntraCommandTests).
- [ ] `web/tests.py` — regression test that the DEBUG dev-login stub still authenticates
      after a second `AUTHENTICATION_BACKENDS` entry is added (guards the `login(..., backend=)` fix),
      plus a logout session-flush test. → `03.1-04-02` (DevLoginCoexistTests, LogoutTests).
- [ ] Settings-wiring assertion — `INSTALLED_APPS` contains `social_django`, both backends
      present in order, PKCE mixin backend is the registered one. → `03.1-04-01` (AuthWiringTests).
- [ ] Refusal-path wiring assertion — `MIDDLEWARE` contains `SocialAuthExceptionMiddleware`
      (after Auth, before Message) and `SOCIAL_AUTH_RAISE_EXCEPTIONS is False`, so a refused
      login redirects to `/login` instead of raising (D-06/D-09#2 blocker fix). →
      `03.1-04-01` (AuthWiringTests).
- [ ] D-10 negative guard — `REST_FRAMEWORK["DEFAULT_AUTHENTICATION_CLASSES"]` still contains
      `SessionAuthentication` (DRF auth unchanged). → `03.1-04-01` (AuthWiringTests).

*No new framework install needed — Django test runner already present.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real Microsoft login → faculty role-home | D-09 #1 | Interactive Entra consent + credential entry cannot run headless in CI; needs the project-owned MMCM tenant | 1. `link_entra mayo <your-real-UPN>` 2. Start server, open `/`, click "Sign in with Microsoft" 3. Authenticate as the bound account 4. Confirm redirect lands on the faculty home |
| Unprovisioned account refused end-to-end | D-09 #2 | Requires a second real tenant account with no seeded User | Sign in with a tenant account that has no matching seeded User → expect refusal, no User row created (pipeline-level deny is unit-tested above; this proves it live) |
| Superuser break-glass under DEBUG=False | D-09 #3 | Manual toggle of DEBUG + password login | Set `DEBUG=False`, confirm superuser can still log in via ModelBackend |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — every task across Plans 01-04 carries an `<automated>` command; Plan 05 is a blocking human-verify UAT gate (interactive Entra consent cannot run headless).
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — each implementation task runs at least `manage.py check` + the existing suite.
- [x] Wave 0 covers all MISSING references — all missing behavioral tests are assigned to Plan 04 (`03.1-04-01` / `03.1-04-02`) and gate before the Plan 05 UAT.
- [x] No watch-mode flags — Django test runner only; no `--watch`.
- [x] Feedback latency < 15s — ~5-15s full suite.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** 2026-07-03 — validation *strategy* approved (design-level Nyquist criteria met). `wave_0_complete` stays **false** until Plan 04 executes and physically authors `accounts/tests.py` / `web/tests.py` (the test files do not yet exist); flip it to `true` when Plan 04's full suite is green.
