---
phase: 03.1-authentication-entra-id-sso-local-dev-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - accounts/backends.py
  - config/settings.py
  - config/urls.py
autonomous: true
requirements: [AUTH-01]
user_setup:
  - service: microsoft-entra-id
    why: "The SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_* creds must already be in .env (D-04). The client-credentials flow was verified working; this plan only reads them."
    env_vars:
      - name: SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_KEY
        source: ".env (already present per D-04)"
      - name: SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_SECRET
        source: ".env (already present per D-04)"
      - name: SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_TENANT_ID
        source: ".env (already present per D-04)"

must_haves:
  truths:
    - "social_django is installed and its migrations apply cleanly on MSSQL (5 social_auth_* tables exist)."
    - "AUTHENTICATION_BACKENDS = [accounts.backends.AzureADTenantOAuth2PKCE, django.contrib.auth.backends.ModelBackend] in that order (D-01/D-03)."
    - "accounts.backends.AzureADTenantOAuth2PKCE inherits BaseOAuth2PKCE so USE_PKCE is actually honored (D-02), and keeps name='azuread-tenant-oauth2' so the callback URL and SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_* env prefix are unchanged."
    - "SOCIAL_AUTH_PIPELINE contains associate_by_email, then accounts.pipeline.deny_unprovisioned, then accounts.pipeline.write_azure_oid, and does NOT contain create_user (D-05/D-06)."
    - "MIDDLEWARE contains social_django.middleware.SocialAuthExceptionMiddleware (after AuthenticationMiddleware, before MessageMiddleware) AND SOCIAL_AUTH_RAISE_EXCEPTIONS is False, so a refused login (AuthForbidden from deny_unprovisioned) is intercepted and redirected to SOCIAL_AUTH_LOGIN_ERROR_URL='/login' with a message instead of raising an unhandled 500 (D-06/D-09#2)."
    - "SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_REDIRECT_URI is pinned to the exact registered value http://localhost:8000/auth/complete/azuread-tenant-oauth2/ (Pitfall 3)."
    - "/auth/complete/azuread-tenant-oauth2/ resolves via social_django.urls (namespace 'social')."
    - "DRF stays on SessionAuthentication + IsAuthenticated (D-10) — unchanged."
  artifacts:
    - requirements.txt
    - accounts/backends.py
    - config/settings.py
    - config/urls.py
  key_links:
    - "The PKCE mixin subclass MUST be the entry registered in AUTHENTICATION_BACKENDS — registering the stock AzureADTenantOAuth2 silently ignores USE_PKCE (Pitfall 1)."
    - "REDIRECT_URI pinned + access via localhost (not 127.0.0.1) — otherwise AADSTS50011 redirect mismatch (Pitfall 3)."
    - "SOCIAL_AUTH_PIPELINE references accounts.pipeline.* (created in Plan 02); the reference is a lazy dotted-string tuple resolved only at auth time, so `check`/`migrate`/existing tests do NOT import it and pass without Plan 02."
---

<objective>
Wire python-social-auth into the running Django app so the Microsoft Entra ID
Authorization-Code + PKCE backend is registered, configured, and migrated — the
structural foundation the real SSO round-trip stands on. This is a LOCAL-DEV PROOF:
production https redirect, secret rotation, and staging are OUT of scope (deploy phase).

Purpose: Install and configure the backend, honoring D-01 (AzureADTenantOAuth2),
D-02 (real PKCE via a mixin subclass), D-03 (keep ModelBackend), D-04 (creds from
.env), and D-10 (session-only, DRF unchanged). Guard the D-02 and dependency-bump
landmines the research flagged.

Output: `social-auth-app-django==6.0.0` in requirements.txt; `accounts/backends.py`
(the PKCE mixin subclass); a fully-wired `config/settings.py` block; the `auth/` URL
include; the applied social_django migrations.
</objective>

<artifacts_this_phase_produces>
New symbols introduced across Phase 03.1 (this plan produces the ones marked [01]):

- [01] `accounts.backends.AzureADTenantOAuth2PKCE` — class (BaseOAuth2PKCE + AzureADTenantOAuth2)
- [01] `config/settings.py` new keys: `AUTHENTICATION_BACKENDS`, `SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_KEY/SECRET/TENANT_ID/USE_PKCE/REDIRECT_URI`, `SOCIAL_AUTH_LOGIN_ERROR_URL`, `SOCIAL_AUTH_RAISE_EXCEPTIONS`, `SOCIAL_AUTH_PIPELINE`; `social_django` added to INSTALLED_APPS; `social_django.middleware.SocialAuthExceptionMiddleware` added to MIDDLEWARE; two social_django context processors
- [01] `config/urls.py` new include: `path("auth/", include("social_django.urls", namespace="social"))`
- [01] social_django migration tables: `social_auth_usersocialauth`, `social_auth_nonce`, `social_auth_association`, `social_auth_code`, `social_auth_partial`
- [02] `accounts.pipeline.deny_unprovisioned` — function
- [02] `accounts.pipeline.write_azure_oid` — function
- [02] `accounts/management/commands/link_entra.py` — `manage.py link_entra` command
- [03] `templates/web/login.html` — always-visible "Sign in with Microsoft" `<a>` to `social:begin`
- [03] `web/views.py` — dev-login `login(request, user, backend="django.contrib.auth.backends.ModelBackend")`
- [04] test classes: `PkceBackendTests`, `AuthWiringTests`, `DenyUnprovisionedTests`, `WriteAzureOidTests`, `LinkEntraCommandTests` (accounts/tests.py); `DevLoginCoexistTests`, `LogoutTests` (web/tests.py)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-CONTEXT.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@config/settings.py
@config/urls.py
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin social-auth-app-django and confirm the dependency bump is regression-free</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (the "Identity (Entra ID SSO) — not yet wired" block at the bottom with PyJWT/cryptography/requests)
    - RESEARCH.md "Standard Stack" + "Pitfall 6: Dependency upgrade surprise" (cryptography 42->46, PyJWT, requests; four transitive packages; Windows wheels, no compiler)
    - RESEARCH.md "Package Legitimacy Audit" (both packages Approved via sdist inspection)
  </read_first>
  <action>
    Add a single new dependency line `social-auth-app-django==6.0.0` to requirements.txt
    (exact pin per D-01/RESEARCH). Replace the stale comment "not yet wired; added in
    the auth cutover phase" above PyJWT/cryptography/requests with a phase-agnostic note
    that these are now transitive deps of social-auth-core (drop the "Phase 2"/"cutover
    phase" wording per Pitfall 8). Do NOT hand-bump PyJWT/cryptography/requests pins;
    installing social-auth-app-django transitively upgrades them and pulls oauthlib,
    requests-oauthlib, defusedxml, python3-openid. Then install and run the existing
    suite to prove the cryptography 42->46 / PyJWT / requests bump introduced no
    regression before any wiring changes land. No blocking-human package checkpoint is
    required: the researcher's Package Legitimacy Audit verdict is Approved (OK) for both
    packages, verified by sdist inspection — no [ASSUMED]/[SUS] entries.
  </action>
  <verify>
    <automated>py -3.12 -m pip install -r requirements.txt && py -3.12 manage.py check && py -3.12 manage.py test accounts web -v1</automated>
  </verify>
  <acceptance_criteria>
    - `social-auth-app-django==6.0.0` is present in requirements.txt with no "Phase 2"/"cutover phase" wording remaining on the identity block.
    - `pip install -r requirements.txt` completes without a compiler prompt (cryptography 46 ships Windows wheels).
    - `manage.py check` reports no issues; the existing `accounts web` suite is green after the transitive dependency upgrade.
  </acceptance_criteria>
  <done>social-auth-app-django==6.0.0 pinned and installed; existing suite green post-upgrade.</done>
</task>

<task type="auto">
  <name>Task 2: Add the PKCE mixin backend and wire the social_django settings block</name>
  <files>accounts/backends.py, config/settings.py, config/urls.py</files>
  <read_first>
    - config/settings.py lines 31-47 (INSTALLED_APPS), 49-58 (MIDDLEWARE — note AuthenticationMiddleware then MessageMiddleware are adjacent; the new middleware goes between them), 62-75 (TEMPLATES context_processors), 108-116 (Auth: AUTH_USER_MODEL, LOGIN_URL, LOGIN_REDIRECT_URL, LOGOUT_REDIRECT_URL), 136-143 (REST_FRAMEWORK — leave unchanged, D-10)
    - RESEARCH.md "Pattern 1: Settings block" note that SocialAuthExceptionMiddleware surfaces deny errors via messages, and "Pitfall" guidance that SOCIAL_AUTH_RAISE_EXCEPTIONS must be False for the middleware to intercept AuthForbidden
    - config/urls.py (root urlpatterns; web.urls is included at "")
    - RESEARCH.md "Pattern 1: Settings block (exact keys)" — copy the exact SOCIAL_AUTH_* keys, pipeline tuple, and context processors from there
    - RESEARCH.md "Pattern 2: PKCE-mixin backend" — the exact class definition and the MRO note
    - RESEARCH.md "Code Examples > URL wiring" — the exact include line
    - RESEARCH.md "Pitfall 1" (why the mixin is mandatory) and "Pitfall 3" (why REDIRECT_URI is pinned)
  </read_first>
  <action>
    Create accounts/backends.py defining class AzureADTenantOAuth2PKCE that mixes
    BaseOAuth2PKCE (from social_core.backends.oauth) before AzureADTenantOAuth2 (from
    social_core.backends.azuread_tenant) and sets DEFAULT_USE_PKCE = True, per RESEARCH
    Pattern 2 — do NOT override `name` (it must stay 'azuread-tenant-oauth2' so the
    callback URL and SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_* setting prefix are unchanged).
    Add a module docstring citing D-02 and Pitfall 1 (the stock backend ignores USE_PKCE).
    In config/settings.py: append "social_django" to INSTALLED_APPS; insert
    "social_django.middleware.SocialAuthExceptionMiddleware" into MIDDLEWARE positioned
    AFTER "django.contrib.auth.middleware.AuthenticationMiddleware" and BEFORE
    "django.contrib.messages.middleware.MessageMiddleware" (so the Django messages framework
    is available to receive the refusal message); add the two social context processors
    (social_django.context_processors.backends and .login_redirect) to the existing
    TEMPLATES[0]["OPTIONS"]["context_processors"] list; add the AUTHENTICATION_BACKENDS list
    with accounts.backends.AzureADTenantOAuth2PKCE first and
    django.contrib.auth.backends.ModelBackend second (D-01/D-03); add the SOCIAL_AUTH_*
    block reading KEY/SECRET/TENANT_ID via the existing env() helper (D-04), USE_PKCE=True
    (D-02), the pinned REDIRECT_URI string, SOCIAL_AUTH_RAISE_EXCEPTIONS = False (the default
    is True; without this the middleware never intercepts and the AuthForbidden that
    deny_unprovisioned raises escapes as a raw traceback under DEBUG=True / a generic 500
    under DEBUG=False — D-06/D-09#2), SOCIAL_AUTH_LOGIN_ERROR_URL="/login" (the explicit
    redirect target the middleware sends a refused login to), and the customized
    SOCIAL_AUTH_PIPELINE exactly as in RESEARCH Pattern 1 — associate_by_email present,
    accounts.pipeline.deny_unprovisioned and accounts.pipeline.write_azure_oid inserted, and
    create_user absent (D-05/D-06). Leave REST_FRAMEWORK untouched (D-10).
    In config/urls.py: add path("auth/", include("social_django.urls", namespace="social"))
    to urlpatterns. Then apply the library migrations. Note: the pipeline setting references
    accounts.pipeline.* which Plan 02 creates; this is a lazy dotted-string tuple resolved
    only during an auth request, so check/migrate/tests do not import it yet.
  </action>
  <verify>
    <automated>py -3.12 manage.py check && py -3.12 manage.py migrate social_django && py -3.12 manage.py test accounts web -v1</automated>
  </verify>
  <acceptance_criteria>
    - `accounts/backends.py` defines `AzureADTenantOAuth2PKCE(BaseOAuth2PKCE, AzureADTenantOAuth2)` with `DEFAULT_USE_PKCE = True` and no `name` override.
    - `manage.py check` passes; `manage.py migrate social_django` applies all social_django migrations on MSSQL with no error (Pitfall 7) — the 5 social_auth_* tables exist.
    - `AUTHENTICATION_BACKENDS` lists the PKCE subclass first, ModelBackend second; both social context processors are in TEMPLATES; `SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_REDIRECT_URI` equals the registered localhost callback string.
    - `MIDDLEWARE` contains `social_django.middleware.SocialAuthExceptionMiddleware` positioned after `AuthenticationMiddleware` and before `MessageMiddleware`; `SOCIAL_AUTH_RAISE_EXCEPTIONS is False`; `SOCIAL_AUTH_LOGIN_ERROR_URL == "/login"` — so a refused login redirects to /login with a message rather than raising an unhandled exception (D-06/D-09#2). (Assertion lands in Plan 04's AuthWiringTests.)
    - Reversing config/urls.py `social:begin` for 'azuread-tenant-oauth2' resolves (no NoReverseMatch); the existing `accounts web` suite is green (test DB re-created with social_django migrations).
  </acceptance_criteria>
  <done>PKCE backend registered, settings block + auth/ include wired, social_django migrated on MSSQL, existing suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Django `/auth/complete/...` | OAuth callback carries `code`/`state` from an external IdP |
| Django -> Entra token endpoint | server-side code+verifier+secret exchange over the network |
| PyPI index -> dev machine | new package + transitive upgrades install into the venv |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Tampering | OAuth authorization-code flow | high | mitigate | Real PKCE via `AzureADTenantOAuth2PKCE` mixin (D-02) + social-auth `state` param — code interception cannot complete without the session-held verifier |
| T-03.1-02 | Tampering | redirect_uri handling | high | mitigate | Pin `SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_REDIRECT_URI` to the exact registered value; open the app via localhost only (Pitfall 3) |
| T-03.1-03 | Info disclosure | dev client secret (shared in a transcript) | medium | accept | Local-dev proof only; secret rotation + prod https redirect are explicitly deferred to the deploy phase (CONTEXT out-of-scope) |
| T-03.1-16 | Info disclosure / DoS | unhandled AuthForbidden on refused login | medium | mitigate | Wire `SocialAuthExceptionMiddleware` (after AuthenticationMiddleware, before MessageMiddleware) + `SOCIAL_AUTH_RAISE_EXCEPTIONS = False` so a refusal redirects to `SOCIAL_AUTH_LOGIN_ERROR_URL='/login'` with a message instead of leaking a raw traceback (DEBUG=True) or a generic 500 (DEBUG=False) — D-06/D-09#2 |
| T-03.1-SC | Tampering | pip install (social-auth-app-django + transitive) | high | mitigate | Researcher's Package Legitimacy Audit = Approved (OK, sdist-inspected) for both packages; exact-pin 6.0.0; run existing suite post-install. No [ASSUMED]/[SUS] -> no blocking-human checkpoint required |
</threat_model>

<verification>
- `py -3.12 manage.py check` — clean.
- `py -3.12 manage.py migrate social_django` — all migrations applied on MSSQL LocalDB (Pitfall 7 verified live).
- `py -3.12 manage.py test accounts web -v1` — existing suite green (regression guard for the dependency bump and the settings change).
- Manual sanity (optional): `py -3.12 manage.py shell -c "from django.urls import reverse; print(reverse('social:begin', args=['azuread-tenant-oauth2']))"` prints `/auth/login/azuread-tenant-oauth2/`.
</verification>

<success_criteria>
social_django is installed and migrated on MSSQL; the PKCE-honoring backend subclass is
the registered Entra backend; the settings block (backends order, creds, pinned redirect,
customized pipeline) and the `auth/` URL include are in place; DRF is untouched; the
existing test suite is green.
</success_criteria>

<output>
Create `.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-01-SUMMARY.md` when done.
</output>
