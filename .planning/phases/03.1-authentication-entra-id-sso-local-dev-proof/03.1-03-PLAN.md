---
phase: 03.1-authentication-entra-id-sso-local-dev-proof
plan: 03
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - web/views.py
  - templates/web/login.html
  - .env.example
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "The DEBUG dev-login POST still authenticates now that two backends are configured — the stub calls login(request, user, backend='django.contrib.auth.backends.ModelBackend') so it no longer raises ValueError (D-08/D-09#3, Pitfall 2)."
    - "The login page ALWAYS shows a 'Sign in with Microsoft' link to {% url 'social:begin' 'azuread-tenant-oauth2' %} (in both DEBUG and non-DEBUG), and the per-user dev-login list stays gated behind dev_mode/DEBUG (D-08)."
    - "A refused Entra login surfaces a visible message on /login (via messages or an ?error param), not a blank page."
    - ".env.example lists the SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_KEY/SECRET/TENANT_ID names (replacing the old ENTRA_* names) and no longer says 'Phase 2' (Pitfall 8)."
    - "Logout stays a local Django session flush -> /login with no Entra global sign-out (D-11) — the existing logout_view already satisfies this; do not add global sign-out."
  artifacts:
    - web/views.py
    - templates/web/login.html
    - .env.example
  key_links:
    - "The dev-login login() call MUST name ModelBackend once AUTHENTICATION_BACKENDS has two entries — without backend= it raises ValueError and the dev-login POST 500s (Pitfall 2), regressing D-09#3."
    - "The Microsoft button MUST render for anonymous users in DEBUG too (move it out of the {% else %} branch) — D-08 requires both paths visible on localhost."
---

<objective>
Make the two login paths coexist on localhost: keep the DEBUG dev-login stub working
after a second AUTHENTICATION_BACKENDS entry is added (Pitfall 2 / D-08 / D-09#3), and
put the real "Sign in with Microsoft" button on the login page in every mode while the
per-user dev list stays behind DEBUG (D-08). Refresh the stale env/copy references.

Purpose: This is the user-facing surface of the proof — the button that starts the
round-trip and the guarantee that adding the Entra backend did not silently break
dev-login or the superuser break-glass path. Logout stays session-only (D-11).

Output: the one-line `login()` backend fix + docstring refresh in web/views.py, the
updated `templates/web/login.html`, and corrected `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-CONTEXT.md
@.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@web/views.py
@templates/web/login.html
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Name ModelBackend in the dev-login stub and refresh its docstring</name>
  <files>web/views.py</files>
  <read_first>
    - web/views.py lines 49-71 (login_view, the DEBUG-gated POST branch calling login(request, user) at line 63, and _login_ctx)
    - RESEARCH.md "Pitfall 2: Adding a second backend breaks the existing dev-login stub" — the exact fix login(request, user, backend="django.contrib.auth.backends.ModelBackend")
    - CONTEXT.md D-08 (both paths kept; dev list stays behind DEBUG) and D-09#3 (dev-login + superuser break-glass must still work)
  </read_first>
  <action>
    In web/views.py login_view, change the dev-login sign-in call so it passes the
    explicit backend argument naming django.contrib.auth.backends.ModelBackend, exactly as
    RESEARCH Pitfall 2 prescribes — this is required because Plan 01 configured two
    AUTHENTICATION_BACKENDS entries and Django's login() otherwise raises ValueError. Keep
    the surrounding DEBUG gate, the is_active lookup, and the redirect unchanged. Update
    the login_view docstring so it no longer says Entra is "not yet implemented"/"auth
    cutover phase"; describe the current state accurately: Microsoft SSO is the real path
    (Plan 01/02) and the username dev-login remains a DEBUG-only convenience (Pitfall 8
    stale-comment cleanup). Do not touch logout_view — its local session flush already
    satisfies D-11.
  </action>
  <verify>
    <automated>py -3.12 manage.py check && py -3.12 manage.py test web -v1</automated>
  </verify>
  <acceptance_criteria>
    - The dev-login POST calls `login(request, user, backend="django.contrib.auth.backends.ModelBackend")`.
    - The DEBUG gate and redirect behavior are unchanged; logout_view is untouched.
    - The docstring no longer claims Entra is unimplemented / references an "auth cutover phase" as pending for the login button.
    - `manage.py check` passes and the existing `web` suite is green. (Coexistence proof lands in Plan 04's DevLoginCoexistTests.)
  </acceptance_criteria>
  <done>dev-login stub names ModelBackend and no longer 500s under two backends; docstring current.</done>
</task>

<task type="auto">
  <name>Task 2: Always-visible Microsoft button on the login page + refresh .env.example</name>
  <files>templates/web/login.html, .env.example</files>
  <read_first>
    - templates/web/login.html (the {% if dev_mode %}...{% else %}<a href="#">Sign in with Microsoft</a>{% endif %} structure — the button currently only renders in the non-DEBUG else branch and points at "#")
    - RESEARCH.md "Code Examples > Sign in with Microsoft button (template)" — the exact <a href="{% url 'social:begin' 'azuread-tenant-oauth2' %}"> markup and the note to move it out of the else branch and surface deny errors
    - CONTEXT.md D-08 (button visible in both modes; dev list under DEBUG) and Claude's Discretion (keep Franken UI styling consistent)
    - .env.example (the commented "# Microsoft Entra ID (Phase 2)" block with ENTRA_TENANT_ID/CLIENT_ID/CLIENT_SECRET)
    - RESEARCH.md "Runtime State Inventory > Secrets/env vars" + Pitfall 8 (update names to SOCIAL_AUTH_* and drop "Phase 2")
  </read_first>
  <action>
    In templates/web/login.html: render the "Sign in with Microsoft" link for all users by
    moving it OUT of the {% else %} branch so it shows in DEBUG and non-DEBUG alike (D-08),
    and point its href at {% url 'social:begin' 'azuread-tenant-oauth2' %} instead of "#".
    Keep the existing per-user dev-login forms inside the {% if dev_mode %} block. Preserve
    the existing Franken UI classes (uk-btn uk-btn-primary w-full h-11) so it matches the
    page. Fix the stale "Entra ID SSO wired in Phase 2" copy in the dev alert to phase-
    agnostic wording. Ensure a refused login is visible: render Django messages if present
    (SocialAuthExceptionMiddleware adds one) or the existing error context, so the user
    sees the refusal rather than a blank page. In .env.example: replace the commented
    ENTRA_TENANT_ID/ENTRA_CLIENT_ID/ENTRA_CLIENT_SECRET block with the real names
    SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_TENANT_ID / _KEY / _SECRET (client id maps to _KEY,
    client secret to _SECRET per D-04) and drop the "Phase 2" label (Pitfall 8).
  </action>
  <verify>
    <automated>py -3.12 manage.py check && grep -c "social:begin" templates/web/login.html && grep -c "SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - The login template renders the Microsoft button (href reversing `social:begin` for 'azuread-tenant-oauth2') for anonymous users in BOTH DEBUG and non-DEBUG; grep finds `social:begin` at least once.
    - The per-user dev-login forms remain inside the dev_mode/DEBUG block; a refused login shows a visible message.
    - .env.example uses the SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2_* names (grep finds them) and contains no "Phase 2" label on the Entra block.
    - `manage.py check` passes.
  </acceptance_criteria>
  <done>Microsoft button always visible and wired to social:begin; dev list stays under DEBUG; .env.example names corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser -> /login | unauthenticated surface exposing both auth entry points |
| DEBUG flag -> available login mechanisms | a config flag decides whether the passwordless dev-login is reachable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-08 | Spoofing | passwordless dev-login stub | high | mitigate | dev-login POST handler + per-user list stay gated behind settings.DEBUG; under DEBUG=False only the Entra button renders (D-08) — the stub is never a prod login path |
| T-03.1-09 | Repudiation / lockout | break-glass superuser | medium | mitigate | ModelBackend retained (Plan 01, D-03) + this plan keeps the dev-login login() working so an admin is never locked out during the local proof |
| T-03.1-13 | Info disclosure | stale env/copy referencing wrong var names | low | mitigate | .env.example corrected to the real SOCIAL_AUTH_* names so an operator does not populate dead ENTRA_* vars and silently break the redirect |
</threat_model>

<verification>
- `py -3.12 manage.py check` — clean.
- `py -3.12 manage.py test web -v1` — existing web suite green after the login() change.
- `grep -c "social:begin" templates/web/login.html` — at least 1 (button wired).
- `grep -c "SOCIAL_AUTH_AZUREAD_TENANT_OAUTH2" .env.example` — at least 1 (names corrected).
- Full coexistence behavior (dev-login POST + superuser login under two backends) is proven
  by Plan 04's DevLoginCoexistTests at the Wave-3 gate.
</verification>

<success_criteria>
Dev-login and Microsoft SSO coexist on localhost: the dev-login POST authenticates under
two backends, the Microsoft button is always visible and correctly wired, the dev list
stays behind DEBUG, logout stays session-only, and env/copy references are current.
</success_criteria>

<output>
Create `.planning/phases/03.1-authentication-entra-id-sso-local-dev-proof/03.1-03-SUMMARY.md` when done.
</output>
