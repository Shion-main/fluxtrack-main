---
status: blocked
phase: 03.1-authentication-entra-id-sso-local-dev-proof
plan: 05
source: [03.1-05-PLAN.md]
started: 2026-07-03
updated: 2026-07-03
blocked_on: "Entra app registration — redirect URI not registered (AADSTS50011); + a second unbound MMCM account for D-09 #2"
---

# Phase 03.1 — D-09 Live Proof (TO BE CONTINUED)

The automated work for this phase is complete and green (plans 03.1-01…04, 125-test
suite). This file tracks the **manual D-09 live proof (plan 03.1-05)**, which was started
live against the real MMCM Entra tenant on 2026-07-03 and **deferred by decision** before
all criteria passed. **The phase's definition of done (D-09) is NOT yet proven** — do not
mark AUTH-01/03/05 acceptance as met until these tests pass.

## What the live attempt already established
- **Code bug found + fixed (commit `c73a123`):** the "Sign in with Microsoft" button was an
  `<a href>` GET link, but `social-auth-app-django==6.0.0` decorates the begin view with
  `@require_POST` → clicking returned **405**. Converted to a CSRF-tokened POST form in
  `templates/web/login.html`; added `web.tests.MicrosoftButtonPostTests` (2 guards). Web suite green.
- **Reached Microsoft** (begin → redirect works), then failed at the IdP with **AADSTS50011
  (redirect-URI mismatch)**. The app correctly sends the pinned callback
  `http://localhost:8000/auth/complete/azuread-tenant-oauth2/` (`config/settings.py:143`),
  but that URI is not registered on app `1610c487-5918-4f04-affd-cbadc4147616`. **Portal
  config, not a code defect.**

## Current Test
number: 1
name: D-09 #1 — bound faculty (jrSabuero@mcm.edu.ph) signs in via Microsoft → faculty home
expected: |
  After registering the redirect URI, a real Microsoft login as the bound faculty account
  round-trips on http://localhost:8000 and lands on the faculty home; User.azure_oid populates.
awaiting: user action (register redirect URI) then user test

## Blocking precondition (do this first)
Register in Entra (portal → App registrations → `1610c487-…4147616` → Authentication → Web
platform), the **exact** redirect URI:
`http://localhost:8000/auth/complete/azuread-tenant-oauth2/`
Binding is already done (`faculty` → `jrSabuero@mcm.edu.ph`, 7 users seeded). For D-09 #2,
have a **second, unbound** real MMCM account ready.

## Tests

### 1. D-09 #1 — bound faculty → faculty home
expected: Microsoft round-trip on localhost lands the bound account on the faculty home; azure_oid set; authorize URL carries a `code_challenge` (PKCE sanity).
result: [pending]

### 2. D-09 #2 — unprovisioned account refused
expected: A different real MMCM account (not bound to any seeded user) is refused → back on /login with a visible message, not signed in; `User.objects.count()` stays 7 (no auto-create).
result: [pending]

### 3. D-09 #3 — dev-login hidden + break-glass under DEBUG=False
expected: With DEBUG=False, the per-user dev-login list is hidden (Microsoft button remains); the superuser still logs in via password at /admin/ (ModelBackend). Restore DEBUG=True after.
result: [pending]

## Summary
total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 3

## Resume
- Register the redirect URI (above), start `py -3.12 manage.py runserver`, then run the three
  tests per `03.1-05-PLAN.md` `<how-to-verify>`.
- Continue via `/gsd-verify-work 03.1` (or re-run `/gsd-execute-phase 03.1`).

## Gaps
