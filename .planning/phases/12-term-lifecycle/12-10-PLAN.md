---
phase: 12-term-lifecycle
plan: 10
type: execute
wave: 8
depends_on: [12-11, 12-12]
files_modified:
  - web/dean.py
  - templates/dean/dashboard.html
  - templates/dean/reports.html
  - web/tests_term_reporting.py
  - web/tests_dean_reporting.py
  - scheduling/tests_term_lifecycle.py
autonomous: true
requirements: [A4, IFO-04]
must_haves:
  truths:
    - "Every Dean dashboard/report/scorecard/CSV/PDF/stored download consumes one explicit ReportScope and preserves term through every link (D-09, D-10)."
    - "Dean department/IDOR scope remains independent and mandatory; choosing another term never grants another department (D-09, D-13)."
    - "Archived Dean reports default full term, active reports retain established window, and all explicit ranges clamp to term bounds (D-11)."
    - "One response/export contains exactly one term; cross-term comparison, unbounded aggregation, cloning, and hidden report session state are absent (D-07, D-10, D-12)."
    - "Phase-wide source guards find no production DEFAULT_TERM, AcademicTerm boolean query/order, implicit report term, reachable destructive rollover, or unguarded known session-write entry point (D-01, D-04, D-16)."
  artifacts:
    - path: web/tests_term_reporting.py
      provides: DeanReportTermTests, DeanReportLinkPropagationTests, WeeklyReportTermTests, Phase12CouplingGuardTests
    - path: scheduling/tests_term_lifecycle.py
      provides: phase-wide active lookup and writer inventory guard
  key_links:
    - "Dean GET scope -> required-term aggregate -> department predicate -> HTML/CSV/PDF"
    - "Dean stored download pk + selected term + request.user.department -> get_object_or_404"
    - "source coupling inventory -> all active/report/write seams -> regression fails on new implicit bypass"
---

<objective>
Finish selected-term reporting for Dean and close the phase with executable cross-surface coupling guards.

Purpose: Dean IDOR boundaries and final source inventory are the last places a dropped term or new writer bypass could undermine the lifecycle.
Output: Term-aware Dean views/templates/tests plus phase-wide coverage and coupling audit.

## Artifacts this phase produces
- Term-aware `web.dean.dashboard`, `reports`, `scorecard`, `scorecard_export`, `report_export`, and `weekly_download`.
- Dean templates with explicit selector and preserved scope links.
- `DeanReportTermTests`, `DeanReportLinkPropagationTests`, `WeeklyReportTermTests`, and `Phase12CouplingGuardTests`.
- Final multi-source coverage audit for GOAL, A4/IFO-04, research constraints, and D-01 through D-16.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-term-lifecycle/12-CONTEXT.md
@.planning/phases/12-term-lifecycle/12-RESEARCH.md
@.planning/phases/12-term-lifecycle/12-PATTERNS.md
@.planning/phases/12-term-lifecycle/12-VALIDATION.md
@.planning/phases/12-term-lifecycle/12-09-SUMMARY.md
@.planning/phases/12-term-lifecycle/12-11-SUMMARY.md
@.planning/phases/12-term-lifecycle/12-12-SUMMARY.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply explicit bounded term scope to Dean HTML, CSV, and PDF</name>
  <files>web/dean.py, templates/dean/dashboard.html, templates/dean/reports.html, web/tests_dean_reporting.py, web/tests_term_reporting.py</files>
  <read_first>
    - web/dean.py:147-325 (dashboard/reports/scorecard/exports/download)
    - templates/dean/dashboard.html and templates/dean/reports.html
    - templates/reports/scorecard.html (scope-preserving shared template from Plan 09)
    - web/tests_dean_reporting.py:36-230 (department/IDOR/read-only contract)
    - web/reporting_common.py ReportScope from Plan 09
  </read_first>
  <action>
    Resolve one `ReportScope` in every Dean management report and pass its term/date/as_of to required-term aggregates. Preserve the current `request.user.department` predicate independently; a Dean with no department remains empty and must never become an all-department query. Use the same scope for HTML, scorecard, CSV, and PDF. Filter latest/stored WeeklyReport cards by both selected term and department; download must `get_object_or_404` by PK, selected term, and the Dean's department.

    Render the shared term selector and propagate normalized scope through reports/dashboard/scorecard/back/reset/export/latest/download links. Keep every Dean view GET-only, report renderers unchanged, and foreign department behavior 404. Add same-date other-term fixtures and assert cross-term and cross-department exclusion compose correctly.
  </action>
  <acceptance_criteria>
    - All Dean report/export paths require selected term and department simultaneously.
    - Fresh ACTIVE, archived full-span, and clamped range behavior matches shared contract.
    - Foreign department or foreign selected-term report PK is 404.
    - Every Dean link preserves term/from/to and every view remains GET-only.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test web.tests_term_reporting.DeanReportTermTests web.tests_term_reporting.DeanReportLinkPropagationTests web.tests_dean_reporting -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add phase-wide implicit-term and archive-writer coupling guards</name>
  <files>web/tests_term_reporting.py, scheduling/tests_term_lifecycle.py</files>
  <read_first>
    - web/tests_term_reporting.py all Phase 12 scope tests
    - scheduling/tests_term_lifecycle.py archive/job/transition tests
    - .planning/phases/12-term-lifecycle/12-RESEARCH.md Current Codebase Inventory and Wave 0 Gaps
    - .planning/phases/12-term-lifecycle/12-VALIDATION.md W0-04/W0-06/W0-07/W0-09
  </read_first>
  <action>
    Add narrowly targeted source/coupling guards that inventory production term seams without matching User.is_active: fail on `DEFAULT_TERM`; AcademicTerm boolean filters/orderings; materializer/importer/report services resolving the first term; report aggregate/generator call sites missing `term=`; lifecycle UI importing/resetting; and the known session-write entry points from RESEARCH Pattern 4 lacking active/writable derivation. Prefer AST or function-scoped assertions over file-wide negative grep. Keep an explicit list of production files so a newly added writer requires deliberate test update.

    Add final behavior aggregation tests that exercise one lifecycle cycle: archived history remains readable/reportable, archived POSTs refuse, new Draft imports schedules only, activation materializes and becomes the sole live term, and reports can switch between old/new with no mixed rows. No cross-term comparison endpoint or session-stored selection is introduced.
  </action>
  <acceptance_criteria>
    - Coupling tests distinguish AcademicTerm lifecycle from accounts.User.is_active.
    - No production hardcoded/default/first-row term lookup survives.
    - Known writer inventory has active/writable enforcement and fails visibly when a seam is added without review.
    - End-to-end cycle proves old history readable but immutable and new active data operationally isolated.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test web.tests_term_reporting.Phase12CouplingGuardTests scheduling.tests_term_lifecycle.ArchivedWriterCouplingTests web.tests_term_reporting.TermLifecycleReportingE2ETests -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run the MSSQL phase gate and reconcile all Phase 12 suites</name>
  <files>web/tests_term_reporting.py, scheduling/tests_term_lifecycle.py</files>
  <read_first>
    - .planning/phases/12-term-lifecycle/12-VALIDATION.md full validation contract
    - .planning/phases/12-term-lifecycle/12-01-SUMMARY.md through 12-09-SUMMARY.md, plus 12-11-SUMMARY.md and 12-12-SUMMARY.md produced by this plan's declared dependencies before wave 8
    - config/settings.py database test configuration and DB_TEST_NAME
    - .planning/codebase/CONCERNS.md MSSQL cursor/parameter notes
  </read_first>
  <action>
    Reconcile any test fixture/call-site fallout from replacing AcademicTerm.is_active and requiring term arguments, changing only test construction or real implicit-term bugs identified by the coupling guard. Run the Phase 12 quick set, affected import/job/report/verification suites, then the full configured MSSQL suite. Run `check`, `makemigrations --check --dry-run`, and inspect both `sqlmigrate scheduling 0008` and `sqlmigrate ops 0006`. Do not claim SQLite results satisfy constraint/race acceptance. Record the baseline duration and set `12-VALIDATION.md` status fields only if the orchestrator authorizes artifact updates; this plan itself owns test files only.
  </action>
  <acceptance_criteria>
    - Phase 12 quick modules and every modified legacy suite pass on configured MSSQL.
    - Full suite has no new failure; any pre-existing failure is evidenced against baseline, not silently accepted.
    - Django check/migration drift gates pass and SQL shows filtered uniqueness/check constraints with no destructive history operation.
    - Test fixtures use AcademicTerm.status while User.is_active semantics remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_term_lifecycle web.tests_term_lifecycle web.tests_term_reporting ops.tests_reports verification.tests -v 2 && py -3.12 manage.py check && py -3.12 manage.py makemigrations --check --dry-run && py -3.12 manage.py test</automated>
    <human-check>On a sanitized production-shaped MSSQL copy, rehearse both migrations, compare pre/post term/schedule/session/attendance/report counts, attempt a second ACTIVE and invalid date range, walk the IFO lifecycle console, and confirm rollover never exposes or invokes reset_term.</human-check>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dean GET/report id -&gt; department and term data | A valid term/report id must not bypass department IDOR scope. |
| Phase source changes -&gt; implicit-term invariants | New callers/writers can silently omit term or archive guard after this phase. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information Disclosure | Dean report/term dropping | high | mitigate | Term + department predicates on every path, object 404 checks, link/leakage tests. |
| T-12-05 | Elevation/IDOR | selected term versus Dean department | high | mitigate | Department scope remains independent and mandatory; NULL department stays empty. |
| T-12-04 | Tampering | future archived writer bypass | high | mitigate | Maintained source inventory plus end-to-end archived POST refusal. |
| T-12-07 | Tampering/Data loss | future implicit/destructive rollover | critical | mitigate | Coupling guard for DEFAULT_TERM/reset/first-row patterns and migration/UI rehearsal. |
| T-12-03 | Tampering | partial bulk/migration failure | high | mitigate | Full MSSQL rollback/rehearsal gates and unchanged row-count checks. |
| T-12-02 | Repudiation | consequential lifecycle actions | high | mitigate | E2E verifies atomic actor/reason/before/after AuditLog and absence on refusal. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | - | Close/archive read-only, create/activate next, preserve attendance history | 01-12 | COVERED | Schema, services, freeze, operations, UI, reports, and phase gate. |
| REQ | A4 | Replace destructive rollover and de-hardcode term selection | 01,02,04,05,06,10,11,12 | COVERED | Reset retired; lifecycle/status/active resolver, Admin freeze, reporting, and E2E preservation. |
| REQ | IFO-04 | IFO term/active-term/offerings management portion | 04,07,12 | COVERED | Draft import plus IFO lifecycle/report console. |
| RESEARCH | migration | Staged MSSQL state/backfill and constraints | 01,03 | COVERED | Fail-loud rehearsed Django migrations. |
| RESEARCH | write freeze | Services, commands, Admin, direct POSTs, jobs | 04-06,11 | COVERED | Service/command and Admin writer matrices plus coupling guard. |
| RESEARCH | stored reports | Term FK, identity, generation, paths/downloads | 03,08,10,12 | COVERED | No legacy path rewrite; no cross-term collision. |
| CONTEXT | D-01 | DRAFT/ACTIVE/ARCHIVED and single ACTIVE | 01,02 | COVERED | DB constraint + service race handling. |
| CONTEXT | D-02 | Audited reason-required reopen to DRAFT | 01,07 | COVERED | Never auto-activates. |
| CONTEXT | D-03 | Close after end, zero ACTIVE sessions | 01,07 | COVERED | No cancellation/deletion. |
| CONTEXT | D-04 | Application-wide archive freeze | 04-06,08,10,11 | COVERED | Import/materialize/services/Admin/commands/live writers/reports. |
| CONTEXT | D-05 | Draft import, no Draft sessions | 02,04 | COVERED | Preview/commit target persisted; activation owns materialization. |
| CONTEXT | D-06 | Separate close and activate | 02,07 | COVERED | Separate URLs/services; another ACTIVE blocks. |
| CONTEXT | D-07 | Blank validated non-overlapping creation, no clone | 01,07 | COVERED | Creation service/UI tests. |
| CONTEXT | D-08 | Activation readiness/materialization rollback | 02 | COVERED | One outer transaction and injected failure test. |
| CONTEXT | D-09 | IFO/Dean/HR selectable; live active-only | 06,08-10,12 | COVERED | Explicit reports, authoritative live scope. |
| CONTEXT | D-10 | GET params propagated, no session state | 09,10,12 | COVERED | Shared scope and link tests. |
| CONTEXT | D-11 | Term-bounded dates/defaults | 09,10,12 | COVERED | Central clamping/default contract. |
| CONTEXT | D-12 | One-term-only reports/exports | 03,08-10,12 | COVERED | Required service signatures and same-date fixtures. |
| CONTEXT | D-13 | IFO authority, superuser break-glass | 01,07,11 | COVERED | Service auth; invariant-preserving lifecycle and Admin paths. |
| CONTEXT | D-14 | Exact name/reason/audit evidence | 01,02,07 | COVERED | High-friction forms and atomic audit. |
| CONTEXT | D-15 | Hard blockers vs acknowledged warnings | 01,02,07 | COVERED | Typed preflight/recomputed warning keys. |
| CONTEXT | D-16 | Atomic transitions/no partial state | 01-06,07,10 | COVERED | Transaction, migration, import, materialization, creation-confirmation, and E2E rollback tests. |
</source_audit>

<verification>
- Run `py -3.12 manage.py test web.tests_dean_reporting web.tests_term_reporting scheduling.tests_term_lifecycle -v 2`.
- Run the Phase 12 quick command, affected suites, full MSSQL suite, `check`, and migration drift/SQL review gates.
- Confirm all source-audit rows are COVERED and no deferred item (timed activation, combined rollover, cloning, multi-term comparison, two-person approval) appears.
</verification>

<success_criteria>
Dean reporting is single-term and department-safe, all locked decisions and research constraints have executable coverage, and the full MSSQL phase gate proves no implicit/destructive term path survives.
</success_criteria>

<output>
Create `.planning/phases/12-term-lifecycle/12-10-SUMMARY.md` when done.
</output>
