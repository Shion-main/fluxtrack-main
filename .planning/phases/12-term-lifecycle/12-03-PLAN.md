---
phase: 12-term-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - ops/models.py
  - ops/migrations/0006_term_ownership.py
  - ops/tests_term_migrations.py
  - ops/tests_reports.py
autonomous: true
requirements: [A4, IFO-04]
must_haves:
  truths:
    - "Every newly staged import is durably bound to one intended term across preview and commit, while legacy unconsumed rows fail safely rather than guessing a target (D-05, D-16)."
    - "WeeklyReport identity is `(term, week_start, department)` and new storage metadata cannot collide across terms (D-09, D-12)."
    - "Legacy WeeklyReport rows are backfilled only when their week intersects exactly one term; unmatched or ambiguous rows abort migration without assigning the current ACTIVE term (D-12, D-16)."
    - "Adding term ownership preserves old report files and all attendance/schedule/session history."
  artifacts:
    - path: ops/models.py
      provides: ImportStaging.term and WeeklyReport.term
    - path: ops/migrations/0006_term_ownership.py
      provides: staged report-term backfill and new uniqueness
    - path: ops/tests_term_migrations.py
      provides: ambiguous/unmatched/successful backfill tests
  key_links:
    - "ImportStaging.term -> preview token ownership -> Plan 04 commit recheck cannot drift targets"
    - "WeeklyReport.term -> uniq_weekly_report_term_week_department -> same week can exist safely for different terms"
    - "migration week intersection -> exactly one AcademicTerm -> non-null WeeklyReport.term"
---

<objective>
Add durable term identity to the two persisted operational records that currently lose it: staged imports and stored weekly reports.

Purpose: Preview/commit and stored report paths cannot be made single-term by controller convention alone; the target must survive in SQL Server.
Output: An ops migration with fail-loud legacy backfill, final model constraints, and migration/model tests.

## Artifacts this phase produces
- `ImportStaging.term` nullable legacy-compatible FK using `PROTECT`.
- `WeeklyReport.term` required `PROTECT` FK.
- `uniq_weekly_report_term_week_department` replacing the old two-field identity.
- `ops/migrations/0006_term_ownership.py`.
- `ops/tests_term_migrations.py`: `WeeklyReportTermBackfillTests` and `ImportStagingTermMigrationTests`.
- `ops.tests_reports.WeeklyReportTermIdentityTests`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-term-lifecycle/12-CONTEXT.md
@.planning/phases/12-term-lifecycle/12-RESEARCH.md
@.planning/phases/12-term-lifecycle/12-VALIDATION.md
@.planning/phases/12-term-lifecycle/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave 0 migration tests for durable term ownership</name>
  <files>ops/tests_term_migrations.py, ops/tests_reports.py</files>
  <read_first>
    - ops/models.py:51-99 (ImportStaging ownership/lifecycle contract)
    - ops/models.py:250-267 (current WeeklyReport identity)
    - ops/migrations/0005_booking_protect_import_staging.py (latest ops migration)
    - ops/tests_reports.py:35-200 (report fixture/storage test style)
    - .planning/phases/12-term-lifecycle/12-RESEARCH.md (Pattern 7 and Runtime State Inventory)
  </read_first>
  <action>
    Create migration-executor tests covering four production-shaped states before implementing the migration: a report week intersecting exactly one term maps to it; two overlapping candidate terms abort; no candidate aborts; and two reports with the same week/department but different terms coexist after migration. Include a legacy ImportStaging row with no term proving the schema permits the old row but new application services must reject it in Plan 04. Extend report model tests to pin required term identity and `PROTECT` deletion semantics. Run the target once and record the expected RED caused by the absent fields/migration; Task 1's commit is the deliberate Wave 0 test contract, and Task 2 must turn the same target GREEN.
  </action>
  <acceptance_criteria>
    - Tests exercise forward migration from ops 0005 and scheduling pre-lifecycle state to the new state using MigrationExecutor.
    - Ambiguous/unmatched rows assert a loud exception, not fallback to ACTIVE or first term.
    - Same week/department is unique only inside one term.
    - Tests do not read or infer from checked-in db.sqlite3.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 -m compileall -q ops/tests_term_migrations.py ops/tests_reports.py</automated>
  </verify>
  <done>The Wave 0 test contract compiles and records the expected pre-implementation RED result.</done>
</task>

<task type="auto">
  <name>Task 2: Add staged import and stored report term fields with fail-loud backfill</name>
  <files>ops/models.py, ops/migrations/0006_term_ownership.py</files>
  <read_first>
    - ops/models.py (Task 1 target fields and existing relationships)
    - scheduling/models.py AcademicTerm final status contract from Plan 01
    - ops/migrations/0002_roomconflictflag.py (constraint operation analog)
    - .planning/phases/12-term-lifecycle/12-PATTERNS.md (`WeeklyReport` and ImportStaging assignments)
  </read_first>
  <action>
    Add nullable `ImportStaging.term` with `on_delete=PROTECT`; the database must retain legacy rows but Plan 04 makes term mandatory for every new stage/resolve/consume flow. Add final required `WeeklyReport.term` with `on_delete=PROTECT`, ordering by newest week, and `UniqueConstraint(fields=["term", "week_start", "department"], name="uniq_weekly_report_term_week_department")`.

    Implement `0006_term_ownership.py` with dependencies on ops 0005 and scheduling 0008. Add both term FKs nullable, then RunPython over a materialized list of legacy WeeklyReport rows. Define its week as `[week_start, week_start + 6 days]`; select AcademicTerm rows whose date spans intersect that interval. Set term only when candidate count is exactly one; raise a diagnostic with report PK/week/candidate ids otherwise. Preserve `csv_path`, `pdf_path`, generated_at, and bytes on storage. Remove old unique_together, add the three-column constraint, and make WeeklyReport.term non-null. Do not rewrite old paths or assign the currently ACTIVE term by convenience.
  </action>
  <acceptance_criteria>
    - Migration has cross-app scheduling 0008 dependency and ordered nullable/backfill/constraint/non-null operations.
    - Legacy report metadata and file paths remain byte-for-byte unchanged except term_id.
    - Ambiguous/unmatched migration states abort before non-null/uniqueness finalization.
    - ImportStaging legacy null is accepted at schema level; all new binding enforcement is delegated to Plan 04 service code.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests_term_migrations ops.tests_reports.WeeklyReportTermIdentityTests -v 2 && py -3.12 manage.py makemigrations --check --dry-run</automated>
    <human-check>Run `py -3.12 manage.py sqlmigrate ops 0006` against the MSSQL configuration and confirm no report-path UPDATE and no DELETE is emitted; rehearse on sanitized production-shaped report rows before deployment.</human-check>
  </verify>
  <done>All acceptance criteria are satisfied and the migration/model verification passes GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy ops rows -&gt; lifecycle-aware schema | Missing term identity cannot be inferred from current state without risking history corruption. |
| Stored report metadata -&gt; private files | DB identity and storage path must not collide or be reassigned across terms. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information Disclosure/Tampering | WeeklyReport identity | high | mitigate | Required term FK and three-column uniqueness; generation/UI scoping follows in Plans 08-10. |
| T-12-08 | Tampering | legacy report backfill | high | mitigate | Exactly-one intersection, fail-loud diagnostics, migration tests, no ACTIVE fallback. |
| T-12-03 | Tampering | staged import target drift | high | mitigate | Persist target FK now; Plan 04 rechecks target state at commit. |
</threat_model>

<verification>
- Run `py -3.12 manage.py test ops.tests_term_migrations ops.tests_reports -v 2`.
- Run `py -3.12 manage.py makemigrations --check --dry-run` and inspect `sqlmigrate ops 0006` on MSSQL.
- Confirm legacy report files are not moved or overwritten by the migration.
</verification>

<success_criteria>
Staged imports and stored reports have durable term identities, and legacy report rows are migrated only with unambiguous evidence.
</success_criteria>

<output>
Create `.planning/phases/12-term-lifecycle/12-03-SUMMARY.md` when done.
</output>
