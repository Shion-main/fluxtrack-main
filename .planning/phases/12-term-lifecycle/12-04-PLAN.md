---
phase: 12-term-lifecycle
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - ops/import_staging.py
  - scheduling/management/commands/import_offerings.py
  - scheduling/tests_import_hardening.py
  - scheduling/tests.py
  - web/ifo.py
  - web/tests_ifo_import.py
  - templates/ifo/import.html
  - templates/ifo/_import_panel.html
autonomous: true
requirements: [A4, IFO-04]
must_haves:
  truths:
    - "IFO can select one DRAFT term, stage/preview/commit schedules into that same durable target while another term remains ACTIVE, and no dated Session is created for the Draft (D-05)."
    - "The importer never creates a term, toggles lifecycle state, deactivates another term, clones schedules, or invokes materialization (D-05, D-06, D-07)."
    - "Preview/commit re-resolves the staged target and refuses if it is no longer DRAFT or has become ARCHIVED; stale preview cannot redirect writes (D-04, D-16)."
    - "CLI imports require an explicit writable term; the browser path is stricter and accepts DRAFT only."
    - "Failed import leaves staging retryable and does not partially consume the token or audit success (D-16)."
  artifacts:
    - path: ops/import_staging.py
      provides: term-bound stage/resolve contract
    - path: scheduling/management/commands/import_offerings.py
      provides: explicit `--term` import adapter without lifecycle side effects
    - path: web/ifo.py
      provides: Draft selector persisted through preview/commit
    - path: web/tests_ifo_import.py
      provides: target-drift, no-session, and archive-refusal tests
  key_links:
    - "POST preview term id -> ImportStaging.term -> token in session -> commit recheck -> explicit importer term"
    - "import_offerings explicit writable term -> Schedule.term; no Session/materializer/lifecycle writes"
---

<objective>
Bind registrar import to an explicit Draft without disturbing the currently ACTIVE term.

Purpose: Preparation must be possible before rollover, and the preview/commit boundary must not lose or silently change its target.
Output: Explicit CLI target, term-bound staging, Draft-only browser workflow, and adversarial tests.

## Artifacts this phase produces
- `import_offerings --term` required option and explicit-term `Command.run` path.
- `stage_upload(..., term=...)` and term-aware `resolve_staged` contract.
- Draft term selector/hidden durable target rendering on `/ifo/import` preview and commit.
- `DraftTermImportTests`, `ImportTargetDriftTests`, and `ImportNoMaterializationTests` in `web/tests_ifo_import.py`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-term-lifecycle/12-CONTEXT.md
@.planning/phases/12-term-lifecycle/12-RESEARCH.md
@.planning/phases/12-term-lifecycle/12-PATTERNS.md
@.planning/phases/12-term-lifecycle/12-02-SUMMARY.md
@.planning/phases/12-term-lifecycle/12-03-SUMMARY.md
@.planning/phases/07-remaining-operational-surfaces/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make import_offerings consume one explicit writable term</name>
  <files>scheduling/management/commands/import_offerings.py, scheduling/tests_import_hardening.py, scheduling/tests.py</files>
  <read_first>
    - scheduling/management/commands/import_offerings.py:70-126 (current options and implicit create/deactivate logic)
    - scheduling/importing.py (shared parsing/reconciliation helpers that must remain unchanged)
    - scheduling/term_scope.py (writable guard)
    - .planning/phases/04.1-real-data-integration/04.1-03-SUMMARY.md (full-term importer invariants)
    - .planning/phases/12-term-lifecycle/12-PATTERNS.md (staged import assignment)
  </read_first>
  <action>
    Replace the implicit term name/default/create/deactivate behavior with required `--term`, resolved by PK or exact name. Call `require_writable_term`; accept explicit DRAFT or ACTIVE for the established operator mid-term import path, but never ARCHIVED. Pass the resolved term through every Schedule lookup/count/create; do not create or update AcademicTerm lifecycle fields. Preserve XLSX/CSV dispatch, reconciliation buckets, faculty/room dedupe, dry-run semantics, atomic run, and list-before-write behavior.

    Add source and behavior tests proving no `AcademicTerm.objects.get_or_create`, mass lifecycle update, materializer call, or reset command is reachable; DRAFT import creates recurring Schedule rows only, all under the chosen term; another ACTIVE term is unchanged; same input under two explicit terms does not cross-count; archived refusal returns non-zero before writes.
  </action>
  <acceptance_criteria>
    - No implicit/default term target remains in importer production source.
    - DRAFT and explicit ACTIVE operator targets are writable; ARCHIVED/missing targets fail before writes.
    - DRAFT import creates zero Session rows and changes no term status.
    - Full-term reconciliation and existing CSV/XLSX parsing tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_import_hardening scheduling.tests.ImportPathTests web.tests_ifo_import.ImportSourceGuardTests -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist the selected Draft through staging and preview</name>
  <files>ops/import_staging.py, web/ifo.py, templates/ifo/import.html, templates/ifo/_import_panel.html, web/tests_ifo_import.py</files>
  <read_first>
    - ops/import_staging.py (stage_upload/resolve_staged/consume/discard APIs)
    - ops/models.py ImportStaging.term from Plan 03
    - web/ifo.py:989-1200 (current staging/preview/commit flow)
    - templates/ifo/import.html and templates/ifo/_import_panel.html
    - web/tests_ifo_import.py (ownership, single-use, multipart, source guards)
  </read_first>
  <action>
    Require `term` in `stage_upload` and persist its FK with the existing owner/path/size metadata. Extend the import page context with DRAFT choices ordered by start date and a required `term` field. On preview, resolve the posted PK server-side as DRAFT, reject invalid/non-Draft/archived choices with HTTP 400 while preserving the upload control behavior, stage bytes with that term, and run the dry-run importer with its explicit id. Render the selected term name/id in the preview and never place target identity only in a hidden session variable; the token remains the sole session value and its database row remains authoritative.

    Re-loading a staged preview must resolve its bound term. Legacy null-term staging rows must be discarded/refused with a friendly message, never defaulted to ACTIVE. Preserve upload size/content validation, path safety, ownership, and single-use rules.
  </action>
  <acceptance_criteria>
    - Browser import offers DRAFT terms only and requires a valid selection.
    - ImportStaging row stores selected term; reload displays the same term from DB.
    - Active/Archived/foreign/null target attempts fail 400 without scheduling writes.
    - Existing multipart, ownership, path, and corrupt-file tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test web.tests_ifo_import.DraftTermImportTests web.tests_ifo_import.StagingLifecycleTests web.tests_ifo_import.StagingOwnershipTests -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recheck Draft target and commit atomically without materialization</name>
  <files>web/ifo.py, templates/ifo/_import_panel.html, web/tests_ifo_import.py</files>
  <read_first>
    - web/ifo.py import_commit and `_staged_for` after Task 2
    - ops/import_staging.py consume/discard semantics
    - scheduling/management/commands/import_offerings.py explicit target from Task 1
    - ops/models.py AuditLog and ImportStaging
    - .planning/phases/12-term-lifecycle/12-CONTEXT.md (D-05, D-16)
  </read_first>
  <action>
    In import commit, re-resolve the token by owner/unconsumed state, lock the staging row and bound AcademicTerm inside `transaction.atomic()`, require that target is still DRAFT, and call the importer with the exact target PK. Count schedules only for that target before/after. Consume staging and write `schedule.imported` AuditLog with term id/name and target-scoped count only after import success; leave the row retryable on expected import failure and never claim success. Do not call materialization or transition the term.

    Add tests that change DRAFT to ACTIVE or ARCHIVED after preview and before commit, swap another term into the request/session, inject importer failure after a write, and double-submit. Assert stale targets refuse, arbitrary request term is ignored in favor of staging.term, atomic failure leaves no partial schedules/consume/audit, and successful Draft commit creates schedules but zero sessions.
  </action>
  <acceptance_criteria>
    - Commit authority is the locked ImportStaging.term, never a fresh client target.
    - Target state is rechecked at commit; changed/archived target produces no writes.
    - Failed commit preserves retryability and no success audit; successful commit consumes exactly once.
    - No dated Session exists for the Draft after successful import.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test web.tests_ifo_import.ImportTargetDriftTests web.tests_ifo_import.ImportNoMaterializationTests web.tests_ifo_import.StagingLifecycleTests -v 2 && py -3.12 manage.py check</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser multipart/session token -&gt; ImportStaging | Client term ids, filenames, and stale previews are untrusted. |
| ImportStaging -&gt; importer | Target state can change between preview and commit. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | preview/commit target drift | high | mitigate | Persist term FK; lock/recheck staging and Draft at commit; ignore client retargeting. |
| T-12-04 | Tampering | archived import | high | mitigate | Shared writable guard plus stricter browser DRAFT check at preview and commit. |
| T-12-07 | Tampering/Data loss | legacy reset/implicit activation | high | mitigate | Source guards prohibit reset/materialize/lifecycle calls and implicit term creation/deactivation. |
| T-12-09 | DoS | hostile upload expansion | medium | mitigate | Preserve byte/content/row caps and broad parse refusal from existing staging flow. |
</threat_model>

<verification>
- Run `py -3.12 manage.py test web.tests_ifo_import scheduling.tests_import_hardening -v 2`.
- Verify target-specific Schedule counts and zero DRAFT Sessions after both preview and commit.
- Run `py -3.12 manage.py check`.
</verification>

<success_criteria>
IFO can safely prepare a future Draft via preview/commit while the current term remains ACTIVE, with durable target identity and no materialized sessions or lifecycle side effects.
</success_criteria>

<output>
Create `.planning/phases/12-term-lifecycle/12-04-SUMMARY.md` when done.
</output>
