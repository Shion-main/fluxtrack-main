---
phase: 12-term-lifecycle
plan: 08
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - scheduling/reporting.py
  - scheduling/tests_reporting.py
  - scheduling/tests_reporting_rooms.py
  - ops/reports.py
  - ops/tests_reports.py
  - scheduling/management/commands/generate_weekly_report.py
  - scheduling/management/commands/runscheduler.py
autonomous: true
requirements: [A4, IFO-04]
must_haves:
  truths:
    - "Every attendance/coverage/scorecard aggregate requires one explicit term and filters `schedule__term=term`; same-date rows from another term never contribute (D-09, D-12)."
    - "Weekly report generation requires one explicit writable/ACTIVE term, keys storage and DB identity by term/week/department, and never spans terms (D-04, D-12)."
    - "Scheduled generation resolves the authoritative ACTIVE term once; no ACTIVE is a safe no-op and archived history is not rewritten."
    - "Existing report metrics, department/faculty predicates, CSV/PDF rendering, formula protection, and MSSQL cursor discipline remain unchanged."
  artifacts:
    - path: scheduling/reporting.py
      provides: mandatory-term aggregate signatures
    - path: ops/reports.py
      provides: term-aware generate_weekly_report(s) and storage paths
    - path: scheduling/management/commands/generate_weekly_report.py
      provides: explicit/ACTIVE term CLI
    - path: ops/tests_reports.py
      provides: WeeklyReportTermTests
  key_links:
    - "controller-selected term -> faculty/dept/coverage/scorecard aggregate required parameter -> schedule__term predicate"
    - "scheduler active term -> generate_week_reports(term=...) -> WeeklyReport(term, week, department) -> reports/term-&lt;pk&gt;/..."
---

<objective>
Make term identity mandatory below report controllers and in stored weekly report generation.

Purpose: A selector is cosmetic if aggregate/generation services can still omit term and widen across history.
Output: Required-term aggregate APIs, term-qualified report generation/storage, scheduler/command adapters, and leakage/idempotency tests.

## Artifacts this phase produces
- Required `term` parameters on `_scoped_sessions`, `faculty_attendance`, `dept_summary`, `coverage_by_building_day`, `zero_coverage_floors`, and `faculty_scorecard`.
- Term-qualified `generate_weekly_report(term, ...)` and `generate_week_reports(term, ...)`.
- Storage path contract `reports/term-<pk>/<week>/<department-code>.{csv,pdf}` for new files.
- `generate_weekly_report --term` explicit option and active-term scheduler wiring.
- `WeeklyReportTermTests` and cross-term aggregate leakage tests.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-term-lifecycle/12-CONTEXT.md
@.planning/phases/12-term-lifecycle/12-RESEARCH.md
@.planning/phases/12-term-lifecycle/12-PATTERNS.md
@.planning/phases/12-term-lifecycle/12-VALIDATION.md
@.planning/phases/12-term-lifecycle/12-03-SUMMARY.md
@.planning/phases/06-reporting-engine-reporting-surfaces/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require explicit term in every attendance management aggregate</name>
  <files>scheduling/reporting.py, scheduling/tests_reporting.py, scheduling/tests_reporting_rooms.py</files>
  <read_first>
    - scheduling/reporting.py:388-706 (`_scoped_sessions` through scorecard/coverage functions)
    - scheduling/reporting.py:809-1410 (room functions already requiring term)
    - scheduling/tests_reporting.py (aggregate fixture and truth tests)
    - scheduling/tests_reporting_rooms.py (existing explicit-term room tests)
    - .planning/phases/12-term-lifecycle/12-PATTERNS.md report aggregate assignment
  </read_first>
  <action>
    Make `term` a required keyword-only parameter in `_scoped_sessions`, `faculty_attendance`, `dept_summary`, `coverage_by_building_day`, `zero_coverage_floors`, and `faculty_scorecard`. Add `schedule__term=term` at the shared session queryset root; retain ScheduleStatus.ACTIVE, date/as_of, department, faculty, HELD/CANCELLED, verification, lateness, and physical-room predicates unchanged. Thread term through internal calls without any default resolver below the controller layer.

    Update focused aggregate tests and fixture construction to use AcademicTerm.Status. Add same-date ACTIVE/ARCHIVED/DRAFT sessions with otherwise identical faculty/room/status and prove each selected term produces only its own rows/counts. Add signature tests that omission raises TypeError, preventing silent cross-term aggregation by future callers.
  </action>
  <acceptance_criteria>
    - All named non-room aggregates require term and share explicit schedule__term filtering.
    - Selected-term counts remain correct with same-date other-term rows.
    - No aggregate resolves ACTIVE internally or accepts `term=None` as all terms.
    - Existing metric/lateness/coverage/utilization truth tests remain green after call-site updates.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_reporting scheduling.tests_reporting_rooms -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Key stored weekly generation and paths by one term</name>
  <files>ops/reports.py, ops/tests_reports.py</files>
  <read_first>
    - ops/reports.py:49-156 (bounds, save, generate, notify orchestration)
    - ops/models.py WeeklyReport.term/constraint from Plan 03
    - scheduling/report_render.py (unchanged CSV/PDF builders and csv_safe)
    - ops/tests_reports.py:35-200 (storage/notify/idempotency tests)
    - .planning/phases/12-term-lifecycle/12-PATTERNS.md stored report assignment
  </read_first>
  <action>
    Require `term` in `generate_weekly_report` and `generate_week_reports`, call term-required aggregates, and call `require_writable_term` before creating/updating WeeklyReport so ARCHIVED history is not rewritten. Use `(term, week_start, department)` for `get_or_create` and new storage names `reports/term-{term.pk}/{week_start}/{code}.csv|pdf`. Keep overwrite idempotency within that exact identity, current report renderers, notifications, department fan-out, and existing legacy file paths untouched.

    Add tests for same week/department in two ACTIVE/Draft-compatible fixture terms at the service boundary as appropriate, path non-collision, selected-term row contents, rerun idempotency, archived generation refusal with unchanged metadata/files, and no cross-term notification link. Use temporary MEDIA_ROOT and do not perform ORM queries inside streaming cursors.
  </action>
  <acceptance_criteria>
    - New WeeklyReport rows always have term and unique term-qualified path.
    - Generation data comes only from the supplied term; same-date other-term rows are absent.
    - Archived generation refuses before metadata/file overwrite.
    - Existing rendering/injection protection and notification recipients remain intact.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests_reports.WeeklyReportTermTests ops.tests_reports -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make command and scheduler report generation explicitly active-term scoped</name>
  <files>scheduling/management/commands/generate_weekly_report.py, scheduling/management/commands/runscheduler.py, ops/tests_reports.py</files>
  <read_first>
    - scheduling/management/commands/generate_weekly_report.py (current on-demand adapter)
    - scheduling/management/commands/runscheduler.py (weekly job registration and four-job invariant)
    - ops/tests.py `NoImplicitSchedulerTests` (existing scheduler-registration invariant; verification-only, not modified here)
    - scheduling/term_scope.py (active resolver)
    - ops/reports.py Task 2 signatures
    - ops/tests_reports.py scheduler/command tests
  </read_first>
  <action>
    Add optional `--term` to on-demand generation: omitted resolves authoritative ACTIVE; supplied resolves exact PK/name but still requires writable ACTIVE for stored generation. Pass term explicitly to report services. Update the existing weekly scheduler callable to resolve ACTIVE once and pass it; no active returns zero/no-op through the JobRun wrapper without creating reports. Keep exactly four registered scheduler jobs, policy cadence, run_job wrapping, and failure notification semantics.

    Add command/scheduler tests with same-date archived data, no active, and explicit wrong-state term. Add a coupling test that all production calls to `generate_week_reports` provide term and no report service performs an implicit term lookup.
  </action>
  <acceptance_criteria>
    - Scheduler creates stored reports only for authoritative ACTIVE term.
    - No ACTIVE creates no report and does not crash/scan history.
    - Explicit archived/Draft stored generation refuses non-zero before file/DB writes.
    - Four-job and NoImplicitScheduler invariants remain green.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test ops.tests_reports ops.tests.NoImplicitSchedulerTests -v 2 && py -3.12 manage.py check</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregate caller -&gt; Session query | Omitted term can silently aggregate official records across history. |
| Scheduler/command -&gt; stored files | Implicit or archived target can overwrite/collide private report artifacts. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Information Disclosure | report aggregates | high | mitigate | Mandatory term signatures and shared schedule__term predicate; same-date leakage tests. |
| T-12-04 | Tampering | archived stored report rewrite | high | mitigate | Writable/ACTIVE guard before DB/file writes; unchanged artifact tests. |
| T-12-03 | Tampering/DoS | multi-file report generation | high | mitigate | Exact identity, server-built paths, idempotent overwrite, focused failure tests. |
| T-12-07 | Tampering | scheduler implicit target | high | mitigate | Authoritative ACTIVE resolution once; no-active no-op; coupling guard. |
</threat_model>

<verification>
- Run `py -3.12 manage.py test scheduling.tests_reporting scheduling.tests_reporting_rooms ops.tests_reports -v 2`.
- Run scheduler registration/command tests and `py -3.12 manage.py check`.
- Confirm no aggregate/generation service has an optional/default term.
</verification>

<success_criteria>
Report computations and stored generation cannot omit term identity, collide across terms, or rewrite an archived term.
</success_criteria>

<output>
Create `.planning/phases/12-term-lifecycle/12-08-SUMMARY.md` when done.
</output>
