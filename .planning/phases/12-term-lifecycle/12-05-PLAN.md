---
phase: 12-term-lifecycle
plan: 05
type: execute
wave: 4
depends_on: [12-02, 12-03, 12-04]
files_modified:
  - scheduling/schedule_ops.py
  - scheduling/suspensions.py
  - scheduling/services.py
  - scheduling/merge.py
  - scheduling/management/commands/reset_term.py
  - scheduling/management/commands/seed_term.py
  - scheduling/management/commands/audit_merge_coverage.py
  - scheduling/tests_term_lifecycle.py
  - scheduling/tests_suspensions.py
  - web/tests_schedule_ops.py
autonomous: true
requirements: [A4, IFO-04]
must_haves:
  truths:
    - "Every normal domain service that changes term-owned schedules, sessions, breaks/suspensions, or modality data refuses an ARCHIVED owner before mutation (D-04)."
    - "The destructive reset command is retired as a rollover path, contains no deletion handler/default term, and exits non-zero with lifecycle guidance (D-04)."
    - "Retained term-aware commands resolve an explicit/ACTIVE term and refuse ARCHIVED targets without partial bulk writes (D-04, D-16)."
    - "Service and command archive refusal is tested as a writer matrix rather than inferred from hidden UI controls; follow-on Plan 12-11 owns Django Admin enforcement for the same D-04 freeze."
  artifacts:
    - path: scheduling/term_scope.py
      provides: shared `require_writable_term` consumed at every writer boundary
    - path: scheduling/management/commands/reset_term.py
      provides: permanently retired destructive command adapter
    - path: scheduling/tests_term_lifecycle.py
      provides: ArchiveFreezeServiceTests and ArchiveFreezeCommandTests
  key_links:
    - "public mutator -> derive/lock AcademicTerm -> require_writable_term -> domain write/audit"
    - "reset_term invocation -> CommandError before any ORM delete"
---

<objective>
Enforce the ARCHIVED write freeze across reusable domain services and management commands.

Purpose: Hiding buttons does not protect official history; reusable service and command writers must refuse the same immutable term, while follow-on Plan 12-11 applies the contract to Django Admin.
Output: Shared guard calls at service boundaries, safe/retired commands, and a service/command mutation writer matrix.

## Artifacts this phase produces
- Archive guard calls in schedule operations, suspensions, modality services, and merge helpers.
- Retired `reset_term` command with no destructive implementation or `DEFAULT_TERM`.
- Explicit active/writable target resolution in `seed_term` and `audit_merge_coverage`.
- `ArchiveFreezeServiceTests`, `ArchiveFreezeCommandTests`, and service/command portions of `ArchivedWriterCouplingTests`.
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-term-lifecycle/12-CONTEXT.md
@.planning/phases/12-term-lifecycle/12-RESEARCH.md
@.planning/phases/12-term-lifecycle/12-PATTERNS.md
@.planning/phases/12-term-lifecycle/12-VALIDATION.md
@.planning/phases/12-term-lifecycle/12-02-SUMMARY.md
@.planning/phases/12-term-lifecycle/12-03-SUMMARY.md
@.planning/phases/12-term-lifecycle/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard reusable schedule, suspension, modality, and merge mutators</name>
  <files>scheduling/schedule_ops.py, scheduling/suspensions.py, scheduling/services.py, scheduling/merge.py, scheduling/tests_term_lifecycle.py, scheduling/tests_suspensions.py, web/tests_schedule_ops.py</files>
  <read_first>
    - scheduling/schedule_ops.py:38-104 (schedule create/update/cancel transactions)
    - scheduling/suspensions.py:37-153 (term default, suspend/lift query/write paths)
    - scheduling/services.py:169-360,492-558 (modality submit/withdraw/approve/reject)
    - scheduling/merge.py:98-191 (status-guarded bulk update helpers)
    - scheduling/term_scope.py (shared writable guard)
    - .planning/phases/12-term-lifecycle/12-RESEARCH.md Pattern 4 writer matrix
  </read_first>
  <action>
    At each public mutator, derive the owning term from the supplied Schedule/Session/request/items, re-fetch/lock it when the operation is transactional, and call `require_writable_term` before any `.save()`, `.update()`, `bulk_create()`, notification, or AuditLog claiming success. Schedule create/edit/cancel and suspension declare/lift must use their explicit term and suspension session queries must include `schedule__term=term`. Modality submit/withdraw/approve/reject must establish one owning term across all items and refuse mixed or archived ownership. Merge propagation must guard the anchor's term before status-guarded bulk updates.

    Preserve existing authorization, status transitions, MSSQL list-before-write/chunk discipline, savepoint semantics, notifications, and audit contracts. Add writer-matrix tests that snapshot every related row/audit count, attempt each service against ARCHIVED, and prove no mutation/notification/audit survives. Add same-date other-term fixtures to suspension tests so explicit term filtering is observable.
  </action>
  <acceptance_criteria>
    - Every named public mutator refuses ARCHIVED before its first domain write.
    - Suspension and modality bulk paths cannot mutate same-date rows from another term.
    - Refused operations leave domain, audit, and notification counts unchanged.
    - Existing ACTIVE/DRAFT-appropriate service behavior and focused suites remain green.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_term_lifecycle.ArchiveFreezeServiceTests scheduling.tests_suspensions web.tests_schedule_ops -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire destructive reset and make retained commands term-safe</name>
  <files>scheduling/management/commands/reset_term.py, scheduling/management/commands/seed_term.py, scheduling/management/commands/audit_merge_coverage.py, scheduling/tests_term_lifecycle.py</files>
  <read_first>
    - scheduling/management/commands/reset_term.py:34-105 (hardcoded target and destructive deletes)
    - scheduling/management/commands/seed_term.py:130-160,250-430 (active lookup and writes)
    - scheduling/management/commands/audit_merge_coverage.py:35-60 (active lookup)
    - scheduling/term_scope.py (active/writable errors)
    - docs/AUDIT-2026-07-19.md A4
  </read_first>
  <action>
    Replace `reset_term.Command.handle` with a permanent non-zero `CommandError` explaining that rollover is close/create/activate and that archival preserves history. Remove `DEFAULT_TERM`, target options, chunk/delete helpers, and all Schedule/Session deletion code from that module; keep the command name only so an old runbook fails loudly instead of becoming an unknown command. Add a source guard proving no delete call or hardcoded term string remains.

    Replace active boolean/first-row discovery in `seed_term` and `audit_merge_coverage` with authoritative active resolution or exact explicit target where the command already accepts one. Before any seed mutation call the writable guard; ARCHIVED returns `CommandError`. Preserve current DEBUG/force safety, ASCII output, term-scoped counts, and bounded MSSQL write patterns.
  </action>
  <acceptance_criteria>
    - Running reset_term always exits non-zero before any database write and points to lifecycle workflow.
    - reset_term has no term-name default and no delete implementation.
    - Retained commands select one authoritative/explicit writable term and never first-match by boolean/date.
    - Archived command tests prove zero related-row and audit changes.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_term_lifecycle.ArchiveFreezeCommandTests -v 2</automated>
  </verify>
  <done>All acceptance criteria are satisfied and the automated verification passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Services/commands -&gt; term-owned ORM rows | Non-HTTP writers can bypass hidden UI and may use bulk operations. |
| Legacy operator command -&gt; official history | Old reset instructions can destroy Schedule/Session records. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | Tampering | archived term-owned services/commands | high | mitigate | Shared guard at every public service/command writer and unchanged-state writer-matrix tests; follow-on Plan 12-11 covers Admin. |
| T-12-07 | Tampering/Data loss | reset_term | critical | mitigate | Retire command; remove all delete/default target code; non-zero source/behavior tests. |
| T-12-03 | Tampering/DoS | bulk suspension/merge/modality writes | high | mitigate | Explicit term predicates, list/chunk discipline, atomic rollback and unchanged-count tests. |
| T-12-02 | Repudiation | privileged command mutation | high | mitigate | Explicit target resolution and shared archive guard; audited lifecycle reopen is required before mutation. |
</threat_model>

<verification>
- Run `py -3.12 manage.py test scheduling.tests_term_lifecycle scheduling.tests_suspensions web.tests_schedule_ops -v 2`.
- Run the retired command and confirm it exits non-zero without row-count changes.
- Run `py -3.12 manage.py check` and the source/coupling inventory for every retained service/command writer.
</verification>

<success_criteria>
Archived history is immutable through reusable services and commands, and the former destructive rollover command can no longer delete it; follow-on Plan 12-11 completes the same freeze in Django Admin.
</success_criteria>

<output>
Create `.planning/phases/12-term-lifecycle/12-05-SUMMARY.md` when done.
</output>
