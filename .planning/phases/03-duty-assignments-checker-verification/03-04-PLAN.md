---
phase: 03-duty-assignments-checker-verification
plan: 04
type: execute
wave: 4
depends_on: ["03-02"]
files_modified:
  - web/checker.py
  - web/urls.py
  - web/views.py
  - templates/checker/floor.html
  - templates/checker/_floor_rows.html
  - verification/tests.py
autonomous: true
requirements: [CHK-07]

must_haves:
  truths:
    - "The floor board shows only F2F/Blended sessions on the checker's active floor(s) for today, with Absent sessions excluded from cards, the priority queue, AND the coverage denominator."
    - "Coverage % = verified / total active (Absent excluded); a fully-verified floor reads 100%."
    - "The priority queue orders active-unverified sessions oldest-scheduled-first (longest-waiting on top)."
    - "The board is htmx-polled on the policy-driven interval (settings.FLUXTRACK_POLICY[poll_interval_seconds]), never a hardcoded number."
  artifacts:
    - templates/checker/floor.html
    - templates/checker/_floor_rows.html
  key_links:
    - "web/checker.floor_rows reuses _active_floor_ids (03-02) so the board and the scan gate see the same on-duty floors."
    - "One shared queryset with .exclude(status=ABSENT) feeds cards, queue, and coverage denominator (Pitfall 5)."
---

<objective>
Build the CHK-07 Checker floor board: a mobile-first, htmx-polled view mirroring the shipped IFO live surface, showing color-coded room cards by verification state, an oldest-unverified-first priority queue, and a coverage-progress indicator — all scoped to the checker's active floor(s) and excluding Absent sessions.

Purpose: A checker on the move needs to see, at a glance, which active rooms are still unverified and which is most overdue. Coverage %, the queue, and the cards must all exclude Absent consistently (research Pitfall 5) or the numbers lie.
Output: `floor_board` + `floor_rows` views in `web/checker.py`, the board templates, the route + home wiring, and the board tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-duty-assignments-checker-verification/03-CONTEXT.md
@.planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md
@.planning/phases/03-duty-assignments-checker-verification/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@web/ifo.py
@web/checker.py
@templates/ifo/live.html
@templates/ifo/_live_rows.html
@scheduling/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FloorBoardTests (RED) — coverage excludes Absent, priority queue oldest-first</name>
  <files>verification/tests.py</files>
  <read_first>
    - the `_CheckerFixtureMixin` added in 03-02 (reuse its Checker/Assignment/Session helpers)
    - scheduling/models.py (SessionStatus, Session.verified_by_checker property, Modality)
    - .planning/phases/03-duty-assignments-checker-verification/03-VALIDATION.md (CHK-07 FloorBoardTests rows)
    - .planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md § Pattern 4 + Pitfall 5
  </read_first>
  <behavior>
    - test_coverage_excludes_absent: with N active sessions on the floor (some verified, one ABSENT), coverage % = verified / (active-excluding-absent); the ABSENT session is absent from the card list and NOT in the denominator; a fully-verified floor reads 100.
    - test_priority_queue_oldest_first: among active-unverified sessions, the returned queue is ordered by scheduled_start ascending (oldest first).
    - test_board_excludes_online: an ONLINE session on the floor's rooms does not appear on the F2F/Blended board.
    - test_board_scoped_to_active_floor: a session on a floor the checker is NOT assigned to does not appear.
  </behavior>
  <action>
    Add `FloorBoardTests(_CheckerFixtureMixin, TestCase)` to `verification/tests.py` implementing the behavior block. Log in the checker (`force_login`) and GET the floor-rows route (named in web/urls.py); assert on the rendered context/markup (coverage number, ordered session list, exclusion of ABSENT/online/off-floor sessions). Build sessions with mixed statuses (active verified via a CheckerValidation(action=verified), active unverified, absent) and mixed modalities. These GET a route that doesn't exist yet -> RED until Task 2.
  </action>
  <acceptance_criteria>
    - `verification/tests.py` defines `FloorBoardTests` with `test_coverage_excludes_absent`, `test_priority_queue_oldest_first`, `test_board_excludes_online`, `test_board_scoped_to_active_floor` (grep each).
    - Before Task 2: `py -3.12 manage.py test verification.tests.FloorBoardTests` fails RED (route/view missing).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 -c "t=open('verification/tests.py').read(); assert 'class FloorBoardTests' in t and 'test_coverage_excludes_absent' in t and 'test_priority_queue_oldest_first' in t, 'board tests incomplete'; print('board-tests-scaffolded')"</automated>
  </verify>
  <done>The coverage/queue/exclusion behaviors are encoded as RED tests wired to the yet-to-build board view.</done>
</task>

<task type="auto">
  <name>Task 2: floor_board + floor_rows views and board templates (color cards, coverage, queue)</name>
  <files>web/checker.py, web/urls.py, web/views.py, templates/checker/floor.html, templates/checker/_floor_rows.html</files>
  <read_first>
    - web/ifo.py lines 61-70 (`live` / `live_rows` — the exact poll-view pair to mirror; poll_ms from settings.FLUXTRACK_POLICY)
    - templates/ifo/live.html + templates/ifo/_live_rows.html (full-page shell polling a rows partial; Franken UI uk-* status labels)
    - web/checker.py (`checker_required`, `_active_floor_ids` from 03-02 — reuse, do not re-query differently)
    - scheduling/models.py (SessionStatus, Session.verified_by_checker, Modality)
    - .planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md § Pattern 4 (floor_rows example) + Pitfall 5
  </read_first>
  <action>
    In `web/checker.py` add `@checker_required floor_board(request)` returning `templates/checker/floor.html` with `{"poll_ms": settings.FLUXTRACK_POLICY["poll_interval_seconds"] * 1000}` (mirror `ifo.live`; NEVER hardcode the interval). Add `@checker_required floor_rows(request)`: compute `floor_ids = _active_floor_ids(request.user, timezone.now())`; build ONE shared queryset `active = Session.objects.filter(room__floor_id__in=floor_ids, date=timezone.localdate()).exclude(status=SessionStatus.ABSENT).select_related("room","schedule","faculty").order_by("scheduled_start")` and further exclude effective-online sessions (F2F/Blended board only — filter out `declared_modality="online"` and `schedule__modality="online"` with declared taking precedence; do this in Python over the materialized list to honor declared-overrides-schedule, or via a combined query that respects it). From that one list compute `total`, `verified = sum(1 for s in active if s.verified_by_checker)`, `coverage = round(100*verified/total) if total else 100`, and the priority queue = the active-unverified subset already ordered oldest-first. Assign each card a color-state token (grey no-active / amber active-unverified / green verified / red flagged / blue verified-empty) computed server-side. Render `templates/checker/_floor_rows.html` with sessions + coverage + verified + total + queue. Create `templates/checker/floor.html` (mirror ifo/live.html: a `#floor-rows` div with `hx-get="/checker/floor/rows" hx-trigger="load, every {{ poll_ms }}ms" hx-swap="innerHTML"`, mobile-first max-w) and `templates/checker/_floor_rows.html` (coverage bar `verified/total`, the priority queue, color-coded room cards with `data-state` hooks; Franken UI uk-* classes; exact colors deferred to /gsd-ui-phase 3). Wire `path("checker/floor", checker.floor_board, name="checker_floor")` and `path("checker/floor/rows", checker.floor_rows, name="checker_floor_rows")` in `web/urls.py`. Point `SURFACES[Role.CHECKER]` "Floor view" href to `/checker/floor` in `web/views.py`.
  </action>
  <acceptance_criteria>
    - `py -3.12 manage.py test verification.tests.FloorBoardTests` exits 0 (coverage excludes Absent; queue oldest-first; online + off-floor excluded).
    - Poll interval is policy-driven, not hardcoded: `py -3.12 -c "s=open('web/checker.py').read(); assert 'poll_interval_seconds' in s; print('ok')"` prints ok, and grep finds no literal `every 8000ms` / hardcoded ms in templates/checker/floor.html.
    - One shared exclude(ABSENT) feeds cards+queue+coverage: grep `exclude(status=SessionStatus.ABSENT)` (or equivalent) appears in floor_rows and coverage is computed off the same list.
    - `web/urls.py` exposes `checker_floor` and `checker_floor_rows`; `web/views.py` SURFACES[Role.CHECKER] "Floor view" href == `/checker/floor`.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test verification.tests.FloorBoardTests</automated>
  </verify>
  <done>A checker sees an htmx-polled floor board of their active-floor F2F/Blended rooms: color-coded cards, an oldest-unverified-first priority queue, and a coverage % that excludes Absent and reads 100% when fully verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker browser → floor_rows poll endpoint | Repeated GETs; must expose only the checker's active-floor sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Information disclosure | floor board leaking other floors' sessions | medium | mitigate | `floor_rows` scopes strictly to `_active_floor_ids(request.user, now)` server-side; a checker never selects the floor from the client |
| T-03-13 | Elevation of Privilege | non-checker viewing the board | medium | mitigate | `checker_required` on both board views (superuser bypass only) |
| T-03-14 | Denial of Service | poll storm from a tight interval | low | mitigate | Interval sourced from `settings.FLUXTRACK_POLICY[poll_interval_seconds]` (single tunable), matching the shipped IFO live surface |
| T-03-SC | Tampering | package installs | low | accept | No new packages this phase (03-RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `py -3.12 manage.py test verification` green (adds FloorBoardTests).
- `py -3.12 manage.py test verification scheduling web` green.
- Manual (deferred to /gsd-ui-phase 3): card colors, coverage bar, one-handed mobile layout.
</verification>

<success_criteria>
- Board scoped to active floor(s), F2F/Blended only, Absent excluded from cards/queue/coverage.
- Coverage % = verified/total active; priority queue oldest-unverified-first; htmx-polled on the policy interval.
</success_criteria>

<output>
Create `.planning/phases/03-duty-assignments-checker-verification/03-04-SUMMARY.md` when done.
</output>
