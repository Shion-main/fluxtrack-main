---
phase: 03-duty-assignments-checker-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - verification/resolver.py
  - verification/models.py
  - verification/migrations/0002_assignment_scope.py
  - verification/migrations/0003_retire_dead_validation_actions.py
  - scheduling/migrations/0002_session_online_checker.py
  - scheduling/models.py
  - verification/tests.py
autonomous: true
requirements: [IFO-06, CHK-01, CHK-03]

must_haves:
  truths:
    - "The pure checker gating core refuses an off-duty scan (OFF_DUTY) and a wrong-floor scan (WRONG_FLOOR) with distinct outcomes, no ORM, no timezone.now()."
    - "distribute_online_sessions returns a deterministic round-robin map and {} when there are no online-duty checkers."
    - "Assignment carries an AssignmentScope (FLOOR default / ONLINE); Session carries a nullable online_checker FK."
    - "ValidationAction no longer contains confirmed_absent or confirmed_empty; verified_empty remains the canonical empty action."
  artifacts:
    - verification/resolver.py
    - verification/migrations/0002_assignment_scope.py
    - verification/migrations/0003_retire_dead_validation_actions.py
    - scheduling/migrations/0002_session_online_checker.py
  key_links:
    - "resolve_checker_scan + distribute_online_sessions are pure (SimpleTestCase, no DB) — the same seam web/checker.py scan AND the offline replay endpoint reuse (coupling guarantee)."
    - "AssignmentScope.ONLINE gates online duty; Session.online_checker is the one-owner link the round-robin apply (03) and Phase-4 modality-shift will write."
---

<objective>
Lay the data-model and pure-decision foundation for the entire Checker verification surface: the ORM-free gating core, the round-robin distribution core, the AssignmentScope extension, the Session.online_checker owner link, and the retirement of the dead ValidationAction members. Everything in plans 02-06 builds on this.

Purpose: A pure, unit-tested decision core (mirroring `scheduling/resolver.py`) is what lets the live checker scan and the offline replay reach provably identical decisions — the same coupling guarantee Phase 2 enforced between scan and sweep. The model extension is the minimal additive surface the online-duty and per-session-owner features need (research-recommended over new models).
Output: `verification/resolver.py` (pure), the model changes on `Assignment`/`Session`, three additive/state-only migrations, and the pure-core Nyquist tests (green at plan end).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-duty-assignments-checker-verification/03-CONTEXT.md
@.planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md
@.planning/phases/03-duty-assignments-checker-verification/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@scheduling/resolver.py
@verification/models.py
@scheduling/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure checker decision cores (resolve_checker_scan + distribute_online_sessions)</name>
  <files>verification/resolver.py, verification/tests.py</files>
  <read_first>
    - scheduling/resolver.py (the pure-core template: module constants, dataclass with __post_init__, no ORM, `now` injected — mirror this exactly)
    - scheduling/tests.py lines 1-40 (SimpleTestCase pure-test style, FakeSession dataclass, resolve() helper)
    - .planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md § Pattern 1 and Pattern 3
    - .planning/codebase/CONVENTIONS.md § "1. Pure resolver" and "Constants"
  </read_first>
  <behavior>
    resolve_checker_scan(active_floor_ids, scanned_floor_id, session_state, now):
    - empty active_floor_ids -> OFF_DUTY (actionable False)
    - scanned_floor_id not in active_floor_ids -> WRONG_FLOOR (actionable False)
    - session_state is None OR status == "scheduled" -> NO_SESSION (actionable True; room empty -> Verified empty)
    - status == "absent" -> ABSENT_EXCLUDED (actionable False)
    - session_state.verified is True -> ALREADY_VERIFIED (actionable False)
    - otherwise (active, unverified) -> ACTIVE_UNVERIFIED (actionable True), carries session_state.id
    distribute_online_sessions(session_ids, checker_ids):
    - empty checker_ids -> {} (caller flags unassigned)
    - even split: {session_id: checker_ids[i % len(checker_ids)]} deterministic by input order
  </behavior>
  <action>
    Create `verification/resolver.py` with a module docstring citing CHK-01 and IFO-06 and the pure-core rule. Define UPPER_SNAKE module constants for the outcomes: OFF_DUTY = "off-duty", WRONG_FLOOR = "wrong-floor", NO_SESSION = "no-session", ACTIVE_UNVERIFIED = "active-unverified", ALREADY_VERIFIED = "already-verified", ABSENT_EXCLUDED = "absent-excluded", and an ACTIONABLE set = {ACTIVE_UNVERIFIED, NO_SESSION}. Define a frozen-style `CheckerResolution` dataclass with fields `outcome: str`, `session_id: int | None = None`, and a computed `actionable: bool` set in `__post_init__` (membership in ACTIONABLE) — mirror the `Resolution.__post_init__` pattern in scheduling/resolver.py. Implement `resolve_checker_scan(active_floor_ids, scanned_floor_id, session_state, now)` exactly per the behavior block: `session_state` is a small value object exposing `.id`, `.status` (lowercase status string), and `.verified` (bool), or None. NO ORM, NO `timezone.now()` inside, NO writes — `now` is a passed argument reserved for future grace use. Implement `distribute_online_sessions(session_ids, checker_ids)` per the behavior block (deterministic round-robin; empty checkers -> {}). In `verification/tests.py`, replace the placeholder stub with pure `SimpleTestCase` classes: `CheckerResolverTests` (methods including `test_off_duty_refused`, `test_wrong_floor_refused`, `test_no_session_is_actionable_empty`, `test_absent_is_excluded`, `test_already_verified`, `test_active_unverified_is_actionable`) and `DistributeTests` (`test_round_robin_even_split`, `test_empty_checkers_returns_empty`). Import the resolver aliased `from verification import resolver as R` per convention. Use a tiny `types.SimpleNamespace` or a local dataclass as the `session_state` test double.
  </action>
  <acceptance_criteria>
    - `py -3.12 manage.py test verification.tests.CheckerResolverTests verification.tests.DistributeTests` exits 0.
    - resolve_checker_scan and distribute_online_sessions perform NO database access: the two test classes are `SimpleTestCase` (would error on any ORM/`timezone.now()` call inside the core).
    - `py -3.12 -c "import ast,sys; src=open('verification/resolver.py').read(); assert 'timezone.now()' not in src and 'objects.' not in src, 'core must stay pure'; print('pure-ok')"` prints `pure-ok`.
    - CheckerResolution exposes `actionable` computed in __post_init__ (grep: `def __post_init__` present in verification/resolver.py).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test verification.tests.CheckerResolverTests verification.tests.DistributeTests</automated>
  </verify>
  <done>Off-duty and wrong-floor scans resolve to distinct refused outcomes; the round-robin distributor is deterministic and safe on an empty checker roster; both cores are pure (SimpleTestCase-verified).</done>
</task>

<task type="auto">
  <name>Task 2: Model extension + additive/state-only migrations (AssignmentScope, Session.online_checker, retire dead actions)</name>
  <files>verification/models.py, scheduling/models.py, verification/migrations/0002_assignment_scope.py, verification/migrations/0003_retire_dead_validation_actions.py, scheduling/migrations/0002_session_online_checker.py, verification/tests.py</files>
  <read_first>
    - verification/models.py (Assignment, ValidationAction — extend, do not recreate)
    - verification/migrations/0001_initial.py (existing field shapes; the dead choices to retire)
    - scheduling/models.py lines 84-125 (Session — where online_checker FK is added)
    - .planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md § Runtime State Inventory + § "Enum retirement migration (state-only on MSSQL)" + Open Question 1 (VERIFIED_EMPTY is canonical)
    - .planning/STATE.md Phase-01 decisions (MSSQL: AlterField choices emits no DDL; nullable FK add is proven)
  </read_first>
  <action>
    In `verification/models.py`: add an `AssignmentScope(models.TextChoices)` with members `FLOOR = "floor", "Floor"` and `ONLINE = "online", "Online"`, then add `scope = models.CharField(max_length=10, choices=AssignmentScope.choices, default=AssignmentScope.FLOOR)` to `Assignment` (comment: IFO-06 online-duty extension; FLOOR gates room scans, ONLINE gates online verification). In the same file, retire the two dead `ValidationAction` members so the remaining members are exactly: VERIFIED, FLAG_IDENTITY_MISMATCH, FLAG_NOT_PRESENT, VERIFIED_EMPTY (remove the "Confirmed absent" member and the "Confirmed empty" member — VERIFIED_EMPTY stays canonical per research Open Q1; Absent is final via the sweep). In `scheduling/models.py`: add `online_checker = models.ForeignKey(settings.AUTH_USER_MODEL, null=True, blank=True, on_delete=models.SET_NULL, related_name="online_verifications")` to `Session` (comment: CHK-02/IFO-06 one online-duty owner per online session; round-robin-assigned in 03-03, Phase-4 modality-shift also sets it). Generate migrations with `py -3.12 manage.py makemigrations verification scheduling`; expect `verification/migrations/0002_assignment_scope.py` (AddField scope), `verification/migrations/0003_...` (AlterField ValidationAction choices — rename the generated file to `0003_retire_dead_validation_actions.py` and keep its dependency on 0002), and `scheduling/migrations/0002_session_online_checker.py` (AddField online_checker). To the ValidationAction AlterField migration, hand-add a forward `RunPython` that asserts no stray rows use the retired values (see 03-RESEARCH § "Enum retirement migration"), reading the model via `apps.get_model("verification", "CheckerValidation")` and a no-op reverse. Keep migration code ASCII-only. Add a `ModelExtensionTests(SimpleTestCase)` to `verification/tests.py` with `test_confirmed_absent_not_in_choices` (asserts neither "confirmed_absent" nor "confirmed_empty" is in `ValidationAction.values`, and "verified_empty" IS) and `test_assignment_scope_defaults_floor` (asserts AssignmentScope has FLOOR + ONLINE and the field default is FLOOR).
  </action>
  <acceptance_criteria>
    - `py -3.12 manage.py makemigrations --check --dry-run` reports no pending changes (the model edits are fully captured by the written migrations).
    - `py -3.12 manage.py migrate` applies clean on SQL Server (0002/0003 verification + 0002 scheduling), no errors.
    - `py -3.12 manage.py test verification.tests.ModelExtensionTests` exits 0 (retirement + scope default asserted).
    - The three migration files exist at the exact paths in <files> and the ValidationAction AlterField migration depends on 0002_assignment_scope.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py makemigrations --check --dry-run && py -3.12 manage.py test verification.tests.ModelExtensionTests</automated>
  </verify>
  <done>Assignment has a FLOOR/ONLINE scope (default FLOOR), Session has a nullable online_checker owner FK, the two dead validation actions are retired with a defensive data-assert, and all migrations apply cleanly on MSSQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | Schema/state change applied to SQL Server; a bad AlterField could silently drop data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | ValidationAction retirement migration | medium | mitigate | State-only AlterField (no DDL on MSSQL) + forward RunPython asserting zero rows use `confirmed_absent`/`confirmed_empty` before the choice is removed; reverse is a no-op |
| T-03-02 | Denial of Service | Session.online_checker / Assignment.scope AddField | low | mitigate | Both additive nullable/defaulted columns (proven MSSQL pattern from Phase-1 azure_oid + JOB-02c); `migrate` runs in CI test DB before prod |
| T-03-SC | Tampering | package installs | low | accept | No new packages this phase (03-RESEARCH § Package Legitimacy Audit — none); nothing to vet |
</threat_model>

<verification>
- `py -3.12 manage.py test verification` is green (pure cores + model tests).
- `py -3.12 manage.py migrate` applies the three new migrations on the MSSQL test/dev DB with no error.
- `py -3.12 manage.py makemigrations --check --dry-run` shows the models and migrations are in sync.
</verification>

<success_criteria>
- verification/resolver.py exists, is pure, and its SimpleTestCase suites pass.
- Assignment.scope (FLOOR/ONLINE, default FLOOR) and Session.online_checker (nullable FK) exist and migrate cleanly.
- ValidationAction contains exactly {verified, flag_identity_mismatch, flag_not_present, verified_empty}.
</success_criteria>

<output>
Create `.planning/phases/03-duty-assignments-checker-verification/03-01-SUMMARY.md` when done.
</output>
