---
phase: 03-duty-assignments-checker-verification
plan: 05
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - web/checker.py
  - web/urls.py
  - web/views.py
  - templates/checker/online_list.html
  - templates/checker/_online_list.html
  - scheduling/jobs.py
  - scheduling/tests.py
  - verification/tests.py
autonomous: true
requirements: [CHK-02, CHK-03]

must_haves:
  truths:
    - "An online-duty checker sees only the online sessions assigned to them (Session.online_checker == request.user) and opens each via its public teams_link (no room state)."
    - "An online Verify sets the session status=ACTIVE + actual_start + checkin_method=ONLINE_MANUAL (the online analog of a faculty check-in) AND records a CheckerValidation(verified)."
    - "An online Flag-not-present drives the session to ABSENT; an F2F Flag-not-present records the flag only (no silent status override)."
    - "The sweep's online exclusion is removed: an un-verified online no-show is marked ABSENT under the shared grace predicate, while a verified (ACTIVE) online session is skipped."
  artifacts:
    - templates/checker/online_list.html
    - templates/checker/_online_list.html
  key_links:
    - "The online Verify-activates logic (web/checker._apply_action online branch) and the sweep online-exclusion removal (scheduling/jobs.py) ship in THIS plan together — decoupling them would mark every online session Absent."
    - "The two scheduling.SweepTests online-exclusion cases are REWRITTEN to inclusion semantics (intentional change, not a regression) + a verified-online-skipped case is added."
    - "teams_link empty -> surface 'no link' + notify(IFO) rather than a dead redirect."
---

<objective>
Ship the online verification path AND remove the JOB-02 sweep's online exclusion in one atomic change. An online-duty checker opens each assigned online session's public MS Teams link, then records Verify (which activates the session, the online analog of a room check-in) or Flag not-present (which drives it Absent). Only because a genuine online attendee is now ACTIVE can the sweep safely include online sessions — so the exclusion guard is removed and its two tests are rewritten in lockstep.

Purpose: This is ROADMAP criterion #6 and the milestone's highest-risk coupling. If the sweep exclusion were removed without the Verify-activates path, every online session would be marked Absent the moment grace passes.
Output: online-to-verify list + open/verify/flag endpoints in `web/checker.py`, the online templates, the sweep exclusion removal in `scheduling/jobs.py`, and the rewritten/added sweep + online tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-duty-assignments-checker-verification/03-CONTEXT.md
@.planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md
@.planning/phases/03-duty-assignments-checker-verification/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@scheduling/jobs.py
@scheduling/resolver.py
@scheduling/models.py
@web/checker.py
@web/scan.py
@ops/notify.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the two sweep online-exclusion tests + add online Verify/redirect/verified-skipped tests (RED)</name>
  <files>scheduling/tests.py, verification/tests.py</files>
  <read_first>
    - scheduling/tests.py lines 347-446 (`SweepTests` — the two cases to REWRITE and the `_JobFixtureMixin` `_session` helper; note `declared_modality` + `schedule_modality` params)
    - scheduling/jobs.py lines 35-76 (sweep_no_shows + the online `continue` guard being removed)
    - the `_CheckerFixtureMixin` from 03-02 (for the online verify/redirect DB tests)
    - scheduling/models.py (SessionStatus, CheckinMethod.ONLINE_MANUAL, Session.online_checker, teams_link, Modality)
    - .planning/phases/03-duty-assignments-checker-verification/03-VALIDATION.md (ROADMAP #6 rows + CheckerScanDBTests online rows) + 03-RESEARCH Pitfall 1
  </read_first>
  <behavior>
    - REWRITE scheduling.tests.SweepTests.test_online_no_show_stays_scheduled_declared -> assert an un-verified online (declared_modality=online) no-show past grace is now marked ABSENT (inclusion). Rename to reflect inclusion (e.g. test_online_no_show_declared_now_absent) OR keep the name and flip the assertion — but the assertion MUST be ABSENT.
    - REWRITE scheduling.tests.SweepTests.test_online_no_show_stays_scheduled_via_schedule -> same, for schedule.modality=online -> ABSENT.
    - ADD scheduling.tests.SweepTests.test_verified_online_not_marked_absent: an online session already ACTIVE (verified) is NOT touched by the sweep.
    - ADD verification.tests.CheckerScanDBTests.test_online_verify_activates_session: an online Verify sets status=ACTIVE, actual_start set, checkin_method=online_manual, and writes CheckerValidation(verified).
    - ADD verification.tests.CheckerScanDBTests.test_online_scan_redirects_to_teams: opening an assigned online session routes to its teams_link (no room-state card); empty teams_link surfaces a "no link" + IFO flag path.
    - ADD verification.tests.CheckerScanDBTests.test_online_flag_not_present_absent: an online Flag-not-present sets status=ABSENT.
  </behavior>
  <action>
    In `scheduling/tests.py` REWRITE the two named online-exclusion `SweepTests` methods to assert the new inclusion semantics (un-verified online no-show past grace -> `SessionStatus.ABSENT`) and ADD `test_verified_online_not_marked_absent` (create an online session with `status="active"`; sweep; assert it stays ACTIVE). Add a comment on each rewritten test citing ROADMAP #6 + this plan: the online exclusion is intentionally removed, so these are behavior changes, not regressions. In `verification/tests.py` APPEND to `CheckerScanDBTests` the four online methods in the behavior block, using `_CheckerFixtureMixin` helpers to create an online-duty Checker + an online Session with `online_checker` set to that checker and a `teams_link`. Drive the online-list/open/verify/flag routes named in web/urls.py via the test Client. These import the not-yet-built online views + depend on the not-yet-removed sweep guard, so they are RED.
  </action>
  <acceptance_criteria>
    - `scheduling/tests.py` no longer asserts online no-shows stay SCHEDULED: `py -3.12 -c "t=open('scheduling/tests.py').read(); assert 'stays_scheduled' not in t or 'ABSENT' in t; assert 'test_verified_online_not_marked_absent' in t; print('sweep-tests-rewritten')"` prints the marker.
    - `verification/tests.py` CheckerScanDBTests has `test_online_verify_activates_session`, `test_online_scan_redirects_to_teams`, `test_online_flag_not_present_absent` (grep each).
    - Before Task 2/3: `py -3.12 manage.py test scheduling.tests.SweepTests verification.tests.CheckerScanDBTests` fails RED (guard still present / online views missing).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 -c "s=open('scheduling/tests.py').read(); v=open('verification/tests.py').read(); assert 'test_verified_online_not_marked_absent' in s and 'test_online_verify_activates_session' in v and 'test_online_flag_not_present_absent' in v, 'online tests incomplete'; print('online-tests-scaffolded')"</automated>
  </verify>
  <done>The sweep online-exclusion tests are rewritten to inclusion semantics and the online Verify/redirect/flag behaviors are encoded as RED tests — the contract for the coupled change in Tasks 2-3.</done>
</task>

<task type="auto">
  <name>Task 2: Online-to-verify list + Teams-link open + online branch of _apply_action</name>
  <files>web/checker.py, web/urls.py, web/views.py, templates/checker/online_list.html, templates/checker/_online_list.html</files>
  <read_first>
    - web/checker.py (`checker_required`, `_apply_action`, resolve/action from 03-02 — extend `_apply_action` here; do not fork it)
    - scheduling/models.py (Session.online_checker, teams_link, SessionStatus.ACTIVE, CheckinMethod.ONLINE_MANUAL, effective modality = declared_modality or schedule.modality)
    - verification/models.py (ValidationAction.VERIFIED / FLAG_NOT_PRESENT)
    - ops/notify.py (notify — for empty teams_link IFO flag) ; ops/policy.py (get_policy)
    - .planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md § Pattern 2 (online status flip) + Pitfall 2 (F2F vs online Verify asymmetry)
  </read_first>
  <action>
    In `web/checker.py` add `@checker_required online_list(request)`: query `Session.objects.filter(online_checker=request.user, date=timezone.localdate())` that are effective-online and not yet verified/completed; render `templates/checker/online_list.html`. Add `@checker_required online_open(request, session_id)`: fetch the assigned online session (404 if not owned by request.user); if `teams_link` is non-empty, route the checker to it (render a page/partial with the link + the same Verify / Flag-not-present action controls, no room-state card); if `teams_link` is empty, render a "no Teams link" state and `notify(role=Role.IFO_ADMIN, type="online_no_link", ...)` so IFO can fix it. EXTEND `_apply_action` (the existing 03-02 function) with an online branch: when `action == ValidationAction.VERIFIED` AND the session is effective-online, additionally set `session.status = SessionStatus.ACTIVE`, `session.actual_start = scanned_at or timezone.now()`, `session.checkin_method = CheckinMethod.ONLINE_MANUAL`, `session.save(update_fields=["status","actual_start","checkin_method"])` (the online analog of a faculty check-in — Pitfall 2); when `action == ValidationAction.FLAG_NOT_PRESENT` AND effective-online, set `session.status = SessionStatus.ABSENT` (`save(update_fields=["status"])`) — authoritative; F2F Flag-not-present keeps the existing record-flag-only behavior (NO status override). Reuse the existing flag notify(IFO+HR) path. Route the online actions through the same `action` endpoint from 03-02 (it already writes CheckerValidation + AuditLog + note-validation). Create `templates/checker/online_list.html` (the per-checker "Online to verify" list, each item linking to `online_open`) and `templates/checker/_online_list.html` (swappable partial). Wire `web/urls.py`: `path("checker/online", checker.online_list, name="checker_online")`, `path("checker/online/<int:session_id>", checker.online_open, name="checker_online_open")`. Add an "Online to verify" card to `SURFACES[Role.CHECKER]` in `web/views.py` (href `/checker/online`).
  </action>
  <acceptance_criteria>
    - `py -3.12 manage.py test verification.tests.CheckerScanDBTests` exits 0 (online verify activates; redirect-to-teams; online flag -> absent; all F2F cases still green).
    - Online Verify flips status: grep `CheckinMethod.ONLINE_MANUAL` and `SessionStatus.ACTIVE` both present in web/checker.py inside the Verify branch.
    - Online ownership enforced: `online_open` filters `online_checker=request.user` (grep `online_checker` present in web/checker.py).
    - `web/urls.py` exposes `checker_online` + `checker_online_open`; `web/views.py` SURFACES[Role.CHECKER] includes `/checker/online`.
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test verification.tests.CheckerScanDBTests</automated>
  </verify>
  <done>An online-duty checker works only their assigned online sessions via the Teams link; Verify activates the session (ACTIVE + ONLINE_MANUAL), Flag-not-present marks it Absent, and a missing Teams link flags IFO.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the sweep online exclusion so online joins JOB-02 (coupled with Task 2)</name>
  <files>scheduling/jobs.py</files>
  <read_first>
    - scheduling/jobs.py lines 35-76 (sweep_no_shows — the `effective_modality == Modality.ONLINE: continue` guard at ~lines 54-61 and the surrounding materialize-first HY010 guard + shared predicate re-affirm)
    - scheduling/resolver.py line 39 (is_no_show_past_grace — the shared predicate online now also flows through)
    - the rewritten scheduling.tests.SweepTests from Task 1 (the contract this change must satisfy)
    - .planning/phases/03-duty-assignments-checker-verification/03-RESEARCH.md Pitfall 1 (why this MUST ship with Task 2)
  </read_first>
  <action>
    In `scheduling/jobs.py`, REMOVE the online-exclusion branch inside `sweep_no_shows` (the `effective_modality = s.declared_modality or s.schedule.modality` / `if effective_modality == Modality.ONLINE: continue` guard) so online sessions are no longer skipped — a still-SCHEDULED online no-show past grace now falls to ABSENT under the SAME `is_no_show_past_grace` predicate. Keep the `list(...)` materialize-first HY010 guard, the shared-predicate re-affirm, the SCHEDULED->ABSENT idempotency guard, the per-absence AuditLog(by=sweep), and the never-stamp-room_released_at guarantee all intact. Update the function/module docstring: replace the "Online sessions are EXCLUDED pending the Phase-3 verify path" wording with a note that online is now included (the Phase-3 online Verify path, shipped in this plan, sets a genuine attendee to ACTIVE so the sweep skips it; only un-verified online no-shows fall to Absent). If `Modality` becomes an unused import after the removal, drop it to keep imports clean.
  </action>
  <acceptance_criteria>
    - The online exclusion is gone (the shared-predicate `continue` legitimately remains): `py -3.12 -c "s=open('scheduling/jobs.py').read(); body=s.split('def detect_room_conflicts')[0]; assert 'Modality.ONLINE' not in body, 'online exclusion still present in sweep_no_shows'; print('exclusion-removed')"` prints `exclusion-removed`.
    - `py -3.12 manage.py test scheduling.tests.SweepTests` exits 0 (rewritten online-inclusion cases + verified-online-skipped + all existing F2F/backfill/idempotency/audit/no-room-release cases green).
    - Coupling proven together: `py -3.12 manage.py test scheduling verification` exits 0 (online Verify-activates from Task 2 + sweep-inclusion from this task pass in the same run).
  </acceptance_criteria>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.SweepTests verification.tests.CheckerScanDBTests</automated>
  </verify>
  <done>The sweep now marks un-verified online no-shows Absent under the shared grace predicate while skipping verified (ACTIVE) online sessions; the online exclusion is gone and every other sweep guarantee is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checker → online session status | An online Verify moves a session out of SCHEDULED — the only non-faculty write that does so |
| sweep → online sessions | The sweep now marks online no-shows Absent; a wrong status decision mislabels attendance |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Tampering | premature Absent for genuine online attendees | high | mitigate | Verify-activates (status=ACTIVE) ships in the SAME plan as the exclusion removal (Task 2 + Task 3); rewritten SweepTests + verified-online-skipped test gate the coupled change |
| T-03-16 | Spoofing | a checker verifying an online session not assigned to them | high | mitigate | `online_open`/actions filter `online_checker == request.user` (404 otherwise); one accountable owner per session (03-03) |
| T-03-17 | Repudiation | silent status override on F2F Flag-not-present | medium | mitigate | Online Flag-not-present sets ABSENT authoritatively; F2F Flag-not-present records the flag + notify only (no silent override) — explicit, audited, per research Open Q2 |
| T-03-18 | Denial of Service | HY010 when the sweep now writes more rows (online included) | medium | mitigate | The materialize-first `list(...)` guard is retained; no `.iterator()` introduced |
| T-03-SC | Tampering | package installs | low | accept | No new packages this phase (03-RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `py -3.12 manage.py test scheduling verification` green (the coupled change proven in one run).
- `py -3.12 manage.py test` (full suite) green before phase gate — the two rewritten sweep cases are intentional changes, not regressions.
- Manual (deferred): open a real public Teams link + human identity match against the faculty photo.
</verification>

<success_criteria>
- Online-duty checker verifies only assigned online sessions via the Teams link; Verify -> ACTIVE + ONLINE_MANUAL, Flag-not-present -> ABSENT.
- Sweep includes online: un-verified online no-show -> ABSENT; verified online skipped; all other sweep guarantees intact.
</success_criteria>

<output>
Create `.planning/phases/03-duty-assignments-checker-verification/03-05-SUMMARY.md` when done.
</output>
