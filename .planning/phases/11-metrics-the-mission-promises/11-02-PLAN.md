---
phase: 11-metrics-the-mission-promises
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - scheduling/report_render.py
  - web/hr.py
  - templates/reports/scorecard.html
  - scheduling/tests_report_render.py
  - web/tests_hr.py
autonomous: true
requirements: [A3]
must_haves:
  truths:
    - "report_render.HEADER carries the two lateness columns and build_csv/build_pdf emit them from FacultyRow.minutes_late_avg / chronic_late for every row (D-03) — named test test_header_has_lateness / test_csv_row_has_lateness / test_pdf_has_lateness_header."
    - "web.hr.CSV_HEADER gains a derived Minutes-late column AND the raw Actual-start cell is still present (D-03 add, don't remove) — named test test_hr_csv_keeps_actual_start_and_adds_lateness."
    - "The HR per-session Minutes-late cell is computed via the shared session_minutes_late helper (no re-derivation), so a session late by N minutes shows N and an ABSENT/early session shows 0 — named test test_hr_late_session_shows_minutes / test_hr_absent_zero_minutes."
    - "The faculty scorecard surface renders avg-minutes-late alongside the chronic-late flag, and the chronic flag is shown only when there are >= 5 held sessions in range (D-02 floor) — named test test_scorecard_page_shows_lateness."
  artifacts:
    - "scheduling/report_render.py::HEADER extended + build_csv/build_pdf lateness cells"
    - "web/hr.py::CSV_HEADER extended + attendance_csv derived minutes-late cell"
    - "templates/reports/scorecard.html lateness card"
  key_links:
    - "FacultyRow.minutes_late_avg / chronic_late (Plan 01) -> report_render.HEADER/build_csv/build_pdf -> weekly report + Dean/IFO scorecard CSV"
    - "session_minutes_late (Plan 01) -> web.hr.attendance_csv per-session derived cell"
    - "faculty_scorecard.minutes_late_avg / chronic_late (Plan 01) -> reports/scorecard.html"
---

<objective>
Surface the Plan-01 lateness aggregates in the three places A3/D-03 name: the weekly
consolidated report (via the shared render layer), the HR payroll CSV (a derived per-session
minutes-late column), and the faculty scorecard page.

Purpose: A3's surface half. The weekly report and scorecard/Dean CSV inherit lateness for free
once `report_render.HEADER` and the two byte-builders gain columns; HR is session-grain and
computes its cell inline via the SAME `session_minutes_late` helper so it cannot drift from the
faculty aggregate (Pitfall 5 — two distinct CSV header contracts).
Output: extended HEADER + byte builders, extended HR CSV header + row, scorecard lateness card,
and tests for each. No schema change.

## Artifacts this plan produces
- `scheduling/report_render.py`: `HEADER` gains "Avg min late" + "Chronic late"; `build_csv` and `build_pdf` emit the two cells.
- `web/hr.py`: `CSV_HEADER` gains "Minutes late"; `attendance_csv` row generator inserts the derived cell after the raw `Actual start`.
- `templates/reports/scorecard.html`: a lateness card (avg minutes late + chronic flag).
</objective>

<execution_context>
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/joshu_mnu8z3u/Downloads/fluxtrack-main/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-metrics-the-mission-promises/11-CONTEXT.md
@.planning/phases/11-metrics-the-mission-promises/11-RESEARCH.md
@.planning/phases/11-metrics-the-mission-promises/11-PATTERNS.md
@.planning/phases/11-metrics-the-mission-promises/11-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lateness columns to the weekly-report render layer</name>
  <files>scheduling/report_render.py, scheduling/tests_report_render.py</files>
  <behavior>
    - test_header_has_lateness: HEADER contains "Avg min late" and "Chronic late" (8 columns total).
    - test_csv_row_has_lateness: build_csv over a FacultyRow with minutes_late_avg=Decimal("4.5"), chronic_late=True emits "4.5" and "Yes" cells; a non-chronic row emits "" for the chronic cell.
    - test_pdf_has_lateness_header: build_pdf output is %PDF bytes and does not raise with the 8-column table.
    - test_empty_rows_header_only: build_csv([]) still returns exactly the 8-column header line.
  </behavior>
  <read_first>
    - scheduling/report_render.py:26-139 (`HEADER` at :29, `build_csv` at :55, `build_pdf` at :91 — the ONE shared HEADER contract)
    - scheduling/reporting.py:60-95 (FacultyRow.minutes_late_avg / late_sessions / chronic_late from Plan 01)
    - scheduling/tests_report_render.py (existing HEADER/build_csv/build_pdf test style)
    - .planning/phases/11-metrics-the-mission-promises/11-RESEARCH.md Pitfall 4 (landscape-A4 column budget) + Pitfall 5 (distinct header contracts)
  </read_first>
  <action>
    Append two columns to `report_render.HEADER` (:29): "Avg min late" and "Chronic late". In
    `build_csv` (:55) append `r.minutes_late_avg` and `("Yes" if r.chronic_late else "")` to the
    per-row list. In `build_pdf` (:91) append the same two cells to each data row so the Table
    data stays in lockstep with HEADER (they share the one constant). Keep the chronic cell TERSE
    ("Yes"/"") so the landscape-A4 table absorbs 8 short columns without overflow (Pitfall 4). Do
    NOT touch `web.hr.CSV_HEADER` — it is a SEPARATE contract (Pitfall 5), edited in Task 2. The
    two lateness cells are numeric/short text; the faculty name cell keeps its existing `csv_safe`.

    Add the four named tests to `scheduling/tests_report_render.py`, constructing `FacultyRow`
    instances directly (DB-free, matching the existing render-test style), including the new
    lateness fields.
  </action>
  <verify>
    <automated>C:\Users\joshu_mnu8z3u\AppData\Local\Programs\Python\Python312\python.exe manage.py test scheduling.tests_report_render -v2</automated>
  </verify>
  <acceptance_criteria>
    - `HEADER` has 8 entries ending "Avg min late", "Chronic late".
    - `build_csv` and `build_pdf` emit the two lateness cells from every FacultyRow; empty rows still yield a header-only CSV.
    - `scheduling.tests_report_render` passes including the four new tests.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the derived per-session minutes-late column to the HR payroll CSV</name>
  <files>web/hr.py, web/tests_hr.py</files>
  <behavior>
    - test_hr_csv_keeps_actual_start_and_adds_lateness: CSV_HEADER contains both "Actual start" and a new "Minutes late" column; the row still emits the raw actual_start value.
    - test_hr_late_session_shows_minutes: a session started 12 min after scheduled_start emits 12 in the Minutes-late cell.
    - test_hr_absent_zero_minutes: an ABSENT session (actual_start NULL) emits 0 in the Minutes-late cell and "" for the raw actual_start.
  </behavior>
  <read_first>
    - web/hr.py:49-54 (`CSV_HEADER` — the payroll contract), :207-246 (`attendance_csv` streaming row generator, insertion point at :230-241; raw `_fmt_dt(s.actual_start)` kept per D-03)
    - web/hr.py:36-40 (imports — `from scheduling.report_render import csv_safe`; add the reporting helper import beside it)
    - scheduling/reporting.py session_minutes_late (Plan 01)
    - web/tests_hr.py:37-198 (`_HrBase` + HrExportTests over make_reporting_fixture; the CSV export assertion style)
  </read_first>
  <action>
    Import `session_minutes_late` from `scheduling.reporting` in web/hr.py. Add a "Minutes late"
    column to `CSV_HEADER` (:51) immediately AFTER "Actual start" (keep the raw Actual-start cell —
    D-03 says ADD the derived column, do not remove the timestamp). In the `attendance_csv` row
    generator (:230-241) insert one derived cell right after the raw `_fmt_dt(s.actual_start)` cell:
    `session_minutes_late(s.scheduled_start, s.actual_start) // 60` (whole minutes late; 0 for
    on-time/early/ABSENT since the helper returns 0 for a NULL start). The cell is numeric, so no
    `csv_safe` is needed. Reuse the SHARED helper — do NOT recompute the delta inline (Pitfall 5 /
    single-definition). Perform NO DB access inside the generator (the value derives from already
    -loaded session fields; the streaming HY010/cursor-open contract at web/hr.py:218-221 holds).

    Add the three named tests to `web/tests_hr.py` (mirror HrExportTests): seed a late session via
    the extended `make_reporting_fixture` `add_session(..., actual_start=..., actual_end=...)` (Plan
    01 Task 2), parse the streamed CSV, and assert the Minutes-late cell and the retained raw cell.
  </action>
  <verify>
    <automated>C:\Users\joshu_mnu8z3u\AppData\Local\Programs\Python\Python312\python.exe manage.py test web.tests_hr -v2</automated>
  </verify>
  <acceptance_criteria>
    - `CSV_HEADER` contains "Actual start" AND "Minutes late" (raw timestamp retained per D-03).
    - The derived cell is computed via `session_minutes_late` (imported), not a re-derivation; ABSENT -> 0.
    - No DB query is issued inside the `rows()` generator.
    - `web.tests_hr` passes including the three new tests.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render avg-minutes-late + chronic flag on the faculty scorecard page</name>
  <files>templates/reports/scorecard.html, web/tests_hr.py</files>
  <read_first>
    - templates/reports/scorecard.html:27-51 (the KPI card grid where a lateness card slots in beside Early ends)
    - scheduling/reporting.py Scorecard.minutes_late_avg / late_sessions / chronic_late + `held` (Plan 01)
    - web/ifo.py:1496-1517 (`scorecard` view — passes `card` = safe_card(faculty_scorecard, ...); no view change needed, only the template reads card.0.*)
  </read_first>
  <action>
    Add a lateness card to the scorecard KPI grid in reports/scorecard.html (inside the
    `{% if card.1 %}...{% else %}` success branch, beside the existing "Early ends" card at
    :47-50). Show `{{ card.0.minutes_late_avg }}` as the headline "Avg min late" figure. Show the
    chronic-late flag ONLY when `card.0.held >= 5` (D-02 floor — never render a chronic verdict for
    a faculty with too few held sessions to evaluate); when shown and `card.0.chronic_late` is true,
    render a terse "Chronic" pill using an existing warn token (e.g. `pill pill--warn` / the
    `--amber` tokens the page already uses), always paired with the numeric average so colour is
    never the only signal. Below >= 5 held, show only the average. Keep the shared scorecard reused
    by the Dean surface intact (this template is also rendered by web.dean) — additive only,
    ASCII-only, honour the existing Franken shell + token system (Claude's Discretion on
    placement/labels).

    Add a view-level test (in web/tests_hr.py or a scorecard test) asserting the IFO scorecard page
    (`/ifo/scorecard/<id>`) renders 200 and the response body contains the avg-minutes-late value
    for a faculty seeded with late sessions, and that a < 5-held faculty does not render the chronic
    verdict.
  </action>
  <verify>
    <automated>C:\Users\joshu_mnu8z3u\AppData\Local\Programs\Python\Python312\python.exe manage.py test web.tests_hr scheduling.tests_reporting -v1</automated>
  </verify>
  <acceptance_criteria>
    - reports/scorecard.html shows the avg-minutes-late figure and shows the chronic pill only when `card.0.held >= 5`.
    - The chronic pill is always paired with the numeric average (colour not the only signal).
    - A page test asserts the scorecard renders the lateness figure at 200 and suppresses the chronic verdict below the 5-held floor.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Faculty display name -> CSV cell | User-controllable text reaching a spreadsheet (formula-injection risk). |
| Aggregate/session data -> HR streaming export | Full-term export streamed with an open server-side cursor (MSSQL HY010 risk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Tampering | CSV formula injection in weekly/HR exports | high | mitigate | Existing `csv_safe` on all text cells is untouched; the new lateness/minutes cells are numeric (no user text). |
| T-11-05 | Denial of Service | DB query inside the HR streaming generator | medium | mitigate | Derived cell uses already-loaded session fields only; no DB access added inside `rows()` (HY010 contract at web/hr.py:218-221). |
| T-11-06 | Information Disclosure | Chronic verdict shown for statistically-insufficient data | low | mitigate | Chronic pill gated at `held >= 5` in the template (D-02 floor). |
</threat_model>

<verification>
- `scheduling.tests_report_render`, `web.tests_hr`, `scheduling.tests_reporting` green under the full Python312 runner.
- Weekly report CSV/PDF and HR CSV both carry lateness (neither surface omits it — Pitfall 5); HR keeps the raw actual_start.
- After a full run, `git checkout FluxTrack_SRS.docx` (MEMORY guard).
</verification>

<success_criteria>
Lateness (avg minutes + chronic flag) appears on the weekly report, the HR payroll CSV (derived
column added, raw timestamp kept), and the faculty scorecard, all deriving from the Plan-01 shared
helper so no two surfaces can disagree.
</success_criteria>

<output>
Create `.planning/phases/11-metrics-the-mission-promises/11-02-SUMMARY.md` when done.
</output>
