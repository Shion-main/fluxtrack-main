---
phase: 04-modality-shift-approval-srs-v1-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings.py
  - scheduling/models.py
  - scheduling/migrations/0003_modality_shift_request.py
  - scheduling/test_support.py
autonomous: true
requirements: [MOD-01]
must_haves:
  truths:
    - "get_policy('modality_shift_lead_days') returns 2 by default and is overridable via a SystemSetting row (D-03)"
    - "One ModalityShiftRequest is an atomic ticket that can span multiple schedules over a date-range window (D-19, D-01)"
    - "Request status is one of pending/approved/rejected/withdrawn/denied — denied is the terminal no-room outcome (D-10, D-07 REVISED)"
    - "Each affected schedule carries its own preferred_room, assigned_room, and optional time-move slot (D-05/D-06/D-16/D-18)"
  artifacts:
    - "scheduling/models.py — ModalityShiftRequest, ModalityShiftItem, ModalityShiftStatus"
    - "config/settings.py — modality_shift_lead_days default in FLUXTRACK_POLICY"
    - "scheduling/migrations/0003_modality_shift_request.py"
    - "scheduling/test_support.py — make_shift_fixture() reusable test builder"
  key_links:
    - "ModalityShiftItem.assigned_room is the room reserved per schedule at Dean approval (D-18); ops/availability reads it as request-aware occupancy"
    - "Session.declared_modality (existing field) becomes the approval-driven modality override written by plans 04-05/04-06 (MOD-06)"
---

<objective>
Lay the Phase 4 data foundation: the `ModalityShiftRequest` + `ModalityShiftItem` models with a five-state lifecycle, the `modality_shift_lead_days` policy default, and one reusable test-fixture builder every later plan imports.

Purpose: Every downstream plan (availability, services, materialize, web) reads or writes these two models and the policy value. Building them once, correctly shaped for the atomic multi-schedule ticket (D-19) and the reserve-at-approval room (D-18), keeps later plans from re-opening the migration graph.
Output: Two new models + status enum, a migration, a policy default, and `scheduling/test_support.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modality-shift-approval-srs-v1-2/04-CONTEXT.md
@.planning/phases/04-modality-shift-approval-srs-v1-2/04-RESEARCH.md
@.planning/phases/04-modality-shift-approval-srs-v1-2/04-PATTERNS.md
@scheduling/models.py
@config/settings.py
@ops/policy.py
@ops/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the modality_shift_lead_days policy default</name>
  <files>config/settings.py</files>
  <read_first>
    - config/settings.py (FLUXTRACK_POLICY dict, lines ~201-211 — the file being modified)
    - ops/policy.py (get_policy resolution: SystemSetting row overrides settings default)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-RESEARCH.md (§Code Examples "Adding the policy default (D-03)")
  </read_first>
  <action>Add a `modality_shift_lead_days` key with integer value 2 to the `FLUXTRACK_POLICY` dict in config/settings.py, alongside the existing `grace_minutes`/`materialization_horizon_days` keys. Add a short trailing comment noting it is the MOD-01/D-02 lead-time gate measured in whole calendar days (Asia/Manila). Do NOT hardcode this value anywhere else in the codebase — every reader must go through `get_policy("modality_shift_lead_days")` per Convention 3. No SystemSetting seed row is required (get_policy falls back to the settings default), but the value MUST be overridable, which it already is via get_policy's SystemSetting lookup.</action>
  <verify>
    <automated>py -3.12 -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings'); django.setup(); from ops.policy import get_policy; assert get_policy('modality_shift_lead_days')==2; print('lead_days ok')"</automated>
  </verify>
  <acceptance_criteria>
    - source: config/settings.py FLUXTRACK_POLICY contains modality_shift_lead_days: 2
    - behavior: get_policy("modality_shift_lead_days") returns int 2 with no SystemSetting row present
    - test-command: the inline python assertion above prints "lead_days ok" and exits 0
  </acceptance_criteria>
  <done>get_policy("modality_shift_lead_days") resolves to 2 by default; no literal "2" lead-day constant exists outside FLUXTRACK_POLICY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ModalityShiftRequest + ModalityShiftItem models + migration</name>
  <files>scheduling/models.py, scheduling/migrations/0003_modality_shift_request.py</files>
  <read_first>
    - scheduling/models.py (Session/Schedule/Modality/SessionStatus — the file being modified; copy the TextChoices + FK + audit-field shapes at lines 6-9, 70-75, 84-119)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-PATTERNS.md (§"scheduling/models.py — ModalityShiftRequest": status enum + FK + audit-field pattern, model fields to add)
    - accounts/models.py (Department, Role.DEAN, User.department — routing targets referenced by the FKs)
    - campus/models.py (Room — preferred_room/assigned_room FK targets)
  </read_first>
  <behavior>
    - A ModalityShiftStatus enum exposes PENDING/APPROVED/REJECTED/WITHDRAWN/DENIED with the five string values.
    - A ModalityShiftRequest persists requester, dean, department, target_modality, window_start, window_end, is_time_move, status (default PENDING), decision_reason, decided_at, decided_by, created_at.
    - A ModalityShiftRequest can hold multiple ModalityShiftItem rows (one per affected schedule) — request.items.all() returns them.
    - A ModalityShiftItem persists request(FK CASCADE), schedule(FK PROTECT), preferred_room(FK SET_NULL null), assigned_room(FK SET_NULL null), new_start_time(null), new_end_time(null).
    - makemigrations produces exactly one new migration and migrate applies it cleanly on the configured DB.
  </behavior>
  <action>In scheduling/models.py add a `ModalityShiftStatus(models.TextChoices)` with members PENDING="pending", APPROVED="approved", REJECTED="rejected", WITHDRAWN="withdrawn", DENIED="denied" (DENIED is the terminal no-room-available outcome per D-07 REVISED; REJECTED is the Dean's explicit no per MOD-02). Add `ModalityShiftRequest(models.Model)` with: `requester` FK to settings.AUTH_USER_MODEL on_delete PROTECT related_name="modality_requests"; `dean` FK to AUTH_USER_MODEL null blank SET_NULL related_name="modality_requests_routed" (the routed department Dean, stamped at submit, D-09); `department` FK to accounts.Department null blank SET_NULL (routing snapshot); `target_modality` CharField max_length 10 choices=Modality.choices (F2F/Blended/Online); `window_start` and `window_end` DateFields (single-session request = same date both, D-01); `is_time_move` BooleanField default False (D-16 — a bundled reschedule); `status` CharField max_length 10 choices=ModalityShiftStatus.choices default PENDING; `decision_reason` CharField max_length 255 blank (Dean reject reason or system deny reason); `decided_at` DateTimeField null blank; `decided_by` FK AUTH_USER_MODEL null blank SET_NULL related_name="modality_requests_decided"; `created_at` DateTimeField auto_now_add. Give Meta ordering ["-created_at"]. Add `ModalityShiftItem(models.Model)` with: `request` FK ModalityShiftRequest on_delete CASCADE related_name="items"; `schedule` FK Schedule on_delete PROTECT related_name="modality_items"; `preferred_room` FK campus.Room null blank SET_NULL related_name="modality_preferred_items" (the faculty pick — a preference only, D-05/D-06); `assigned_room` FK campus.Room null blank SET_NULL related_name="modality_assigned_items" (the room finalized at Dean approval and reserved for every in-window session including future ones, D-18); `new_start_time` and `new_end_time` TimeField null blank (the time-move slot, D-16 — null when not a time-move). Do NOT add a Session.modality column (RESEARCH anti-pattern) — declared_modality is the override. Run `py -3.12 manage.py makemigrations scheduling` then `py -3.12 manage.py migrate` and commit the generated 0003 migration.</action>
  <verify>
    <automated>py -3.12 manage.py makemigrations scheduling --check --dry-run && py -3.12 manage.py migrate && py -3.12 -c "import django,os; os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings'); django.setup(); from scheduling.models import ModalityShiftRequest, ModalityShiftItem, ModalityShiftStatus; assert set(ModalityShiftStatus.values)=={'pending','approved','rejected','withdrawn','denied'}; print('models ok')"</automated>
  </verify>
  <acceptance_criteria>
    - source: scheduling/models.py defines ModalityShiftStatus, ModalityShiftRequest, ModalityShiftItem with the fields named above
    - source: no new modality column is added to Session
    - behavior: makemigrations --check reports no pending changes after the migration is created; migrate applies cleanly
    - test-command: the inline assertion prints "models ok"; ModalityShiftStatus.values equals the five expected strings
  </acceptance_criteria>
  <done>The two models + status enum exist, one migration is committed, migrate is clean, and request.items relates a ticket to multiple schedules (D-19).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the shared make_shift_fixture test-support builder</name>
  <files>scheduling/test_support.py, scheduling/tests.py</files>
  <read_first>
    - scheduling/tests.py (existing _JobFixtureMixin / TestCase fixture style at lines ~347-465 — mirror how sessions/rooms are seeded)
    - accounts/management/commands/seed_demo.py is referenced by RESEARCH §Wave 0; seed a Dean + faculty in one Department here rather than depending on seed_demo
    - campus/models.py, scheduling/models.py, accounts/models.py (the objects the builder constructs)
  </read_first>
  <behavior>
    - make_shift_fixture() returns a namespace/dict with: a Department, a Dean user (role=DEAN, department set), a faculty user (role=FACULTY, same department), a Building with two Floors/Rooms, an F2F Schedule in an active AcademicTerm, at least one in-window Session, plus an Online schedule+room and a competing Session/Booking usable for conflict cases.
    - Calling it inside a TestCase produces valid, saved rows (no IntegrityError) reusable by ops/scheduling/web tests.
  </behavior>
  <action>Create scheduling/test_support.py exposing `make_shift_fixture()` (a plain helper, NOT a TestCase, so Django's test runner never executes it as tests). It builds and returns the minimal graph the Phase 4 test suites need: one accounts.Department; one Dean User (role=Role.DEAN, department=that dept, is_active=True); one faculty User (role=Role.FACULTY, same dept); a campus.Building with two Floors each holding one Room (opaque qr_token/manual_code must be unique — generate distinct values); one active scheduling.AcademicTerm spanning the test dates; one F2F Schedule (faculty, room A, a fixed day_of_week/time) with at least one in-window SCHEDULED Session materialized on a concrete date; one Online Schedule (room B) with a Session for the born-released/online cases; and a competing occupant (a second Session holding room A at an overlapping slot, or an ops.Booking) for availability conflict tests. Return everything in a simple object or dict keyed by role (dept, dean, faculty, room_a, room_b, building, term, f2f_schedule, session, online_schedule, online_session, competitor). Keep it ASCII-only. Add one lightweight `FixtureSmokeTests(TestCase)` to scheduling/tests.py that calls make_shift_fixture() and asserts the returned dean has role DEAN and the faculty shares the dean's department (proves the builder wires routing correctly for later plans).</action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests.FixtureSmokeTests -v2</automated>
  </verify>
  <acceptance_criteria>
    - source: scheduling/test_support.py defines make_shift_fixture returning the keyed graph above
    - behavior: FixtureSmokeTests passes — dean.role == Role.DEAN and faculty.department_id == dean.department_id
    - test-command: `py -3.12 manage.py test scheduling.tests.FixtureSmokeTests -v2` exits 0
  </acceptance_criteria>
  <done>make_shift_fixture() is importable by ops/scheduling/web test modules and its smoke test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema ↔ future writers | The model fields define what later plans may reserve/override; a missing field would force a schema change mid-phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01a | Elevation | ModalityShiftRequest.requester/dean FKs | medium | mitigate | Model carries requester + department so later plans can enforce object-level ownership + department gates (IDOR defense lives in 04-04/04-05/04-08) |
| T-04-05a | Tampering | ModalityShiftItem.assigned_room | medium | mitigate | assigned_room is the server-written reservation (never a client field); request-aware availability (04-03) reads it so a late-materialized session cannot escape the shift (D-18) |
| T-04-SC | Tampering | migration integrity | low | accept | Additive migration only; makemigrations --check gates drift; no external packages installed in this plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `ModalityShiftStatus` (TextChoices: PENDING/APPROVED/REJECTED/WITHDRAWN/DENIED) — scheduling/models.py
- `ModalityShiftRequest` model — scheduling/models.py
- `ModalityShiftItem` model — scheduling/models.py
- `modality_shift_lead_days` policy key (default 2) — config/settings.py FLUXTRACK_POLICY
- `scheduling/migrations/0003_modality_shift_request.py`
- `make_shift_fixture()` — scheduling/test_support.py
</artifacts_produced>

<verification>
- `py -3.12 manage.py makemigrations scheduling --check --dry-run` reports no changes (migration committed).
- `py -3.12 manage.py migrate` applies cleanly.
- `py -3.12 manage.py test scheduling.tests.FixtureSmokeTests -v2` green.
- `py -3.12 manage.py test` full suite still green (additive change breaks nothing).
</verification>

<success_criteria>
The two models, the status enum, the policy default, the migration, and the reusable fixture builder all exist and are exercised by a green smoke test; downstream plans can import all four without further schema work.
</success_criteria>

<output>
Create `.planning/phases/04-modality-shift-approval-srs-v1-2/04-01-SUMMARY.md` when done.
</output>
