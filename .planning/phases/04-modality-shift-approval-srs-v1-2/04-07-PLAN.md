---
phase: 04-modality-shift-approval-srs-v1-2
plan: 07
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - web/faculty.py
  - web/urls.py
  - web/tests.py
  - templates/faculty/modality_new.html
  - templates/faculty/_modality_form.html
  - templates/faculty/modality_mine.html
autonomous: true
requirements: [MOD-01, MOD-05, MOD-06]
must_haves:
  truths:
    - "A faculty member can open a submit form showing their schedules and, for a ->F2F/Blended target, the rooms/times actually available for the affected session(s) — pick a specific room/time OR let the app decide (D-05/D-12/D-15)"
    - "Submitting posts through submit_modality_shift: server-side lead-time gate + Dean routing + never trusting a client room pk; invalid input renders a friendly 400, never a 500 (MOD-01)"
    - "A faculty member sees a 'my requests' list with each request's status (pending/approved/rejected/withdrawn/denied) and can withdraw while pending (MOD-05/D-10/D-12)"
    - "This request workflow is the sole faculty entry point for changing a session's modality — the FAC-07 self-declare path is retired (MOD-06/D-13)"
  artifacts:
    - "web/faculty.py — modality_new, modality_mine, modality_withdraw views"
    - "templates/faculty/modality_new.html, _modality_form.html, modality_mine.html"
    - "web/urls.py — faculty_modality_new / _mine / _withdraw routes"
    - "web/tests.py — FacultyModalityAuthzTests"
  key_links:
    - "The submit form's availability-first picker reads ops/availability.available_rooms_for/available_times_for; the POST calls scheduling.services.submit_modality_shift; withdraw calls withdraw_modality_shift"
---

<objective>
Build the faculty D-12 surface: an availability-first submit form (D-05/D-15) and a "my requests" status list with withdraw (MOD-05/D-10), wiring the faculty side of the workflow to the 04-04/04-05 services. Retire the FAC-07 self-declare entry point (MOD-06/D-13) by making this request the only faculty modality-change path.

Purpose: This is where faculty actually request a shift. The picker shows real availability and lets them pick a room/time or delegate to the app ("the faculty knows what to book, the app knows the schedule"); the concrete selection goes to the Dean.
Output: faculty views + templates + URLs + faculty authz tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-modality-shift-approval-srs-v1-2/04-CONTEXT.md
@.planning/phases/04-modality-shift-approval-srs-v1-2/04-PATTERNS.md
@web/faculty.py
@web/ifo.py
@web/urls.py
@scheduling/services.py
@ops/availability.py
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: modality_new view + availability-first picker template</name>
  <files>web/faculty.py, web/urls.py, templates/faculty/modality_new.html, templates/faculty/_modality_form.html</files>
  <read_first>
    - web/faculty.py (faculty_required decorator + schedule view at lines 14-35 — the file being modified)
    - web/ifo.py (assignment_create lines 131-211 — the validated-POST, 400-not-500, server-side object-resolution pattern to mirror)
    - web/urls.py (grouped path() entries + the `from . import` line — add faculty routes here)
    - ops/availability.py (available_rooms_for, available_times_for — the picker data)
    - scheduling/services.py (submit_modality_shift signature — the POST target)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-CONTEXT.md (D-05 pick-or-let-app-decide; D-15 resolution order; D-16 time-move bundled with F2F/Blended)
  </read_first>
  <action>Add `modality_new(request)` to web/faculty.py under @faculty_required. GET renders templates/faculty/modality_new.html with the faculty's active schedules and a form: target_modality (F2F/Blended/Online), a window (window_start/window_end dates, single-session = same date), a schedule multi-select (D-19 atomic ticket across multiple classes), and — when the target is F2F/Blended — the availability-first picker fed by available_rooms_for/available_times_for for the affected session(s): the faculty may pick a specific room, pick an alternative time (a bundled time-move, D-16), or choose "let the app decide" (leave room blank, D-05/D-06). POST validates FORMAT before any write exactly like assignment_create (parse_date on window fields, target_modality in Modality.values, floor/room pks numeric) and, on any bad input, re-renders templates/faculty/_modality_form.html with an error at status=400 (never a 500). On valid input, call scheduling.services.submit_modality_shift(faculty=request.user, schedules=..., target_modality=..., window_start=..., window_end=..., preferred_rooms=..., time_move=...) — the service re-resolves rooms and never trusts the client pk — and on the service refusing (too-late lead-time, no Dean, double-book) surface that reason at status=400. On success redirect/render the "my requests" list. Add the route `path("faculty/modality/new", faculty.modality_new, name="faculty_modality_new")` to web/urls.py. Keep templates Franken-UI/htmx consistent with the existing faculty templates and ASCII-safe.</action>
  <verify>
    <automated>py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2</automated>
  </verify>
  <acceptance_criteria>
    - source: web/faculty.py modality_new is @faculty_required, validates format before write, renders _modality_form.html at status=400 on error, and calls submit_modality_shift on success
    - source: web/urls.py has the faculty_modality_new route; the picker template uses available_rooms_for/available_times_for
    - behavior: a non-faculty user is denied; malformed input yields a 400 (not a 500); a valid submit creates a pending request
    - test-command: `py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2` exits 0
  </acceptance_criteria>
  <done>Faculty can submit an availability-first modality-shift request; the server gates, routes, and re-resolves rooms; bad input is a friendly 400.</done>
</task>

<task type="auto">
  <name>Task 2: modality_mine list + withdraw</name>
  <files>web/faculty.py, web/urls.py, templates/faculty/modality_mine.html</files>
  <read_first>
    - web/faculty.py (Task 1 output + the schedule list view pattern at lines 24-35 — the file being modified)
    - scheduling/services.py (withdraw_modality_shift — requester+PENDING guarded)
    - web/scan.py (@require_http_methods(["POST"]) usage for the withdraw POST)
    - scheduling/models.py (ModalityShiftRequest, ModalityShiftStatus — status labels for display)
  </read_first>
  <action>Add `modality_mine(request)` (@faculty_required) rendering templates/faculty/modality_mine.html with request.user's ModalityShiftRequests (filter requester=request.user, select_related dean/department, prefetch items__schedule, order -created_at), showing each request's target modality, window, affected schedules, status (pending/approved/rejected/withdrawn/denied with the decision_reason for rejected/denied), and a Withdraw control shown ONLY when status==PENDING. Add `modality_withdraw(request, pk)` under @faculty_required and @require_http_methods(["POST"]): re-fetch the request by pk, call scheduling.services.withdraw_modality_shift(request_obj, request.user) which itself re-checks requester==user AND status==PENDING (never trust the earlier snapshot / client); on refusal render the list with an error, on success re-render the updated list. Add routes `path("faculty/modality/mine", faculty.modality_mine, name="faculty_modality_mine")` and `path("faculty/modality/<int:pk>/withdraw", faculty.modality_withdraw, name="faculty_modality_withdraw")` to web/urls.py.</action>
  <verify>
    <automated>py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2</automated>
  </verify>
  <acceptance_criteria>
    - source: modality_mine lists only the requester's requests with per-status display; modality_withdraw is POST-only and delegates the guard to withdraw_modality_shift
    - behavior: the Withdraw control appears only for PENDING requests; a withdraw on a non-pending or foreign request is refused
    - test-command: `py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2` exits 0
  </acceptance_criteria>
  <done>Faculty see their request statuses and can withdraw only their own pending requests (MOD-05/D-10/D-12).</done>
</task>

<task type="auto">
  <name>Task 3: Faculty authz/validation tests + FAC-07 retirement note</name>
  <files>web/tests.py, web/faculty.py</files>
  <read_first>
    - web/tests.py (existing test style/imports — the file being modified)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-VALIDATION.md (web.tests ModalityShiftAuthzTests — IDOR foreign withdraw; this plan owns the faculty half)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-CONTEXT.md (D-13/MOD-06 — replaces FAC-07 self-declare; same-day falls back to scan-time behavior)
  </read_first>
  <action>Add `FacultyModalityAuthzTests(TestCase)` to web/tests.py (using make_shift_fixture + the test Client): (1) a non-faculty user (e.g. the Dean) GET/POSTing faculty_modality_new is denied (PermissionDenied/403); (2) a faculty user withdrawing ANOTHER faculty's pending request via faculty_modality_withdraw is refused and the request stays PENDING (IDOR guard); (3) malformed submit input (bad date / bad modality) returns status 400, not 500; (4) a valid submit creates exactly one PENDING ModalityShiftRequest routed to the department Dean. For the FAC-07 retirement (MOD-06/D-13): confirm there is NO faculty self-declare modality view/route (the modality-shift request is the sole faculty modality-change entry point); add a short module docstring/comment in web/faculty.py stating the modality-shift request workflow replaces the FAC-07 self-declare path (same-day changes fall back to existing scan-time behavior). Do NOT add any self-declare form.</action>
  <verify>
    <automated>py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2</automated>
  </verify>
  <acceptance_criteria>
    - source: FacultyModalityAuthzTests covers non-faculty denial, foreign-withdraw IDOR refusal, 400-not-500, and a valid routed submit
    - source: web/faculty.py documents the FAC-07 replacement; no self-declare route exists
    - behavior: all four faculty authz/validation cases pass
    - test-command: `py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2` exits 0
  </acceptance_criteria>
  <done>The faculty surface is authz-guarded and validated; the FAC-07 self-declare entry point is retired in favor of this workflow (MOD-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| faculty browser → submit/withdraw views | Untrusted modality, dates, room pk, and a target request pk cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation | foreign withdraw (IDOR) | high | mitigate | modality_withdraw delegates to withdraw_modality_shift which re-checks requester==user AND status==PENDING; FacultyModalityAuthzTests proves a foreign withdraw is refused |
| T-04-02 | Tampering | forged room/modality in submit POST | high | mitigate | View validates format then calls submit_modality_shift, which re-resolves rooms server-side and validates the modality enum; client room pk is never trusted |
| T-04-05v | Input Validation | malformed submit input | medium | mitigate | parse_date/enum checks before ORM write; error renders the form partial at status=400, never a 500 (assignment_create pattern) |
| T-04-10 | Elevation | non-faculty access | medium | mitigate | @faculty_required on every view; non-faculty denied (superuser exempt per existing convention) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `modality_new(request)` — web/faculty.py
- `modality_mine(request)` — web/faculty.py
- `modality_withdraw(request, pk)` — web/faculty.py
- `templates/faculty/modality_new.html`, `templates/faculty/_modality_form.html`, `templates/faculty/modality_mine.html`
- URL names `faculty_modality_new`, `faculty_modality_mine`, `faculty_modality_withdraw` — web/urls.py
- `FacultyModalityAuthzTests` — web/tests.py
</artifacts_produced>

<verification>
- `py -3.12 manage.py test web.tests.FacultyModalityAuthzTests -v2` green.
- `py -3.12 manage.py test web` green (no regression in existing web views).
</verification>

<success_criteria>
Faculty can submit an availability-first modality-shift request (MOD-01/D-05/D-15), track its status, and withdraw while pending (MOD-05/D-10); the workflow is the sole faculty modality-change entry point (MOD-06/D-13), authz-guarded and input-validated.
</success_criteria>

<output>
Create `.planning/phases/04-modality-shift-approval-srs-v1-2/04-07-SUMMARY.md` when done.
</output>
