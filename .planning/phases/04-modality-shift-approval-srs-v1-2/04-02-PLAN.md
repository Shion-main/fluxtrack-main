---
phase: 04-modality-shift-approval-srs-v1-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - scheduling/management/commands/regenerate_srs_docx.py
  - FluxTrack_SRS.md
  - FluxTrack_SRS.docx
  - scheduling/tests_srs.py
autonomous: false
requirements: [DOC-01, MOD-06]
user_setup:
  - service: pypandoc_binary
    why: "Markdown->DOCX conversion for the SRS v1.2 regeneration (DOC-01); pandoc is NOT on PATH"
    env_vars: []
must_haves:
  truths:
    - "FluxTrack_SRS.md is at v1.2: new MOD area (MOD-01..06), DEAN-04 added, FAC-07 marked superseded, CHK-03 amended (no Confirm absent; online applies), CHK-06 removed, RPT-02 notifies IFO and Deans, modality_shift_lead_days in the §8 policy register (D-14)"
    - "A single management command regenerates FluxTrack_SRS.docx from the .md so the two never drift (D-14)"
    - "The .docx regenerates without error via the bundled pandoc engine (no system pandoc, no PATH dependency)"
  artifacts:
    - "scheduling/management/commands/regenerate_srs_docx.py"
    - "FluxTrack_SRS.md (v1.2)"
    - "FluxTrack_SRS.docx (regenerated)"
    - "requirements.txt (pypandoc_binary==1.17 pinned)"
    - "scheduling/tests_srs.py (SRS marker smoke test)"
  key_links:
    - "The SRS FAC-07 amendment + CHK-06 removal is the documentation half of MOD-06; the code half (declared_modality as approval override) ships in 04-05/04-07"
---

<objective>
Revise the SRS to v1.2 in Markdown and regenerate the DOCX from it via a repeatable management command, closing DOC-01. Install the one new dependency (`pypandoc_binary`) behind a package-legitimacy human checkpoint.

Purpose: The SRS is the project's contract document; a reader who trusts only the SRS would otherwise build the superseded FAC-07 self-declare flow (per CONCERNS.md). Bringing the SRS in sync with the modality-shift reality — and generating the .docx from the .md so they can never drift — is the deliverable.
Output: v1.2 `.md`, a regenerated `.docx`, a regenerate command, a pinned dependency, and a marker smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-modality-shift-approval-srs-v1-2/04-RESEARCH.md
@FluxTrack_SRS.md
@requirements.txt
@scheduling/management/commands/assign_online.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0 (checkpoint): Approve the pypandoc_binary package install</name>
  <action>BLOCKING human-verify gate that MUST clear before Task 1 installs anything. This is the package-legitimacy checkpoint required by protocol for the one new dependency (`pypandoc_binary==1.17`). Do NOT run pip install until the human approves. Never auto-approve (workflow.auto_advance is ignored for legitimacy checkpoints). If the human names a fallback instead, install system pandoc + plain `pypandoc` per RESEARCH §DOC-01 Toolchain.</action>
  <what-built>Nothing yet — this checkpoint gates the ONE new dependency install. RESEARCH.md § Package Legitimacy Audit flags `pypandoc_binary==1.17` as [SUS] (a PyPI downloads-visibility artifact, not a genuine risk signal): it exists, has the canonical maintained repo github.com/JessicaTegner/pypandoc, is not deprecated, and has no postinstall hook. Per protocol a human must confirm before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pypandoc_binary/ and confirm version 1.17 exists and the project links to github.com/JessicaTegner/pypandoc.
    2. Confirm the maintainer/repo looks legitimate (the de-facto Python pandoc wrapper) and there is no suspicious postinstall.
    3. If satisfied, approve so the executor runs `py -3.12 -m pip install pypandoc_binary==1.17` and pins it in requirements.txt. If not, respond with the alternative (system pandoc + plain `pypandoc`).
  </how-to-verify>
  <resume-signal>Type "approved" to install pypandoc_binary==1.17, or name the fallback.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install pypandoc_binary and pin it in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (the "# Media / reports" section near qrcode/Pillow — the file being modified)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-RESEARCH.md (§DOC-01 Toolchain + §Package Legitimacy Audit)
  </read_first>
  <action>After the checkpoint approval, run `py -3.12 -m pip install pypandoc_binary==1.17`. Add `pypandoc_binary==1.17` to requirements.txt under the existing "# Media / reports" section (below Pillow), with a one-line comment: bundled pandoc engine for DOC-01 SRS md->docx (pandoc not on PATH). Do not add plain `pypandoc` as well — the `_binary` wheel supersedes it. Verify the engine imports and finds its bundled pandoc.</action>
  <verify>
    <automated>py -3.12 -c "import pypandoc; print('pandoc', pypandoc.get_pandoc_version())"</automated>
  </verify>
  <acceptance_criteria>
    - source: requirements.txt contains pypandoc_binary==1.17 under Media / reports
    - behavior: `import pypandoc; pypandoc.get_pandoc_version()` returns a version string without raising (bundled engine present, no PATH pandoc needed)
    - test-command: the inline import prints "pandoc <version>"
  </acceptance_criteria>
  <done>pypandoc_binary==1.17 is installed, pinned, and its bundled pandoc engine is importable.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the SRS v1.2 edit map to FluxTrack_SRS.md</name>
  <files>FluxTrack_SRS.md</files>
  <read_first>
    - FluxTrack_SRS.md (Revision History ~line 23-28; §4.3 FAC table FAC-07 line ~268; §4.4 CHK table CHK-03 line ~281 and CHK-06 line ~284; §4.8 Dean table lines ~325-329; §4.9 RPT-02 line ~336; §8 Policy Assumptions Register table lines ~460-471 — the exact anchors to edit)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-RESEARCH.md (§DOC-01 Toolchain — the SRS v1.2 edit map table)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-CONTEXT.md (D-14 and the MOD-01..06 / DEAN-04 requirement text to transcribe)
  </read_first>
  <action>Edit FluxTrack_SRS.md in place to v1.2 using the seven anchored edits. (1) Revision History: add a row `| 1.2 | 2026-07-03 | ... | Modality-shift approval workflow (MOD-01..06); DEAN-04 added; FAC-07 superseded by MOD approval; CHK-03 amended; CHK-06 removed; RPT-02 notifies Deans; modality_shift_lead_days added to the policy register. |` and update the document status/version header to 1.2. (2) Add a new "4.x Modality Shift Approval (MOD)" subsection under §4 (after §4.3 Faculty) with a requirements table row per MOD-01..MOD-06 transcribing the REQUIREMENTS.md text (lead-time-gated request; department-Dean approve/reject; ->Online release; ->F2F auto-assign or clean fail; IFO informational notify + withdraw-while-pending; replaces FAC-07). (3) Amend the FAC-07 row so its requirement text states the per-session modality change is now made through the Dean-approved modality-shift workflow (MOD area), no longer a faculty self-declare — retain the row but mark it superseded. (4) Amend CHK-03: remove "Confirm absent" from the offered actions and state the actions (Verify, Flag identity mismatch, Flag not present, Confirm empty / Verified empty) apply to online sessions too. (5) Delete the CHK-06 row entirely (auto-Absent->Present override removed; Absent is final). (6) RPT-02: change "notifying IFO" to "notifying IFO and the relevant Dean(s)". (7) §8 Policy Assumptions Register: add a table row `| Modality-shift lead time (modality_shift_lead_days) | 2 days | Operational — whole calendar days, Asia/Manila |`. Keep all edits ASCII/markdown-table clean so pandoc converts them faithfully.</action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_srs -v2</automated>
  </verify>
  <acceptance_criteria>
    - source: FluxTrack_SRS.md Revision History has a 1.2 row and the header version reads 1.2
    - source: a MOD subsection with MOD-01..MOD-06 exists; a DEAN-04 row exists; the CHK-06 row is gone; §8 contains modality_shift_lead_days
    - behavior: the marker smoke test (Task 4) passes against the edited .md
    - test-command: `py -3.12 manage.py test scheduling.tests_srs -v2` exits 0
  </acceptance_criteria>
  <done>FluxTrack_SRS.md reflects every item in the v1.2 edit map; markers are present and CHK-06 is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Add the regenerate_srs_docx management command and regenerate the DOCX</name>
  <files>scheduling/management/commands/regenerate_srs_docx.py, FluxTrack_SRS.docx</files>
  <read_first>
    - scheduling/management/commands/assign_online.py (BaseCommand + add_arguments + handle + ASCII self.stdout.write(self.style.SUCCESS(...)) skeleton to copy)
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-PATTERNS.md (§"regenerate_srs_docx.py (new)" — command skeleton + pypandoc.convert_file call)
  </read_first>
  <action>Create scheduling/management/commands/regenerate_srs_docx.py as a BaseCommand mirroring assign_online.py's shape. In handle(), resolve the repo-root paths to FluxTrack_SRS.md (input) and FluxTrack_SRS.docx (output) relative to settings.BASE_DIR, call `pypandoc.convert_file(str(md_path), "docx", outputfile=str(docx_path))`, and print an ASCII success line with the output path. Guard: if the .md is missing, write an ERROR to stderr and return (do not raise). Then RUN the command once (`py -3.12 manage.py regenerate_srs_docx`) to regenerate FluxTrack_SRS.docx from the freshly-edited v1.2 markdown and commit the regenerated binary. Keep all command output ASCII-only (Windows cp1252 convention).</action>
  <verify>
    <automated>py -3.12 manage.py regenerate_srs_docx && py -3.12 -c "import os; assert os.path.getsize('FluxTrack_SRS.docx')>0; print('docx regenerated')"</automated>
  </verify>
  <acceptance_criteria>
    - source: scheduling/management/commands/regenerate_srs_docx.py exists as a BaseCommand calling pypandoc.convert_file
    - behavior: running the command regenerates a non-empty FluxTrack_SRS.docx without raising
    - test-command: the command exits 0 and the inline size assertion prints "docx regenerated"
  </acceptance_criteria>
  <done>`manage.py regenerate_srs_docx` deterministically rebuilds the .docx from the .md; the committed .docx is the v1.2 output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: SRS v1.2 marker smoke test</name>
  <files>scheduling/tests_srs.py</files>
  <read_first>
    - .planning/phases/04-modality-shift-approval-srs-v1-2/04-VALIDATION.md (DOC-01 row: ".md has v1.2 markers ...; .docx regenerates without error" — smoke)
    - scheduling/management/commands/assign_online.py (BaseCommand invocation via call_command in a test)
  </read_first>
  <behavior>
    - Reading FluxTrack_SRS.md asserts: it contains "MOD-01" and "MOD-06", contains "DEAN-04", contains "modality_shift_lead_days", a v1.2 Revision History marker, and does NOT contain a "CHK-06" requirement row.
    - Invoking regenerate_srs_docx via call_command writes a non-empty FluxTrack_SRS.docx without raising.
  </behavior>
  <action>Create scheduling/tests_srs.py with a SimpleTestCase `SrsV12DocTests`. One test reads FluxTrack_SRS.md text and asserts the presence of the v1.2 markers (MOD-01, MOD-06, DEAN-04, modality_shift_lead_days, a "1.2" revision row) and asserts the CHK-06 requirement row is absent. A second test calls `call_command("regenerate_srs_docx")` and asserts FluxTrack_SRS.docx exists and is non-empty afterward. Use settings.BASE_DIR to locate both files. Keep assertions concept-based (search for the requirement IDs and the policy key), not brittle whole-line matches. Place this in tests_srs.py (its own module) so it never contends with scheduling/tests.py written by other plans; Django's test discovery picks up test*.py.</action>
  <verify>
    <automated>py -3.12 manage.py test scheduling.tests_srs -v2</automated>
  </verify>
  <acceptance_criteria>
    - source: scheduling/tests_srs.py defines SrsV12DocTests with a marker test and a regenerate test
    - behavior: both tests pass against the edited .md and the regenerate command
    - test-command: `py -3.12 manage.py test scheduling.tests_srs -v2` exits 0
  </acceptance_criteria>
  <done>The smoke test asserts the v1.2 markers and a clean docx regeneration; it is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI ↔ local env | A new package is installed from an external registry — supply-chain surface |
| doc source ↔ generated binary | The .docx is generated from the .md; a hand-edited .docx would drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | pypandoc_binary install | high | mitigate | Blocking human-verify checkpoint before install; verify on pypi.org/project/pypandoc_binary + canonical repo; pin ==1.17; never auto-approve (workflow.auto_advance ignored) |
| T-04-DOC1 | Repudiation | SRS .md vs .docx drift | medium | mitigate | The .docx is only ever produced by regenerate_srs_docx from the .md; smoke test asserts regeneration; do not hand-edit the .docx |
| T-04-DOC2 | Tampering | command file path handling | low | accept | Paths resolved from settings.BASE_DIR, not user input; command is offline/dev-only |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `regenerate_srs_docx` management command — scheduling/management/commands/regenerate_srs_docx.py
- `FluxTrack_SRS.md` at v1.2 (MOD area, DEAN-04, FAC-07 superseded, CHK-03 amended, CHK-06 removed, RPT-02 notifies Deans, policy row)
- `FluxTrack_SRS.docx` (regenerated v1.2)
- `pypandoc_binary==1.17` pin — requirements.txt
- `SrsV12DocTests` — scheduling/tests_srs.py
</artifacts_produced>

<verification>
- `py -3.12 manage.py regenerate_srs_docx` exits 0 and rebuilds a non-empty .docx.
- `py -3.12 manage.py test scheduling.tests_srs -v2` green.
- Manual (per 04-VALIDATION Manual-Only): open the regenerated .docx and confirm the MOD area, DEAN-04, policy row, and removed CHK-06 render correctly (tables/headings intact).
</verification>

<success_criteria>
DOC-01 is closed: the SRS reads v1.2 in both formats, the .docx is reproducible from the .md via one command, and MOD-06's documentation half (FAC-07 superseded, CHK-06 removed) is in the spec.
</success_criteria>

<output>
Create `.planning/phases/04-modality-shift-approval-srs-v1-2/04-02-SUMMARY.md` when done.
</output>
