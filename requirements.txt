# FluxTrack — Python dependencies
# Core
Django==6.0.6
mssql-django==1.7.3          # pulls pyodbc; needs ODBC Driver 18 installed system-wide
djangorestframework>=3.15
python-dotenv>=1.0
whitenoise>=6.6              # static file serving on EC2

# Media / reports
qrcode[pil]>=7.4            # room QR posters (IFO-01)
Pillow>=10.0
pypandoc_binary==1.17       # bundled pandoc engine for DOC-01 SRS md->docx (pandoc not on PATH)

# Scheduled jobs (JOB-01/02/03) — pinned <4: the 4.0 pre-release rewrites the
# BlockingScheduler/jobstore API (ENV-04 relies on 3.x); do NOT adopt 4.0.
APScheduler>=3.10,<4

# Web push (NOTIF-02) — VAPID send; pulls py-vapid + http-ece, uses existing cryptography
pywebpush>=2.3,<3

# Identity (Entra ID SSO) — python-social-auth wiring against Microsoft Entra ID
social-auth-app-django==6.0.0   # social_django: models, URLs, views, middleware, pipeline

# Transitive deps of social-auth-core (>= minimums it requires); installing
# social-auth-app-django upgrades these and pulls oauthlib/requests-oauthlib/
# defusedxml/python3-openid automatically — do NOT hand-bump these pins.
PyJWT>=2.8
cryptography>=42.0
requests>=2.31
