# Linux deployment entrypoints must not acquire Windows CRLF line endings.
*.sh text eol=lf
deploy/systemd/* text eol=lf
deploy/nginx/* text eol=lf
# Third-party distribution artifacts stay byte-for-byte identical to npm.
static/vendor/** -text -whitespace
